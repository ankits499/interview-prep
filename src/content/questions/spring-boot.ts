import type { Question } from '../../types'

export const springBootQuestions: Question[] = [
  // ── IoC & DI Questions ──────────────────────────────────────────
{
    id: 'sb-di-q1',
    question: 'A senior engineer on your team insists on field injection because "it\'s less boilerplate and every tutorial does it." What\'s the strongest technical counterargument, beyond style preference?',
    shortAnswer: 'Field injection prevents `final` fields, hides a class\'s real dependencies from its public constructor signature, requires reflection or framework support to unit-test without a Spring context, and leaves the object in a partially-wired state right after the no-arg constructor runs but before fields are populated.',
    detailedAnswer: 'Constructor injection makes dependencies part of the class\'s explicit contract — anyone reading the constructor signature knows exactly what\'s required, and the object is guaranteed fully wired the moment it exists (no window of partial construction). It also allows `final` fields, which the compiler enforces are set exactly once. Testability is the practical payoff: `new MyService(mockDep)` works with zero framework involvement, whereas field-injected dependencies need Mockito\'s @InjectMocks or manual reflection. Field injection\'s only real advantage is less visual boilerplate, which most IDEs auto-generate anyway for constructor injection.',
    keyPoints: [
      'No final fields with field injection — nothing prevents reassignment later',
      'Dependencies invisible from the public API, only visible by reading class internals',
      'Partially-wired object exists between no-arg construction and field population',
      'Testing requires reflection-based tooling instead of plain constructor calls',
    ],
    seniorFollowUps: [
      'Is there ever a legitimate case for field injection in production code?',
      'How does a bloated constructor parameter list function as an early warning for a design problem?',
    ],
    example: '@Autowired private PaymentService paymentService; // vs constructor: private final PaymentService paymentService; MyClass(PaymentService paymentService) { this.paymentService = paymentService; }',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['field-injection', 'constructor-injection', 'testability'],
  },
  {
    id: 'sb-di-q2',
    question: 'Two beans implement the same interface, PaymentGateway. At startup you get NoUniqueBeanDefinitionException. Walk through the resolution order Spring attempted before failing, and two distinct ways to fix it.',
    shortAnswer: 'Spring tries type match first, then field/parameter name match, then @Qualifier, then @Primary — if none disambiguate, it fails rather than guessing. Fix it by marking one implementation @Primary as the default, or by adding @Qualifier at each injection point that needs a specific one.',
    detailedAnswer: 'Autowiring resolution is: (1) match by type — here both beans qualify, so this alone is ambiguous; (2) match by bean name against the field/parameter name — if the injection point is named exactly `paypalPaymentGateway` and a bean of that name exists, it wins; (3) explicit @Qualifier on the injection point naming a specific bean; (4) a bean marked @Primary as the fallback default. If none of these narrow it to exactly one candidate, Spring refuses to guess and throws NoUniqueBeanDefinitionException at context refresh. The two idiomatic fixes are marking the default implementation @Primary (global default) and/or using @Qualifier at specific injection points that need the non-default bean.',
    keyPoints: [
      'Type match -> name match -> @Qualifier -> @Primary, in that order',
      'Fails loudly at context startup rather than picking arbitrarily',
      '@Primary sets a global default; @Qualifier overrides it per injection point',
    ],
    seniorFollowUps: [
      'What happens if a bean is both @Primary and targeted by a conflicting @Qualifier elsewhere?',
      'How would you handle this if the two beans came from a third-party library you can\'t annotate?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['autowiring', 'qualifier', 'primary', 'ambiguous-dependency'],
  },
  {
    id: 'sb-di-q3',
    question: 'Why does Spring Boot\'s default ApplicationContext eagerly instantiate all singleton beans at startup instead of lazily on first use, and what tradeoff does that involve?',
    shortAnswer: 'Eager instantiation surfaces misconfiguration (missing beans, bad @Value properties, failed @Bean factory methods) at deploy time instead of at an unpredictable point in production traffic. The tradeoff is slower startup, which matters for things like serverless cold starts or fast rolling deployments — Spring Boot 2.2+ offers @Lazy or lazy-initialization mode to opt out per-bean or globally.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['application-context', 'startup', 'fail-fast'],
  },
  {
    id: 'sb-di-q4',
    question: 'You have a DAO class annotated @Repository that catches a vendor-specific SQLException. A teammate suggests changing it to @Component since "they do the same thing." What actually breaks?',
    shortAnswer: '@Repository enables PersistenceExceptionTranslationPostProcessor, which wraps platform-specific persistence exceptions into Spring\'s unchecked DataAccessException hierarchy. Swapping to plain @Component still registers the bean but silently disables that translation, so vendor-specific exceptions (like a raw SQLException) leak up through layers that expect a consistent DataAccessException type.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['stereotype-annotations', 'repository', 'exception-translation'],
  },
  {
    id: 'sb-di-q5',
    question: 'Two of your own services depend on each other and both use constructor injection. What happens at startup, and why is this actually a useful outcome rather than a bug to work around?',
    shortAnswer: 'Spring throws BeanCurrentlyInCreationException because it cannot supply a fully-constructed instance of either bean to satisfy the other\'s constructor. This is useful because it forces the cycle to be fixed at design time — via an interface, an event, or merging responsibilities — rather than letting setter or field injection quietly wire a half-formed object into place.',
    detailedAnswer: 'With constructor injection, Spring needs bean A fully built to inject into bean B\'s constructor, and vice versa — there\'s no valid order, so it fails outright rather than returning a partially-initialized reference. This is a deliberate consequence of choosing constructor injection: it converts a subtle runtime coupling problem into a loud, unmissable startup failure. Setter or field injection can technically resolve the same cycle by injecting a not-yet-fully-populated bean reference, but that just defers the same design flaw into production behavior that\'s harder to reason about. The right response to the exception is refactoring the dependency graph, not switching to setter injection to make the error disappear.',
    keyPoints: [
      'Constructor injection cannot resolve mutual dependencies — no valid construction order exists',
      'The failure is intentional friction, not a limitation to route around with setter injection',
      'The actual resolution mechanics (how setter/field injection handles cycles) is bean-lifecycle territory, not a DI-style decision',
    ],
    seniorFollowUps: [
      'Given a real circular dependency between OrderService and InventoryService, what refactor would you reach for first?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['circular-dependency', 'constructor-injection', 'fail-fast'],
  },
  {
    id: 'sb-di-q6',
    question: 'When would you reach for an @Bean method in an @Configuration class instead of letting component scanning pick up a class automatically?',
    shortAnswer: 'When the class comes from a third-party library you can\'t annotate, when construction needs conditional logic or configuration values decided at wiring time, or when you need multiple differently-configured beans of the same type (e.g. two RestTemplate instances with different timeouts).',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['bean-methods', 'configuration', 'component-scanning'],
  },
  {
    id: 'sb-di-q7',
    question: 'What\'s the practical difference between BeanFactory and ApplicationContext, and which one does a typical Spring Boot application actually use?',
    shortAnswer: 'BeanFactory is the base container contract with lazy, on-demand bean instantiation; ApplicationContext extends it with eager singleton pre-instantiation, event publishing, AOP support, and environment/property abstraction. Spring Boot apps run on ApplicationContext (via @SpringBootApplication) — raw BeanFactory is essentially never used directly in application code.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['application-context', 'bean-factory'],
  },
  {
    id: 'sb-di-q8',
    question: 'Why is a constructor with eight injected dependencies a more useful warning sign than the same eight dependencies injected via @Autowired fields?',
    shortAnswer: 'A bloated constructor signature is immediately visible to anyone reading or calling the class — it\'s an unmissable signal the class is doing too much. The same bloat via field injection is invisible unless someone scrolls through and counts @Autowired fields, so single-responsibility violations accumulate unnoticed.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['design-judgment', 'constructor-injection', 'single-responsibility'],
  },
  {
    id: 'sb-di-q9',
    question: 'What actually differs between @Service, @Controller, and @Component, mechanically?',
    shortAnswer: 'Nothing mechanically — all three are meta-annotated with @Component, so component scanning registers a bean identically regardless of which one you use. They exist purely as semantic markers for readability and for tools (e.g. AOP pointcuts, static analysis) that key off the specific annotation. The one stereotype with real added container behavior is @Repository, via exception translation.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['stereotype-annotations', 'component-scanning'],
  },
  {
    id: 'sb-di-q10',
    question: 'Why does constructor injection make a class trivial to unit test without starting a Spring context?',
    shortAnswer: 'Because dependencies are ordinary constructor parameters, the class can be instantiated directly with `new ClassUnderTest(mockDep1, mockDep2)` in a plain unit test — no ApplicationContext, no reflection, no framework-specific test annotation required.',
    topic: 'spring-boot',
    subtopic: 'sb-ioc-di',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['testability', 'constructor-injection'],
  },

  // ── Bean Lifecycle Questions ─────────────────────────────────────
{
    id: 'sb-lifecycle-q1',
    question: 'A singleton OrderService has a prototype-scoped CartItem field-injected via @Autowired with no proxyMode set. Every order ends up sharing the same CartItem data. Why, and what\'s the actual fix?',
    shortAnswer: 'Field injection resolves the dependency once, when OrderService (a singleton) is constructed — so it captures exactly one CartItem instance forever, defeating prototype scope. The fix is @Scope(proxyMode = ScopedProxyMode.TARGET_CLASS) on CartItem, which injects a proxy that looks up a fresh instance on every method call instead of a frozen reference.',
    detailedAnswer: 'Spring builds the singleton graph once at startup (or on first lazy access). At that point it resolves @Autowired CartItem cartItem to whatever the prototype-scoped bean factory returns at that instant, and that reference is stored as a plain field forever — there\'s no future re-resolution because the singleton itself is never reconstructed. A scoped proxy changes what gets injected: instead of a real CartItem, OrderService holds a lightweight proxy (CGLIB subclass for TARGET_CLASS, JDK dynamic proxy for INTERFACES) whose every method delegates to "fetch the currently correct scoped instance, then call the real method on it." That lookup happens per call, so each use gets a genuinely fresh prototype instance.',
    keyPoints: [
      'Root cause: singleton construction happens once, so any naive injection of a narrower scope resolves once too',
      'proxyMode injects an indirection object, not the real bean, deferring resolution to call time',
      'Same fix pattern applies to request/session-scoped beans injected into singletons',
    ],
    seniorFollowUps: [
      'What\'s the runtime cost of the scoped proxy indirection, and when would it actually matter?',
      'Could you solve this without a scoped proxy, e.g. by injecting an ObjectFactory<CartItem> or Provider<CartItem> instead?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['scope', 'prototype', 'scoped-proxy'],
  },
  {
    id: 'sb-lifecycle-q2',
    question: 'Two singleton beans, PaymentService and RefundService, depend on each other. With setter injection it starts fine. Switch both to constructor injection and it fails at startup. Explain exactly why.',
    shortAnswer: 'Setter/field injection separates instantiation from property population, so Spring can hand out an early, not-yet-populated reference to the bean still under construction and complete the wiring afterward. Constructor injection collapses both steps into one — a bean can\'t exist until its constructor arguments already exist — so there\'s no partially-built instance to hand out, and Spring throws BeanCurrentlyInCreationException instead of deadlocking.',
    detailedAnswer: 'When Spring builds PaymentService via setters, it first calls the no-arg (or factory) constructor to get a raw instance, registers an early reference to it, then starts injecting properties/setters — one of which needs RefundService. Building RefundService hits the same situation in reverse and gets handed the early PaymentService reference already registered. Both objects finish their setter injection using each other\'s not-fully-initialized-but-existing instances, and by the time both complete their own init phases the graph is consistent. With constructor injection, PaymentService cannot be instantiated at all until a RefundService instance is available to pass into its constructor, and vice versa — there is no "raw instance to register early" because instantiation IS where the dependency is needed. Spring detects it\'s already in the process of creating both and fails fast rather than looping forever.',
    keyPoints: [
      'Setter injection cycle resolution relies on instantiation and population being separate steps',
      'Constructor injection needs the full dependency graph to be already built at construction time — no separation to exploit',
      'The failure (BeanCurrentlyInCreationException) is by design — it surfaces a real circular design problem immediately instead of hiding it',
    ],
    seniorFollowUps: [
      'Is fixing this with @Lazy on a constructor argument a legitimate solution, or a code smell you should push back on in review?',
      'How would you refactor two mutually dependent services to remove the cycle altogether?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['circular-dependency', 'constructor-injection', 'lifecycle'],
  },
  {
    id: 'sb-lifecycle-q3',
    question: 'You need to rewrite a property value on every DataSource bean definition before any bean in the context is created — for example, injecting an environment-specific JDBC URL that isn\'t known until the container starts. Would you implement a BeanPostProcessor or a BeanFactoryPostProcessor, and why does the other one not work here?',
    shortAnswer: 'BeanFactoryPostProcessor — it runs once, before any bean is instantiated, and operates on bean DEFINITIONS (metadata), which is exactly what\'s needed to change a property value before the DataSource is built. A BeanPostProcessor runs after a bean instance already exists, so by then the DataSource would already be constructed with the wrong URL — too late to change how it was built, only able to wrap or mutate the finished object.',
    detailedAnswer: 'BeanFactoryPostProcessor.postProcessBeanFactory(ConfigurableListableBeanFactory) gives access to BeanDefinition objects — the "recipe" Spring will use to construct beans, including class, scope, and property values — before instantiation begins. Rewriting a property value there changes what the constructor/setters will actually receive. BeanPostProcessor, by contrast, is invoked around each bean\'s initialization callbacks after the object already exists with whatever properties it was built with; it can wrap the instance in a proxy or swap it for a different object, but it can\'t retroactively change what values were fed into its constructor. PropertySourcesPlaceholderConfigurer is the standard example of a BeanFactoryPostProcessor doing exactly this kind of definition-time rewrite for ${...} placeholders.',
    keyPoints: [
      'BeanFactoryPostProcessor: definition-time, once, before instantiation',
      'BeanPostProcessor: instance-time, per bean, after instantiation',
      'Changing how a bean gets built requires the definition-time hook, not the instance-time one',
    ],
    seniorFollowUps: [
      'What happens if a BeanFactoryPostProcessor implementation accidentally triggers eager instantiation of another bean while it runs?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['bean-factory-post-processor', 'bean-post-processor', 'lifecycle'],
  },
  {
    id: 'sb-lifecycle-q4',
    question: 'A bean implements both @PostConstruct and InitializingBean. During a refactor someone adds logic in afterPropertiesSet() that depends on state the @PostConstruct method sets up. Is this safe to rely on?',
    shortAnswer: 'Yes — Spring always runs @PostConstruct before InitializingBean.afterPropertiesSet() when both exist on the same bean, so afterPropertiesSet() can safely assume @PostConstruct already ran. It\'s still fragile design to depend on ordering between two competing init mechanisms rather than consolidating into one.',
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['postconstruct', 'initializingbean', 'lifecycle'],
  },
  {
    id: 'sb-lifecycle-q5',
    question: 'A prototype-scoped bean holds a file handle it needs to close. @PreDestroy is defined on it but never fires when the application shuts down. What\'s going on, and how would you actually get cleanup to happen?',
    shortAnswer: 'Spring only manages the destruction phase of the lifecycle for beans it fully owns — singletons. Once a prototype instance is handed out, the container stops tracking it entirely, so it has no way to call @PreDestroy at shutdown. The fix is manual: the code that requests the prototype must call the cleanup method itself (or you register the instance with a custom tracker/BeanPostProcessor that calls DisposableBean.destroy() on it explicitly).',
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['prototype', 'predestroy', 'lifecycle'],
  },
  {
    id: 'sb-lifecycle-q6',
    question: 'What does @Lazy actually trade off, and why might a team decide against using it broadly even though it improves startup time?',
    shortAnswer: '@Lazy defers singleton creation until first use, shortening startup — but it also defers discovery of misconfiguration (bad property, missing dependency, wiring error) from deploy time to first-request time, which is a worse place to fail in production. Broad use also means a "successful startup" no longer proves the app is actually ready to serve traffic.',
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['lazy', 'startup', 'tradeoffs'],
  },
  {
    id: 'sb-lifecycle-q7',
    question: 'Why is a mutable, non-thread-safe field on a singleton-scoped @Service considered a design smell rather than an occasional acceptable convenience?',
    shortAnswer: 'A singleton bean is one shared instance handling every concurrent request/thread in the application — any mutable state on it is effectively shared mutable state across threads, so it needs explicit synchronization or it will produce race conditions under load. The safer default is to keep singleton services stateless and push per-request state into method parameters, request-scoped beans, or thread-local-free designs.',
    topic: 'spring-boot',
    subtopic: 'sb-bean-lifecycle',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['singleton', 'thread-safety', 'scope'],
  },

  // ── Auto-Configuration Questions ──────────────────────────────────
{
    id: 'sb-auto-q1',
    question: 'A junior engineer says "Spring Boot is a different framework from Spring, it does the DI itself." How do you correct that, precisely?',
    shortAnswer: 'Spring Boot doesn\'t replace the Spring Framework\'s IoC container or DI mechanism at all — it runs on the exact same ApplicationContext. What Boot adds is auto-configuration (pre-wiring sensible beans conditionally), embedded servers, and curated starter dependencies, so you write far less manual configuration to get a running app.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['fundamentals', 'spring-vs-boot'],
  },
  {
    id: 'sb-auto-q2',
    question: 'A team adds spring-boot-starter-data-redis to their build but nothing about Redis gets auto-configured — no RedisTemplate bean appears. Where do you look first, and what\'s the likely root cause if the dependency really is on the classpath?',
    shortAnswer: 'Starters carry no logic themselves — they just pull in jars. The actual auto-configuration is in spring-boot-autoconfigure, gated by @ConditionalOnClass and friends. First check --debug output / the condition evaluation report for RedisAutoConfiguration\'s negative matches — a likely cause is a required class not actually resolving (version conflict, exclusion in the build) even though the starter is declared, or a property-based condition not being satisfied.',
    detailedAnswer: 'The mental model to check is: starter presence != auto-configuration activation. A starter only changes the classpath; whether an auto-configuration class actually applies is a separate, conditional evaluation. Walk through: (1) confirm the actual Redis client classes (e.g. Lettuce) are really resolved, not excluded by a BOM or another dependency forcing a different version; (2) run with --debug and read RedisAutoConfiguration in the "Negative matches" section for the specific condition that failed; (3) check for a competing user-defined bean of the same type that would have satisfied @ConditionalOnMissingBean and silently suppressed the auto-configured bean.',
    keyPoints: [
      'Starter = dependency aggregation only, no code',
      'Auto-configuration activation is a separate conditional evaluation step',
      'Condition evaluation report is the direct diagnostic tool',
      'A silently-satisfying user bean is as likely a cause as a missing dependency',
    ],
    seniorFollowUps: [
      'How would you verify this at runtime in a deployed environment without restarting with --debug?',
      'What would change in your diagnosis if the missing bean were name-qualified rather than type-only?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['debugging', 'starters', 'conditional-annotations'],
  },
  {
    id: 'sb-auto-q3',
    question: 'Explain, mechanically, why defining your own @Bean of type DataSource prevents Boot\'s auto-configured DataSource from ever being created — no exception, no duplicate-bean error.',
    shortAnswer: 'Boot\'s auto-configured DataSource bean method is annotated @ConditionalOnMissingBean(DataSource.class). User @Configuration classes are processed before auto-configuration classes, so by the time that condition is evaluated your bean is already registered, the condition evaluates false, and the auto-configured method body simply never runs — there\'s no conflict because the second bean definition never gets created in the first place.',
    detailedAnswer: 'This is a two-part mechanism worth separating in an answer: (1) processing order — Spring Boot deliberately processes the application\'s own configuration classes before importing auto-configuration classes, specifically so that conditions like @ConditionalOnMissingBean can "see" user-supplied beans; (2) the condition itself, which does a type-match (and can be narrowed by name) against beans already registered in the BeanFactory at evaluation time. Because the auto-configured bean method is skipped entirely rather than evaluated and then rejected, there\'s no duplicate-definition collision to report — from Spring\'s point of view only one bean definition for that type was ever created.',
    keyPoints: [
      'Processing order: user config before auto-configuration',
      '@ConditionalOnMissingBean does type (optionally name) matching against the current BeanFactory state',
      'The auto-configured @Bean method is skipped, not overridden — no collision occurs',
    ],
    seniorFollowUps: [
      'What happens if your bean is of a subtype rather than the exact type the condition checks for?',
      'How would multiple auto-configuration classes, each contributing a related bean with interdependent @ConditionalOnMissingBean checks, be affected by ordering?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['conditional-on-missing-bean', 'internals', 'overriding'],
  },
  {
    id: 'sb-auto-q4',
    question: 'A multi-module Gradle project has its @SpringBootApplication class in com.acme.orders.app, but a shared module contributes @Service beans in com.acme.common.audit. At startup those beans never appear, with no error logged. Diagnose it.',
    shortAnswer: 'The @ComponentScan bundled in @SpringBootApplication defaults its base package to the annotated class\'s own package — com.acme.orders.app — so com.acme.common.audit is outside the scanned tree and its beans are simply never found; there\'s no error because component scanning finding zero matches in an unscanned package isn\'t a failure condition. Fix by adding explicit basePackages, moving the main class to a shared root package, or @Import-ing the specific configuration.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['component-scan', 'gotcha', 'multi-module'],
  },
  {
    id: 'sb-auto-q5',
    question: 'What actually changed between spring.factories-based auto-configuration registration and the newer AutoConfiguration.imports file, and why does it matter beyond being a file rename?',
    shortAnswer: 'spring.factories was a general-purpose key-value properties file shared by many Spring Boot extension points, so loading auto-configuration candidates meant parsing and filtering a much larger combined file across every jar. AutoConfiguration.imports (introduced in Boot 2.7) is a plain newline-separated list dedicated solely to auto-configuration class names, which is faster to read and keeps auto-configuration registration decoupled from other spring.factories-based extensions.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['internals', 'history', 'startup-performance'],
  },
  {
    id: 'sb-auto-q6',
    question: 'You\'re writing a custom auto-configuration for an internal HTTP client library. Its bean creation needs to reference a ConnectionPoolProperties-derived bean that another team\'s auto-configuration produces. What do you need to get right for this to reliably work, and what goes wrong if you skip it?',
    shortAnswer: 'You need @AutoConfigureAfter pointing at the other team\'s auto-configuration class, so yours is guaranteed to run after theirs and the bean is definitely present. Skip it and you\'re relying on incidental classpath/discovery order — it may work in dev and fail intermittently (or after a Boot/dependency upgrade reshuffles ordering) in other environments, with @ConditionalOnBean evaluating false because the dependency hasn\'t been registered yet at evaluation time.',
    detailedAnswer: 'Auto-configuration ordering isn\'t guaranteed by discovery order (the imports file isn\'t necessarily processed in a stable, dependency-aware sequence) — it\'s explicit ordering annotations that give you a contract. @AutoConfigureAfter(TargetAutoConfiguration.class) declares "don\'t evaluate me until that one has run," which is exactly what a @ConditionalOnBean check on a bean from that class needs to be reliable. Without it, the bug is nondeterministic-looking: it can pass for months and then break after an unrelated dependency bump reorders auto-configuration classes, which makes it a nasty one to diagnose without knowing this mechanism exists.',
    keyPoints: [
      '@AutoConfigureAfter/@AutoConfigureBefore give explicit, contractual ordering',
      'Without it, correctness depends on incidental/unstable ordering',
      '@ConditionalOnBean is only safe to depend on when ordering relative to the bean\'s producer is guaranteed',
    ],
    seniorFollowUps: [
      'How would you write a test that actually catches an ordering regression like this before it reaches production?',
      'Is there a way to make your auto-configuration robust to the dependency being entirely absent, not just mis-ordered?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['autoconfigure-after', 'ordering', 'custom-autoconfiguration'],
  },
  {
    id: 'sb-auto-q7',
    question: 'A production incident report says "we didn\'t know DataSourceAutoConfiguration was active, it wasn\'t in our application.yml." How would you have used Boot\'s own tooling to see, ahead of time, exactly which auto-configurations were active and why?',
    shortAnswer: 'Run (or restart) the app with --debug to get the Condition Evaluation Report, which lists every auto-configuration as a positive match (applied, with which conditions passed), a negative match (skipped, with the specific failing condition), or unconditional. For a live instance with Actuator on the classpath, the same data is available at /actuator/conditions without a restart.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['debugging', 'condition-evaluation-report', 'actuator'],
  },
  {
    id: 'sb-auto-q8',
    question: 'When would you reach for spring.autoconfigure.exclude (a property) instead of @SpringBootApplication(exclude = ...) (a compile-time attribute) to disable an auto-configuration, and what\'s the tradeoff of each?',
    shortAnswer: 'Use the property when the exclusion needs to vary per environment or profile without recompiling — e.g. excluding a DataSource auto-config only in a test profile. The compile-time exclude attribute fails fast at startup with a clear error if the class name is wrong or unavailable, which is safer against typos, whereas the property is a plain string with no such compile-time check.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['exclusion', 'configuration'],
  },
  {
    id: 'sb-auto-q9',
    question: 'You need to publish an internal starter so other teams get sensible defaults for your company\'s message-queue client with zero setup. Sketch the module structure and the one file most people forget, causing "I added the dependency and nothing happened."',
    shortAnswer: 'Two modules: an autoconfigure module holding a @Configuration class (with @ConditionalOnClass on the client library and @ConditionalOnMissingBean on the beans it creates) and a thin starter module that just depends on the autoconfigure module plus the client library, with no code of its own. The commonly forgotten file is META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports in the autoconfigure module — without listing the configuration class\'s fully-qualified name there, Boot never discovers it, even though everything else is correctly written.',
    detailedAnswer: 'The two-module split mirrors Spring Boot\'s own starters: acme-mq-spring-boot-autoconfigure carries the @Configuration class plus conditions (@ConditionalOnClass on the MQ client, @ConditionalOnMissingBean on any bean it produces so callers can still override), while acme-mq-spring-boot-starter is essentially an empty artifact whose only job is to declare a dependency on the autoconfigure module and the underlying client library, giving consumers one clean line to add. The failure mode described is almost always the missing AutoConfiguration.imports entry: the @Configuration class is perfectly valid Spring config, it just never gets imported by @EnableAutoConfiguration because Boot only auto-applies classes it discovers through that file — nothing about the class itself makes it "an auto-configuration."',
    keyPoints: [
      'Two-module convention: autoconfigure (logic) + starter (dependency bundle)',
      'AutoConfiguration.imports is the sole discovery mechanism — a valid @Configuration class is inert without it',
      '@ConditionalOnMissingBean on produced beans preserves the override contract for consumers',
    ],
    seniorFollowUps: [
      'How would you add a regression test that would have caught the missing imports-file entry before publishing?',
      'What would you do differently if this starter needed to support both Boot 2.7+ (imports file) and older Boot versions (spring.factories) simultaneously?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['custom-starter', 'autoconfiguration-imports', 'gotcha'],
  },
  {
    id: 'sb-auto-q10',
    question: 'What\'s the practical difference between @ConditionalOnBean and @ConditionalOnMissingBean in terms of where it\'s safe to use them — auto-configuration code vs. your own application\'s @Configuration classes?',
    shortAnswer: '@ConditionalOnMissingBean is commonly and safely used inside your own application config (to conditionally skip defining something someone else already provided). @ConditionalOnBean, especially inside your own application-level @Configuration, is fragile because Spring doesn\'t guarantee a processing order between arbitrary user configuration classes the way it does between auto-configuration classes — the bean you\'re checking for might not be registered yet at evaluation time even though it eventually will be.',
    topic: 'spring-boot',
    subtopic: 'sb-autoconfiguration',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['conditional-on-bean', 'best-practices'],
  },

  // ── Spring MVC Questions ──────────────────────────────────────
{
    id: 'sb-mvc-q1',
    question: 'Walk through what happens, component by component, between a browser sending GET /orders/42 and the JSON response being written back.',
    shortAnswer: 'The request hits DispatcherServlet, which asks HandlerMapping to match the URL/method to a HandlerMethod, then RequestMappingHandlerAdapter resolves the method\'s arguments (e.g. @PathVariable id) via argument resolvers, invokes the controller method, and passes the return value to a ReturnValueHandler which picks an HttpMessageConverter (Jackson by default) matching the Accept header to serialize the object onto the response.',
    detailedAnswer: 'DispatcherServlet is the single front controller registered for the app. It delegates URL-to-handler resolution to RequestMappingHandlerMapping, which matches path, HTTP method, and any produces/consumes constraints to find the right @RequestMapping method. The matched HandlerMethod is wrapped and handed to RequestMappingHandlerAdapter, which resolves each parameter through the chain of HandlerMethodArgumentResolvers (PathVariableMethodArgumentResolver for the id, etc.), invokes the method, then passes the return value through HandlerMethodReturnValueHandlers. For a @RestController, that ends at RequestResponseBodyMethodProcessor, which content-negotiates against the Accept header and delegates to the matching HttpMessageConverter to write bytes to the response.',
    keyPoints: [
      'DispatcherServlet -> HandlerMapping -> HandlerAdapter -> Controller -> MessageConverter',
      'Argument resolution and return value handling are both pluggable strategy chains',
      'Content negotiation happens at the message-converter selection step, driven by Accept',
    ],
    seniorFollowUps: ['Where would you plug in logic that needs to run after the handler is resolved but before the controller method executes?'],
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['dispatcherservlet', 'request-lifecycle', 'internals'],
  },
  {
    id: 'sb-mvc-q2',
    question: 'A junior engineer converts a @Controller class to @RestController and a previously-working HTML page starts returning the raw view name as plain text instead of rendering the template. What went wrong, and what\'s the fix?',
    shortAnswer: '@RestController applies @ResponseBody to every method, so the string the method returns (previously interpreted as a logical view name and resolved to a template) now gets written directly as the response body. Split the class: keep view-returning endpoints under @Controller and move JSON-returning ones to a separate @RestController, or annotate the API methods individually with @ResponseBody instead of converting the whole class.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['restcontroller', 'responsebody', 'gotcha'],
  },
  {
    id: 'sb-mvc-q3',
    question: 'Your team is deciding whether a piece of request-handling logic (checking a custom rate-limit header and rejecting the request) should be a Servlet Filter or a HandlerInterceptor. Which do you pick, and why?',
    shortAnswer: 'A Filter, because rate limiting should reject the request as early and cheaply as possible, ideally before Spring MVC does any handler resolution work at all, and it doesn\'t need any information about which controller matched — filters also apply uniformly to any request path including static resources or paths outside Spring MVC entirely.',
    detailedAnswer: 'HandlerInterceptor only runs after DispatcherServlet has already resolved the handler, meaning routing, argument-resolver setup work, and potentially other earlier interceptors have already run — wasted work if the request is about to be rejected anyway. A Filter sits in the Servlet container\'s chain ahead of DispatcherServlet, so it can inspect headers and short-circuit with a response before any Spring MVC machinery engages. The one case that would flip the answer is if the decision needed to inspect the matched HandlerMethod itself (e.g. a per-endpoint annotation) — that information only exists inside Spring MVC, which is exactly what HandlerInterceptor.preHandle has access to and Filter does not.',
    keyPoints: [
      'Filters run before DispatcherServlet even routes the request; interceptors run after handler resolution',
      'Prefer Filter for framework-agnostic, cheap, early-reject logic',
      'Prefer HandlerInterceptor when the logic needs the resolved handler (e.g. its annotations)',
    ],
    seniorFollowUps: ['If Spring Security is also in the mix, where does its filter chain sit relative to your custom rate-limit filter?'],
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['filter', 'interceptor', 'design-decision'],
  },
  {
    id: 'sb-mvc-q4',
    question: 'Why does a curl request to your API succeed even though the browser console shows a CORS error for the same endpoint?',
    shortAnswer: 'CORS is a browser-enforced policy, not a server-side access control — the server processes and returns the response regardless of origin. curl (and server-to-server calls) never apply same-origin restrictions, so they see the real response; the browser separately receives that response but blocks the page\'s JavaScript from reading it because the required Access-Control-Allow-Origin header wasn\'t present or didn\'t match.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['cors', 'gotcha'],
  },
  {
    id: 'sb-mvc-q5',
    question: 'You need an endpoint to accept a partial update to a resource. Should it be PUT or PATCH, and how would you express the response in Spring so a senior reviewer wouldn\'t flag it?',
    shortAnswer: 'PATCH, since PUT implies a full idempotent replacement of the resource and PATCH is the semantically correct verb for a partial update. Implement it returning ResponseEntity<Order> with either 200 OK and the updated resource body, or 204 No Content if the API contract doesn\'t return the updated state — a plain return type can\'t express the 204 option and shouldn\'t be used if that\'s a live possibility.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['put-vs-patch', 'responseentity', 'rest-semantics'],
  },
  {
    id: 'sb-mvc-q6',
    question: 'A controller has a private List<String> auditLog field that handler methods append to and periodically flush. Under load, entries go missing or get corrupted. Why, and what\'s the actual fix (not just synchronizing the field)?',
    shortAnswer: 'Controllers are singleton beans by default, so all concurrent requests share the same instance and therefore the same mutable field — concurrent appends without synchronization race and can lose or corrupt entries. Synchronizing the field would fix correctness but serialize all requests through that lock, killing throughput; the real fix is to remove per-request mutable state from the controller entirely and write each entry through a thread-safe, purpose-built component (e.g. an injected audit service backed by a concurrent queue or persisted immediately) instead of accumulating it on the controller.',
    detailedAnswer: 'The root issue is a category error: request-scoped data was put in a place with singleton lifecycle. Spring doesn\'t create a new controller instance per request (that would be wasteful and isn\'t how the framework is designed), so any field written during request handling is inherently shared and needs either to not exist (use local variables/method parameters) or to be backed by something explicitly designed for concurrent access (an injected thread-safe collaborator, not a raw field on the controller).',
    keyPoints: [
      'Controllers are singleton-scoped by default like any other component',
      'Mutable instance fields written during request handling are shared, racy state',
      'Fix is architectural (move state out of the controller), not just adding synchronization',
    ],
    seniorFollowUps: ['How would a request-scoped bean (proxy mode) change this, and what\'s the tradeoff of using one?'],
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['thread-safety', 'controller-scope', 'concurrency'],
  },
  {
    id: 'sb-mvc-q7',
    question: 'A client sends a request with Content-Type: application/xml to an endpoint whose parameter is annotated @RequestBody, and the app only has the default Jackson JSON dependency on the classpath. What HTTP status comes back and why?',
    shortAnswer: '415 Unsupported Media Type, because no registered HttpMessageConverter declares support for reading application/xml — Spring can\'t find a converter to deserialize the body into the target type, regardless of the type\'s own shape.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['message-converters', 'content-negotiation'],
  },
  {
    id: 'sb-mvc-q8',
    question: 'Two teams disagree on whether a validation failure inside a controller method should be handled by returning a plain error object versus throwing an exception for a global handler to catch. Which approach fits better in a well-structured Spring MVC app, and why does that matter for the DispatcherServlet flow?',
    shortAnswer: 'Throwing (letting a centralized @ControllerAdvice / @ExceptionHandler translate it into a response) keeps error-response shaping out of every controller method and out of the DispatcherServlet\'s normal handler-adapter -> return-value-handler path, since DispatcherServlet has a distinct exception-resolution path (HandlerExceptionResolver chain) specifically for this; returning error objects inline mixes success and failure response shaping into ordinary return-value handling and doesn\'t compose across dozens of endpoints.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['exception-handling', 'architecture'],
  },
  {
    id: 'sb-mvc-q9',
    question: 'What\'s the practical difference between letting a method just return the object versus wrapping it in ResponseEntity, and when would a code reviewer push back on the plain return type?',
    shortAnswer: 'A plain return type always resolves to 200 OK with the serialized object as the body — Spring infers the status and offers no way to set headers. A reviewer should push back whenever the endpoint has more than one legitimate outcome to express as HTTP semantics: creation (201 + Location), deletion (204 + empty body), conditional responses, or any custom header — all of which require ResponseEntity.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['responseentity', 'status-codes'],
  },
  {
    id: 'sb-mvc-q10',
    question: 'Why does Spring Boot need @CrossOrigin or a CorsConfigurationSource bean at all — why isn\'t CORS just "on" for a REST API by default?',
    shortAnswer: 'CORS is opt-in on the server side by design: the browser blocks cross-origin reads unless the server explicitly asserts (via Access-Control-Allow-Origin and related headers) that it permits that origin, so Spring intentionally requires you to configure which origins/methods/headers are allowed rather than defaulting to permissive cross-origin access, which would be a security regression for any API not intended to be called from arbitrary frontends.',
    topic: 'spring-boot',
    subtopic: 'sb-mvc',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['cors', 'security'],
  },

  // ── Validation & Exception Handling Questions ──────────────────
{
    id: 'sb-valid-q1',
    question: 'A DTO field is annotated @NotNull String middleName, and a client submits an empty string "". Does validation fail, and what would you change if blank strings should be rejected too?',
    shortAnswer: '@NotNull only rejects null — an empty or whitespace string passes, so validation succeeds. To reject "" and whitespace-only strings, swap in @NotBlank (or add it alongside), since @NotBlank trims and requires non-zero length.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['bean-validation', 'notnull', 'notblank'],
  },
  {
    id: 'sb-valid-q2',
    question: 'What is the practical difference between @Valid and @Validated when both could be used to validate a @RequestBody?',
    shortAnswer: '@Valid (JSR 380) triggers standard cascading validation with no group support. @Validated (Spring-specific) does the same but additionally supports selecting validation groups, and is the only one of the two that enables method-level parameter validation via AOP when placed on a class.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['valid', 'validated', 'bean-validation'],
  },
  {
    id: 'sb-valid-q3',
    question: 'A service method is declared as `public void transfer(@Min(1) long amountCents)` on a plain @Service class with no other annotations. A caller passes 0 and no exception is thrown. Why not, and what is the minimal fix?',
    shortAnswer: 'Method-parameter validation is implemented via an AOP proxy that Spring only creates when the class is annotated @Validated — without it, @Min is inert metadata that nothing evaluates. The fix is adding @Validated at the class level; no changes to the method signature are needed.',
    detailedAnswer: 'Spring registers a MethodValidationPostProcessor as a BeanPostProcessor. For any bean whose class carries @Validated, it wraps the bean in a proxy backed by a MethodValidationInterceptor, which runs Bean Validation against @Constraint-annotated parameters/return values before/after the real method executes. Without @Validated on the class, the bean is never wrapped, so the interceptor never runs and the constraint annotations are silently ignored — no error, no exception, just bad data flowing through.',
    keyPoints: [
      'AOP proxy creation is conditional on class-level @Validated, not on the presence of parameter constraints',
      'Failure mode throws ConstraintViolationException, not MethodArgumentNotValidException',
      'Same proxy limitations as any Spring AOP feature: self-invocation and private methods bypass it',
    ],
    seniorFollowUps: [
      'If this service method is called from another method in the same class via `this.transfer(...)`, will the validation still run even with @Validated present?',
      'How would you unit-test that this validation actually fires, without spinning up the full Spring context?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['method-validation', 'aop', 'validated', 'gotcha'],
  },
  {
    id: 'sb-valid-q4',
    question: 'Your @ControllerAdvice has separate @ExceptionHandler methods for MethodArgumentNotValidException and ConstraintViolationException, but a validation failure from a service-layer @Validated method is still returning a generic 500. What is the most likely cause?',
    shortAnswer: 'The exception is probably a ConstraintViolationException thrown deep in a service call, but the handler for it either is not registered on the same @ControllerAdvice instance being used, or (more commonly) the exception is being thrown from code invoked outside the servlet dispatch (e.g. an async thread or a scheduled job) where @ControllerAdvice cannot intercept it at all — it only wraps exceptions from the DispatcherServlet\'s controller invocation path.',
    detailedAnswer: '@ControllerAdvice hooks into Spring MVC\'s HandlerExceptionResolver chain, which only runs for exceptions that propagate out of a DispatcherServlet-invoked handler method. If the @Validated service method is called synchronously from within the controller\'s call stack, the ConstraintViolationException does propagate up through the handler and should be caught — so the real culprit is usually that the service call happens on a different thread (an @Async method, a message listener, a scheduled job) where there is no active DispatcherServlet dispatch for @ControllerAdvice to attach to, or that a broader catch block elsewhere on the call path is swallowing or rewrapping the exception before it reaches the top.',
    keyPoints: [
      '@ControllerAdvice only intercepts exceptions that propagate through the DispatcherServlet\'s handler invocation',
      '@Async, scheduled, and message-listener-invoked code runs outside that dispatch and needs its own error handling path',
      'An intermediate try/catch that logs-and-swallows or wraps the exception in something unhandled is a common silent culprit',
    ],
    seniorFollowUps: [
      'How would you add centralized error handling for exceptions thrown inside an @Async method, given @ControllerAdvice cannot reach it?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['exception-handling', 'constraint-violation', 'controller-advice'],
  },
  {
    id: 'sb-valid-q5',
    question: 'Design the validation rules for a UserDto used by both POST /users (create) and PUT /users/{id} (update), where id must be absent on create but required on update, and email is always required. How do you express this without two separate DTOs?',
    shortAnswer: 'Use validation groups: define marker interfaces OnCreate and OnUpdate, annotate id with @Null(groups=OnCreate.class) and @NotNull(groups=OnUpdate.class), annotate email with @NotBlank(groups={OnCreate.class, OnUpdate.class}), then use @Validated(OnCreate.class) on the create handler parameter and @Validated(OnUpdate.class) on the update handler parameter.',
    detailedAnswer: 'Validation groups exist precisely for this "same shape, different rules per operation" scenario. Each constraint\'s groups() attribute opts it into one or more group markers; a constraint with no groups() belongs to the implicit Default group and runs unless explicitly excluded by selecting a narrower group. @Validated(SomeGroup.class) on the controller parameter tells Bean Validation to evaluate only constraints tagged with that group (plus any left in Default that you intentionally still want). This avoids maintaining CreateUserRequest and UpdateUserRequest as near-duplicate classes that drift out of sync.',
    keyPoints: [
      'Groups only work with @Validated, not plain @Valid',
      'An untagged constraint belongs to Default and still runs unless you scope your group selection to exclude it',
      'Alternative if groups feel heavyweight: two distinct DTOs — reasonable when the divergence is large',
    ],
    seniorFollowUps: [
      'What happens to a constraint with no groups() attribute when you validate with @Validated(OnCreate.class) — does it still run?',
      'When would you prefer two separate DTOs over validation groups, even though it means more classes?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['validation-groups', 'design', 'dto'],
  },
  {
    id: 'sb-valid-q6',
    question: 'A DTO has `@Valid Address shippingAddress` but a sibling field `List<LineItem> items` with no annotation. Constraints inside LineItem never trigger. Fix it, and explain why the parent-level @Valid on the DTO itself was not enough.',
    shortAnswer: 'Add @Valid on the items field itself: `@Valid List<LineItem> items`. Cascading validation only descends into a nested object or collection when the specific field holding it carries @Valid — validating the top-level DTO does not implicitly validate every field it contains, only its own direct constraints.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['nested-validation', 'cascade', 'gotcha'],
  },
  {
    id: 'sb-valid-q7',
    question: 'You need to validate that a username is not already taken in the database. Walk through how you would implement this as a reusable @UniqueUsername annotation, and note one subtlety about using it under load.',
    shortAnswer: 'Define @UniqueUsername with the standard message/groups/payload attributes and @Constraint(validatedBy = UniqueUsernameValidator.class), then implement UniqueUsernameValidator as a Spring-managed bean implementing ConstraintValidator<UniqueUsername, String>, injecting the repository via constructor and checking existsByUsername in isValid(). The subtlety: this check-then-act is inherently racy — two concurrent requests can both pass validation before either commits, so a DB-level unique constraint plus handling the resulting DataIntegrityViolationException is still required as the real guarantee; the validator is a UX nicety, not the source of truth.',
    detailedAnswer: 'Spring registers a SpringConstraintValidatorFactory so ConstraintValidator implementations are created through the Spring bean factory rather than reflection, which is what allows constructor injection of a repository or service into a custom validator. isValid should return true for null (delegate null-checking to @NotNull separately) to keep the validator single-purpose. Because the existence check happens at request-validation time and the actual insert happens later in the transaction, there is a window for a race between two near-simultaneous signups with the same username — the validator reduces the frequency of the conflict but a unique index in the database is the only thing that actually prevents the duplicate.',
    keyPoints: [
      'Custom validators are Spring beans, so they can inject repositories/services',
      'isValid should treat null as valid and let @NotNull own null-rejection',
      'Uniqueness checks via a validator are advisory — a DB unique constraint is the actual invariant enforcer',
    ],
    seniorFollowUps: [
      'How would you handle the DataIntegrityViolationException that still occurs on the rare race, so the client gets a clean 409 instead of a 500?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['custom-validator', 'constraint-validator', 'concurrency'],
  },
  {
    id: 'sb-valid-q8',
    question: 'Two @ExceptionHandler methods in the same @ControllerAdvice are declared for IllegalArgumentException and RuntimeException. A handler throws an IllegalArgumentException. Which method runs, and does declaration order in the file matter?',
    shortAnswer: 'The handler for IllegalArgumentException runs, because Spring resolves the most specific matching exception type in the class hierarchy relative to the thrown exception, not the order the @ExceptionHandler methods appear in the source file.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['controller-advice', 'exception-handler', 'resolution-order'],
  },
  {
    id: 'sb-valid-q9',
    question: 'What does extending ResponseEntityExceptionHandler give you that hand-writing @ExceptionHandler methods in a plain @ControllerAdvice does not, and what does it not cover?',
    shortAnswer: 'It gives you pre-built, framework-consistent handling for exceptions Spring MVC itself throws internally (HttpMessageNotReadableException, MethodArgumentNotValidException, HttpRequestMethodNotSupportedException, etc.), with overridable hook methods to customize the response body while reusing Spring\'s resolution wiring. It does not cover your own business/domain exceptions — those still need their own @ExceptionHandler methods on top.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['response-entity-exception-handler', 'framework-exceptions'],
  },
  {
    id: 'sb-valid-q10',
    question: 'Design the JSON error response for a failed @Valid @RequestBody submission with two invalid fields, using RFC 7807 ProblemDetail. What goes in the standard fields vs an extension property, and why prefer this over a hand-rolled {errorCode, message} DTO?',
    shortAnswer: 'Standard fields: status=400, title="Validation failed", detail with a short human summary, type as a URI identifying the error category. The field-level breakdown (e.g. {"email": "must not be blank", "age": "must be >= 18"}) goes in an extension property via setProperty("errors", ...), since RFC 7807 explicitly allows additional members beyond the core five. Preferring ProblemDetail over a custom DTO means every error across the API — validation, not-found, conflict — shares one predictable, spec-documented shape that any RFC 7807-aware client tooling already understands, instead of every team inventing its own ad hoc envelope.',
    detailedAnswer: 'ProblemDetail (Spring Framework 6+, default in Boot 3) models RFC 7807: type (a URI reference identifying the problem type, defaults to "about:blank"), title (short summary), status, detail (specific explanation), instance (URI of the specific occurrence), plus arbitrary extension members added via setProperty. Content type is application/problem+json. The value over a hand-rolled DTO is less about capability — a custom DTO can express the same fields — and more about consistency and interoperability: it is a documented standard rather than a bespoke convention every new engineer has to learn, and libraries/gateways that understand RFC 7807 can parse it generically.',
    keyPoints: [
      'type/title/status/detail/instance are the RFC-defined core; anything else is an extension property',
      'application/problem+json content type distinguishes it from a generic JSON error body',
      'Standardization value compounds across a large API surface more than any single endpoint benefits',
    ],
    seniorFollowUps: [
      'Would you expose the raw field name from the DTO (e.g. "email") in the error response, or a client-facing alias — what breaks if the DTO field gets renamed later?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['problem-detail', 'rfc7807', 'error-response-design'],
  },
  {
    id: 'sb-valid-q11',
    question: 'Where should a NullPointerException thrown deep inside a repository call ultimately be turned into an HTTP response — in the repository, the service, or a global handler — and why?',
    shortAnswer: 'A global handler (a catch-all @ExceptionHandler(Exception.class) in @ControllerAdvice), not the repository or service. Business code should not know about HTTP status codes, and catching broadly at the source just to build a ResponseEntity duplicates that logic everywhere. The global fallback logs the full exception server-side and returns a generic, sanitized message — never the raw exception text.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['exception-architecture', 'layering'],
  },
  {
    id: 'sb-valid-q12',
    question: 'A teammate proposes catching business exceptions inside every @Service method and building the ResponseEntity there, so "the service returns exactly what the controller needs." What is the architectural cost of this, and what would you propose instead?',
    shortAnswer: 'It couples business logic to the web layer: the same service can no longer be reused from a batch job, message listener, or another service without dragging HTTP semantics along, and every method duplicates similar try/catch-and-shape code. Prefer services throwing specific, meaningful exceptions (OrderNotFoundException, InsufficientStockException) and translating them to HTTP responses in exactly one place — a global @ControllerAdvice — keeping the service layer transport-agnostic.',
    detailedAnswer: 'This is a classic layering violation dressed up as convenience. The service layer\'s job is to enforce business invariants and signal failure in a way meaningful to any caller, not just an HTTP controller. When services build ResponseEntity or reference HttpStatus directly, that logic cannot be reused by a scheduled job, a Kafka listener, or a gRPC handler without either duplicating a parallel error-shaping path or awkwardly unwrapping the HTTP-flavored result. The alternative — throw a small hierarchy of domain exceptions and let a single @ControllerAdvice map each to its HTTP status and body — keeps the mapping decision in exactly one place, makes it trivial to add a new client to the service without touching it, and makes tests of the service layer simpler (assert an exception type, not a ResponseEntity).',
    keyPoints: [
      'Domain exceptions belong to the business layer; HTTP status mapping belongs to the web layer',
      'Centralizing translation in @ControllerAdvice avoids duplicated error-shaping code',
      'A reusable service (batch job, message consumer) cannot depend on HTTP-specific return types',
    ],
    seniorFollowUps: [
      'How would you structure the domain exception hierarchy so the global handler can map broad categories (not-found, conflict, validation) without an @ExceptionHandler per concrete exception class?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['architecture', 'exception-handling', 'layering', 'design-judgment'],
  },
  {
    id: 'sb-valid-q13',
    question: 'A production incident review finds that a 500 error response included the full exception message: "could not execute statement; SQL [insert into users ...]; constraint [uk_users_email]". What is the risk here, and what configuration or code change prevents it going forward?',
    shortAnswer: 'This leaks internal schema details (table/column/constraint names, that the DB is likely relational) to an external client, which helps an attacker map the system and is unnecessary information disclosure. Fix it by ensuring the catch-all exception handler never forwards ex.getMessage() for unexpected exceptions — log the full detail server-side and return a generic message — and confirm server.error.include-stacktrace / include-message are not set to always in production.',
    topic: 'spring-boot',
    subtopic: 'sb-validation-exceptions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['security', 'information-leakage', 'error-response'],
  },

  // ── Spring Data JPA Questions ──────────────────────────────────
{
    id: 'sb-jpa-q1',
    question: 'Why does virtually every Spring Data repository just extend JpaRepository directly instead of picking CrudRepository or PagingAndSortingRepository?',
    shortAnswer: 'JpaRepository extends both, plus adds JPA-specific extras (saveAndFlush, deleteInBatch, getReferenceById), so extending it gives everything with no downside for typical application code. Extending a narrower interface is a deliberate restriction, useful mainly for storage-agnostic library code that shouldn\'t assume JPA.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['repository-hierarchy', 'jparepository'],
  },
  {
    id: 'sb-jpa-q2',
    question: 'Concretely, how does a Spring Data repository interface become a working bean with no implementation class written anywhere?',
    shortAnswer: 'At startup, a RepositoryFactoryBean scans for interfaces extending Repository and generates a JDK dynamic proxy implementing that interface. The proxy delegates standard CRUD to SimpleJpaRepository and routes custom finder methods through the derived-query or @Query execution path, which is parsed and validated during context startup.',
    detailedAnswer: 'The proxy is not a hand-generated class at compile time — it\'s a java.lang.reflect.Proxy (or CGLIB in some paths) created reflectively when the ApplicationContext starts. Each method call is dispatched to a RepositoryFactorySupport-produced query executor: standard methods (save, findById, delete) go to SimpleJpaRepository, which wraps an EntityManager; derived-name methods (findByX) get their JPQL built from the parsed method name against the entity metamodel; @Query-annotated methods get their JPQL/SQL parsed once and cached. Because parsing happens eagerly, a malformed method name or invalid query string fails the application at startup rather than surfacing as a runtime exception on first call.',
    keyPoints: [
      'RepositoryFactoryBean + dynamic proxy, not a generated implementation class',
      'CRUD delegates to SimpleJpaRepository; custom methods route through derivation or @Query',
      'Query parsing/validation happens at startup, which is why bad method names fail fast',
    ],
    seniorFollowUps: [
      'What happens if you need custom logic that neither derivation nor @Query can express — how do you extend the proxy?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['repository-proxy', 'internals'],
  },
  {
    id: 'sb-jpa-q3',
    question: 'A derived query method findByCustomerLastNameAndStatusAndCreatedAtBetweenAndTotalGreaterThanOrderByCreatedAtDesc compiles and works fine. What\'s the actual argument for rewriting it, given it already works?',
    shortAnswer: 'It works, but the method name is now a maintenance liability: every reader has to parse the naming grammar to understand the query, adding one more condition makes it longer still, and it can\'t express things like a join-based filter, an aggregation, or a DTO projection. Past 2-3 conditions, @Query (or a Specification for dynamic combinations) is more readable and more flexible for the same or less effort.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['derived-query-methods', 'readability'],
  },
  {
    id: 'sb-jpa-q4',
    question: 'When would you reach for a native SQL query over JPQL in a repository method, and what do you give up by doing so?',
    shortAnswer: 'Native SQL is needed for database-specific features JPQL can\'t express — window functions, CTEs, vendor query hints, or full-text search functions. You give up database portability (the query is tied to that SQL dialect), and you lose JPQL\'s startup-time validation against the entity metamodel, since native SQL isn\'t checked against mapped properties.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['query-annotation', 'jpql', 'native-sql'],
  },
  {
    id: 'sb-jpa-q5',
    question: 'Your bulk-insert job switched an entity\'s ID generation from SEQUENCE to IDENTITY (because a junior dev thought it looked simpler) and insert throughput dropped noticeably. Explain what happened.',
    shortAnswer: 'IDENTITY relies on the database to generate the key on insert, so Hibernate must execute each insert individually to learn the generated ID before it can process the next entity — this disables JDBC statement batching for that entity type. SEQUENCE lets Hibernate pre-allocate a block of IDs from a database sequence in memory (per allocationSize) and assign them before touching the database, preserving batch inserts.',
    detailedAnswer: 'With IDENTITY, the generated-value contract requires the ID to be known immediately after each row is inserted (since it may be needed for a subsequent operation or a returned reference), so Hibernate cannot defer or coalesce those inserts into a JDBC batch — each becomes its own round trip. SEQUENCE generators, by contrast, ask the database for the next value(s) of a sequence up front; with an allocationSize > 1, Hibernate grabs a block of IDs in one round trip and hands them out to entities before any insert happens, meaning the actual inserts can be batched normally via hibernate.jdbc.batch_size. The tradeoff is that allocationSize must match the sequence\'s actual INCREMENT BY or you get ID gaps (harmless) or, worse, if misconfigured, ID collisions.',
    keyPoints: [
      'IDENTITY = one round trip per insert, no batching, because the key isn\'t known until the DB assigns it',
      'SEQUENCE = pre-allocated ID blocks in memory, inserts remain batchable',
      'allocationSize must match the DB sequence INCREMENT BY',
    ],
    seniorFollowUps: [
      'Why was IDENTITY historically the default choice on MySQL specifically?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['id-generation', 'sequence', 'identity', 'batch-insert'],
  },
  {
    id: 'sb-jpa-q6',
    question: 'You add an OrderItem to an Order\'s items collection (the mappedBy side) and call orderRepository.save(order). The item never gets persisted with a valid order_id. What\'s wrong?',
    shortAnswer: 'The mappedBy side of a bidirectional relationship is inverse — it doesn\'t control the foreign key and JPA doesn\'t cascade a write from it. The @ManyToOne side (OrderItem.order) is the owner and actually holds the FK column, so you must set item.setOrder(order) on the owning side; the parent\'s collection is only for object-graph navigation unless you also call setOrder yourself (often via a helper method that keeps both sides in sync).',
    detailedAnswer: 'JPA only persists what the owning side of a relationship declares. In a @OneToMany(mappedBy = "order") / @ManyToOne pair, OrderItem.order is the owning reference because it\'s the side with the actual foreign-key column in the database. Order.items with mappedBy is purely informational to Hibernate — "read this collection by querying the other side\'s FK," not "write these rows." Adding to that collection changes the in-memory object graph but produces no SQL by itself. The standard fix is a helper method on the aggregate root, e.g. addItem(item) that does items.add(item); item.setOrder(this);, so both directions of the association are always kept consistent by construction rather than relying on callers to remember both calls.',
    keyPoints: [
      'mappedBy side is inverse/read-only for persistence purposes',
      '@ManyToOne side owns the foreign key and must be set explicitly',
      'A synchronized helper method on the aggregate root avoids this class of bug',
    ],
    seniorFollowUps: [
      'How does this same ownership rule apply to a @ManyToMany relationship, where neither side has an obvious FK column?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['relationships', 'mappedby', 'ownership'],
  },
  {
    id: 'sb-jpa-q7',
    question: 'A code reviewer flags a @RestController that returns a JPA @Entity directly from a GET endpoint. What are the concrete risks, beyond "it\'s not best practice"?',
    shortAnswer: 'Serializing a managed entity outside the persistence context can throw LazyInitializationException on any unfetched association the serializer touches. It also couples the API contract to the persistence model, so a schema refactor becomes a breaking API change, and it risks leaking fields (internal flags, audit columns, other entities reachable via relationships) that were never meant to be public.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dto-projection', 'entity-exposure', 'api-design'],
  },
  {
    id: 'sb-jpa-q8',
    question: 'What\'s the practical difference between an interface-based projection and a class-based (constructor expression) DTO projection in Spring Data JPA?',
    shortAnswer: 'An interface projection is a plain interface with getters matching a subset of entity properties — for a "closed" projection (getters only, no derived logic), Spring Data can generate a query that selects only those columns. A class-based projection uses a JPQL constructor expression (new com.example.Dto(...)) to build an arbitrary DTO, including computed fields or values joined from other entities, giving more control at the cost of writing the query explicitly.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dto-projection', 'interface-projection'],
  },
  {
    id: 'sb-jpa-q9',
    question: 'Within a single @Transactional method, you call orderRepository.findById(id) twice with the same ID. Are you guaranteed to get the same Java object reference back both times? Does calling save() matter here?',
    shortAnswer: 'Yes — within one persistence context (one EntityManager, scoped to the transaction), the first-level cache acts as an identity map, so both calls return the exact same object reference without a second SELECT. And no, save() isn\'t what makes a mutation persist: a managed entity is dirty-checked automatically, so simply mutating a field is enough for the change to be written on flush, typically at transaction commit.',
    detailedAnswer: 'The persistence context tracks every entity it has loaded, keyed by its identity (entity type + primary key). A second find() for an ID already in that map short-circuits to the cached instance rather than issuing SQL — this is why code that fetches an entity, hands it to two different methods, and mutates it in both sees consistent state without any explicit synchronization. This guarantee is scoped to the persistence context: fetch the same row in a different transaction (different EntityManager) and you get a distinct object instance with equal but not identical state. Dirty checking works by comparing each managed entity\'s current field values against a snapshot taken when it was loaded; anything different gets included in the UPDATE issued at flush. Flush usually happens at commit (or earlier, if a query would otherwise return stale results), which is why an explicit save() call is redundant for already-managed entities — it\'s required only for genuinely new/detached entities.',
    keyPoints: [
      'First-level cache = identity map, scoped to one persistence context/transaction',
      'Dirty checking makes explicit save() unnecessary for managed entities',
      'Flush (usually at commit) is what actually produces SQL, not the mutation itself',
    ],
    seniorFollowUps: [
      'What happens to dirty checking once the entity becomes detached, and how do you get changes on a detached entity persisted again?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['persistence-context', 'dirty-checking', 'first-level-cache'],
  },
  {
    id: 'sb-jpa-q10',
    question: 'A search endpoint accepts five optional filters that can appear in any combination. Why is neither a derived query method nor a single @Query a good fit, and what would you use instead?',
    shortAnswer: 'Five independent optional filters means up to 2^5 combinations of which are present — neither a fixed method name nor a single static query can express "filter by whichever fields the caller actually supplied" without either writing every combination or resorting to messy null-checking JPQL. A Specification<T> (via JpaSpecificationExecutor) lets you build the predicate programmatically, composing only the conditions for filters that are actually non-null.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['specification', 'criteria-api', 'dynamic-queries'],
  },
  {
    id: 'sb-jpa-q11',
    question: 'You\'ve added @CreatedBy and @LastModifiedBy to an entity and enabled @EnableJpaAuditing, but both fields stay null on every insert. What\'s the most likely missing piece?',
    shortAnswer: 'Auditing needs a registered AuditorAware<T> bean to tell Spring Data how to resolve "the current user" — without one, @CreatedDate/@LastModifiedDate still populate (they need no external input) but @CreatedBy/@LastModifiedBy have nothing to pull from and stay null. The entity also needs @EntityListeners(AuditingEntityListener.class), directly or via a shared @MappedSuperclass, or the listener never fires at all.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['auditing', 'auditoraware'],
  },
  {
    id: 'sb-jpa-q12',
    question: 'True or false: PagingAndSortingRepository is a prerequisite for using Pageable with a Spring Data repository method.',
    shortAnswer: 'False in current Spring Data versions — Pageable/Sort support is available on any repository through the query-method mechanism itself; PagingAndSortingRepository specifically contributes the findAll(Pageable) and findAll(Sort) convenience methods, but a JpaRepository (which already extends it) or even a custom finder method can accept a Pageable parameter regardless of which base interface you extend.',
    topic: 'spring-boot',
    subtopic: 'sb-data-jpa',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['pagingandsorting', 'pageable'],
  },

  // ── Transaction Management Questions ──────────────────────────────────────────

  {
    id: 'sb-tx-q1',
    question: 'A developer calls a @Transactional method from another method in the same @Service class, and on failure nothing rolls back — no exception is thrown about it either. What\'s happening, and how would you fix it?',
    shortAnswer: 'Self-invocation bypasses Spring\'s AOP proxy entirely — the call goes through `this`, not through the proxy that intercepts @Transactional, so the inner method silently runs with no transaction at all. Fix by moving the method to a separate bean and calling it through that bean, or by injecting a self-reference (e.g. a @Lazy-autowired proxy of the class itself) and calling through that.',
    detailedAnswer: 'Spring\'s declarative transaction management works by wrapping the target bean in a proxy at startup. External callers get a reference to this proxy, and any call through it is intercepted to open/commit/rollback a transaction around the method. But a call from one method to another *inside the same object* — even in the same class — never goes through the proxy; it\'s a direct JVM method call on `this`. Since the interception logic lives entirely in the proxy, that inner call\'s @Transactional annotation is just inert metadata. There is no exception because nothing failed from Spring\'s point of view — it simply never engaged.',
    keyPoints: [
      'Root cause: proxy-based AOP only intercepts calls arriving through the proxy, not internal `this` calls',
      'Fix 1: extract the method into a separate collaborator bean, call it via dependency injection',
      'Fix 2: self-inject a @Lazy reference to the bean\'s own proxy and call through that reference',
      'AspectJ compile-time/load-time weaving is an alternative that avoids this limitation entirely, at the cost of a more complex build',
    ],
    seniorFollowUps: [
      'How would you catch this class of bug in code review or with tooling before it reaches production?',
      'Would switching to AspectJ weaving be worth it just to fix this? What are the tradeoffs?',
    ],
    example: 'saveOrder() called from placeOrder() in the same OrderService class never runs in a transaction, even though saveOrder() is annotated @Transactional.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['transactional', 'proxy', 'self-invocation', 'aop'],
  },
  {
    id: 'sb-tx-q2',
    question: 'You need an audit-log entry to persist even when the business operation that triggered it later fails and rolls back. How do you design this with @Transactional, and what does it cost you?',
    shortAnswer: 'Put the audit-log write in its own method on a separate bean, annotated @Transactional(propagation = Propagation.REQUIRES_NEW). REQUIRES_NEW suspends the caller\'s transaction and runs the audit write in its own independent transaction that commits immediately, so it survives even if the outer transaction later rolls back. The cost is a second physical database transaction/connection for every call, which adds connection-pool pressure and means the audit entry can be visible to other readers before the outer operation has even finished.',
    detailedAnswer: 'REQUIRED (the default propagation) would make the audit write part of the same transaction as the caller, so a rollback of the outer operation would take the audit entry down with it — defeating the purpose. REQUIRES_NEW physically suspends the outer transaction\'s resources, opens a brand-new transaction on (usually) a second connection, and commits it independently before control returns to the caller. This must be invoked through the Spring proxy (i.e., from a different bean, not via self-invocation) for the new propagation setting to actually take effect.',
    keyPoints: [
      'REQUIRED shares the caller\'s transaction (rolls back together); REQUIRES_NEW is fully independent (commits/rolls back on its own)',
      'Must call across a bean boundary — self-invocation would silently ignore the propagation setting',
      'Cost: extra connection checked out from the pool for the duration of the nested call',
      'The audit row becomes visible to other transactions immediately on commit, ahead of the outer transaction\'s own outcome',
    ],
    seniorFollowUps: [
      'What happens if the REQUIRES_NEW audit write itself fails — does it affect the outer transaction?',
      'Under what load conditions would you avoid REQUIRES_NEW here and reach for something else, like an async event or outbox pattern instead?',
    ],
    example: 'auditService.logAttempt(order) on a separate AuditService bean, annotated @Transactional(propagation = Propagation.REQUIRES_NEW), called from OrderService.placeOrder().',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Expert',
    seniority: 'Senior',
    tags: ['propagation', 'requires_new', 'audit-log', 'transactional'],
  },
  {
    id: 'sb-tx-q3',
    question: 'A @Transactional method writes to two tables, then throws a checked FileProcessingException. QA reports that both writes committed anyway. Is this a bug in Spring?',
    shortAnswer: 'No — it\'s Spring\'s documented default behavior. Rollback only triggers automatically for unchecked exceptions (RuntimeException/Error); checked exceptions are treated as expected/recoverable outcomes and are allowed to commit. Fix by adding rollbackFor = FileProcessingException.class (or FileProcessingException.class extends a designated rollback superclass) to the @Transactional annotation.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['rollback', 'checked-exceptions', 'transactional'],
  },
  {
    id: 'sb-tx-q4',
    question: 'Explain the difference between REPEATABLE_READ and SERIALIZABLE isolation in terms of what anomaly each does and doesn\'t prevent, and why choosing SERIALIZABLE everywhere isn\'t free.',
    shortAnswer: 'REPEATABLE_READ guarantees that if you read the same row twice in a transaction you get the same value (no non-repeatable reads), but by the SQL standard it can still permit phantom reads — a range query returning a different set of rows on a second execution because another transaction inserted matching rows. SERIALIZABLE closes that gap by making transactions behave as if fully serialized. The cost is much heavier locking or serialization-conflict aborts, which throttles concurrency and can force retries under contention — so it\'s reserved for the few operations that truly need it, not applied blanket.',
    detailedAnswer: 'The anomaly hierarchy is: dirty read < non-repeatable read < phantom read < full serializability. Each isolation level up the chain closes one more class of anomaly at increasing cost. Note that real databases don\'t always match the standard exactly — MySQL InnoDB\'s REPEATABLE_READ uses MVCC snapshots plus gap locking on indexed ranges, which in practice blocks phantom reads for most common query patterns, something PostgreSQL\'s REPEATABLE_READ does not do (Postgres would let a phantom insert land, though its actual implementation uses snapshot isolation with its own conflict-detection nuances). This means "REPEATABLE_READ" is not a fully portable guarantee across database vendors — you have to know the specific engine\'s implementation, not just the isolation level name, when correctness depends on it.',
    keyPoints: [
      'Non-repeatable read: same row, different value on re-read within one transaction',
      'Phantom read: same range query, different row set on re-read',
      'SERIALIZABLE cost: heavy locking, or optimistic engines abort/retry on detected conflicts',
      'Engine-specific behavior varies — InnoDB REPEATABLE_READ behaves more strictly than the SQL standard requires',
    ],
    seniorFollowUps: [
      'How would you diagnose in production whether a bug was actually caused by an isolation-level anomaly versus an application logic error?',
      'If you can\'t afford SERIALIZABLE\'s throughput cost, what application-level techniques (optimistic locking, explicit row locks) achieve a similar guarantee for just the hot path that needs it?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['isolation', 'phantom-read', 'repeatable-read', 'serializable'],
  },
  {
    id: 'sb-tx-q5',
    question: 'You mark a reporting method @Transactional(readOnly = true) for a performance boost. A teammate later adds a small "fix the count" write inside it during a hotfix. What actually happens?',
    shortAnswer: 'readOnly=true is only a hint, not an enforced constraint — Spring does not block the write. What happens next depends entirely on the JPA provider and driver: Hibernate might still flush and persist the change (since FlushMode.MANUAL suppresses automatic flush-before-query, not an explicit save), some drivers configured for a read-only connection might throw, and others will simply let the write through and commit it. You cannot rely on readOnly as a safety net — it needs to be enforced by code review or static analysis, not the runtime.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['readonly', 'transactional', 'hibernate'],
  },
  {
    id: 'sb-tx-q6',
    question: 'A @Transactional method catches an exception thrown by a repository call, logs it, and returns normally without rethrowing. The developer expected the transaction to roll back because the underlying exception was a RuntimeException. Why didn\'t it?',
    shortAnswer: 'Spring\'s proxy decides whether to roll back based on whether an exception propagates out of the method call — it inspects what actually escapes, not what was thrown internally. Catching the exception and not rethrowing means the proxy sees a normal, successful method return, so it commits. To force a rollback while still handling the exception locally, call TransactionAspectSupport.currentTransactionStatus().setRollbackOnly() inside the catch block instead of just swallowing it.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['rollback', 'exception-handling', 'transactional'],
  },
  {
    id: 'sb-tx-q7',
    question: 'When would you reach for TransactionTemplate / programmatic transaction management instead of @Transactional?',
    shortAnswer: 'When only part of a method needs to be transactional (e.g. an expensive non-transactional fetch followed by a short transactional write), or when the transaction boundary depends on a runtime condition that a static annotation can\'t express. It also sidesteps the self-invocation limitation for cases where splitting into another bean feels like unnecessary architectural overhead just to satisfy the proxy.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['transactiontemplate', 'programmatic-transactions'],
  },
  {
    id: 'sb-tx-q8',
    question: 'What is the difference between NESTED and REQUIRES_NEW propagation, and why can\'t you always substitute one for the other?',
    shortAnswer: 'REQUIRES_NEW suspends the outer transaction and opens a completely independent one on its own connection — it commits or rolls back on its own, unaffected by the outer transaction\'s eventual outcome. NESTED runs inside the same physical transaction and connection as the caller but marks a savepoint, so a failure there can roll back just to that savepoint while the outer transaction continues and still commits everything before it — but NESTED requires a resource manager that supports JDBC savepoints, and its failure is scoped by the outer transaction catching the exception, unlike REQUIRES_NEW\'s already-committed independence.',
    detailedAnswer: 'They solve different problems that look superficially similar. REQUIRES_NEW is for "this must survive independently of the outer transaction\'s fate" (audit logs, notification records). NESTED is for "this is a sub-step I want to be able to partially undo without aborting everything" (e.g. attempting several optional enrichment steps where one failing shouldn\'t kill the whole batch, but you still want atomicity with the rest of the work in one commit). Using REQUIRES_NEW for the NESTED use case is more expensive (second connection) and semantically wrong if you actually wanted the enrichment step\'s success to be part of the same commit unit as everything else. Using NESTED for the audit-log use case is wrong because if the outer transaction as a whole rolls back, the savepoint-scoped write rolls back with it — the opposite of what "must survive independently" requires.',
    keyPoints: [
      'REQUIRES_NEW: separate connection, separate commit, truly independent outcome',
      'NESTED: same connection, savepoint-based partial rollback, still bound to the outer transaction\'s ultimate commit/rollback',
      'NESTED needs nestedTransactionAllowed support (works with DataSourceTransactionManager on most JDBC drivers; not guaranteed under JTA)',
    ],
    seniorFollowUps: ['If your transaction manager doesn\'t support savepoints, what alternative would you use to approximate NESTED semantics?'],
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['propagation', 'nested', 'requires_new', 'savepoint'],
  },
  {
    id: 'sb-tx-q9',
    question: 'What does Propagation.MANDATORY guard against, and where would you actually use it?',
    shortAnswer: 'MANDATORY throws an exception if the method is called without an already-active transaction, rather than silently starting one or running without one. It\'s useful on internal helper/utility methods that should only ever be invoked from within a larger transactional operation — it turns a "this was supposed to already be in a transaction" assumption into a fail-fast check instead of a silent correctness bug.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['propagation', 'mandatory'],
  },
  {
    id: 'sb-tx-q10',
    question: 'Your integration test is annotated @Transactional and verifies that a REQUIRES_NEW audit write survived a simulated failure in the outer operation, but the test framework\'s auto-rollback makes you unsure whether you\'re actually testing the right thing. What\'s going on?',
    shortAnswer: 'REQUIRES_NEW commits independently from the test transaction, so the test rollback does not remove its audit row. Verify it through a separate transaction or connection.',
    detailedAnswer: 'Spring wraps a @Transactional test in an outer transaction that normally rolls back after the test. A REQUIRES_NEW method suspends that transaction, opens and commits a separate physical transaction, then resumes the test transaction. The audit row therefore persists. Query it outside the wrapping transaction, or use an explicit commit boundary, to verify the behavior accurately.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['testing', 'transactional', 'requires_new', 'rollback'],
  },
  {
    id: 'sb-tx-q11',
    question: 'A service needs to atomically update its own database and publish a message to another system as one unit — the classic "dual write" problem. Would you reach for JTA/XA transactions in a modern Spring Boot microservice?',
    shortAnswer: 'Usually no. Prefer a transactional outbox: commit business data and an outbox row together, then publish asynchronously. XA offers atomicity but adds coordination, lock time, and operational cost.',
    detailedAnswer: 'JTA/XA can coordinate an XA-capable database and broker with two-phase commit, but it requires compatible drivers and a transaction coordinator while holding resources across both systems. A transactional outbox keeps the atomic boundary local: store the business change and event record together, then relay the event with retries and idempotency.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['jta', 'xa', 'distributed-transactions', 'outbox'],
  },
  {
    id: 'sb-tx-q12',
    question: 'What\'s the practical difference between setting a transaction timeout via @Transactional(timeout = N) and relying on a downstream HTTP client\'s own timeout?',
    shortAnswer: 'A transaction timeout bounds the whole transaction and its database resources. An HTTP timeout bounds only that network call, so both protect against different stalls.',
    topic: 'spring-boot',
    subtopic: 'sb-transactions',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['timeout', 'transactional', 'connection-pool'],
  },
  // ── JPA Performance & Pitfalls Questions ──────────────────────────────────────────

  {
    id: 'sb-perf-q1',
    question: 'An endpoint that lists 500 orders and shows each customer\'s name takes 3 seconds. The order query itself, run alone, takes 20ms. Diagnose what\'s happening and give two different fixes with different tradeoffs.',
    shortAnswer: 'Classic N+1: the order list query is fast, but Order.customer is LAZY and accessed once per order in the response-building loop, firing 500 extra single-row SELECTs. Fix with JOIN FETCH/@EntityGraph to collapse it to one query (risky if you also paginate or fetch another collection), or with batch fetching (@BatchSize/default_batch_fetch_size) to bound it to a handful of IN-clause queries while staying pagination-safe.',
    detailedAnswer: 'Confirm first with SQL logging or Hibernate statistics — you should see 1 query for orders followed by ~500 near-identical single-row customer SELECTs. JOIN FETCH o.customer collapses this to one query but only works cleanly because customer is a *ToOne; if the endpoint also needed items (*ToMany) fetch-joined at the same time, that would risk MultipleBagFetchException or row multiplication. Batch fetching is safer as a default fix: it doesn\'t change cardinality of the base query and composes fine with pagination, at the cost of still being N/batchSize+1 queries rather than 1.',
    keyPoints: [
      'Fast base query + high total latency scaling with row count is the fingerprint of N+1, not a slow single query',
      'JOIN FETCH is best for *ToOne or a single *ToMany without pagination; batch fetching is safer when pagination or multiple collections are involved',
      'Verify the fix by checking the actual query count dropped, not just that the page loads faster'
    ],
    seniorFollowUps: ['How would you make sure a future PR can\'t silently reintroduce this N+1?'],
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['n-plus-one', 'diagnosis', 'lazy-loading'],
  },
  {
    id: 'sb-perf-q2',
    question: 'A team adds Pageable to a repository method that does JOIN FETCH o.items (a @OneToMany List) and finds page 1 returns the right count but page 2 is missing rows that exist in the database. What\'s going wrong?',
    shortAnswer: 'Hibernate can\'t apply SQL LIMIT/OFFSET to a query that fetch-joins a *ToMany, because the join multiplies each parent row by its number of items — applying LIMIT at the SQL level would cut off items mid-collection. Hibernate detects this and instead pulls the full multiplied result set into memory and paginates there, which is both slow and semantically wrong once the in-memory page boundary doesn\'t line up with distinct parent entities.',
    detailedAnswer: 'The safe pattern is a two-step fetch: run the paginated query without the collection fetch-join to get the correct page of parent IDs, then a second query with JOIN FETCH WHERE id IN (:pageIds) to hydrate the collection for just that page. Alternatively use @EntityGraph or batch fetching (@BatchSize) for the collection instead of an explicit JOIN FETCH, since batch fetching doesn\'t change query cardinality and is pagination-safe by design.',
    keyPoints: [
      'LIMIT/OFFSET on a fetch-joined *ToMany is unreliable because the join multiplies rows before pagination applies',
      'Fix: paginate first (no collection join), then hydrate associations in a second query keyed on the page\'s IDs',
      'Batch fetching sidesteps the issue entirely since it never changes the base query\'s row count'
    ],
    seniorFollowUps: ['Would using a Set instead of a List for items change this behavior?'],
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['pagination', 'join-fetch', 'gotcha'],
  },
  {
    id: 'sb-perf-q3',
    question: 'Why does Spring Boot enable Open Session In View by default, and why do many senior teams turn it off?',
    shortAnswer: 'OSIV keeps the Persistence Context (and its DB connection) open through the whole request so lazy fields can still be accessed during view rendering or JSON serialization without throwing LazyInitializationException. Teams disable it because that convenience hides N+1 and lazy-loading problems that should be solved explicitly at the service layer, and it holds a DB connection for the entire request lifecycle — including template rendering or downstream calls — increasing connection pool pressure under load.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['osiv', 'lazy-loading', 'connection-pool'],
  },
  {
    id: 'sb-perf-q4',
    question: 'A bulk import inserts 10,000 new rows through a JPA repository with hibernate.jdbc.batch_size=50 configured, but SQL logging shows individual INSERT statements, not batches. What\'s the likely cause?',
    shortAnswer: 'The entity almost certainly uses GenerationType.IDENTITY for its primary key. IDENTITY requires Hibernate to execute each INSERT immediately to learn the generated key before it can process the next entity, which disables JDBC batching regardless of the batch_size setting. Switching to SEQUENCE (with a larger allocationSize) or TABLE generation restores batching because IDs can be pre-allocated before the inserts run.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['batch-insert', 'identity', 'id-generation'],
  },
  {
    id: 'sb-perf-q5',
    question: 'What\'s the real difference between JOIN FETCH and @EntityGraph, and when would you reach for one over the other?',
    shortAnswer: '@EntityGraph is a declarative annotation on a repository method that lets you eagerly load named associations without hand-writing JPQL, so it composes well with Spring Data\'s derived query methods. JOIN FETCH is written directly into JPQL, giving more explicit control (e.g. combining with a WHERE clause or ordering) at the cost of writing the query by hand. Both produce a single SQL join and both share the same collection-pagination and multi-bag-fetch limitations.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['join-fetch', 'entitygraph', 'fetch-strategy'],
  },
  {
    id: 'sb-perf-q6',
    question: 'When would enabling Hibernate\'s second-level cache actually hurt rather than help?',
    shortAnswer: 'For entities that are frequently updated, or in a horizontally-scaled deployment without a properly distributed/invalidated cache provider, the second-level cache introduces stale-read risk that outweighs the read savings. It\'s a good fit for rarely-changing, read-heavy lookup data accessed by ID across many transactions — not for frequently-written transactional entities where the cache would be constantly invalidated or, worse, silently stale across nodes.',
    detailedAnswer: 'The cache is keyed and invalidated per entity on write within the same JVM/cache cluster, so if writes happen through a different process or a non-JPA path (a batch SQL update, another service writing to the same table), the second-level cache doesn\'t know to invalidate and serves stale data indefinitely. In a multi-instance deployment, each instance\'s local cache also needs a distributed backend (or replication) to stay consistent — without that, one instance\'s write is invisible to another instance\'s cached read.',
    keyPoints: [
      'Best fit: rarely-changing, read-heavy, ID-keyed lookup data',
      'Worst fit: frequently-written entities, or multi-instance deployments without a properly distributed cache backend',
      'Writes outside the JPA/Hibernate path (raw SQL, another service) bypass cache invalidation entirely'
    ],
    seniorFollowUps: ['How would you decide the right cache concurrency strategy (READ_ONLY vs READ_WRITE) for a given entity?'],
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['second-level-cache', 'caching', 'stale-data'],
  },
  {
    id: 'sb-perf-q7',
    question: 'Should you ever change an association from LAZY to EAGER to "fix" a LazyInitializationException? Why or why not?',
    shortAnswer: 'No — that trades a loud, immediately visible exception for a silent, permanent cost applied to every load of that entity everywhere in the codebase, even code paths that never needed the association. The correct fix is to load what\'s needed during the active Persistence Context, using JOIN FETCH, @EntityGraph, or a DTO projection at the point the data is actually required.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['lazy-loading', 'eager', 'antipattern'],
  },
  {
    id: 'sb-perf-q8',
    question: 'How would you actually confirm N+1 is happening in a running application, beyond just reading the entity mappings?',
    shortAnswer: 'Reading mappings only tells you an association is LAZY, not whether a given code path accesses it inside a loop — that requires observing real query counts. Enable Hibernate SQL logging or statistics (hibernate.generate_statistics, SessionFactory.getStatistics()) or attach a JDBC proxy like p6spy/datasource-proxy, then look for a base query followed by a repeated near-identical query pattern scaling with result size.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['diagnosis', 'sql-logging', 'n-plus-one'],
  },
  {
    id: 'sb-perf-q9',
    question: 'A read-only endpoint returns 5 fields from an entity that has 15 columns and 3 lazy associations. What\'s the performance case for using a DTO projection instead of returning the entity?',
    shortAnswer: 'A DTO projection\'s SELECT only pulls the columns actually needed, and since no managed entity is returned there\'s no lazy proxy left to accidentally trigger extra queries or a LazyInitializationException later in serialization. It sidesteps both the column-fetch overhead and the entire fetch-strategy/N+1 risk class by construction, at the cost of losing dirty-checking and cascading writes, which read paths don\'t need anyway.',
    topic: 'spring-boot',
    subtopic: 'sb-jpa-performance',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['dto-projection', 'performance'],
  },
  // ── Spring Security Fundamentals Questions ──────────────────────────────────────────

  {
    id: 'sb-sec-q1',
    question: 'Why is Spring Security implemented as a chain of many single-responsibility filters instead of one filter that does authentication, authorization, and exception handling together?',
    shortAnswer: 'Each filter owns one concern (extracting credentials from a form vs a Basic header, translating security exceptions into HTTP responses, making the final authorization decision), so mechanisms can be added, removed, or reordered independently. It also lets a shared final step (authorization) work the same way regardless of which authentication filter populated the Authentication.',
    detailedAnswer: 'Composability is the main driver: adding OAuth2 resource server support, for example, is just inserting another filter/provider pair into the existing chain rather than rewriting a monolith. It also isolates failure handling — ExceptionTranslationFilter is the single place that converts AuthenticationException/AccessDeniedException into the right HTTP response, so individual filters don\'t each need their own error-formatting logic. Ordering is critical: authentication filters must run before the authorization filter at the end of the chain, since authorization needs a populated Authentication to evaluate against.',
    keyPoints: [
      'Single-responsibility filters compose independently — new auth mechanisms are additive, not rewrites',
      'ExceptionTranslationFilter centralizes security-exception-to-HTTP-response mapping',
      'Order is load-bearing: authentication must precede the authorization filter at chain\'s end',
    ],
    seniorFollowUps: [
      'How would you insert a custom filter to validate an API key header before the standard authentication filters run?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['filter-chain', 'architecture'],
  },
  {
    id: 'sb-sec-q2',
    question: 'A REST API disables CSRF protection because "tokens don\'t need it." Under what circumstance does that reasoning break down?',
    shortAnswer: 'It breaks down if any part of the API still authenticates via a browser-attached session cookie instead of a header-only token — CSRF exploits the browser\'s automatic cookie attachment, which a bearer-token-in-header scheme never triggers, but a lingering cookie-based endpoint reopens the exact attack CSRF protection exists to stop.',
    detailedAnswer: 'CSRF disabling is safe specifically because a stateless, header-based bearer token is never sent automatically by the browser on a cross-site request — an attacker\'s page has no mechanism to attach a header the way it can rely on cookie auto-attachment. The reasoning fails the moment any endpoint accepts session-cookie auth as a fallback (e.g. a mixed SPA that also sets a session cookie for some legacy route, or a "remember me" cookie used for auto-login), because that endpoint is now cookie-authenticated with no CSRF token requirement. It also fails if the token itself is stored in a cookie (common with some SPA setups) rather than only sent via an explicit Authorization header, since a cookie-stored token gets auto-attached too.',
    keyPoints: [
      'CSRF disabling is only safe if auth is genuinely header-only, never cookie-based, anywhere in the API',
      'A token stored in a cookie (vs an Authorization header) reintroduces auto-attachment and thus CSRF exposure',
      'Mixed auth models (some cookie routes, some token routes) need per-scope CSRF decisions, not a blanket disable',
    ],
    seniorFollowUps: [
      'If a frontend team insists on storing the JWT in a cookie for XSS-mitigation reasons, what do you need to re-enable to stay safe?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['csrf', 'configuration', 'rest-api'],
  },
  {
    id: 'sb-sec-q3',
    question: 'A colleague calls one @PreAuthorize-annotated public method from another method inside the same @Service class, and the authorization check silently never fires. Why?',
    shortAnswer: 'Method-level security is implemented via an AOP proxy wrapping the bean. A call from another method within the same class is a direct self-invocation on `this`, bypassing the proxy entirely, so the advice (and the @PreAuthorize check) never runs — the same self-invocation gotcha that affects @Transactional.',
    detailedAnswer: 'Spring creates a proxy (JDK dynamic proxy or CGLIB) around the bean; external callers go through that proxy, which is where the @PreAuthorize interceptor is applied. Internal calls use the raw `this` reference, never touching the proxy, so any advice attached only at the proxy layer — transactions, caching, and method security alike — is skipped. Fixes mirror the @Transactional fix: inject the bean into itself and call through the injected reference, split the secured method into a separate bean, or use AspectJ compile/load-time weaving instead of proxy-based AOP so the interception isn\'t proxy-dependent.',
    keyPoints: [
      'Method security runs through an AOP proxy; self-invocation calls the raw object, not the proxy',
      'Same root cause and same fixes as the @Transactional self-invocation gotcha',
      'AspectJ weaving avoids the issue entirely by not relying on a proxy',
    ],
    seniorFollowUps: [
      'Would splitting the two methods into separate Spring beans reliably fix this, and why?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['method-security', 'aop', 'preauthorize'],
  },
  {
    id: 'sb-sec-q4',
    question: 'Why does BCryptPasswordEncoder produce a different hash every time you encode the same password, and why is that a feature rather than a bug?',
    shortAnswer: 'BCrypt generates a random salt per invocation and embeds it in the output, so identical passwords produce different stored hashes. This defeats precomputed rainbow-table attacks and stops an attacker from spotting that two accounts share a password just by comparing stored hashes.',
    detailedAnswer: 'The salt is stored inline as part of the BCrypt output string (along with the algorithm version and cost factor), so matches() can extract it and re-derive the hash for comparison — you never need a separate salt column. Because the salt is random per password, no single precomputed rainbow table can attack every hash in the database at once; an attacker has to brute-force each hash individually. This also closes an information leak: without salting, matching stored hashes would reveal that two users chose the same password, which itself is exploitable information.',
    keyPoints: [
      'Salt is random per encode and embedded in the stored hash string, no separate column needed',
      'Defeats rainbow tables — no single precomputed table covers every possible salt',
      'Prevents leaking "these two users share a password" via identical stored hashes',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['password-encoding', 'bcrypt'],
  },
  {
    id: 'sb-sec-q5',
    question: 'What is the practical difference between CORS and CSRF, and why do people conflate them?',
    shortAnswer: 'CORS relaxes the browser\'s same-origin policy to let a page on one origin call an API on another and read the response; CSRF is an attack where a malicious page triggers an authenticated cross-site request using the victim\'s existing session. They\'re conflated because both are about cross-origin browser requests, but CORS governs whether a script can read a response, while CSRF exploits that the browser sends the request (with cookies) regardless of CORS.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['cors', 'csrf'],
  },
  {
    id: 'sb-sec-q6',
    question: 'Why does going stateless (SessionCreationPolicy.STATELESS) make it harder to instantly revoke a logged-in user\'s access compared to session-based auth?',
    shortAnswer: 'A stateful session lives server-side, so invalidating it immediately blocks further requests. A stateless bearer token is self-contained and verified without a server-side lookup, so it stays valid until it expires unless you add extra infrastructure (a token blacklist/allowlist, short expiry plus refresh, or a revocation check) to fake the same immediacy.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['session-management', 'stateless', 'tradeoffs'],
  },
  {
    id: 'sb-sec-q7',
    question: 'In an authorizeHttpRequests block, why does rule order matter, and what\'s the risk of putting a broad permitAll matcher before a more specific one?',
    shortAnswer: 'Rules are evaluated in declaration order and the first match wins, so a broad matcher declared first will shadow a more specific rule that comes after it — e.g. permitAll on "/api/**" declared before a requestMatcher locking down "/api/admin/**" means the admin rule never gets evaluated, silently exposing the admin path.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['configuration', 'authorization', 'httpsecurity'],
  },
  {
    id: 'sb-sec-q8',
    question: 'Your Spring Boot app exposes Actuator on the same port as the API and someone finds /actuator/heapdump publicly accessible. What went wrong from a Spring Security configuration standpoint?',
    shortAnswer: 'Actuator endpoints aren\'t secured by default just because the rest of the app requires authentication — they need their own explicit authorizeHttpRequests rule (or should be moved to a separate management port and firewalled). A blanket anyRequest().authenticated() only helps if the Actuator paths actually match that rule rather than being permitted or bypassed elsewhere.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['actuator', 'misconfiguration'],
  },
  {
    id: 'sb-sec-q9',
    question: 'What does AuthenticationManager delegate to when there are several possible ways to authenticate (e.g. username/password plus LDAP), and how does it decide which one succeeded?',
    shortAnswer: 'ProviderManager (the standard AuthenticationManager) holds an ordered list of AuthenticationProvider beans, each declaring which Authentication type it supports via supports(). It tries each supporting provider in order and accepts the first one that authenticates successfully, rather than requiring every provider to agree.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['authenticationmanager', 'authenticationprovider'],
  },
  {
    id: 'sb-sec-q10',
    question: 'Why should loadUserByUsername in a custom UserDetailsService avoid revealing whether the failure was "user not found" vs "wrong password"?',
    shortAnswer: 'Distinguishing the two responses lets an attacker enumerate valid usernames/emails by observing which error they get, turning a login form into a username-discovery oracle. Both cases should surface as the same generic authentication failure to the caller.',
    topic: 'spring-boot',
    subtopic: 'sb-security-core',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['userdetailsservice', 'security-practices'],
  },
  // ── OAuth2 & JWT Questions ────────────────────
  {
    id: 'sb-oauth-q1',
    question: "A partner integration lets service A fetch a token from your shared authorization server, and that same token turns out to work against service B's API too, even though B never issued it. What's misconfigured, and how do you fix it?",
    shortAnswer: "Service B isn't validating the aud (audience) claim, so it accepts any token signed by the trusted issuer regardless of which service it was actually issued for. Fix by adding an explicit audience validator to each resource server's JwtDecoder so a token must name that specific service as its intended audience.",
    detailedAnswer: "Spring's default JwtDecoder validates signature, exp, and nbf, but does not validate iss or aud unless you add it. In a multi-service setup sharing one authorization server, every resource server trusts the same issuer, so signature verification alone can't distinguish 'a token meant for A' from 'a token meant for B' — only the aud claim does that. The fix is a custom OAuth2TokenValidator<Jwt> (or delegating validator combining the default timestamp validator with an audience check) registered on the JwtDecoder, rejecting any token whose aud doesn't include this service's expected value. The authorization server also needs to actually populate distinct audiences per client/resource when issuing tokens, or the fix has nothing to check against.",
    keyPoints: [
      'Signature + issuer trust does not imply the token was meant for this service',
      'Missing aud validation is one of the most common real-world OAuth2 misconfigurations',
      'Requires cooperation from the auth server (setting aud) and each resource server (checking it)',
    ],
    seniorFollowUps: [
      'How would you retrofit audience validation across a fleet of existing microservices without a coordinated deploy?',
      "What's the blast radius if this went undetected in production for six months?",
    ],
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jwt', 'audience-validation', 'vulnerability', 'resource-server'],
  },
  {
    id: 'sb-oauth-q2',
    question: "Walk through how an attacker exploits a JWT verifier that picks its verification algorithm based on the token's own alg header, and how Spring Security's standard config avoids it.",
    shortAnswer: "The attacker takes the authorization server's public RSA key (which is public by design) and uses it as the HMAC secret to sign a forged token with alg=HS256. A verifier that trusts the incoming alg header switches to HMAC verification using what it thinks is a shared secret but is actually the public key, and the forged signature validates. Spring Security's JwtDecoder built from a JWKS URI sidesteps this because it resolves the verification key and algorithm from server-side configuration, not from the untrusted token header.",
    detailedAnswer: "This is the classic RS256-to-HS256 downgrade. Because RS256 public keys are, by definition, publicly distributed (via the JWKS endpoint), nothing stops an attacker from obtaining one. If the verifying code does something like 'read alg from the token, then call the matching verify function with a key it looks up generically,' and that key lookup for HS256 happens to resolve to the same PEM/string as the RSA public key, the attacker can forge a signature the naive verifier accepts as valid. The core mistake is letting attacker-controlled input (the alg header) drive a security-critical decision (which verification strategy and which key to use). Spring's NimbusJwtDecoder configured via jwkSetUri or a JwtDecoders.fromIssuerLocation factory pins the expected signing algorithm(s) server-side and resolves keys by kid from the JWKS set — the token's claimed algorithm doesn't dictate the verification path.",
    keyPoints: [
      'Public RSA key doubles as a usable HMAC secret if a verifier is naive about algorithm selection',
      "Root cause: trusting the token's own header to choose the verification method",
      'Mitigation: pin expected algorithm(s) server-side, never derive from the token',
    ],
    seniorFollowUps: [
      'Would you trust a third-party JWT library by default here, or explicitly audit its algorithm handling — what would you look for?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jwt', 'algorithm-confusion', 'security', 'vulnerability'],
  },
  {
    id: 'sb-oauth-q3',
    question: 'Your team wants to log a specific user out immediately (compromised account) but access tokens are stateless JWTs valid for another 45 minutes. What are your actual options, and what do you give up with each?',
    shortAnswer: "Options: shorten access token TTL going forward (reduces but doesn't eliminate the window), maintain a server-side deny-list/blacklist checked on every request (reintroduces state and a lookup cost, defeating pure statelessness), or switch to opaque tokens with introspection (revocation becomes instant but adds a network call per request). In practice, revoking the refresh token immediately and accepting that the current access token remains valid until its short expiry is the common pragmatic tradeoff.",
    detailedAnswer: "This is the core statelessness-vs-revocability tension. A pure JWT design has no server-side record of issued tokens, so 'kill this token' has no natural mechanism — the token is self-validating until exp. Three real approaches: (1) keep access tokens genuinely short (2-15 min) so any compromise self-heals quickly, and revoke the refresh token immediately so no new access token can be minted — the residual risk window is bounded by the access token TTL; (2) maintain a lightweight deny-list (e.g. Redis, keyed by jti) checked on each request — this reintroduces a stateful check but only for revoked tokens, keeping the common path fast; (3) drop JWTs for opaque tokens entirely, paying an introspection round-trip per request in exchange for instant, exact revocation. Most production systems land on (1) plus refresh-token revocation as the pragmatic default, escalating to (2) only for genuinely high-risk scenarios like a confirmed account compromise.",
    keyPoints: [
      "Stateless JWTs fundamentally can't be revoked before expiry without adding state back",
      'Short TTL + refresh token revocation bounds the exposure window without a deny-list',
      'Deny-list and opaque-token-with-introspection both trade some statelessness for exact revocation',
    ],
    seniorFollowUps: [
      "How would you size the access token TTL — what factors into that decision beyond 'shorter is safer'?",
    ],
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['jwt', 'revocation', 'stateless', 'refresh-token'],
  },
  {
    id: 'sb-oauth-q4',
    question: 'Why is the OAuth2 implicit grant considered deprecated, and why does authorization code + PKCE replace it for the same SPA use case rather than just falling back to client credentials?',
    shortAnswer: "Implicit grant returns the access token directly in the browser's URL fragment with no client authentication step, exposing it to browser history, referrer leakage, and any script on the page. Auth code + PKCE keeps the token exchange server-side (or in a dynamically-generated code-verifier the browser holds only briefly) and never puts the token in a URL. Client credentials doesn't fit an SPA use case because it has no concept of a logged-in user — it's for service identity, not delegated user authorization.",
    detailedAnswer: "The implicit grant was designed for public clients (SPAs) that can't safely hold a client secret, returning the access token straight in the redirect URI's fragment. Two problems: the fragment can leak (browser history, extensions, referrer headers in some configurations), and there's no code-exchange step to bind the token request to a specific, verified request. PKCE (Proof Key for Code Exchange) fixes the auth-code flow for public clients instead: the client generates a random code_verifier, sends its hashed form (code_challenge) with the initial authorization request, then must present the original code_verifier when exchanging the authorization code for a token. Even if the authorization code is intercepted, an attacker without the code_verifier can't complete the exchange. Client credentials is a different grant entirely — no user is involved, it authenticates the application itself for machine-to-machine calls, so it can't represent 'this specific logged-in user authorized this app.'",
    keyPoints: [
      'Implicit grant exposes tokens via URL fragment with no client authentication',
      'PKCE binds the code exchange to the original request via a verifier/challenge pair',
      'Client credentials has no user context — wrong tool for delegated user auth',
    ],
    seniorFollowUps: ["For a mobile app, why is PKCE still required even though it's not a browser?"],
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['oauth2', 'pkce', 'implicit-grant', 'grant-types'],
  },
  {
    id: 'sb-oauth-q5',
    question: "A microservice validates JWTs using a shared HMAC secret with the authorization server. What's the risk as you add more resource servers to the system, and what would you migrate to?",
    shortAnswer: 'Every resource server holding the same HMAC secret can also forge valid tokens, since HMAC verification and signing use the same key — a compromise of any one service compromises the ability to mint tokens for the whole system. Migrate to asymmetric signing (RS256/ES256): the auth server keeps the private signing key, and resource servers verify with the public key published at a JWKS endpoint, so no resource server can forge tokens.',
    detailedAnswer: "HMAC (HS256) verification uses the exact same secret that was used to sign, so any party capable of verifying a token is, by construction, also capable of forging one. In a single-service setup that's fine — the one service that verifies is the only one that would ever need to sign. Once multiple independent resource servers share that secret purely so each can verify tokens, you've handed all of them signing power they don't need, and a breach of the least-secure one lets an attacker mint tokens accepted everywhere. Asymmetric signing (RS256/ES256) breaks that coupling: the authorization server alone holds the private key and signs; every resource server holds only the public key (fetched and cached from the JWKS endpoint, keyed by kid for rotation) and can verify but never sign. This is also why Spring's JwtDecoder built from jwkSetUri/issuer-uri is the standard config for anything beyond a single trusted service.",
    keyPoints: [
      'HMAC verification key == signing key, so every verifier is also a forger',
      'Asymmetric signing separates signing (auth server, private key) from verification (resource servers, public key)',
      'JWKS + kid header supports key rotation without redeploying resource servers',
    ],
    seniorFollowUps: ['How would you migrate a live system off a shared HMAC secret without an auth outage?'],
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['jwt', 'hmac', 'rsa', 'jwks'],
  },
  {
    id: 'sb-oauth-q6',
    question: 'When would you choose opaque tokens with introspection over JWTs for a resource server, despite the extra network hop on every request?',
    shortAnswer: "When instant, exact revocation matters more than per-request latency — e.g. high-value financial actions or admin sessions where a compromised token must be killable immediately, not just left to expire. Opaque tokens also avoid exposing any claims to the client, which matters if the token's metadata itself is sensitive.",
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['opaque-token', 'introspection', 'jwt', 'revocation'],
  },
  {
    id: 'sb-oauth-q7',
    question: 'Your @PreAuthorize("hasAuthority(\'SCOPE_read\')") checks are silently failing even though the token clearly has a read scope. What\'s the likely mismatch?',
    shortAnswer: "Spring's default JwtAuthenticationConverter maps the scope/scp claim to authorities with a SCOPE_ prefix — but if a custom JwtAuthenticationConverter was configured (or the claim name isn't scope/scp), the mapped authority name won't match what @PreAuthorize expects. Check the actual GrantedAuthority names produced by the converter against what the annotation checks for.",
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['spring-security', 'scopes', 'authorities', 'jwt-converter'],
  },
  {
    id: 'sb-oauth-q8',
    question: "Why shouldn't an OAuth2 scope granted to a client application be treated as equivalent to that user's application-level role or permission?",
    shortAnswer: "A scope describes what the client app was authorized to request on the user's behalf, not what the user themselves is allowed to do — an over-broadly-scoped client shouldn't be able to bypass the app's own RBAC. Treat scope as an additional gate layered on top of, not a replacement for, application-level authorization checks.",
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['scopes', 'authorization', 'rbac'],
  },
  {
    id: 'sb-oauth-q9',
    question: 'Why is storing an access token in localStorage generally considered riskier than an httpOnly cookie, and what does the cookie approach cost you in return?',
    shortAnswer: 'localStorage is readable by any JavaScript running on the page, so a single XSS vulnerability anywhere in the app can exfiltrate the token; an httpOnly cookie is invisible to page JavaScript, closing that theft path. In exchange, cookies reintroduce CSRF as a concern, typically mitigated with SameSite=Lax/Strict plus, where needed, an explicit CSRF token for state-changing requests.',
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['token-storage', 'xss', 'csrf', 'cookies'],
  },
  {
    id: 'sb-oauth-q10',
    question: "What does refresh token rotation protect against that a single long-lived refresh token doesn't?",
    shortAnswer: "Without rotation, a stolen refresh token stays valid until its full expiry with no way to notice the theft. With rotation, each use issues a new refresh token and invalidates the old one — if a previously-used (rotated-out) token is ever presented again, that's a strong signal of theft, and the whole token family can be revoked immediately.",
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['refresh-token', 'rotation', 'token-theft'],
  },
  {
    id: 'sb-oauth-q11',
    question: 'Why is the resource-owner-password grant considered an anti-pattern for third-party client integrations?',
    shortAnswer: 'It requires the user to hand their raw username and password directly to the client application, which then forwards them to the authorization server — the client sees the actual credentials, defeating the entire purpose of delegated authorization and training users to trust random apps with their password.',
    topic: 'spring-boot',
    subtopic: 'sb-oauth-jwt',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['password-grant', 'oauth2', 'anti-pattern'],
  },
  // ── Testing Spring Boot Applications Questions ────────────────────
  {
    id: 'sb-test-q1',
    question: "A CI suite of ~300 tests takes 25 minutes. Nearly every test class is annotated @SpringBootTest, some with a @MockBean or two sprinkled in inconsistently. What's actually driving the runtime, and how would you fix it?",
    shortAnswer: "Two compounding costs: @SpringBootTest boots the full application context per unique configuration, and each inconsistent @MockBean changes that context's cache signature, so Spring can't reuse contexts across test classes — most tests end up paying a fresh context startup. Fix it by moving layer-scoped tests to slice annotations (@WebMvcTest, @DataJpaTest) that need far less context, and centralizing shared mock configuration (a common base class or @TestConfiguration) so the classes that do need a broader context at least share one cache key.",
    detailedAnswer: "Context startup dominates suite time far more than the actual test bodies once you're past a handful of classes. @SpringBootTest instantiates every bean in the app — all auto-configuration, all @Component/@Service/@Repository — which for a nontrivial app is seconds per startup. Spring's TestContext framework tries to help by caching contexts keyed on their full configuration signature: config classes, active profiles, property sources, and critically, declared @MockBean/@SpyBean types. If two test classes both use @SpringBootTest but one mocks OrderService and another mocks PaymentService, those are two different cache keys — two full context builds, even though both classes are 'basically' the same integration test shape. Multiply that by inconsistent mocking across 300 classes and caching effectively stops helping. The fix has two parts: (1) push tests down to the narrowest slice that can prove the behavior — most controller tests don't need JPA, most repository tests don't need MVC, so @WebMvcTest/@DataJpaTest/@JsonTest load a fraction of the beans and start much faster; (2) for the tests that genuinely need @SpringBootTest, standardize the mock/config surface (shared base class, consistent @TestConfiguration) so they collapse onto one cache key instead of many.",
    keyPoints: [
      '@SpringBootTest cost scales with total bean count, not test complexity',
      'Context cache key includes @MockBean/@SpyBean types — inconsistent mocking multiplies distinct contexts',
      'Slice annotations reduce the beans loaded, which is the direct lever on startup time',
      'Centralizing mock/config setup restores cache hits across classes that would otherwise diverge',
    ],
    seniorFollowUps: [
      'How would you detect which test classes are causing cache misses without manually diffing every annotation?',
      'Is there a scenario where a slower @SpringBootTest suite is still the right tradeoff over heavier use of slices?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['spring-boot-test', 'context-caching', 'mockbean', 'performance'],
  },
  {
    id: 'sb-test-q2',
    question: "A @DataJpaTest passes locally and in CI, but the same query fails against the real Postgres database in staging. What's the most likely explanation, and how do you close the gap without giving up fast unit-level tests entirely?",
    shortAnswer: '@DataJpaTest defaults to an embedded H2 database, whose SQL dialect and behavior diverge from Postgres — a native query, JSON column, or Postgres-specific function that H2 tolerates differently (or not at all) can pass in the test and fail in production. Close the gap by using @AutoConfigureTestDatabase(replace = Replace.NONE) with Testcontainers running real Postgres for the tests that exercise dialect-sensitive SQL, while keeping H2 (or no swap needed) for tests that are genuinely dialect-agnostic.',
    detailedAnswer: "The root cause is a false sense of coverage: @DataJpaTest's embedded-database default exists purely for test speed and convenience, and Spring silently substitutes it for whatever DataSource the app would normally use. For simple CRUD via Spring Data repository methods this substitution is usually harmless. It stops being harmless the moment SQL gets dialect-specific — native @Query strings, Postgres JSON/array types, window functions, or locking clauses H2 emulates imperfectly. The test suite reports green because H2 either accepts syntax Postgres wouldn't, or interprets semantics slightly differently, so a real bug never surfaces until staging. The practical fix isn't to abandon fast tests wholesale — it's to be deliberate about which repository tests actually need Testcontainers-backed Postgres (anything touching native SQL or DB-specific features) versus which are safe on H2 (simple generated queries). Testcontainers with a singleton/reused container pattern keeps the Postgres-backed subset reasonably fast even with real Docker startup cost.",
    keyPoints: [
      '@DataJpaTest swaps in H2 by default via auto-configured test database replacement',
      'H2 dialect divergence (native SQL, JSON types, locking) is the actual failure surface',
      'Replace.NONE + Testcontainers opts specific tests into real-database fidelity',
      'Not every repository test needs Testcontainers — reserve it for dialect-sensitive queries',
    ],
    seniorFollowUps: [
      'How would you structure the test suite so contributors know which tests need Testcontainers versus which can stay on H2?',
      "What's the tradeoff of starting one Testcontainers instance per test class versus one shared container for the whole suite?",
    ],
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['data-jpa-test', 'testcontainers', 'h2', 'integration-testing'],
  },
  {
    id: 'sb-test-q3',
    question: "A test verifies a service persists an entity inside a @Transactional test method, then asserts the row exists via a raw JDBC query in the same test. The assertion sometimes fails even though the entity manager clearly has the object. Why, and what's the fix?",
    shortAnswer: "Hibernate batches and delays writes until flush time, and inside a rolled-back test transaction that write may still only exist in the persistence context, not yet issued as SQL — a raw JDBC read outside that persistence context won't see it. Call TestEntityManager.flush() (or entityManager.flush()) before the raw query to force the SQL to actually execute against the database within the same transaction.",
    detailedAnswer: "Hibernate's first-level cache means a persist() call doesn't necessarily emit SQL immediately — Hibernate is free to delay the INSERT/UPDATE until it decides a flush is needed (end of transaction, an explicit flush() call, or when a query that could be affected by pending changes is about to run). Inside a test, entityManager.find() or a repository read against the same persistence context will transparently see the pending change because Hibernate resolves it from its own session state, creating the illusion that the write already happened. A raw JDBC query, or any query issued through a separate connection/session, bypasses that persistence context entirely and only sees what has actually been flushed to the database. Since the test's @Transactional wraps everything in one transaction that rolls back at the end, timing matters: without an explicit flush, the raw JDBC check can run before Hibernate has decided to flush, and the assertion sees stale (pre-write) state. TestEntityManager.flush() (or the underlying EntityManager's flush()) forces Hibernate to synchronize its session state to the database immediately, so subsequent raw reads within the same transaction see the write.",
    keyPoints: [
      'Hibernate delays writes until flush, not at persist() call time',
      'Reads through the same persistence context resolve pending writes transparently; raw JDBC/other connections do not',
      "flush() forces SQL to execute without needing a commit (which the test transaction won't reach anyway)",
      'This is a common source of flaky assertions in @DataJpaTest-style tests mixing ORM and raw SQL checks',
    ],
    seniorFollowUps: [
      'Would the same issue occur if the raw JDBC query used the exact same DataSource connection as the EntityManager?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['transactional', 'test-entity-manager', 'flush', 'data-jpa-test'],
  },
  {
    id: 'sb-test-q4',
    question: "Why can't @Transactional test rollback be relied on to validate behavior that depends on a transaction actually committing, such as an @TransactionalEventListener(phase = AFTER_COMMIT) or a database trigger?",
    shortAnswer: "The test's transaction is deliberately rolled back at the end of the method, so it never reaches the commit point — anything gated on an actual commit (AFTER_COMMIT listeners, DB triggers, another connection observing the row) simply never fires during the test, regardless of whether the in-memory state looks correct.",
    detailedAnswer: "Spring's test-managed transaction wraps the whole test method and rolls back afterward specifically so tests don't leave persistent side effects for later tests to trip over. That's a testing-framework behavior, not something @Transactional itself does in production — in production the transaction commits normally. The problem is that some application behavior is intentionally coupled to the commit event itself, not to the in-transaction state: AFTER_COMMIT listeners, outbox-pattern publishers, DB triggers, or a second connection reading committed data. None of those fire inside a transaction that never commits, so a test relying on default rollback semantics will show the entity looks persisted (visible within the same transaction) while completely failing to exercise the commit-triggered logic. To actually test that logic you need either a transaction that really commits (disable the default rollback, or use TestTransaction.flagForCommit() plus explicit commit, or run outside a test-managed transaction and clean up manually) or a full integration test against a running service boundary.",
    keyPoints: [
      'Test rollback is a test-framework convenience, not production @Transactional behavior',
      'AFTER_COMMIT listeners, triggers, and separate-connection reads require an actual commit to observe',
      'In-transaction visibility (via the same EntityManager) is not proof commit-time logic ran',
      'Testing commit-coupled behavior requires disabling default rollback or a true integration test',
    ],
    seniorFollowUps: [
      'How would you structure a test to safely commit data and still clean up afterward without polluting other tests?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['transactional', 'rollback', 'event-listener', 'gotcha'],
  },
  {
    id: 'sb-test-q5',
    question: 'When would you reach for WebTestClient over MockMvc even in a plain Spring MVC (non-reactive) application?',
    shortAnswer: "When the test needs to run against an actually-running server rather than dispatching in-process — WebTestClient's bindToServer() mode can hit a real bound port over the network (e.g. black-box tests, or verifying behavior that depends on the real servlet container/filters), which MockMvc's in-process dispatch can't do. MockMvc remains the faster, simpler default for pure @WebMvcTest controller-layer checks.",
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['mockmvc', 'webtestclient', 'web-layer'],
  },
  {
    id: 'sb-test-q6',
    question: "A @WebMvcTest for OrderController fails to start with a NoSuchBeanDefinitionException for OrderService, even though OrderService is a perfectly normal @Service bean in the app. What's happening?",
    shortAnswer: '@WebMvcTest only auto-configures the web layer — controllers, converters, @ControllerAdvice, and MVC infrastructure — it deliberately does not instantiate @Service or @Repository beans. OrderController still needs OrderService injected, so the test must supply it as a @MockBean; without one, context creation fails wiring the controller.',
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['web-mvc-test', 'test-slices', 'mockbean'],
  },
  {
    id: 'sb-test-q7',
    question: "What's the difference in intent between @MockBean and @SpyBean, and why does the choice matter beyond just 'mock vs partial mock'?",
    shortAnswer: "@MockBean replaces the bean entirely with a mock (no real logic runs unless stubbed); @SpyBean wraps the real bean so its actual methods execute unless explicitly stubbed. Beyond behavior, both change the test's application-context cache signature identically, so switching between them (or adding either) still costs a fresh context — the choice is about what the test needs to verify, not about avoiding the caching cost.",
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['mockbean', 'spybean', 'mocking'],
  },
  {
    id: 'sb-test-q8',
    question: "Why is reaching for @DirtiesContext as a default fix for 'flaky tests that seem to affect each other' a code smell rather than a solution?",
    shortAnswer: '@DirtiesContext forces a full context rebuild after the annotated test, which papers over shared mutable state instead of fixing it, and it defeats context caching for every test that would otherwise share that context — a suite that reaches for it liberally trades correctness debugging for a much slower suite. The better fix is usually resetting the specific mutated state in @BeforeEach/@AfterEach.',
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dirties-context', 'context-caching', 'test-hygiene'],
  },
  {
    id: 'sb-test-q9',
    question: 'What does @JsonTest auto-configure, and when would you reach for it instead of a broader slice?',
    shortAnswer: "@JsonTest configures only the Jackson (or Gson) infrastructure — ObjectMapper, registered modules, and any custom JsonComponent/serializers/deserializers — with JacksonTester/GsonTester helpers for asserting serialized JSON shape. Use it to unit-test custom (de)serialization logic in isolation, without paying for MVC or persistence infrastructure you don't need for that concern.",
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['json-test', 'test-slices', 'jackson'],
  },
  {
    id: 'sb-test-q10',
    question: 'Is @SpringBootTest(webEnvironment = RANDOM_PORT) with TestRestTemplate a slice test or a full integration test?',
    shortAnswer: "It's a full integration test — RANDOM_PORT starts a real embedded servlet container on an actual port, and the whole application context (not a slice) is loaded, so requests go through the genuine HTTP stack end to end. It's the heaviest and slowest option, reserved for a small number of true end-to-end checks.",
    topic: 'spring-boot',
    subtopic: 'sb-testing',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['spring-boot-test', 'random-port', 'integration-testing'],
  },
  // ── Spring Boot Actuator & Observability Questions ────────────────────
  {
    id: 'sb-act-q1',
    question: 'A postmortem found that /actuator/env was reachable from the public internet and leaked a database password. The team had never explicitly configured actuator exposure. How did this happen, and what are the two independent layers that should have prevented it?',
    shortAnswer: "Someone likely set management.endpoints.web.exposure.include=* to make debugging easier and never reverted it, exposing every endpoint including /env. The two independent layers that should have caught this: exposure config should only ever include what's actually needed (never *  in production), and actuator endpoints should sit behind their own SecurityFilterChain or separate management port so exposure alone isn't the only line of defense.",
    detailedAnswer: "By default only /health is web-exposed, so this required an explicit include=* (or an include list that happened to contain env). That alone isn't fatal if actuator also requires authentication — the deeper failure is that the app's security config didn't have a filter chain matching /actuator/**, so requests fell through to a permissive default. Defense in depth here means: (1) never use include=* in production, enumerate only what's needed; (2) register a dedicated, higher-precedence SecurityFilterChain scoped to EndpointRequest.toAnyEndpoint() requiring authentication/authorization; (3) consider a separate management.server.port that isn't internet-routable at all, so even a security misconfiguration doesn't reach the public network; (4) rely on Spring Boot's property sanitization for known secret-key patterns as a last-resort mitigation, not a primary control.",
    keyPoints: [
      'Web exposure and security are separate, both must be configured correctly',
      'include=* is a common accidental-exposure footgun',
      'Separate management port adds network-level isolation independent of app-level auth bugs',
    ],
    seniorFollowUps: [
      'How would you write an automated check in CI to catch an accidental include=* before it reaches production?',
      "What's the tradeoff of putting actuator on a separate port versus the same port with path-based security rules?",
    ],
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['actuator', 'security', 'production-incident'],
  },
  {
    id: 'sb-act-q2',
    question: "Pods running your service started restart-looping whenever a downstream payment provider had a brief outage, even though the app itself was fine. What's the likely root cause and fix?",
    shortAnswer: "The payment provider's health check was probably wired into the liveness probe group instead of readiness. Liveness failures cause Kubernetes to kill and restart the pod, which does nothing to fix an external outage and just adds churn — that check belongs in readiness, so the pod is pulled from the load balancer but stays alive and recovers automatically once the dependency comes back.",
    detailedAnswer: "Spring Boot separates liveness (is this process internally broken and unrecoverable) from readiness (can this instance currently serve traffic). A custom HealthIndicator is added to a health group via management.endpoint.health.group.<name>.include, and it's easy to mistakenly add an external-dependency check to the liveness group, or to leave group assignment at a default that lumps everything together. The fix is to explicitly assign dependency-health checks to the readiness group only, verify with management.endpoint.health.group.liveness.include restricted to livenessState (and any genuinely internal indicators), and confirm via /actuator/health/liveness and /actuator/health/readiness that they diverge correctly under a simulated dependency failure.",
    keyPoints: [
      'Liveness = internal process health, restart is the correct remedy',
      'Readiness = current ability to serve traffic, removal from LB is the correct remedy',
      'External dependency checks belong in readiness, not liveness',
    ],
    seniorFollowUps: [
      'How would you test this distinction locally before it reaches production, given liveness/readiness are primarily a Kubernetes-integration concern?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['actuator', 'kubernetes', 'health-checks', 'readiness', 'liveness'],
  },
  {
    id: 'sb-act-q3',
    question: "Your Prometheus instance's memory usage grew tenfold within a week after a team started instrumenting a new endpoint with custom Micrometer metrics. What's the most likely cause and how do you confirm it?",
    shortAnswer: "Almost certainly unbounded tag cardinality — a tag whose value set isn't small and known in advance (e.g. user ID, raw request path, session ID) turns into a new time series per unique value. Confirm by checking Prometheus's per-metric series count (e.g. via count by (__name__)({...})) for the new metric and looking at which tag is driving it.",
    detailedAnswer: "Micrometer creates one distinct time series per unique combination of tag values on a meter. A tag like user_id or a non-templated raw URL path effectively has unbounded cardinality, so every new user or request adds a permanent new series that Prometheus must store and index forever (or until retention expires). This is exactly why Spring MVC's built-in http.server.requests timer templates the URI (/orders/{id}) rather than using the raw path. The fix is to remove or replace the offending tag with a bounded categorical value, and as a safety net register a MeterFilter that denies or caps tags matching risky patterns so a future mistake fails safe instead of silently degrading the metrics backend.",
    keyPoints: [
      'Cardinality = number of unique tag-value combinations, each becomes a separate time series',
      'Unbounded identifiers (user id, request id, raw path) are the classic culprit',
      "Spring's own http.server.requests timer templates paths specifically to avoid this",
    ],
    seniorFollowUps: [
      'How would you set up a MeterFilter as an organizational safety net rather than relying on code review alone?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['actuator', 'micrometer', 'metrics', 'cardinality', 'prometheus'],
  },
  {
    id: 'sb-act-q4',
    question: 'Why does Spring Boot only expose /health over HTTP by default, while auto-configuring dozens of other endpoint beans that exist but return 404?',
    shortAnswer: "Web exposure and endpoint bean creation are separate concerns by design — the bean existing on the classpath (e.g. because a dependency like a DataSource triggered a HealthIndicator) doesn't mean it should be reachable by anyone who can hit the app's HTTP port. Defaulting to deny-all except /health keeps the out-of-the-box posture safe while still letting load balancers do basic liveness checks.",
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['actuator', 'auto-configuration', 'exposure'],
  },
  {
    id: 'sb-act-q5',
    question: 'You add a custom HealthIndicator that calls a downstream service with no explicit timeout. What can go wrong under load, beyond the health check itself being slow?',
    shortAnswer: "If the downstream call hangs, the health check hangs, which can cause the readiness (or worse, liveness) probe to time out and fail — Kubernetes then pulls the pod from traffic or restarts it, compounding an already-struggling dependency's problems instead of isolating them. The indicator should use a short, explicit timeout and fail fast to DOWN rather than blocking.",
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['actuator', 'health-checks', 'timeouts'],
  },
  {
    id: 'sb-act-q6',
    question: "What's the difference between /actuator/metrics and /actuator/prometheus, and why do both exist?",
    shortAnswer: "/actuator/metrics is Micrometer's own generic, human-browsable JSON view of registered meters, available with just the core actuator dependency. /actuator/prometheus is the Prometheus-specific text-exposition-format scrape endpoint, which only appears once micrometer-registry-prometheus is added — Micrometer is backend-agnostic, so each registry implementation contributes its own format-specific endpoint.",
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['actuator', 'micrometer', 'prometheus'],
  },
  {
    id: 'sb-act-q7',
    question: "A teammate adds @Timed to a service method to capture latency but the metric never shows up in /actuator/metrics. What's missing?",
    shortAnswer: "@Timed is just an annotation — it does nothing unless a TimedAspect bean is registered to actually intercept the annotated method and record the timing via AOP. It's a common gotcha because the annotation compiles and appears to 'just work' with no error when the aspect bean is absent.",
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['actuator', 'micrometer', 'timed', 'aop'],
  },
  {
    id: 'sb-act-q8',
    question: 'During a rolling deployment, a few requests failed with connection errors right as old pods were terminating, even though graceful shutdown was enabled. What else is usually needed?',
    shortAnswer: "Kubernetes removes a terminating pod from service endpoints asynchronously, so there's a brief window after SIGTERM where traffic can still be routed to it even as the app begins shutting down. Pairing server.shutdown=graceful with a short preStop hook delay (giving endpoint propagation time to catch up before the app actually stops accepting connections) closes that race.",
    detailedAnswer: "server.shutdown=graceful only controls what the JVM does after it receives SIGTERM: stop accepting new requests, let in-flight ones finish (up to spring.lifecycle.timeout-per-shutdown-phase), then close. It says nothing about when Kubernetes actually stops sending new traffic. Kubernetes sends SIGTERM to the container and, in parallel, asynchronously updates the Service's Endpoints/EndpointSlice to remove the terminating pod — that propagation to kube-proxy and any in-cluster load balancers is not instantaneous or guaranteed to land before or exactly at SIGTERM. Requests already routed before the update propagates land on a pod that may have just stopped accepting new connections. The standard fix is a preStop lifecycle hook that sleeps a few seconds before the container actually receives SIGTERM, buying time for endpoint removal to propagate cluster-wide while the app is still fully up and accepting the tail of in-flight traffic.",
    keyPoints: [
      'Graceful shutdown only governs in-process behavior after SIGTERM, not traffic routing',
      'Endpoint removal from the Service is asynchronous relative to SIGTERM delivery',
      'A preStop sleep hook is the standard way to bridge that propagation gap',
    ],
    seniorFollowUps: [
      'How would you size the preStop delay versus the terminationGracePeriodSeconds to avoid the pod being SIGKILLed before it finishes?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['actuator', 'graceful-shutdown', 'kubernetes'],
  },
  {
    id: 'sb-act-q9',
    question: 'Why might disabling an actuator endpoint via management.endpoint.<id>.enabled=false be a stronger guarantee than just excluding it from web exposure?',
    shortAnswer: "Disabling the endpoint prevents its bean from being created at all, so there's nothing to accidentally expose later (e.g. via a careless include=* change or another actuator technology like JMX). Excluding it only from web exposure still leaves the bean present and reachable through any other enabled exposure channel.",
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['actuator', 'auto-configuration', 'endpoint-exposure'],
  },
  {
    id: 'sb-act-q10',
    question: "What does a Gauge measure differently from a Counter or Timer in Micrometer, and what's the classic mistake when registering one?",
    shortAnswer: 'A Gauge samples a current value on demand (e.g. queue size right now) rather than accumulating like a Counter or recording durations like a Timer. The classic mistake is registering a Gauge against a snapshot number instead of a live reference (like an AtomicInteger or a collection) — since Gauges are evaluated lazily on scrape, a captured static value never updates and the metric silently goes stale.',
    topic: 'spring-boot',
    subtopic: 'sb-actuator',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['actuator', 'micrometer', 'gauge'],
  },
  // ── Aspect-Oriented Programming Questions ────────────────────
  {
    id: 'sb-aop-q1',
    question: 'A public placeOrder() method calls this.auditOrder() inside the same bean, and auditOrder() carries a custom @Audited annotation. External calls to auditOrder() are logged, but calls from placeOrder() are not. Diagnose and redesign it.',
    shortAnswer: 'The external call enters the Spring proxy; self-invocation calls the target directly through this and bypasses the interceptor chain. Move auditOrder behind a separate Spring bean and call that dependency, or move the annotation to the externally invoked boundary. Self-injection and AopContext can force proxy traversal but create circularity and framework coupling.',
    detailedAnswer: 'Spring injects a proxy wherever the bean is referenced from outside, but once execution reaches the target, this is the target object. Calling this.auditOrder() therefore uses normal Java dispatch and no proxy gets a chance to inspect @Audited. The clean design is usually a real boundary: extract auditing behavior or the audited operation into another bean, making interception and testing explicit. If the intent is to audit the whole use case, annotate placeOrder() instead. Self-injection can work when carefully configured and AopContext.currentProxy requires exposing the proxy, but both hide a structural dependency and are poor defaults.',
    keyPoints: [
      'Advice runs only when invocation crosses the proxy',
      'Self-invocation bypasses Spring AOP',
      'Prefer a separate bean or annotate the public use-case boundary',
      'Avoid framework-coupled proxy lookups as the default fix',
    ],
    seniorFollowUps: [
      'Would AspectJ weaving intercept this call, and what operational cost would that introduce?',
      'How would you write a test proving both matched and unmatched calls?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['aop', 'self-invocation', 'proxy', 'design'],
  },
  {
    id: 'sb-aop-q2',
    question: 'A retry aspect and @Transactional both advise chargeCustomer(). Should retry wrap the transaction, or should the transaction wrap retry?',
    shortAnswer: 'Usually retry should be outside transaction advice so each attempt gets a fresh transaction; retrying inside one transaction can keep using a rollback-only or otherwise invalid persistence context. But the required semantics depend on what is retried, idempotency, and whether remote side effects participate in the transaction.',
    detailedAnswer: 'Advisor order creates nested control flow. With retry outside, each proceed call enters transaction advice, opens a transaction, commits or rolls it back, and returns control to the retry policy before another independent attempt. With transaction outside, all attempts occur inside one transaction; after a database failure the transaction or EntityManager may already be unusable, and a later successful method return may still end in UnexpectedRollbackException. Neither ordering makes a remote payment call atomic with a database transaction. That requires idempotency keys and an outbox, saga, or compensation design rather than assuming AOP composition supplies distributed atomicity.',
    keyPoints: [
      'Advisor order controls transaction lifetime per attempt',
      'Database failures can poison the current transaction',
      'Retries require idempotent operations',
      'A local transaction does not make remote side effects atomic',
    ],
    seniorFollowUps: [
      'Where should metrics advice sit if you need both per-attempt and end-to-end latency?',
      'Which failures should never be retried?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['aop', 'ordering', 'retry', 'transactions', 'idempotency'],
  },
  {
    id: 'sb-aop-q3',
    question: 'A generic logging aspect suddenly doubles API latency and exposes authorization headers after its pointcut is broadened during a package refactor. What failed in the design?',
    shortAnswer: 'The pointcut was treated as a convenience expression rather than a reviewed contract. It matched hot and sensitive paths, serialized arbitrary arguments, and likely advised more infrastructure than intended. Replace it with a narrow package plus marker annotation, redact by allowlist, avoid eager toString or body serialization, and add match-boundary and performance tests.',
    detailedAnswer: "Cross-cutting code multiplies its cost and risk by every matched invocation. Package-only expressions are fragile under refactors, while logging Object[] arguments can trigger expensive serialization, lazy-loading queries, recursion, or secret disclosure. Define an explicit @Observed or @Audited boundary, capture stable identifiers rather than whole objects, apply redaction before formatting, and exclude the aspect's collaborators. Test representative methods that must and must not match, then measure allocations and latency with production-like traffic. Framework tracing or Micrometer observation is preferable when it already supplies the required signal safely.",
    keyPoints: [
      'Pointcut breadth determines blast radius',
      'Argument logging can execute expensive or unsafe code',
      'Use explicit annotations and allowlisted fields',
      'Verify negative matches and overhead',
    ],
    seniorFollowUps: [
      'How can lazy JPA entities make argument logging unexpectedly query the database?',
      'When would a servlet filter or Micrometer Observation be a better boundary?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['aop', 'pointcut', 'logging', 'security', 'performance'],
  },
  {
    id: 'sb-aop-q4',
    question: "A method annotated with your aspect is public, but its class and method are final. The aspect never runs under Spring Boot's default proxy configuration. Why?",
    shortAnswer: 'Spring Boot normally favors CGLIB class proxies, which work by subclassing and overriding methods. A final class cannot be subclassed and a final method cannot be overridden, so it cannot be advised that way. Remove final where appropriate, expose an interface and choose JDK proxies, redesign with composition, or explicitly adopt weaving if its complexity is justified.',
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['aop', 'cglib', 'final', 'proxy'],
  },
  {
    id: 'sb-aop-q5',
    question: 'An @Around metrics aspect records successful calls but not failures. The advice starts a timer, calls proceed(), then records duration. What is wrong?',
    shortAnswer: 'When proceed throws, control skips the recording statement. Stop and record the timer in finally, tag the outcome in catch without swallowing the exception, and rethrow. Be careful that metrics code itself cannot mask the business exception or create unbounded labels from exception messages or IDs.',
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['aop', 'around-advice', 'metrics', 'exceptions'],
  },
  {
    id: 'sb-aop-q6',
    question: 'Why is adding a broad Spring AOP aspect not a solution for intercepting constructors and field writes in domain objects created with new?',
    shortAnswer: 'Spring AOP advises method calls on Spring-managed proxies; those domain objects and join points never cross such a proxy. Use explicit domain design or a decorator, make the lifecycle Spring-managed if appropriate, or choose AspectJ weaving as a deliberate build/runtime decision when non-method join points are truly required.',
    topic: 'spring-boot',
    subtopic: 'sb-aop',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['aop', 'aspectj', 'weaving', 'proxy'],
  },
  // ── Configuration Properties & Profiles Questions ────────────────────
  {
    id: 'sb-config-q1',
    question: 'application-prod.yml says payments.timeout: 2s, but production consistently binds 500ms. How do you investigate without guessing?',
    shortAnswer: 'Inspect the effective Environment and property origin because a higher-precedence source is overriding the file. Check command-line arguments, system properties, environment variables such as PAYMENTS_TIMEOUT, imported Config Data, deployment manifests, and test/runtime overrides. Use sanitized Actuator env/configprops output or startup binding diagnostics, then remove or document the unintended authoritative source.',
    detailedAnswer: 'Spring does not treat application-prod.yml as inherently authoritative; it contributes one property source to an ordered Environment. Start from the deployed process specification and active profiles, then inspect the resolved key and its origin through secured, sanitized diagnostics. Environment-variable relaxed binding commonly turns PAYMENTS_TIMEOUT into payments.timeout, while an imported configuration source or command-line option may outrank packaged files. Do not fix the symptom by duplicating the desired value into another higher-precedence source: define ownership for the key and delete the conflicting override so future operators can predict resolution.',
    keyPoints: [
      'Inspect the effective property and its origin',
      'Environment variables use relaxed binding',
      'Config imports and deployment arguments can override packaged files',
      'Fix configuration ownership, not merely precedence escalation',
    ],
    seniorFollowUps: [
      'How would you expose origin information without leaking secrets?',
      'What deployment policy would prevent undocumented command-line overrides?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['configuration', 'precedence', 'environment', 'diagnostics'],
  },
  {
    id: 'sb-config-q2',
    question: 'A service boots successfully with pool.max-size=0 and fails only when traffic arrives. Redesign its configuration contract.',
    shortAnswer: 'Bind the pool namespace to a typed @ConfigurationProperties object, add @Validated constraints such as @Min(1), validate nested values with @Valid, and avoid a default that converts a missing required setting into zero. Register the properties explicitly so context startup fails during deployment with a binding error.',
    detailedAnswer: 'Scattered @Value fields tend to validate late and independently. A configuration-properties type makes the namespace one contract and supports numeric, Duration, DataSize, nested, and collection binding without manual parsing. Jakarta constraints provide actionable startup failure before readiness succeeds; cross-field rules such as min-size <= max-size need a class-level constraint or explicit validator. Immutable constructor binding prevents runtime mutation. A fallback is appropriate only when zero is a valid documented policy, not when it hides absent deployment configuration.',
    keyPoints: [
      'Bind related settings as one typed contract',
      'Validate during context creation',
      'Cascade nested validation',
      'Do not hide required configuration behind unsafe defaults',
    ],
    seniorFollowUps: [
      'How would you validate max-size against a database connection limit?',
      'Would you make this configuration refreshable at runtime?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['configuration-properties', 'validation', 'fail-fast', 'binding'],
  },
  {
    id: 'sb-config-q3',
    question: 'A team uses profiles named prod, eu, tenant-a, kafka-v2, and feature-x in combinations. Releases increasingly start with the wrong bean graph. What would you change?',
    shortAnswer: 'Profiles are being used as independent feature and identity dimensions, creating a combinatorial configuration space. Keep a small set of coherent runtime modes, move scalar environment/region/tenant values to external typed properties, move dynamic features to a feature-management mechanism, and use explicit profile groups only for supported bundles. Test every supported bean graph at context startup.',
    detailedAnswer: 'Each @Profile changes which beans exist, so several orthogonal profiles create combinations the team is unlikely to test. Region and tenant are data, not usually alternate dependency graphs; feature rollout needs targeting, audit, and often runtime control; infrastructure generation can often be a typed strategy property rather than multiple overlapping profiles. Define a supported-mode matrix, consolidate legitimate groups, remove negative-profile expressions that accidentally load fallback beans, log effective profiles, and write ApplicationContextRunner or integration tests that assert both required and forbidden beans for every supported mode.',
    keyPoints: [
      'Profiles alter bean topology',
      'Orthogonal profile dimensions create an untestable matrix',
      'Use properties for data and feature systems for rollout',
      'Test positive and negative bean presence',
    ],
    seniorFollowUps: [
      'When is @ConditionalOnProperty preferable to @Profile?',
      'How would you migrate without changing all environments at once?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['profiles', 'configuration-design', 'feature-flags', 'testing'],
  },
  {
    id: 'sb-config-q4',
    question: 'A Kubernetes deployment imports optional:configtree:/run/secrets/ and silently uses a built-in default password when the mount is absent. Why is optional the bug?',
    shortAnswer: 'optional says absence is acceptable, so the Config Data phase continues and the unsafe fallback wins. A production credential is a required dependency: make the import mandatory, remove the password default, validate the bound property, and fail readiness/startup if the secret is unavailable. Reserve optional imports for genuinely optional developer or feature configuration.',
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['config-data', 'configtree', 'secrets', 'kubernetes'],
  },
  {
    id: 'sb-config-q5',
    question: 'A record annotated @ConfigurationProperties has one canonical constructor. Does it need @ConstructorBinding in current Spring Boot?',
    shortAnswer: "No. A single parameterized constructor is inferred for constructor binding in current Boot, and a record's canonical constructor fits that model. @ConstructorBinding is mainly useful to select the binding constructor when multiple parameterized constructors exist. The properties type still needs registration through scanning or @EnableConfigurationProperties.",
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['configuration-properties', 'constructor-binding', 'records'],
  },
  {
    id: 'sb-config-q6',
    question: 'Why should spring.profiles.active not be placed only inside a YAML document guarded by spring.config.activate.on-profile: prod?',
    shortAnswer: 'That document is loaded only after prod is already active, so it cannot reliably bootstrap the condition that activates itself. Select active/default profiles from an unconditional document or an external source such as an environment variable or command line, then let conditional documents contribute their settings.',
    topic: 'spring-boot',
    subtopic: 'sb-properties-profiles',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['profiles', 'config-data', 'activation', 'yaml'],
  },
  // ── Reactive Programming with WebFlux Questions ────────────────────
  {
    id: 'sb-wf-q1',
    question: 'A WebFlux service has low CPU but p99 spikes when one endpoint calls a legacy JDBC repository. Every controller already returns Mono. Why is the service still stalling?',
    shortAnswer: 'Returning Mono does not make JDBC non-blocking. If the repository call runs on a Netty event-loop thread, that thread waits for the database and cannot process other connections. Replace the driver with R2DBC where the tradeoff is justified, or wrap the blocking call in Mono.fromCallable and subscribeOn boundedElastic, then monitor that bounded pool and its queue.',
    detailedAnswer: 'Confirm the hypothesis with event-loop thread stacks, BlockHound in tests, and scheduler/latency metrics. The isolation pattern is local to the blocking source: Mono.fromCallable(repository::call).subscribeOn(Schedulers.boundedElastic()). A publishOn placed after repository.call() is too late, and calling block() anywhere on the request path recreates the same problem. Isolation restores event-loop responsiveness but does not make the database faster; boundedElastic saturation must feed a timeout or load-shedding decision rather than an unbounded backlog.',
    keyPoints: [
      'Reactive return types do not transform blocking dependencies',
      'Move the source, not merely downstream processing, off the event loop',
      'Isolation requires bounded capacity and saturation observability',
    ],
    seniorFollowUps: [
      'When is a servlet stack with virtual threads simpler than this hybrid design?',
      'How would you prove an SDK is blocking if its documentation is unclear?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['webflux', 'event-loop', 'blocking', 'jdbc'],
  },
  {
    id: 'sb-wf-q2',
    question: 'An ingestion endpoint adds onBackpressureBuffer with no size limit to stop MissingBackpressureException. Under a downstream outage the pod is OOM-killed. What did the operator actually do?',
    shortAnswer: 'It converted downstream slowness into an unbounded in-memory queue. Backpressure only helps when the producer honors demand; otherwise buffering postpones failure while memory grows. Use a bounded buffer with an explicit overflow policy, reject or shed at ingress, and expose queue depth and dropped/rejected work. If loss is unacceptable, durably queue outside the process.',
    detailedAnswer: 'First identify whether the source is demand-aware. A compliant Publisher should slow emission; a push-only callback or incoming network peer may not. An unbounded buffer acknowledges work faster than the service can complete it and removes the only natural pressure signal. Choose based on semantics: return 429/503 to callers, drop a specifically safe class of updates, block a controlled producer, or persist to a broker. Capacity must derive from acceptable memory and recovery time, not from a large arbitrary number.',
    keyPoints: [
      'Buffering stores pressure; it does not resolve a rate mismatch',
      'Backpressure ends where a producer or protocol ignores demand',
      'Overflow behavior is a product correctness decision',
    ],
    seniorFollowUps: [
      'How does Reactor prefetch affect the amount already in flight?',
      'When would latest-value dropping be correct?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['backpressure', 'buffer', 'oom', 'load-shedding'],
  },
  {
    id: 'sb-wf-q3',
    question: "A correlation ID is present in the first WebFlux log line but disappears after flatMap, and occasionally another request's ID appears. Why?",
    shortAnswer: 'MDC is ThreadLocal, but reactive signals can execute on different pooled threads; the original value does not follow the subscription, and failing to restore or clear a bridge leaks values between requests. Store request metadata in Reactor Context, read it with deferContextual, and use a tested context-propagation bridge for logging and tracing.',
    detailedAnswer: 'The context belongs to a subscription, not to the assembly thread. Enrich it with contextWrite near the request boundary and avoid capturing MDC while building the pipeline. Modern Reactor and Micrometer context propagation can restore registered ThreadLocals around supported operators, but custom schedulers, callbacks, and third-party publishers still need verification. A correct test forces scheduler hops and concurrent requests with distinct IDs, then asserts both presence and isolation.',
    keyPoints: [
      'ThreadLocal identity is not subscription identity',
      'Read subscriber context lazily, not during pipeline assembly',
      'Test for cross-request leakage as well as missing context',
    ],
    seniorFollowUps: [
      'Where should security context live in a reactive application?',
      'How can a custom operator accidentally lose context?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['reactor-context', 'mdc', 'tracing', 'threadlocal'],
  },
  {
    id: 'sb-wf-q4',
    question: 'A WebClient POST is wrapped in retryWhen and customers occasionally receive duplicate orders after timeouts. Why is the reactive retry especially easy to misread?',
    shortAnswer: 'retryWhen resubscribes to the cold publisher, re-executing the HTTP request. A timeout is ambiguous: the server may have created the order but the response was lost. Retry only with a server-enforced idempotency key or for operations proven idempotent, bound attempts and total time, and record retry outcomes separately from original calls.',
    detailedAnswer: 'The pipeline looks declarative, but subscription is execution. Each retry creates another attempt, so placement around a larger chain may repeat more side effects than intended. Generate one stable idempotency key outside the retried publisher and reuse it across attempts; do not generate a new key per subscription. Make the retry predicate exception-aware, exclude validation and most 4xx responses, add jitter, and ensure the outer request deadline can accommodate the backoff budget.',
    keyPoints: [
      'Retry is resubscription and cold sources execute again',
      'Stable idempotency identity must span all attempts',
      'Attempt timeout, backoff, and overall deadline need one budget',
    ],
    seniorFollowUps: [
      'Where would you place retry relative to timeout?',
      'How do you avoid counting every attempt as a separate business failure?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['webclient', 'retry', 'idempotency', 'cold-publisher'],
  },
  {
    id: 'sb-wf-q5',
    question: 'A developer calls subscribe() inside a controller to start an audit write and then returns the business Mono. What production problems can this cause?',
    shortAnswer: 'The audit becomes a detached second subscription with no shared cancellation, error handling, tracing, or transactional lifecycle; failures can vanish and shutdown may abandon it. It can also duplicate work if the same cold source is subscribed elsewhere. Compose required work into the returned chain, or hand durable asynchronous work to a queue with explicit delivery semantics.',
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['subscribe', 'side-effects', 'cancellation', 'audit'],
  },
  {
    id: 'sb-wf-q6',
    question: "A request times out after two seconds, but database load continues rising for minutes. Doesn't cancellation stop the work?",
    shortAnswer: 'Cancellation is cooperative. A reactive driver may cancel an in-flight operation, but a blocking call running on boundedElastic may ignore interruption and continue after downstream has stopped waiting. Configure driver/server-side query timeouts, propagate deadlines, bound concurrency, and verify cancellation behavior rather than assuming timeout means the resource was released.',
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['cancellation', 'timeout', 'database', 'resource-leak'],
  },
  {
    id: 'sb-wf-q7',
    question: 'The same WebClient call occurs twice although the method constructing the Mono is invoked once. What is the first thing you inspect?',
    shortAnswer: 'Inspect subscription count and operators that resubscribe, especially multiple consumers, retry, repeat, cache placement, and manual subscribe calls. A cold publisher represents deferred work, not a stored result, so each subscription performs the request independently. Share or cache only if one execution and replay are semantically correct and bounded by lifecycle or TTL.',
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['cold-publisher', 'subscription', 'webclient', 'duplicate-call'],
  },
  {
    id: 'sb-wf-q8',
    question: 'When is WebFlux a poor choice even if the team expects high request volume?',
    shortAnswer: 'When the workload is CPU-bound, the dependency stack is predominantly blocking, or the team cannot operate and debug reactive pipelines, WebFlux adds scheduler and context complexity without increasing CPU capacity. A servlet stack with bounded pools or virtual threads may provide equal throughput and simpler traces; choose WebFlux for end-to-end non-blocking I/O and streaming needs, not request count alone.',
    topic: 'spring-boot',
    subtopic: 'sb-webflux',
    difficulty: 'Intermediate',
    seniority: 'Lead',
    tags: ['webflux', 'tradeoffs', 'virtual-threads', 'architecture'],
  },
  // ── Spring Cloud: Config, Discovery, and Gateway Questions ────────────────────
  {
    id: 'sb-cloud-q1',
    question: 'Config Server is unavailable during a regional restart. Should every service fail startup or use optional config?',
    shortAnswer: 'There is no fleet-wide answer. Fail closed when missing or stale values could violate security, data routing, or compatibility; allow optional import only when safe local defaults or a previously validated snapshot preserve correct behavior. Bound retries with jitter, expose the config revision in health data, and avoid a policy that turns one control-plane outage into either silent misconfiguration or endless crash loops.',
    detailedAnswer: 'Classify properties by risk and decide at the service boundary. Credentials, tenant routing, schema modes, and kill switches commonly require authoritative config; UI text or noncritical sampling rates may safely default. If cached snapshots are used, sign/version them and define maximum age. Kubernetes restart behavior matters: an unlimited fail-fast loop can stampede Config Server during recovery, while optional startup can make a newly deployed incompatible binary appear healthy with old settings. Test the chosen behavior during dependency loss, not only the happy path.',
    keyPoints: [
      'Startup policy is a correctness and availability decision',
      'Bounded jittered retries protect Config Server recovery',
      'Every instance needs observable config version and source',
    ],
    seniorFollowUps: [
      'How would you handle emergency revocation of a compromised secret?',
      'What changes for a service that can continue reads but must stop writes?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['config-server', 'fail-fast', 'startup', 'control-plane'],
  },
  {
    id: 'sb-cloud-q2',
    question: 'Two related limits are refreshed through Spring Cloud Bus. Half the fleet applies the first value before the second and starts rejecting valid traffic. How do you prevent this class of incident?',
    shortAnswer: 'Treat coupled settings as one versioned, validated configuration object and assume refresh is asynchronous across instances. Publish a complete compatible version, reject partial or invalid combinations locally, observe adoption by version, and roll progressively. If old and new values cannot coexist safely, use a coordinated deployment or restart rather than live refresh.',
    detailedAnswer: 'Bus delivery does not create a distributed transaction. Even if one event announces both fields, each instance applies at a different time and RefreshScope may recreate beans while requests are in flight. Design old and new configurations for an overlap window, include an activation version or timestamp when coordination is required, and supply an immediate rollback version. Avoid exposing a mutable bag of independent primitive properties to code; bind and validate one immutable properties object before swapping it.',
    keyPoints: [
      'Fleet-wide config refresh is eventually consistent',
      'Atomic local binding does not remove mixed fleet versions',
      'Compatibility and observable adoption make rollback possible',
    ],
    seniorFollowUps: [
      'Which properties should never be refreshable?',
      'How would you prevent two operators from publishing conflicting versions?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['refresh', 'spring-cloud-bus', 'consistency', 'configuration'],
  },
  {
    id: 'sb-cloud-q3',
    question: 'During a rolling deploy, Spring Cloud LoadBalancer intermittently selects pods that have already terminated even though the registry now shows only healthy instances. Where can the stale view live?',
    shortAnswer: 'In several places: registry heartbeat and eviction state, the discovery client cache, a ServiceInstanceListSupplier cache, DNS or connection pools, and an already-selected in-flight request. Measure propagation at each layer, coordinate readiness and deregistration before termination, avoid redundant caches, and only retry an idempotent call against a newly selected instance within its deadline.',
    detailedAnswer: "Do not stop at a screenshot of current registry state; the failing caller's view at selection time matters. Log or trace the resolved instance and discovery snapshot version with bounded sampling, inspect cache TTLs and connection reuse, and compare them with termination timestamps. Graceful shutdown should mark the instance unavailable, wait for propagation and draining, then exit. Making every TTL tiny can overload the registry and reduce resilience during registry partitions, so tune total staleness rather than one cache in isolation.",
    keyPoints: [
      "Registry truth and each client's cached truth converge over time",
      'Multiple independent caches compound staleness',
      'Deploy lifecycle must allow deregistration and drain propagation',
    ],
    seniorFollowUps: [
      'How would self-preservation behavior in Eureka affect this tradeoff?',
      'When should retry choose another instance and when should it not retry?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['service-discovery', 'loadbalancer', 'cache', 'rolling-deploy'],
  },
  {
    id: 'sb-cloud-q4',
    question: 'A Gateway filter logs request bodies for debugging. After clients upload large payloads, direct memory grows and all routes slow down. Explain the failure.',
    shortAnswer: 'The reactive body is a one-shot stream, so the filter likely joins or caches it to log and then re-expose it downstream. Large concurrent bodies accumulate Netty data buffers and can exhaust direct memory or stall event loops, especially if logging blocks. Enforce body-size limits before buffering, redact and sample bounded content, release buffers correctly, and send logs through a non-blocking bounded path.',
    detailedAnswer: 'Gateway filters run on a shared reactive infrastructure, so this cross-cutting feature affects every route. Inspect filter order, DataBuffer release behavior, direct-buffer metrics, event-loop stacks, and log-appender backpressure. Full payload logging is usually unacceptable for memory, latency, privacy, and cost reasons. Prefer metadata and trace IDs, allow short diagnostic sampling behind a kill switch, and never cache an unbounded stream merely to make it readable twice.',
    keyPoints: [
      'Reading and forwarding a request body requires explicit caching',
      'Gateway-wide filters multiply per-request memory across all traffic',
      'Size limits and bounded diagnostic sampling belong before body aggregation',
    ],
    seniorFollowUps: [
      'How would this interact with a retry filter?',
      'What evidence distinguishes a DataBuffer leak from legitimate buffering under load?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['gateway', 'databuffer', 'direct-memory', 'filter'],
  },
  {
    id: 'sb-cloud-q5',
    question: 'A new broad /api/** route is added above /api/admin/** and admin requests stop receiving the authorization filter. Why can the configuration be valid but unsafe?',
    shortAnswer: "Both predicates match, and route order selects the broad route first, so the narrower route's filters never run. Schema validation proves that each route is syntactically valid, not that predicates are non-overlapping. Add route-resolution tests that assert the selected route ID and filter set for sensitive hosts, methods, headers, and paths, and review route changes as security code.",
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['gateway', 'route-order', 'security', 'predicates'],
  },
  {
    id: 'sb-cloud-q6',
    question: 'An IP allowlist at a downstream service trusts X-Forwarded-For, and an external caller bypasses it by supplying that header directly. What must the gateway architecture guarantee?',
    shortAnswer: 'The external edge must remove untrusted forwarded and identity headers, reconstruct them from the actual connection, and define trusted proxy hops or ranges. Downstream services must be unreachable through a path that bypasses that sanitization, and multi-proxy parsing must use one documented first/last-hop convention rather than trusting any client-provided value.',
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['forwarded-headers', 'gateway', 'spoofing', 'trust-boundary'],
  },
  {
    id: 'sb-cloud-q7',
    question: 'Zone preference is enabled, and a caller returns no instances when its local zone is empty even though remote zones are healthy. What design rule was missed?',
    shortAnswer: 'Preference was implemented as a hard filter without a fallback. The supplier chain should prefer local healthy instances but fall back to remote candidates according to latency and data-residency policy. Test the empty-local-set case, observe cross-zone selection, and ensure cache and health-filter ordering does not preserve an empty result longer than intended.',
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['loadbalancer', 'zones', 'supplier', 'failover'],
  },
  {
    id: 'sb-cloud-q8',
    question: 'Why is putting business aggregation and blocking SDK calls directly in Spring Cloud Gateway risky?',
    shortAnswer: 'Gateway is a shared reactive choke point: blocking or heavy per-request work stalls its event loops, and business coupling makes one gateway deploy and outage affect unrelated APIs. Keep routing and cross-cutting policy lightweight; move client-specific composition to a BFF or service, or isolate unavoidable blocking work with strict bounds while planning its removal.',
    topic: 'spring-boot',
    subtopic: 'sb-spring-cloud',
    difficulty: 'Intermediate',
    seniority: 'Lead',
    tags: ['gateway', 'event-loop', 'bff', 'architecture'],
  },
  // ── Resilience4j and Policy Composition Questions ────────────────────
  {
    id: 'sb-res-q1',
    question: 'A method has both Retry and CircuitBreaker annotations. In staging the breaker opens after a handful of user requests, far sooner than the team expected. What do you inspect first?',
    shortAnswer: 'Inspect the actual aspect/decorator order and metrics. If CircuitBreaker wraps each retry attempt, one user request can contribute several failures to its window; if it wraps the whole retry, it sees one final outcome. Neither order is universally right. Decide whether the breaker should react to downstream attempts or caller-visible operations, then configure and test that order explicitly.',
    detailedAnswer: 'Walk a concrete request through all policies, including fallback. With three attempts and the breaker inside retry, ten user calls can produce thirty recorded calls and satisfy minimumNumberOfCalls quickly. With the breaker outside, retries may continue loading a sick dependency before the breaker gathers enough final failures. Also check which exceptions retry and record, because ignored exceptions alter the denominator. Write an integration test asserting attempt count, breaker buffered calls, state transition, and final response rather than relying on annotation names.',
    keyPoints: [
      'Decorator order changes the unit counted by the breaker',
      'Retry attempts and user requests are different traffic volumes',
      'Policy-order tests should assert metrics and state, not only output',
    ],
    seniorFollowUps: [
      'Where should a rate limiter sit if every downstream attempt must consume quota?',
      'How would a fallback inside versus outside the breaker change recorded outcomes?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['resilience4j', 'decorator-order', 'retry', 'circuit-breaker'],
  },
  {
    id: 'sb-res-q2',
    question: 'A low-traffic administrative API never opens its breaker during a dependency outage, while a bursty endpoint opens on two random failures after deploy. Which settings are likely wrong?',
    shortAnswer: 'The window and minimumNumberOfCalls do not fit either traffic shape. A high minimum gives the admin endpoint an impractically long detection delay; an extremely low minimum makes the bursty endpoint react to noise. Choose time- or count-based windows per operation traffic, set enough samples for a stable decision, and use timeouts or bulkheads for immediate protection before statistical opening.',
    detailedAnswer: 'Circuit breakers are not instant health checks; they infer from sampled calls. Estimate how long it takes each operation to collect the minimum at normal and degraded rates. A time window can age out stale history, while a count window gives a fixed sample but may span hours at low traffic. For very low traffic, fail-fast timeouts and shared dependency-level policy may be more meaningful than a per-endpoint breaker, provided operations share comparable failure semantics.',
    keyPoints: [
      'Minimum calls defines detection delay as well as statistical confidence',
      'One configuration rarely fits both sparse and bursty operations',
      'Timeout and isolation protect before a breaker has evidence',
    ],
    seniorFollowUps: [
      'When is sharing one breaker across operations unsafe?',
      'How would you test half-open behavior without waiting in real time?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['sliding-window', 'minimum-calls', 'traffic-shape', 'tuning'],
  },
  {
    id: 'sb-res-q3',
    question: "TimeLimiter returns timeout errors after 500 ms, yet thread count and downstream queries keep growing. Why didn't fail-fast protect capacity?",
    shortAnswer: 'TimeLimiter bounded caller wait, not necessarily underlying execution. The Future or blocking driver may ignore cancellation or interruption, so timed-out work continues and new calls pile on top. Configure connection, socket, and server-side query timeouts; bound concurrency with a bulkhead; propagate a total deadline; and measure work still in flight after caller timeout.',
    detailedAnswer: 'Trace one timed-out call through the executor and client. cancellation mayCancelIfRunning only requests interruption, and many blocking or native operations do not stop. If a thread-pool bulkhead queues work, calls can consume most of the 500 ms before execution and then continue after the caller leaves. Set queue and execution budgets deliberately, ensure the client abort mechanism is real, and reject before admission when remaining deadline is insufficient. A timeout metric without an in-flight gauge hides zombie work.',
    keyPoints: [
      'Caller timeout and operation cancellation are separate guarantees',
      'Every resource layer needs a compatible timeout or deadline',
      'Bulkheads bound damage even when cancellation fails',
    ],
    seniorFollowUps: [
      'How does this differ for a Reactor Netty request?',
      'Where should queue wait count against the deadline?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['timelimiter', 'cancellation', 'bulkhead', 'timeout'],
  },
  {
    id: 'sb-res-q4',
    question: 'A downstream returns HTTP 200 but takes four seconds, exhausting caller threads. The breaker is configured only for exceptions and remains closed. How should Resilience4j model this failure?',
    shortAnswer: "Configure slow-call duration and slow-call-rate thresholds from the caller's latency budget so successful-but-harmful calls contribute to opening. Pair that with a real client timeout and bulkhead; the breaker still needs minimum samples, and a threshold below normal cold or batch latency will create false opens.",
    detailedAnswer: 'First segment operations with genuinely different latency profiles rather than forcing one threshold across them. Use production percentiles and the caller deadline to choose a slow duration that signals lost capacity before timeout. Observe slow-call rate, buffered calls, state transitions, and concurrent resource occupancy. The breaker is a feedback mechanism, not a substitute for the per-call timeout that releases capacity promptly.',
    keyPoints: [
      'Operational failure can be latency without exceptions',
      'Slow-call thresholds must reflect operation-specific budgets',
      'Breaker, timeout, and bulkhead protect at different stages',
    ],
    seniorFollowUps: [
      'How do deployments and cache warm-up affect slow-call tuning?',
      'Would you count HTTP 429 as a failure, slow call, or ignored outcome?',
    ],
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['slow-call', 'circuit-breaker', 'latency', 'capacity'],
  },
  {
    id: 'sb-res-q5',
    question: 'When would you choose a semaphore bulkhead over a thread-pool bulkhead in a Spring service?',
    shortAnswer: "Use a semaphore when work can execute safely on the caller's execution model and you only need a concurrency cap, especially for non-blocking reactive calls. Use a thread-pool bulkhead to isolate blocking execution, accepting queueing, context propagation, and scheduler overhead. In either case the permit must cover the real operation lifetime, not just creation of a Future or Publisher.",
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['bulkhead', 'semaphore', 'thread-pool', 'reactive'],
  },
  {
    id: 'sb-res-q6',
    question: 'A payment fallback returns a synthetic success response whenever the provider fails, keeping error dashboards green. What is wrong with this design?',
    shortAnswer: 'The fallback violates the business contract and masks both customer impact and breaker evidence; unavailable authorization cannot safely become success. Fail fast with a retriable or pending outcome, or queue only if the workflow explicitly supports asynchronous completion. Emit fallback/degraded metrics and restrict fallback exception types so programming defects are not converted into plausible data.',
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['fallback', 'payment', 'correctness', 'observability'],
  },
  {
    id: 'sb-res-q7',
    question: 'Final request error rate falls after enabling retries, but the downstream now receives four times the traffic and becomes less stable. Which Resilience4j signals were missing?',
    shortAnswer: 'Attempt-level retry metrics and downstream call volume. Track successful-with-retry, failed-with-retry, total attempts per original request, breaker outcomes, bulkhead rejections, and latency budget exhaustion. Apply exception-aware bounded retries with jitter and a retry budget; a healthy caller dashboard can conceal harmful amplification downstream.',
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['retry', 'amplification', 'metrics', 'retry-budget'],
  },
  {
    id: 'sb-res-q8',
    question: 'Why is creating one circuit breaker name per discovered service instance a poor production design?',
    shortAnswer: 'Each breaker receives too little traffic for stable window decisions, instance churn leaks breaker state, and per-instance metric labels explode cardinality. Use stable dependency or operation-level names unless instance isolation is explicitly required, let load balancing remove unhealthy instances, and keep target identity in sampled traces rather than metric dimensions.',
    topic: 'spring-boot',
    subtopic: 'sb-resilience',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['circuit-breaker', 'cardinality', 'service-discovery', 'metrics'],
  },
  // ── Async & Scheduling in Spring Questions ────────────────────
  {
    id: 'sb-as-q1',
    question: 'A service method calls this.sendEmail() where sendEmail is annotated @Async, yet the request still waits for SMTP. Why?',
    shortAnswer: "The call is self-invocation on this, so it never crosses Spring's async proxy and runs on the request thread. Put sendEmail on a separate managed bean and call that bean, or redesign the boundary; making the method private or merely enabling async does not fix proxy bypass.",
    detailedAnswer: "Spring's AsyncAnnotationBeanPostProcessor wraps eligible beans with an interceptor that submits intercepted calls to an executor. A direct JVM call from one method to another on the same target bypasses that wrapper completely. The clean fix is a separate collaborator whose public method represents the background boundary. Self-proxy injection can work but couples business code to proxy mechanics and makes tests and call paths harder to reason about.",
    keyPoints: [
      '@Async requires proxy traversal',
      'Self-invocation and unmanaged construction bypass interception',
      'Separate bean boundaries make execution semantics visible',
    ],
    seniorFollowUps: ['What other Spring annotations share this self-invocation trap?'],
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['async', 'proxy', 'self-invocation'],
  },
  {
    id: 'sb-as-q2',
    question: "An @Async void method throws after the controller has returned 202, but the controller's catch block never runs and callers see no failure. Where did it go?",
    shortAnswer: "The exception occurs later on another thread, outside the caller's stack, and a void method has no result channel. Configure AsyncUncaughtExceptionHandler for last-resort reporting, but return CompletableFuture for observable completion and model durable business work through a queue or job record rather than fire-and-forget memory.",
    detailedAnswer: 'A 202 response only confirms acceptance by the web layer; it says nothing about eventual success. With void @Async, Spring routes uncaught failures to AsyncUncaughtExceptionHandler, which can log or emit a metric but cannot return an error to the original caller. A CompletableFuture lets an in-process caller compose failure, but still disappears on process crash. Durable workflows require persisted state or messaging plus an API for status.',
    keyPoints: [
      'Cross-thread exceptions cannot be caught by the original stack',
      'void needs AsyncUncaughtExceptionHandler',
      'Durability requires persistence, not just an executor',
    ],
    seniorFollowUps: ['When is returning 202 misleading even if task submission succeeded?'],
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['async', 'exception', 'durability', '202'],
  },
  {
    id: 'sb-as-q3',
    question: "During a downstream outage, an @Async executor's queue grows for twenty minutes and then the pod OOMs. What should the executor contract have been?",
    shortAnswer: "Bound the queue and concurrency to the downstream's real capacity, choose an explicit rejection policy, and surface queue depth, active threads, rejection count, and task latency. A bounded system must shed or throttle overload; silently storing unlimited delayed work is not resilience.",
    detailedAnswer: 'An unbounded queue decouples admission from completion until memory becomes the only limit, so the application accepts work whose deadline has already become impossible. Configure ThreadPoolTaskExecutor with a capacity derived from acceptable wait time, isolate unrelated workloads, and decide whether overload should run on the caller, reject immediately, or persist elsewhere. Rejection must be translated into a truthful API/business outcome, not swallowed by an async boundary.',
    keyPoints: [
      'Bound queue and concurrency',
      'Make overload visible through rejection and metrics',
      'Do not accept work that cannot meet its useful deadline',
    ],
    seniorFollowUps: ['When is CallerRunsPolicy dangerous on a servlet request thread?'],
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['executor', 'backpressure', 'rejection', 'overload'],
  },
  {
    id: 'sb-as-q4',
    question: "Logs inside @Async work have no trace ID, and occasionally show another request's user ID. What implementation mistake explains both symptoms?",
    shortAnswer: "Request ThreadLocal state such as MDC or SecurityContext was either not propagated or copied without guaranteed cleanup on pooled threads. Use a TaskDecorator/delegating executor to capture the submitter's context, install it for the task, and restore the previous worker context in finally; never assume the transaction itself propagates.",
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['taskdecorator', 'mdc', 'security-context', 'threadlocal'],
  },
  {
    id: 'sb-as-q5',
    question: 'A nightly @Scheduled reconciliation ran four times after the service scaled to four pods. How would you make it production-safe?',
    shortAnswer: '@Scheduled is local to each ApplicationContext, so each pod correctly fired. Use an external scheduler or a distributed lease/lock so only one instance owns a run, and keep the job idempotent with a run key because crashes, retries, and lease expiry can still produce duplicate attempts.',
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['scheduled', 'distributed-lock', 'idempotency', 'kubernetes'],
  },
  {
    id: 'sb-as-q6',
    question: 'A fixed-rate job normally takes 10 seconds but occasionally takes 90 seconds. What must you decide before increasing the scheduler pool size?',
    shortAnswer: 'Whether overlapping executions are valid. More scheduler threads can turn lateness into concurrent duplicate work and overload dependencies. If overlap is unsafe, serialize with fixed delay or a job lock; if it is safe, still cap concurrency, define misfire behavior, and ensure shutdown/interruption leaves recoverable checkpoints.',
    topic: 'spring-boot',
    subtopic: 'sb-async-scheduling',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['scheduled', 'fixed-rate', 'overlap', 'shutdown'],
  },
  // ── Messaging Integration Questions ────────────────────
  {
    id: 'sb-msg-q1',
    question: 'A Kafka listener charges a card, writes success to the database, then crashes before its offset commits. The message is delivered again. Is Kafka broken, and how do you prevent a double charge?',
    shortAnswer: 'No; this is the normal at-least-once crash window. Use the event ID as an idempotency key persisted with the business result, and call the payment provider with its own idempotency key. On redelivery, return the recorded outcome rather than repeat the effect, then allow the offset to commit.',
    detailedAnswer: 'Offset commit and external side effects are not one atomic transaction. Committing first can lose work; committing afterward can duplicate it. The safe design gives every command stable identity, records processing or the resulting state under a unique constraint, and makes downstream calls idempotent too. A database transaction can atomically combine the inbox marker and local state, but it cannot retroactively make a non-idempotent remote charge safe.',
    keyPoints: [
      'At-least-once requires duplicate-safe effects',
      'Persist idempotency with the business result',
      'Propagate idempotency to remote side effects',
    ],
    seniorFollowUps: ['How do you handle a crash after the provider charged but before your database recorded success?'],
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['kafka', 'idempotency', 'offset', 'payments'],
  },
  {
    id: 'sb-msg-q2',
    question: 'An order transaction commits, but the process dies between repository.save(order) and kafkaTemplate.send(event). How does an outbox close this gap?',
    shortAnswer: 'Write the order and an outbox row in the same database transaction. A separate relay publishes committed rows and marks them delivered. A crash can cause the relay to publish twice, but cannot produce a committed order with no durable publication intent, so consumers remain idempotent.',
    detailedAnswer: 'Direct database-plus-broker publishing is a dual write: either order can succeed alone. The outbox reduces the atomic boundary to one resource the database can guarantee. Polling with row claiming or CDC then transfers events to the broker. Delivery marking must not assume publish acknowledgment and database update are atomic, so duplicate relay publication is expected. Operationally, alert on oldest unpublished age and relay failures, and define retention and ordering by aggregate.',
    keyPoints: [
      'Business row and outbox intent commit atomically',
      'Relay is at-least-once',
      'Monitor event age and preserve aggregate order',
    ],
    seniorFollowUps: ['When would CDC be preferable to polling?'],
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['outbox', 'dual-write', 'kafka', 'consistency'],
  },
  {
    id: 'sb-msg-q3',
    question: 'A six-thread Kafka listener consumes a topic with three partitions, but throughput does not improve beyond three threads. Why?',
    shortAnswer: 'A consumer group assigns each partition to only one consumer, so three partitions permit at most three active consumers. Increase partitions if the keying and ordering model allows it, then ensure downstream capacity can absorb the parallelism; otherwise optimize processing rather than adding idle threads.',
    detailedAnswer: 'Listener concurrency creates consumer instances, but assignment cannot split one partition across two consumers in the same group because that would destroy its ordered offset stream. Additional consumers remain idle. Adding partitions increases the ceiling but changes key distribution and cannot necessarily preserve a former global ordering assumption. Scale only after checking handler CPU, downstream pools, batch size, poll interval, and lag by partition.',
    keyPoints: [
      'Partitions cap active consumers in a group',
      'Ordering and key distribution constrain repartitioning',
      'Downstream capacity must scale with consumer concurrency',
    ],
    seniorFollowUps: ['What happens to assignment and lag during a rebalance?'],
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['kafka', 'partition', 'consumer-group', 'scaling'],
  },
  {
    id: 'sb-msg-q4',
    question: 'One customer generates most events and all share customerId as the Kafka key. Consumer lag grows on one partition while others are idle. What tradeoff caused this?',
    shortAnswer: 'The key correctly preserves per-customer order but makes that hot customer a single-partition bottleneck. Revisit the true ordering scope, shard the hot key with a sequence-aware downstream design, or isolate it; simply adding consumers cannot parallelize one partition without relaxing ordering.',
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['kafka', 'hot-partition', 'ordering', 'key'],
  },
  {
    id: 'sb-msg-q5',
    question: 'After increasing Rabbit listener concurrency, a pod restart causes thousands of messages to redeliver at once and other consumers had been mostly idle. Which setting is suspect?',
    shortAnswer: 'Prefetch is likely too high. Each consumer can accumulate that many unacknowledged deliveries, so concurrency times prefetch creates a large in-flight set and lets early consumers hoard work. Tune prefetch near useful per-consumer parallelism and processing latency, then measure throughput and redelivery exposure.',
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['rabbitmq', 'prefetch', 'redelivery', 'fairness'],
  },
  {
    id: 'sb-msg-q6',
    question: "A malformed event retries forever and blocks a Kafka partition. What should a senior failure policy specify beyond 'send it to a DLQ'?",
    shortAnswer: 'Classify retryable versus permanent errors, cap attempts, use backoff with jitter, retain original metadata and failure context, alert on DLQ arrival and age, define an owner and replay procedure, and ensure replay is idempotent. Some contract failures should be quarantined immediately rather than retried.',
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['dlq', 'retry', 'poison-message', 'operations'],
  },
  {
    id: 'sb-msg-q7',
    question: 'A producer adds a required field and deploys before all consumers. Why can a healthy rolling deployment still break message processing?',
    shortAnswer: 'Messages are durable contracts consumed by mixed application versions, so deploy order alone cannot guarantee lockstep compatibility. Prefer additive optional fields with defaults, tolerant readers, schema compatibility checks, and a staged migration before making a field required or changing its meaning.',
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['schema-evolution', 'rolling-deployment', 'compatibility'],
  },
  {
    id: 'sb-msg-q8',
    question: 'During pod termination, listener threads are interrupted after the datasource has already closed, producing failures and redeliveries. What shutdown ordering should the application enforce?',
    shortAnswer: 'Mark the instance unready and stop listener intake/polling first, allow in-flight handlers bounded time to commit and acknowledge while the datasource and broker connection remain available, then close executors and infrastructure. Work that misses the deadline must be safe to redeliver, so shutdown correctness still depends on idempotency.',
    detailedAnswer: "Graceful shutdown is a dependency-order problem. Consumers are upstream of handler executors, and handlers depend on database and broker clients. Closing those dependencies first guarantees in-flight failure. The lifecycle should remove the instance from service, pause containers, drain active records within spring.lifecycle.timeout-per-shutdown-phase, commit/ack successful work, then tear down clients. Kubernetes termination allowance must exceed the application's drain budget, and metrics should show active handlers and forced termination.",
    keyPoints: [
      'Stop intake before closing handler dependencies',
      'Drain is bounded, not indefinite',
      'Forced termination must lead to safe redelivery',
    ],
    seniorFollowUps: ['How would long Kafka max.poll.interval settings interact with a slow drain?'],
    topic: 'spring-boot',
    subtopic: 'sb-messaging',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['graceful-shutdown', 'listener', 'drain', 'redelivery'],
  },
  // ── Production Spring Boot Questions ────────────────────
  {
    id: 'sb-prod-q1',
    question: 'A container has a 1 GiB memory limit and -Xmx1g, yet Kubernetes OOMKills it while heap usage is only 750 MiB. Explain the missing memory.',
    shortAnswer: 'The cgroup limit covers the whole process, not just heap: metaspace, code cache, direct buffers, thread stacks, native libraries, GC structures, and JVM overhead also count. Leave measured headroom, reduce excessive threads/direct memory if needed, and alert on process RSS versus the limit in addition to heap occupancy.',
    detailedAnswer: 'Xmx equal to the container limit leaves zero room for mandatory non-heap memory. The kernel may terminate the process externally, so there may be no Java OutOfMemoryError or heap dump. Size heap as a fraction of the limit based on workload evidence, inspect native memory and thread counts, cap direct-memory consumers where appropriate, and preserve enough margin for traffic variation. Determine whether the event was Java heap exhaustion or cgroup OOMKill before tuning GC or increasing heap.',
    keyPoints: [
      'Container limit covers all process memory',
      'OOMKill may bypass JVM diagnostics',
      'Measure RSS, native memory, threads, and direct buffers',
    ],
    seniorFollowUps: ['How would virtual threads change the thread-stack part of this budget?'],
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jvm', 'container', 'memory', 'oomkill'],
  },
  {
    id: 'sb-prod-q2',
    question: 'Tomcat maxThreads is raised from 200 to 800 while Hikari remains at 30 connections. Throughput stays flat and p99 worsens. Why?',
    shortAnswer: 'The database pool is the constrained resource, so extra request threads mostly wait for the same 30 connections while consuming memory and scheduling overhead. Measure connection-acquisition time, size concurrency to downstream capacity and latency, apply timeouts/backpressure, and optimize queries before assuming larger pools create capacity.',
    detailedAnswer: 'Concurrency settings form a queueing system. Once all database connections are busy, another 600 servlet threads cannot execute database work; they lengthen the queue and amplify timeout/retry pressure. Increasing Hikari blindly can move the bottleneck into the database. Establish a concurrency budget from database capacity, bound request and async queues, use short acquisition timeouts, monitor active/pending connections and query latency, and shed overload before every request consumes a waiting thread.',
    keyPoints: [
      'The smallest downstream pool caps useful concurrency',
      'Extra threads increase queueing and tail latency',
      'Tune the system end to end, not one pool independently',
    ],
    seniorFollowUps: ['When could a larger request-thread pool still be useful with a small database pool?'],
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['tomcat', 'hikari', 'capacity', 'latency'],
  },
  {
    id: 'sb-prod-q3',
    question: 'A deployment adds a non-null database column and immediately updates code to require it. Half the pods are still on the old version when failures begin. What migration sequence was missing?',
    shortAnswer: 'Use expand-migrate-contract: add the nullable column or safe default first; deploy code that can read both forms and begins writing the new field; backfill and verify; then enforce the constraint; remove old behavior only in a later release after no old instances or rollback path depend on it.',
    detailedAnswer: 'Rolling deployments deliberately create a mixed-version window, and rollback extends the compatibility requirement further. Expansion introduces a representation every version can tolerate. Migration populates data while both read paths remain valid. Contract removes the obsolete representation only after telemetry proves no old writer, reader, or rollback target depends on it. Large backfills should be throttled and separated from latency-sensitive deployment startup.',
    keyPoints: [
      'Mixed versions require bidirectional compatibility',
      'Separate expand, migrate, and contract releases',
      'Data rollback constraints differ from code rollback',
    ],
    seniorFollowUps: ['How would you handle renaming a heavily used column without a long table lock?'],
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['rolling-deployment', 'database', 'migration', 'compatibility'],
  },
  {
    id: 'sb-prod-q4',
    question: 'A Spring Boot app needs 90 seconds for cold startup, but liveness begins after 20 seconds and repeatedly restarts it. Should you simply increase liveness initial delay?',
    shortAnswer: 'Use a startup probe to gate liveness until initialization succeeds, while readiness remains false until the app can serve. Then investigate the 90 seconds rather than masking it indefinitely—especially synchronous migrations, lazy network calls, or resource starvation—and set a bounded startup failure window.',
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['startup-probe', 'liveness', 'readiness', 'kubernetes'],
  },
  {
    id: 'sb-prod-q5',
    question: 'The same jar is rebuilt separately for staging and production with different application.yml files. What operational risk does that create?',
    shortAnswer: 'You are no longer promoting the tested artifact; code and dependency bytes can differ between environments and configuration provenance becomes opaque. Build one immutable image, inject validated environment-specific configuration and secrets at deployment, record the artifact digest and config version, and define how secret/config changes roll out.',
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['configuration', 'immutable-artifact', 'deployment', 'secrets'],
  },
  {
    id: 'sb-prod-q6',
    question: 'CPU, heap, and average latency look healthy, but users report intermittent 8-second requests. Which observability path gives the fastest useful investigation?',
    shortAnswer: 'Start with a tail-latency metric such as p99 to establish scope, pivot to sampled traces for slow requests to find the waiting span, then use structured logs correlated by trace ID for the concrete event. Check pool-acquisition and queue time separately from downstream execution; averages commonly hide a small saturated path.',
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['observability', 'tracing', 'structured-logging', 'tail-latency'],
  },
  {
    id: 'sb-prod-q7',
    question: "Why can moving Flyway migrations into every pod's startup path make a rolling deployment fragile?",
    shortAnswer: 'Many replicas can contend to migrate, startup and readiness become coupled to schema duration, and a long or incompatible migration can keep the whole rollout unavailable. Run migrations once through a controlled deployment job, make changes rolling-compatible, expose migration status, and keep application rollback requirements in the schema plan.',
    topic: 'spring-boot',
    subtopic: 'sb-production',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['flyway', 'migration', 'startup', 'deployment'],
  },
]
