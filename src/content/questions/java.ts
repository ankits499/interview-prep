import type { Question } from '../../types'

export const javaQuestions: Question[] = [
  {
    id: 'java-fund-q1',
    question: 'Is Java pass-by-value or pass-by-reference?',
    shortAnswer:
      'Always pass-by-value. For objects, the value passed is a copy of the reference — so reassigning the parameter never affects the caller, but mutating the object through it does.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['references', 'pass-by-value'],
  },
  {
    id: 'java-fund-q2',
    question: 'Is a class with private fields and public getters/setters properly encapsulated?',
    shortAnswer:
      'Not necessarily. Encapsulation is about controlling state and protecting invariants, not simply hiding fields — a setter with no validation is barely different from a public field.',
    detailedAnswer:
      'Real encapsulation means the class enforces its own invariants: state can only change through methods that guarantee the object never becomes invalid. Getters and setters are the mechanical form; whether they actually protect anything depends on whether they validate.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['encapsulation'],
  },
  {
    id: 'java-fund-q3',
    question: 'How is abstraction different from encapsulation?',
    shortAnswer:
      'Abstraction hides complexity behind a simpler contract (what an object does); encapsulation protects internal state from corruption (how it\'s guarded). A class can have one without the other.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['abstraction', 'encapsulation'],
  },
  {
    id: 'java-fund-q4',
    question: 'You inherit a codebase where changing an internal detail of a base class keeps unexpectedly breaking subclasses. What\'s this called, and what\'s the fix?',
    shortAnswer:
      'The fragile base class problem — subclasses are coupled to the parent\'s implementation, not just its contract. Refactor toward composition: extract the shared behavior into a collaborator the classes hold a reference to instead of extending.',
    seniorFollowUps: ['How would you migrate an existing inheritance hierarchy to composition without a big-bang rewrite?'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['inheritance', 'composition', 'design'],
  },
  {
    id: 'java-fund-q5',
    question: 'When is inheritance still the right call over composition?',
    shortAnswer:
      'When the relationship is a genuine, Liskov-substitutable "is-a" and you specifically need callers to treat every subtype polymorphically through the parent type — composition alone can\'t give you that without also defining an interface.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['inheritance', 'composition', 'design'],
  },
  {
    id: 'java-fund-q6',
    question: 'What does the Liskov Substitution Principle forbid, with a concrete example?',
    shortAnswer:
      'A subclass being unusable wherever its parent is expected. Classic case: Square extends Rectangle — overriding setWidth/setHeight to keep width==height breaks any caller that sets them independently expecting predictable area.',
    keyPoints: ['"Is-a" in the English sense isn\'t enough — the subclass must honor every behavioral contract, not just the structural shape'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['solid', 'liskov', 'design'],
  },
  {
    id: 'java-fund-q7',
    question: 'What\'s the real difference between overloading and overriding, in terms of when it\'s resolved?',
    shortAnswer:
      'Overloading is resolved at compile time from the static (declared) type of the arguments. Overriding is resolved at runtime from the object\'s actual type — that\'s the mechanism behind true polymorphism.',
    seniorFollowUps: ['If a method is overloaded with both a supertype and subtype parameter, which one runs when you pass an object whose static type differs from its runtime type?'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['overloading', 'overriding', 'polymorphism'],
  },
  {
    id: 'java-fund-q8',
    question: 'Does dynamic dispatch apply to field access the same way it applies to method calls?',
    shortAnswer:
      'No. Field access is resolved statically by the reference\'s declared type; only method calls use the object\'s actual runtime type. A common trick question in "what does this print" snippets involving field shadowing.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['dynamic-dispatch', 'polymorphism'],
  },
  {
    id: 'java-fund-q9',
    question: 'After Java 8 added default methods, what actually still distinguishes an interface from an abstract class?',
    shortAnswer:
      'State. An interface still cannot declare instance fields, so it can never carry shared mutable state — only shared default behavior. An abstract class can hold both.',
    detailedAnswer:
      'Use an interface for a capability a type can implement alongside unrelated other interfaces (a class can implement many). Use an abstract class when subclasses genuinely need to share mutable state or a behavior template, since Java only allows single class inheritance.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['interfaces', 'abstract-classes', 'java8'],
  },
  {
    id: 'java-fund-q10',
    question: 'Two interfaces a class implements each declare a default method with the same signature. What happens?',
    shortAnswer:
      'Compile error — Java refuses to silently pick one. The implementing class must override the method explicitly, optionally delegating via InterfaceName.super.method(). This is Java\'s answer to the multiple-inheritance diamond problem.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['interfaces', 'diamond-problem'],
  },
  {
    id: 'java-fund-q11',
    question: 'What breaks if you override equals() but not hashCode()?',
    shortAnswer:
      'Two "equal" objects can land in different HashMap/HashSet buckets, so get()/contains() silently fail to find an entry that logically exists — no exception, just wrong behavior.',
    keyPoints: ['hashCode() picks the bucket; equals() only disambiguates within it', 'Fields used in the contract should be immutable, or an object can become unfindable after mutation'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['equals-hashcode', 'collections'],
  },
  {
    id: 'java-fund-q12',
    question: 'You put a custom Point object into a HashSet, then create a "new" Point with the same x/y and check `set.contains(newPoint)` — it returns false even though the fields match. Why, and what\'s missing?',
    shortAnswer:
      'HashSet uses hashCode() and equals() to check membership, and the class never overrode either — so it\'s using Object\'s default implementations, which compare references, not field values. Two Points with identical x/y are still different objects with different identity, so contains() correctly reports "not found" by that definition.',
    detailedAnswer:
      'This is exactly why classes meant to be used as values — compared for what they represent, not which specific instance they are — need to override equals()/hashCode() together. Without it, every object-contract-based collection (HashSet, HashMap keys, contains(), etc.) silently falls back to identity comparison.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['equality', 'equals-hashcode', 'collections'],
  },
  {
    id: 'java-fund-q13',
    question: 'Can a static method be overridden?',
    shortAnswer:
      'No — it can be hidden by a same-signature static method in a subclass, but that\'s resolved at compile time by the reference type, not runtime polymorphic dispatch like an overridden instance method.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['static', 'polymorphism'],
  },
  {
    id: 'java-fund-q14',
    question: 'Why does immutability matter specifically for concurrency?',
    shortAnswer:
      'An immutable object has no mutable state to race on — it can be freely shared across threads with zero synchronization, because it can never be observed in an inconsistent, half-updated state.',
    seniorFollowUps: ['Does declaring a field `final` make the object it references immutable?'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['immutability', 'concurrency'],
  },
  {
    id: 'java-fund-q15',
    question: 'Give a real example of an Open/Closed Principle violation you\'d flag in code review.',
    shortAnswer:
      'A growing if/else or switch on type that needs a new branch every time a new case is added. The Strategy pattern (polymorphism through an interface) usually fixes it by letting new cases be added as new classes instead of edited branches.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['solid', 'design'],
  },
  {
    id: 'java-fund-q16',
    question: 'What\'s the difference between dependency injection and dependency inversion?',
    shortAnswer:
      'Dependency inversion is the principle: high-level modules should depend on abstractions, not concrete implementations. Dependency injection is the mechanism that supplies those abstractions from outside (constructor/setter/framework) instead of the class constructing them itself.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['solid', 'dependency-injection', 'design'],
  },
  {
    id: 'java-fund-q17',
    question: 'How do you avoid over-engineering "extensibility" that nobody actually needs?',
    shortAnswer:
      'Don\'t add a seam (interface, strategy, plugin point) until there\'s a real, current need for variation. Speculative flexibility adds indirection and cost with no payoff — and is itself a design smell.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['design', 'over-engineering'],
  },
  {
    id: 'java-fund-q18',
    question: 'Design a small class hierarchy for payment methods (credit card, bank transfer, wallet) in a checkout system. Interface, abstract class, or composition — and why?',
    shortAnswer:
      'A PaymentMethod interface for the contract (charge(), refund()), implemented by each concrete type — payment methods don\'t share meaningful mutable state, so an abstract class would only add coupling with no benefit. If some logic genuinely repeats (retry/backoff), pull it into a shared collaborator via composition, not a base class.',
    seniorFollowUps: ['How would this design change if you later need to add logging or fraud-checking around any payment method?'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['design', 'interfaces', 'composition'],
  },
  {
    id: 'java-str-q1',
    question: 'You pass a String into a method that appears to modify it, but the caller\'s variable is unchanged. Why?',
    shortAnswer:
      'String is immutable — every "modifying" method (concat, replace, toUpperCase, etc.) returns a new String rather than mutating the original. If the method doesn\'t reassign and return the result, the caller never sees a change.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['immutability'],
  },
  {
    id: 'java-str-q2',
    question: 'Why does `new String("hi") == "hi"` evaluate to false, but `"hi" == "hi"` evaluate to true?',
    shortAnswer:
      'Two identical literals are both resolved to the same pooled object at compile time, so == (reference equality) holds. `new String(...)` explicitly forces a fresh heap allocation outside that pooled reference, so == fails even though the content is identical.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['string-pool', 'equality'],
  },
  {
    id: 'java-str-q3',
    question: 'Since Java 7 moved the string pool into the heap, what practical problem did that actually fix?',
    shortAnswer:
      'Before Java 7 the pool lived in PermGen, a small fixed-size space — heavy literal/intern() usage could exhaust it and throw OutOfMemoryError: PermGen space independent of how much heap was actually free. Moving the pool to the heap ties its size to normal heap capacity and makes it subject to standard GC.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['string-pool', 'jvm', 'memory'],
  },
  {
    id: 'java-str-q4',
    question: 'A service parses a 500MB file into memory, then extracts thousands of tiny substrings and discards the original. On an older JVM this held onto huge amounts of memory. On a current JVM it doesn\'t. Why?',
    shortAnswer:
      'Pre-Java 7, substring() shared the original char array and just changed the offset/length — every extracted substring kept the entire original array reachable. Java 7+ always copies only the needed characters, so the original becomes eligible for GC once nothing else references it.',
    seniorFollowUps: ['If you were stuck supporting an app on a pre-Java-7 JVM, how would you work around this without changing the JVM version?'],
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['substring', 'memory', 'jvm'],
  },
  {
    id: 'java-str-q5',
    question: 'When would you actually reach for String.intern() in production code, versus leaving it alone?',
    shortAnswer:
      'When you\'re holding a large number of runtime-built strings with heavy duplication — e.g. parsed tokens from a huge log or dataset — where collapsing duplicates to one pooled reference meaningfully reduces memory. It\'s not a default habit: intern() has its own lookup and pool-growth cost, so it should follow profiling, not precede it.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['string-pool', 'intern', 'performance'],
  },
  {
    id: 'java-str-q6',
    question: 'A code reviewer flags `result += line;` inside a loop processing 100,000 lines. What\'s the actual cost, and what\'s the fix?',
    shortAnswer:
      'Each += allocates a new String (Strings are immutable), copying everything accumulated so far — that\'s O(n²) total work across n iterations, not O(n). Replace with a single StringBuilder and append() inside the loop, building once and calling toString() at the end.',
    detailedAnswer:
      'The compiler can optimize a single expression like `a + b + c` into one StringBuilder automatically, but it cannot do that across separate loop iterations — each iteration\'s += is its own independent allocate-and-copy.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['performance', 'stringbuilder'],
  },
  {
    id: 'java-str-q7',
    question: 'Why does StringBuffer still exist in the JDK given that StringBuilder is recommended almost everywhere?',
    shortAnswer:
      'Legacy compatibility — StringBuffer predates StringBuilder (added in Java 5 specifically to offer an unsynchronized alternative). StringBuffer\'s per-method synchronization is rarely the right concurrency tool anyway: it only makes individual method calls atomic, not multi-step sequences of them, so code that actually needs thread-safety across several operations still needs external locking regardless.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['stringbuilder', 'stringbuffer', 'concurrency'],
  },
  {
    id: 'java-str-q8',
    question: 'A log line renders emoji as two garbled characters instead of one when you print `s.charAt(i)` in a loop. What\'s going on?',
    shortAnswer:
      'The emoji is outside the Basic Multilingual Plane, so it\'s represented as a surrogate pair — two 16-bit chars for one Unicode code point. Iterating by char index (charAt/length) treats each half of the pair as a separate "character." Iterate by code point (codePoints(), codePointAt) instead when correctness with extended Unicode matters.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['unicode', 'surrogate-pairs'],
  },
  {
    id: 'java-str-q9',
    question: 'A file upload works fine locally but produces corrupted text once deployed to a different server. The code calls `new String(bytes)` with no other arguments. What\'s the likely cause?',
    shortAnswer:
      'Omitting the Charset makes String use the JVM\'s platform default encoding, which can differ between the local machine and the deployment environment (e.g. UTF-8 vs a legacy default). Always pass an explicit charset — normally StandardCharsets.UTF_8 — on both getBytes() and the String(byte[], Charset) constructor.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['charset', 'encoding', 'production'],
  },
  {
    id: 'java-str-q10',
    question: 'CSV parsing code using `line.split(",")` silently loses the last column whenever it\'s empty. Why, and how do you fix it?',
    shortAnswer:
      'split() drops trailing empty strings from the result by default. Passing a negative limit — split(",", -1) — preserves them. This is a common, silent CSV/TSV parsing bug precisely because it only shows up when the *last* field happens to be empty.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['split', 'regex', 'gotcha'],
  },
  {
    id: 'java-str-q11',
    question: 'A validation method calling `input.matches(regex)` inside a hot request path shows up in a profiler. Why, and what\'s the fix?',
    shortAnswer:
      'String.matches() compiles the regex into a Pattern internally on every single call — fine for one-off use, wasteful when called repeatedly with the same pattern. Compile the Pattern once as a static final field and reuse Matcher instances from it instead.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['regex', 'performance', 'pattern'],
  },
  {
    id: 'java-str-q12',
    question: 'Why do password/secret-handling APIs (like javax.crypto or KeyStore) take a char[] instead of a String?',
    shortAnswer:
      'A String is immutable and may sit in the string pool indefinitely — there\'s no way to force its backing data to be overwritten in memory. A char[] can be explicitly zeroed out (Arrays.fill) the moment the secret is no longer needed, limiting how long it\'s exposed in a memory dump or swap.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['security', 'immutability'],
  },
  {
    id: 'java-str-q13',
    question: 'Two equal Strings are guaranteed to have the same hashCode() — why does that matter beyond correctness?',
    shortAnswer:
      'Because String caches its hashCode() after computing it once (immutability guarantees it can never go stale), repeated HashMap/HashSet lookups on the same String key reuse the cached value instead of rescanning every character — part of why String is such a fast, popular map key.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['hashcode', 'performance', 'collections'],
  },
  {
    id: 'java-str-q14',
    question: 'When would `String.format()` be the wrong choice even though it\'s more readable than manual concatenation?',
    shortAnswer:
      'In a hot loop or high-throughput path — format() re-parses the format string on every call, which is measurably slower than StringBuilder.append() calls. Readability wins for one-off or low-frequency formatting; raw throughput favors StringBuilder.',
    topic: 'java',
    subtopic: 'strings',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['string-format', 'performance'],
  },
  {
    id: 'java-fund-q19',
    question: 'What can go wrong when a constructor calls an overridable method?',
    shortAnswer:
      'The override can run on the subclass instance before the subclass\'s own field initializers have executed, so it operates on partially-built state — often silently reading defaults (0/null) instead of the values the subclass constructor was about to set.',
    detailedAnswer:
      'Construction order is: superclass constructor runs first, then subclass field initializers, then subclass constructor body. If the superclass constructor calls an overridable method and the subclass overrides it, that override executes during step one — before step two has run. Any subclass field it reads is still at its default value.',
    seniorFollowUps: ['How would you refactor a constructor that needs subclass-specific setup without this hazard?'],
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['constructor', 'inheritance', 'gotcha'],
  },
  {
    id: 'java-fund-q20',
    question: 'Why do wait() and notify() live on Object instead of Thread?',
    shortAnswer:
      'Because every object in Java has an intrinsic lock (monitor), not just Thread instances — wait/notify coordinate threads through whichever object\'s monitor a synchronized block uses, so the methods have to be available on every object, not one special class.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['object-class', 'concurrency'],
  },
  {
    id: 'java-fund-q21',
    question: 'What does the Interface Segregation Principle actually forbid, and what does violating it look like in practice?',
    shortAnswer:
      'Forcing a class to implement methods it doesn\'t need just because they\'re bundled into one fat interface. In practice: a Worker interface with work() and eat() that a RobotWorker is forced to implement eat() for, usually with a UnsupportedOperationException or empty stub — a sign the interface should be split.',
    topic: 'java',
    subtopic: 'fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['solid', 'interfaces', 'design'],
  },
{
  id: 'java-java8-q1',
  question: 'A method takes a Runnable and you pass `() -> counter++` where counter is a local int. It won\'t compile. Why, and what\'s the idiomatic fix?',
  shortAnswer:
    'Lambdas can only capture local variables that are final or effectively final — counter++ reassigns it, so it fails that check. Wrap it in an AtomicInteger (or a single-element array) so the lambda captures a reference whose internal state can change, rather than trying to reassign the captured variable itself.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['lambdas', 'variable-capture'],
},
{
  id: 'java-java8-q2',
  question: 'What actually distinguishes a functional interface from any other interface with default methods?',
  shortAnswer:
    'Exactly one abstract method, full stop — default and static methods, and Object\'s public methods redeclared, don\'t count toward that total. An interface with two default methods and one abstract method is still a valid lambda target; two abstract methods makes it invalid regardless of how many defaults it also has.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['functional-interfaces'],
},
{
  id: 'java-java8-q3',
  question: 'You have `Function<String,String> f = String::toUpperCase;` — is this a bound or unbound method reference, and what does it mean for how f is invoked?',
  shortAnswer:
    'Unbound — the receiver isn\'t fixed at reference-creation time, so the first argument passed to f.apply(x) becomes the object toUpperCase() is called on. f.apply("hi") returns "HI". Contrast with a bound reference like someString::toUpperCase, which ignores any argument and always operates on someString.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['method-references'],
},
{
  id: 'java-java8-q4',
  question: 'Two interfaces a class implements both provide a default `describe()` method — what actually happens, and how do you resolve it?',
  shortAnswer:
    'Compile error — Java refuses to guess which default wins. The implementing class must override describe() explicitly, optionally delegating to one via InterfaceA.super.describe(). This is unrelated to Java 8-specific stream/lambda mechanics but is exactly the diamond-avoidance mechanism default methods introduced.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['default-methods', 'interfaces'],
},
{
  id: 'java-java8-q5',
  question: 'A teammate writes `list.stream().filter(x -> expensiveCheck(x));` with no terminal operation and is confused why expensiveCheck() never runs. Explain why, precisely.',
  shortAnswer:
    'Intermediate operations like filter() are lazy — they just record a pipeline stage, they don\'t execute anything. Nothing runs until a terminal operation (collect, forEach, count, etc.) is called, which pulls elements through the whole chain. Without one, the pipeline is built but never executed.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['streams', 'laziness'],
},
{
  id: 'java-java8-q6',
  question: 'You call `.stream()` on the same List twice inside one method and both work fine, but calling `.forEach()` twice on the same Stream reference throws IllegalStateException. Why isn\'t this a contradiction?',
  shortAnswer:
    'The List is reusable storage — .stream() creates a fresh, independent Stream from it each time. A Stream itself is not reusable: once a terminal operation runs, that specific Stream instance is consumed and any further operation on it throws IllegalStateException. Get a new stream from the source instead of trying to reuse one.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['streams', 'gotcha'],
},
{
  id: 'java-java8-q7',
  question: 'You have a List<List<Integer>> and need a single flat List<Integer> of all values. Why won\'t plain map() get you there?',
  shortAnswer:
    'map() is strictly 1-to-1 — mapping each inner List<Integer> through a function still leaves you with a Stream of some mapped value per inner list, not a flat stream of ints. flatMap(List::stream) replaces each inner list with its own stream of elements and flattens them all into one combined stream.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['streams', 'flatmap'],
},
{
  id: 'java-java8-q8',
  question: 'Why does the no-identity overload of reduce() return Optional<T> while the identity-supplied overload returns T directly?',
  shortAnswer:
    'Without an identity value, reducing an empty stream has no meaningful result to fall back to — Optional models that absence honestly. With an identity supplied, that identity itself serves as the result for an empty stream, so there\'s always a concrete T to return, no wrapping needed.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['streams', 'reduce', 'optional'],
},
{
  id: 'java-java8-q9',
  question: 'Collectors.toMap() throws IllegalStateException in production because two elements produce the same key. What are the two ways to fix this, and what\'s the tradeoff?',
  shortAnswer:
    'Either supply a merge function as the third argument (e.g. (a, b) -> a to keep the first, or a combining function to merge both) or change the key extractor so keys are actually unique. The merge function is a deliberate choice about what "collision" should mean for your data — silently picking one without thinking about it can quietly drop real data.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['collectors', 'streams'],
},
{
  id: 'java-java8-q10',
  question: 'You need to group a List<Employee> by department and get the average salary per department in one pass. What\'s the idiomatic way, without writing a manual loop with a HashMap?',
  shortAnswer:
    'Collectors.groupingBy(Employee::getDepartment, Collectors.averagingDouble(Employee::getSalary)) — groupingBy takes a downstream Collector as a second argument, which lets you aggregate within each bucket in the same pass instead of grouping first and then iterating the resulting Map to compute averages separately.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['collectors', 'groupingBy'],
},
{
  id: 'java-java8-q11',
  question: 'A junior engineer converts a hot-path sequential stream to parallelStream() to "make it faster" and the service starts timing out intermittently elsewhere. What likely went wrong?',
  shortAnswer:
    'parallelStream() uses the JVM-wide common ForkJoinPool by default, sized to the number of CPU cores. If the parallel stream\'s work blocks (I/O, network calls) or the collection is small enough that fork/join overhead dominates, it can starve every other unrelated parallel stream and CompletableFuture in the JVM competing for that same small pool — a systemic issue, not a local one.',
  seniorFollowUps: ['How would you run CPU-bound parallel work without risking starving the common pool used elsewhere in the app?'],
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['parallel-streams', 'performance', 'production'],
},
{
  id: 'java-java8-q12',
  question: 'Why is `optional.orElse(computeExpensiveDefault())` a subtle performance bug even when the Optional is present nearly every time?',
  shortAnswer:
    'orElse() unconditionally evaluates its argument every single call, present or not, because Java evaluates method arguments eagerly before the call. computeExpensiveDefault() runs even on the fast path where its result is discarded. orElseGet(() -> computeExpensiveDefault()) defers the computation to only when actually needed.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['optional', 'performance', 'gotcha'],
},
{
  id: 'java-java8-q13',
  question: 'A code reviewer flags a class with a field of type `Optional<String> middleName`. What\'s the concern, and what should it be instead?',
  shortAnswer:
    'Optional wasn\'t designed as a field/parameter type — it\'s not Serializable, adds an allocation and indirection layer for no real safety benefit as a field, and its whole value proposition is signaling absence at a method return boundary. Use a plain nullable String field (with clear null-handling conventions) instead, and reserve Optional for return types of accessor methods if the API benefits from it.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['optional', 'design'],
},
{
  id: 'java-java8-q14',
  question: 'A service persists user event timestamps as LocalDateTime, and now that it\'s deployed across two regions, event ordering across regions looks wrong. What\'s the root cause?',
  shortAnswer:
    'LocalDateTime carries no time zone — "2024-03-15T14:30" is genuinely ambiguous in absolute terms; the same stored value corresponds to a different real-world instant depending on which region\'s server wrote it. Events should be stored as Instant (or an explicit UTC-anchored type) so every region\'s timestamp is directly comparable, converting to a ZonedDateTime only for display.',
  seniorFollowUps: ['How would you migrate an existing LocalDateTime column safely without losing historical ordering?'],
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Staff',
  tags: ['java.time', 'production', 'design'],
},
{
  id: 'java-java8-q15',
  question: 'Why was SimpleDateFormat historically dangerous as a shared static field, and how does DateTimeFormatter avoid the same problem?',
  shortAnswer:
    'SimpleDateFormat is mutable and stateful internally — concurrent format()/parse() calls on a shared instance corrupt each other\'s internal state with no exception, just wrong output. DateTimeFormatter is immutable, so multiple threads calling it concurrently is inherently safe with zero synchronization needed.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['java.time', 'concurrency'],
},
{
  id: 'java-java8-q16',
  question: 'Compare Stream.reduce() and Collectors.collect() as ways to accumulate results — when would you reach for collect() instead of reduce()?',
  shortAnswer:
    'reduce() is meant for combining immutable values into a single immutable result (sum, max, string via concatenation of immutables) — using it to build a mutable container like a List means creating and discarding intermediate containers repeatedly, which is inefficient and awkward in parallel. collect() is designed exactly for accumulating into a mutable result container (supplier + accumulator + combiner), which is why it, not reduce(), is what backs toList/toMap/groupingBy.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['streams', 'reduce', 'collect'],
},
{
  id: 'java-java8-q17',
  question: 'A pipeline uses `.peek(x -> auditLog.add(x))` to build an audit trail as a side effect, and some entries are mysteriously missing in production. What\'s wrong with relying on peek() this way?',
  shortAnswer:
    'peek() is documented as intended for debugging visibility, not for real application logic — the JVM is permitted to elide calling it for elements the terminal operation doesn\'t actually need (e.g. certain short-circuiting or count() cases where the source size is already known). Side effects meant to reliably happen for every element belong in forEach() or the collector logic itself, not peek().',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Staff',
  tags: ['streams', 'peek', 'gotcha'],
},
{
  id: 'java-java8-q18',
  question: 'When is a plain for-loop still the better engineering choice over an equivalent stream pipeline?',
  shortAnswer:
    'When you need multiple related running aggregates in a single pass, complex conditional branching that would force nested lambdas, or operations that throw checked exceptions (which don\'t compose cleanly inside a functional interface). A nine-stage stream chain crammed to avoid "using a loop" is often a readability regression, not an improvement — streams are a tool for the common transform/filter/collect shape, not a mandate.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Lead',
  tags: ['streams', 'design', 'readability'],
},
{
  id: 'java-java8-q19',
  question: 'A method inside a stream lambda needs to throw a checked IOException, but Function<T,R>.apply() doesn\'t declare any checked exceptions. What are your realistic options?',
  shortAnswer:
    'Wrap the checked exception in an unchecked one inside the lambda (e.g. catch and rethrow as UncheckedIOException), write a small helper that adapts a checked-throwing lambda to the functional interface, or avoid the stream for that step and use a plain loop where the checked exception can be declared normally. There\'s no way to make a standard java.util.function interface declare a checked exception without writing your own functional interface.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['streams', 'lambdas', 'exceptions'],
},
{
  id: 'java-java8-q20',
  question: 'What\'s the practical difference between IntStream and Stream<Integer>, beyond just "one is primitive"?',
  shortAnswer:
    'IntStream avoids boxing every element into an Integer object, which matters for both memory and CPU in large numeric pipelines, and it exposes numeric-specific terminal operations (sum, average, max, summaryStatistics) that a generic Stream<Integer> doesn\'t have, since Stream has no built-in notion of "numeric." mapToInt/boxed() convert between the two when needed.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['streams', 'primitive-streams', 'performance'],
},
{
  id: 'java-java8-q21',
  question: 'A List<Order> needs to become a Map<CustomerId, List<Order>> grouping each customer\'s orders. Walk through the Collector you\'d use and why not toMap().',
  shortAnswer:
    'Collectors.groupingBy(Order::getCustomerId) — toMap() maps each key to exactly one value and throws on a duplicate key, but here every customer legitimately has many orders sharing one key. groupingBy is built for exactly this one-to-many bucketing case, defaulting to List as the downstream container.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['collectors', 'groupingBy', 'toMap'],
},
{
  id: 'java-java8-q22',
  question: 'Since interfaces can now have default and static methods, why do we still need abstract classes at all for Java 8+ code?',
  shortAnswer:
    'Interfaces still cannot declare instance fields, so they can never hold shared mutable state — only shared default behavior operating on values passed in or accessed via other interface methods. An abstract class can hold real shared state across subclasses. Default methods closed the gap on shared *behavior*, not shared *state*.',
  topic: 'java',
  subtopic: 'java8',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['default-methods', 'interfaces', 'abstract-classes'],
},
{
  id: 'java-coll-q1',
  question: 'A hot path does frequent insertions at the front of a large List. Someone suggests switching from ArrayList to LinkedList. Do you agree?',
  shortAnswer:
    'Only if profiling actually shows the shift cost dominating — LinkedList\'s O(1) head insert avoids ArrayList\'s O(n) shift, but LinkedList pays for it with per-node pointer overhead and worse cache locality everywhere else. Often the better fix is ArrayDeque (if order allows) or restructuring to append at the end instead.',
  detailedAnswer:
    'In practice, ArrayList\'s contiguous memory makes even its O(n) operations fast in wall-clock terms for moderate sizes due to cache locality, while LinkedList\'s O(1) claims come with constant-factor overhead (object headers, pointer chasing) that often loses for real workloads. Measure before switching.',
  seniorFollowUps: ['What data structure would you reach for if you needed O(1) at both ends and O(1) random access?'],
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['arraylist', 'linkedlist', 'performance'],
},
{
  id: 'java-coll-q2',
  question: 'Why does the JDK documentation recommend ArrayDeque over both LinkedList and the legacy Stack class for stack/queue use?',
  shortAnswer:
    'ArrayDeque is backed by a contiguous circular array, giving it better cache locality and lower per-element overhead than LinkedList\'s node pointers. Stack additionally extends the legacy, fully-synchronized Vector, paying locking overhead even in single-threaded code.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['arraydeque', 'stack', 'performance'],
},
{
  id: 'java-coll-q3',
  question: 'You put custom Point objects into a HashSet, then mutate the x field of an object already in the set. contains() with an equal object now returns false. What happened?',
  shortAnswer:
    'HashSet placed the object into a bucket based on its hashCode() at insertion time. Mutating a field the hashCode() depends on doesn\'t move the object to a new bucket — it\'s now "stuck" in the wrong one, and a fresh lookup computes a different bucket than where it actually lives, so it silently can\'t be found.',
  keyPoints: ['This is why keys used in hash-based collections should be immutable, or at least never mutated in fields that participate in equals()/hashCode()'],
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['hashset', 'hashcode', 'gotcha'],
},
{
  id: 'java-coll-q4',
  question: 'Walk through what actually happens internally when you call HashMap.get(key) — from the key\'s hashCode() to the returned value.',
  shortAnswer:
    'The key\'s hashCode() is computed and spread (XORed with its own upper bits) to reduce clustering, then masked against (capacity-1) to pick a bucket index. Within that bucket, entries are scanned using equals() to find the matching key (a short chain, or a red-black tree if the bucket was treeified), and that entry\'s value is returned.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['hashmap', 'internals'],
},
{
  id: 'java-coll-q5',
  question: 'What triggers HashMap bucket treeification, and why was it added in Java 8?',
  shortAnswer:
    'A single bucket accumulating more than 8 entries (with the table capacity at least 64) converts that bucket\'s linked list into a red-black tree, capping worst-case lookup at O(log n) instead of O(n). It was added specifically to defend against pathological or adversarially-crafted hashCode() collisions degrading a HashMap to linked-list-like performance.',
  seniorFollowUps: ['Why is there also a threshold to convert it back to a list, rather than leaving it as a tree once treeified?'],
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['hashmap', 'treeification', 'internals'],
},
{
  id: 'java-coll-q6',
  question: 'A service constructs a HashMap and immediately bulk-inserts 100,000 known entries. A reviewer suggests specifying an initial capacity. Why would that matter?',
  shortAnswer:
    'Without it, the map starts at capacity 16 and resizes (doubles + rehashes every existing entry) repeatedly as it grows past each load-factor threshold — each resize is an O(n) full rehash. Pre-sizing to roughly (expectedEntries / loadFactor) avoids that churn entirely for a known bulk load.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['hashmap', 'load-factor', 'performance'],
},
{
  id: 'java-coll-q7',
  question: 'What\'s the practical difference between HashMap and ConcurrentHashMap beyond "one is thread-safe"?',
  shortAnswer:
    'ConcurrentHashMap uses fine-grained locking (per-bucket in Java 8+, via CAS and synchronized on head nodes) so multiple threads can read and write different parts of the map concurrently, and its iterators are weakly consistent — they never throw ConcurrentModificationException. It also disallows null keys/values to avoid ambiguity under concurrent access, unlike HashMap.',
  detailedAnswer:
    'Wrapping a HashMap with Collections.synchronizedMap gives correctness but locks the entire map on every call, serializing all access even to unrelated keys — a much coarser and slower approach than ConcurrentHashMap\'s design.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['concurrenthashmap', 'concurrency'],
},
{
  id: 'java-coll-q8',
  question: 'You need a simple in-memory LRU cache with a fixed max size. How would you implement it with a single JDK class?',
  shortAnswer:
    'Extend LinkedHashMap constructed with accessOrder=true, and override removeEldestEntry() to return true once size() exceeds the cache limit — the map\'s own linked list already tracks recency, so eviction needs no extra bookkeeping.',
  example:
    'class LruCache<K,V> extends LinkedHashMap<K,V> {\n    private final int max;\n    LruCache(int max) { super(16, 0.75f, true); this.max = max; }\n    protected boolean removeEldestEntry(Map.Entry<K,V> e) { return size() > max; }\n}',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['linkedhashmap', 'lru', 'design'],
},
{
  id: 'java-coll-q9',
  question: 'A for-each loop that calls list.remove(item) mid-iteration throws ConcurrentModificationException — in single-threaded code with no other thread involved. Why, and what\'s the fix?',
  shortAnswer:
    'The list\'s iterator is fail-fast: it tracks a modCount and detects that the collection was structurally modified by something other than the iterator itself, throwing on the next next() call. Fix by using Iterator.remove() directly, or replacing the loop with list.removeIf(predicate).',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['fail-fast', 'concurrentmodificationexception', 'iteration'],
},
{
  id: 'java-coll-q10',
  question: 'How does CopyOnWriteArrayList avoid ConcurrentModificationException, and what\'s the cost of that approach?',
  shortAnswer:
    'Every write (add/remove/set) copies the entire backing array; iterators hold a reference to the snapshot array taken when they were created, so they never observe concurrent structural changes and never throw. The cost is that every single write is an O(n) full array copy, making it a poor fit for write-heavy collections.',
  seniorFollowUps: ['What workload characteristics make CopyOnWriteArrayList the right choice despite that cost?'],
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['copyonwritearraylist', 'concurrency'],
},
{
  id: 'java-coll-q11',
  question: 'A TreeSet<BigDecimal> ends up with fewer elements than expected after adding "1.0" and "1.00" — size() is 1, not 2. Why, given that BigDecimal("1.0").equals(BigDecimal("1.00")) is false?',
  shortAnswer:
    'TreeSet uses compareTo() (BigDecimal\'s natural ordering ignores scale) as its sole notion of "already present," not equals() (which does consider scale). compareTo() says they\'re equal, so the second add() is silently treated as a duplicate and dropped — a well-known compareTo/equals inconsistency trap.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['treeset', 'comparable', 'gotcha'],
},
{
  id: 'java-coll-q12',
  question: 'What\'s the real difference between Comparable and Comparator, and when would a class need both?',
  shortAnswer:
    'Comparable defines one natural ordering baked into the class itself (compareTo). Comparator defines an external, swappable ordering you can pass to sort()/TreeMap/TreeSet without touching the class — useful when you need multiple orderings of the same type, or can\'t modify the class at all (e.g. a third-party type).',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['comparable', 'comparator', 'ordering'],
},
{
  id: 'java-coll-q13',
  question: 'A getter returns Collections.unmodifiableList(this.internalList). A caller reports the "immutable" list they received changed after the fact. What\'s going on, and how do you actually fix it?',
  shortAnswer:
    'unmodifiableList only blocks writes attempted through that specific returned reference — it\'s a thin view over the still-mutable original list, and any code (including the same class) that mutates internalList directly is visible through the view. For a real guarantee, return a defensive copy — List.copyOf(internalList) or new ArrayList<>(internalList) wrapped unmodifiable.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['unmodifiable', 'immutability', 'encapsulation'],
},
{
  id: 'java-coll-q14',
  question: 'What\'s the difference between List.of("a", "b") and Collections.unmodifiableList(new ArrayList<>(List.of("a","b")))?',
  shortAnswer:
    'List.of() produces a genuinely immutable collection — fixed at creation, rejects null elements, throws immediately on any mutation attempt. Collections.unmodifiableList wraps a mutable list in a read-only view; the view itself can\'t be written to, but the underlying list can still change and that change shows through the view.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['list-of', 'unmodifiable', 'immutability'],
},
{
  id: 'java-coll-q15',
  question: 'Why does iterating a PriorityQueue directly with a for-each loop not yield elements in sorted order, when polling one at a time does?',
  shortAnswer:
    'PriorityQueue is a binary heap — it only guarantees the smallest element sits at the root (index 0); the rest of the backing array is merely heap-ordered, not fully sorted. Direct iteration walks the raw array; repeated poll() re-heapifies after each removal, which is what actually produces sorted output.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['priorityqueue', 'heap', 'gotcha'],
},
{
  id: 'java-coll-q16',
  question: 'A design review flags a service using HashMap<String, List<Order>> without synchronization, accessed from multiple request-handling threads. What are your options, in order of preference?',
  shortAnswer:
    'Prefer ConcurrentHashMap<String, List<Order>> with a thread-safe or immutable value list (e.g. building via computeIfAbsent + a CopyOnWriteArrayList, or replacing the whole list atomically on update) over Collections.synchronizedMap, which serializes all access with one coarse lock and still requires manual synchronization for compound operations.',
  seniorFollowUps: ['How would you atomically append to the per-key list without a race between two threads both doing get-then-add on a missing key?'],
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['concurrenthashmap', 'design', 'concurrency'],
},
{
  id: 'java-coll-q17',
  question: 'Why does ConcurrentHashMap disallow null keys and null values, when HashMap allows both?',
  shortAnswer:
    'In a concurrent context, get(key) returning null is ambiguous — it could mean "no mapping exists" or "mapped to null," and a second thread could have removed or added the mapping between a containsKey() check and a get() call, making the distinction unsafely racy to resolve. Disallowing null entirely removes the ambiguity.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['concurrenthashmap', 'null-handling', 'concurrency'],
},
{
  id: 'java-coll-q18',
  question: 'You need to process items in strict insertion order but also check membership in O(1) and never allow duplicates. Which collection, and why not the obvious alternatives?',
  shortAnswer:
    'LinkedHashSet — it gives O(1) average add/contains like HashSet while additionally maintaining insertion order via its internal linked list, at only a modest memory cost. A TreeSet would give sorted, not insertion, order at O(log n); a List would need an O(n) contains() check for uniqueness.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['linkedhashset', 'design'],
},
{
  id: 'java-coll-q19',
  question: 'Arrays.asList(1, 2, 3) is passed to a method that calls list.add(4) on it — this throws UnsupportedOperationException. Why, when it looks like a normal List?',
  shortAnswer:
    'Arrays.asList() returns a fixed-size list backed directly by the array — it supports set() (which writes through to the array) but not structural changes like add/remove, since those can\'t be represented in a fixed-length array. Wrap it in new ArrayList<>(Arrays.asList(...)) if a genuinely resizable list is needed.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['arrays-aslist', 'gotcha'],
},
{
  id: 'java-coll-q20',
  question: 'What\'s the actual cost difference between ArrayList\'s add(element) at the end versus add(index, element) in the middle?',
  shortAnswer:
    'add(element) is O(1) amortized — it just writes to the next free slot, occasionally triggering an O(n) resize. add(index, element) is always O(n) because every element from that index onward must be shifted right by one slot to make room, regardless of whether a resize is also needed.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Intermediate',
  seniority: 'Mid',
  tags: ['arraylist', 'complexity'],
},
{
  id: 'java-coll-q21',
  question: 'A batch job builds a Set<Long> of about 5 million unique IDs by repeated add() calls with the default HashSet constructor. What\'s the likely performance issue, and the one-line fix?',
  shortAnswer:
    'Starting at capacity 16 with load factor 0.75 forces roughly 20 resize-and-rehash cycles as the set grows to 5 million entries, each one an O(n) full rehash of everything inserted so far. Constructing with new HashSet<>((int)(5_000_000 / 0.75) + 1) pre-sizes the table and avoids that churn.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['hashset', 'performance', 'load-factor'],
},
{
  id: 'java-coll-q22',
  question: 'Two engineers disagree: one wants to use a HashMap<String, List<String>> as a multimap, the other wants Guava\'s Multimap. What\'s the actual tradeoff, and what would you check first?',
  shortAnswer:
    'A hand-rolled HashMap<K, List<V>> needs manual computeIfAbsent boilerplate everywhere and is easy to get subtly wrong (empty lists left behind after removal, no built-in flatten/size-across-keys). A dedicated Multimap type handles that consistently and gives you views (asMap, entries) for free — but it\'s an added dependency and a less familiar API to the team. Check whether Guava (or a similar library) is already a dependency before introducing it just for this.',
  topic: 'java',
  subtopic: 'collections',
  difficulty: 'Advanced',
  seniority: 'Staff',
  tags: ['design', 'multimap', 'tradeoffs'],
},
{
  id: 'java-gen-q1',
  question: 'A colleague writes `List<Object> list = new ArrayList<String>();` and it fails to compile. They expected it to work since String is an Object. Why doesn\'t it, and what\'s the actual fix if they need the flexibility they wanted?',
  shortAnswer: 'Generics are invariant — List<String> is not a List<Object> — because allowing it would let you insert a non-String into what\'s really backed by a String array. Use List<? extends Object> (or just Object where read-only access is enough) if they only need to read.',
  detailedAnswer: 'If the assignment compiled, you could then call list.add(42) on what the compiler thinks is List<Object>, but is actually the caller\'s List<String> underneath — a runtime ClassCastException waiting to happen on the next String read. Invariance closes that hole at compile time. The fix depends on intent: if the method only reads elements, declare the parameter as List<? extends Object> (functionally just List<?> here since Object is the top type); if it needs a genuinely polymorphic mutable collection, the design question is why — usually the answer is to not need Object at all and use a proper bound.',
  keyPoints: ['Generics do not mirror reference-type subtyping', 'Invariance is a safety feature, not a limitation to route around carelessly', '? extends fixes the read case; there is no fix that gives back full read+write with a supertype element type'],
  seniorFollowUps: ['How is this different from array covariance, and why did the language make that choice differently for arrays?', 'What would go wrong at runtime if invariance weren\'t enforced?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['generics', 'invariance', 'type-safety'],
},
{
  id: 'java-gen-q2',
  question: 'You\'re reviewing a PR where a method signature is `static <T> void copy(List<? super T> dest, List<? extends T> src)`. A teammate asks why not just `List<T> dest, List<T> src` — simpler, and it would still work for their test case. What do you tell them?',
  shortAnswer: 'The wildcard version is strictly more flexible: it accepts src as a List<Integer> and dest as a List<Number> in the same call, which List<T> for both could never do since it would force identical type arguments. It\'s the PECS pattern — producer extends, consumer super.',
  detailedAnswer: 'With List<T> dest, List<T> src, both parameters are locked to the exact same type argument T — you couldn\'t copy a List<Integer> into a List<Number>, even though that\'s perfectly type-safe (every Integer is a Number). The wildcard signature decouples them: src just needs to produce something assignable to T (? extends T), dest just needs to accept something of type T (? super T). Their test case passing with the simpler signature doesn\'t prove equivalence — it just means their test happened to pass matching types on both sides.',
  keyPoints: ['Get Maximum flexibility from the API rather than from the test case that happens to pass', 'This is exactly Collections.copy\'s real signature', 'Wildcards widen the range of legal call sites without weakening type safety'],
  seniorFollowUps: ['What would you lose if you used List<?> for both instead?', 'Where in java.util.Collections else is this pattern visible?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['generics', 'wildcards', 'pecs', 'api-design'],
},
{
  id: 'java-gen-q3',
  question: 'Why does `List<String>[] arr = new List<String>[10];` fail to compile, but `List[] arr = new List[10];` is fine?',
  shortAnswer: 'Arrays are reified and enforce their element type at every store (ArrayStoreException); a generic array\'s element type is erased at runtime, so the JVM would have no way to enforce or even represent that check — the language forbids creating it outright rather than allow an unsound array.',
  detailedAnswer: 'Arrays remember their component type at runtime and throw ArrayStoreException if you try to store an incompatible element. Generics have no such runtime memory — List<String> and List<Integer> are the same erased type at runtime. If you could create a List<String>[], you could alias it through Object[] and store a List<Integer> into a slot, and there would be zero runtime mechanism to catch it — a silent heap-pollution bug rather than a clean exception. Raw List[] is fine because there\'s no generic type argument being lied about — every element is just "a List", checked as such.',
  keyPoints: ['Arrays: reified, checked at runtime', 'Generics: erased, checked at compile time only', 'Combining the two loses the runtime check without gaining the compile-time one'],
  seniorFollowUps: ['How does the JDK implement something like `toArray()` on a generic collection given this restriction?', 'What is the standard workaround, and what warning does it require suppressing?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['generics', 'type-erasure', 'arrays'],
},
{
  id: 'java-gen-q4',
  question: 'A junior engineer asks why they can\'t write `T create() { return new T(); }` inside a generic class, and how they should build a generic object factory instead.',
  shortAnswer: 'Erasure removes the concrete type of T by runtime, so there\'s no class for new T() to allocate. Instead, accept a Supplier<T> (or a Class<T> token) from the caller and delegate construction to it.',
  detailedAnswer: 'The compiler can\'t emit a `new` bytecode instruction without knowing the exact class, and after erasure T is just Object (or its bound) at runtime — there\'s nothing concrete left to instantiate. The standard fix is dependency injection at the type level: have the caller, who does know the concrete type, hand in either a Supplier<T> lambda (`() -> new Foo()`) or a Class<T> reference and use reflection (Class.getDeclaredConstructor().newInstance()) as a fallback. The Supplier approach is preferred in modern code — no reflection, no checked exceptions to wrangle.',
  keyPoints: ['new T() is a compile error, not a runtime one — the compiler catches it directly', 'Supplier<T> is the idiomatic fix in modern Java', 'Class<T> + reflection is the older, heavier alternative'],
  seniorFollowUps: ['What are the downsides of the reflection-based Class<T> approach in a hot path?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['generics', 'type-erasure', 'factory-pattern'],
},
{
  id: 'java-gen-q5',
  question: 'You inherit a codebase with `@SuppressWarnings("unchecked")` scattered across dozens of methods, some suppressing at the class level. How do you evaluate whether this is a real problem, and how would you clean it up?',
  shortAnswer: 'Unchecked warnings mean the compiler couldn\'t verify a cast or array creation is safe — some are genuinely unavoidable (varargs, array-backed generics) but class-level suppression hides real bugs alongside the fine ones. Narrow every suppression to the smallest scope, verify each one is actually safe, and delete any that turn out to mask an actual erasure-related bug.',
  detailedAnswer: 'Class-level @SuppressWarnings silences every unchecked operation in the class, including ones added later by someone who never intended to suppress anything — that\'s the real risk, not the annotation itself. The cleanup process: move each suppression down to the single statement or local variable that needs it, which forces you to look at each one individually; for each, confirm the underlying cast really is safe (e.g. a Supplier-based Object[] to T[] cast guarded by construction logic) versus one that\'s just papering over an actual type confusion; delete suppressions that no longer apply after refactors. A pile of accumulated unchecked warnings is exactly the kind of place a ClassCastException likes to hide months later.',
  keyPoints: ['Not all unchecked warnings are false alarms — some code really is unsafe', 'Suppress at the narrowest scope possible, always', 'Treat suppression review as a code-quality task, not a nuisance to silence and move on from'],
  seniorFollowUps: ['Give an example of an unchecked cast that is actually safe versus one that isn\'t.'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Staff',
  tags: ['generics', 'unchecked-warnings', 'code-quality'],
},
{
  id: 'java-gen-q6',
  question: 'What is heap pollution, and how can a generic varargs method cause it even with @SafeVarargs applied?',
  shortAnswer: 'Heap pollution is when a variable of a parameterized type ends up referencing an object of a different, incompatible parameterized type — typically via a generic varargs array, which is really a T[] under the hood. @SafeVarargs suppresses the compiler warning; it doesn\'t make unsafe code inside the method safe.',
  detailedAnswer: '`static <T> void f(T... args)` compiles to a T[] parameter, which after erasure is really an Object[] (or the bound\'s array type). If the method itself stores something of the wrong type into that array, or leaks a reference to it that a caller then misuses, you get heap pollution — a slot that claims to be T but isn\'t. @SafeVarargs is a developer promise, checked only superficially by the compiler (it verifies the method doesn\'t do the classic unsafe things like returning the array or storing into it from outside), not a guarantee — a method can still misuse the array internally and heap-pollute despite the annotation, if the "obviously safe" pattern is violated in a non-obvious way.',
  keyPoints: ['Heap pollution: a parameterized-type variable referencing the wrong parameterized type underneath', '@SafeVarargs restricted to static/final/private/constructors — those can\'t be overridden unsafely', 'The annotation documents a promise; it doesn\'t enforce it beyond simple compiler checks'],
  seniorFollowUps: ['Why is @SafeVarargs disallowed on a non-final instance method?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Advanced',
  seniority: 'Staff',
  tags: ['generics', 'varargs', 'heap-pollution'],
},
{
  id: 'java-gen-q7',
  question: 'Compare `List<Object>` and `List<?>` as a method parameter type. When would each be the wrong choice?',
  shortAnswer: 'List<Object> is a real, fully generic type — you can add any Object into it — but a caller\'s List<String> can\'t be passed to it (invariance). List<?> accepts a list of literally any element type but, because that type is unknown, you can\'t add anything into it (except null) — only read as Object.',
  detailedAnswer: 'List<Object> is the wrong choice when you want the method to accept any caller\'s list (List<String>, List<Integer>, etc.) — invariance blocks all of those from binding to List<Object>. List<?> is the wrong choice when the method actually needs to insert elements of a specific type, since the unknown wildcard type makes every add (other than null) a compile error. In practice: use List<?> for read-only utility methods (size, iteration as Object); use a bounded wildcard (? extends X) when you need typed reads; use a type parameter <T> when the method needs to both read and write T consistently.',
  keyPoints: ['List<Object> ≠ "accepts anything" — it only accepts an actual List<Object>', 'List<?> accepts any list but is nearly read-only', 'The right choice depends on whether the method reads, writes, or both'],
  seniorFollowUps: ['Where would a bounded wildcard like List<? extends Number> be strictly better than either of these?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['generics', 'wildcards', 'api-design'],
},
{
  id: 'java-gen-q8',
  question: 'A legacy method returns a raw `List` from a library you don\'t control. What are the concrete risks of assigning it directly to a `List<String>` variable, and how would you defensively handle it?',
  shortAnswer: 'The compiler lets the raw-to-generic assignment through with only an unchecked warning — if the underlying list actually contains non-String elements, you get a ClassCastException later, potentially far from the actual bug. Defensively validate elements at the boundary rather than trusting the raw type.',
  detailedAnswer: 'Assigning a raw List to List<String> compiles with an unchecked warning, not an error — the compiler is trusting you that the library really does return Strings. If that trust is misplaced (a bug in the library, or a contract change), the ClassCastException surfaces wherever the first bad element is actually read as a String, which could be deep in unrelated code, making it hard to trace back to the real source. Defensive handling: at the integration boundary, iterate and cast/validate explicitly (or use a stream with an instanceof filter), so the failure surfaces immediately at the boundary with a clear stack trace rather than silently downstream.',
  keyPoints: ['Raw-to-generic assignment is legal with just a warning, not blocked', 'The resulting ClassCastException can surface far from its actual cause', 'Validate at the trust boundary rather than deferring the check'],
  seniorFollowUps: ['How would Collections.checkedList help here, and what\'s its runtime cost?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Advanced',
  seniority: 'Senior',
  tags: ['generics', 'raw-types', 'legacy-interop'],
},
{
  id: 'java-gen-q9',
  question: 'Explain why `Enum<E>` is declared as `Enum<E extends Enum<E>>`. What breaks if you strip the self-bound down to just `Enum<E>`?',
  shortAnswer: 'The self-bound lets Enum\'s compareTo (and other supertype methods) be typed against the concrete enum subclass E, instead of the generic Enum base. Without it, compareTo would accept any Enum, meaning you could legally compare two completely unrelated enum types, which is never sensible.',
  detailedAnswer: 'With the self-bound `E extends Enum<E>`, when Day implements Enum<Day> (implicitly, via `enum Day`), Comparable\'s compareTo becomes compareTo(Day), not compareTo(Enum). This gives you compile-time protection against comparing a Day to a Color. Without the self-bound, the declaration would just be Enum<E>, and E would only be constrained to "some type" with no relationship back to Enum itself — you could plausibly compare across unrelated enum families and only catch the mistake, if ever, via a nonsensical runtime result. This is the "curiously recurring generic pattern," and it shows up whenever supertype behavior needs to stay typed to the concrete subclass — fluent builder chains that return `this` typed correctly are the other common example.',
  keyPoints: ['Self-bound ties E back to the type extending Enum<E>', 'Prevents comparing across unrelated enum families', 'Same pattern used for fluent builders that need subclass-typed return values'],
  seniorFollowUps: ['Where would you apply this pattern in your own API design, outside of enums?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Advanced',
  seniority: 'Staff',
  tags: ['generics', 'self-bounded-types', 'enum'],
},
{
  id: 'java-gen-q10',
  question: 'You need a method that deserializes JSON into a `List<User>` using a generic JSON library. Passing `List.class` loses the element type entirely. How do libraries like Jackson solve this, given that generics are erased?',
  shortAnswer: 'They use the super type token trick: the caller creates an anonymous subclass of a generic abstract class capturing the full parameterized type (e.g. new TypeReference<List<User>>(){}), and the library reads that off the anonymous subclass\'s reflective generic superclass — information the JVM does retain for class declarations, unlike for erased local type usages.',
  detailedAnswer: 'A bare Class<T> token (List.class) can only express a raw class, never a parameterized type — Class<List<User>> doesn\'t exist as a distinct runtime object separate from Class<List>. The super type token pattern works around this by exploiting that a class\'s generic superclass information is NOT erased — javac records it in the .class file for reflection (getGenericSuperclass()). So `new TypeReference<List<User>>(){}` creates an actual (anonymous) class whose generic superclass is literally TypeReference<List<User>>, and the library reflects on that anonymous subclass to recover the full parameterized type at runtime.',
  keyPoints: ['Class<T> alone cannot represent a parameterized type', 'Generic superclass info on a class declaration survives erasure, unlike local generic usage', 'Anonymous subclassing is the mechanism that exploits this'],
  seniorFollowUps: ['Why does this technique require an anonymous (or otherwise concrete) subclass rather than just instantiating TypeReference directly?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['generics', 'type-erasure', 'reflection', 'super-type-token'],
},
{
  id: 'java-gen-q11',
  question: 'A repository interface in your codebase looks like `interface Repository<T, ID, X, Y>` where X and Y are barely used and every current implementation passes the same two concrete types for them. How would you push back on this design in a code review?',
  shortAnswer: 'Extra type parameters that don\'t vary across real implementations add ceremony without buying flexibility — they should be collapsed to concrete types or dropped, and reintroduced only when a second, genuinely different implementation actually needs them.',
  detailedAnswer: 'Generics are a tool for expressing real variation across call sites — when every current (and foreseeable) implementation instantiates X and Y identically, those parameters aren\'t doing type-safety work, they\'re just noise every reader has to parse through. The review pushback: ask what concrete problem X and Y solve today, not hypothetically; if the answer is "future flexibility," point out that adding a type parameter later, when a second real implementation actually needs it, is a much smaller change than the interface change would suggest — Java lets you generify incrementally far more easily than most engineers assume, especially behind a well-encapsulated interface. YAGNI applies to type parameters just as much as to code.',
  keyPoints: ['A type parameter should track real variation, not speculative flexibility', 'Over-parameterized generics hurt readability at every call site', 'Generifying later is usually cheaper than carrying unused flexibility now'],
  seniorFollowUps: ['What would make you agree the extra parameters are justified?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Staff',
  tags: ['generics', 'api-design', 'over-engineering'],
},
{
  id: 'java-gen-q12',
  question: 'What is a bridge method, and why does the compiler need to generate one when a generic interface method is overridden with a more specific parameter type?',
  shortAnswer: 'A bridge method is a synthetic compiler-generated method that preserves polymorphic dispatch after erasure — when compareTo(T) erases to compareTo(Object) in the interface but the implementation writes compareTo(ConcreteType), the compiler adds a compareTo(Object) override that casts and delegates, so virtual dispatch through the interface reference still finds the right method.',
  detailedAnswer: 'Comparable<Day>.compareTo(T) erases to compareTo(Object) in the interface\'s bytecode. If Day writes `public int compareTo(Day o)`, that\'s a different erased signature (compareTo(Day) vs compareTo(Object)) — from the JVM\'s perspective, without help, Day.compareTo(Day) would not actually override Comparable.compareTo(Object), breaking polymorphism through a Comparable reference. The compiler fixes this by synthesizing a second method, compareTo(Object o) { return compareTo((Day) o); }, which does have the matching erased signature and simply forwards to the type-specific one. This is invisible in source but shows up if you reflectively enumerate a class\'s declared methods.',
  keyPoints: ['Erasure can make an override\'s signature not literally match the interface\'s erased signature', 'Bridge methods reconcile this so polymorphic dispatch still works', 'They are synthetic — generated, not written by hand'],
  seniorFollowUps: ['How would you spot a bridge method if you were debugging via reflection or bytecode inspection?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['generics', 'type-erasure', 'bridge-methods', 'polymorphism'],
},
{
  id: 'java-gen-q13',
  question: 'Two overloaded methods, `void process(List<String> l)` and `void process(List<Integer> l)`, fail to compile in the same class with an erasure clash error. What are the real alternatives to fix this, and which would you pick for a public API?',
  shortAnswer: 'Rename one method, use a single method taking List<?> and branch internally on the element type, or overload on a genuinely different erased type (e.g. accept Collection vs List). For a public API, prefer distinct method names — it\'s clearer to callers and avoids brittle runtime type-checking inside the method.',
  detailedAnswer: 'The two signatures both erase to process(List), which the compiler treats as a duplicate method — this isn\'t a generics-specific rule so much as ordinary method overloading rules applied after erasure. Options: (1) give the methods different names (processStrings/processIntegers) — most readable, no runtime type inspection needed; (2) collapse to a single process(List<?> l) and use instanceof checks inside — works but pushes a type-safety problem back to runtime, which generics exist to avoid; (3) change one parameter\'s erased type (e.g. one takes List, the other Collection) — fragile and confusing. For a public API, distinct names win: callers get compile-time clarity about which behavior they\'re invoking, and the implementation doesn\'t need defensive runtime checks.',
  keyPoints: ['Erasure clash is an ordinary overload-collision rule, just triggered by generics', 'Distinct method names are the cleanest fix for public APIs', 'A single List<?> method trades compile-time safety for runtime type inspection'],
  seniorFollowUps: ['Would your answer change for a private/internal-only method?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['generics', 'type-erasure', 'overloading', 'api-design'],
},
{
  id: 'java-gen-q14',
  question: 'Given `<T extends Comparable<T>> T max(List<T> list)`, a caller wants to call it with a `List<Shape>` where Shape only implements `Comparable<Object>`, not `Comparable<Shape>`. Does it compile, and what does that tell you about writing bounds correctly?',
  shortAnswer: 'It does not compile — T is bound to Comparable<T> exactly, and Shape implementing Comparable<Object> doesn\'t satisfy Comparable<Shape>. The fix, and the general lesson, is to bound against a wildcard when you want to accept a Comparable of any supertype: <T extends Comparable<? super T>>.',
  detailedAnswer: 'The naive bound T extends Comparable<T> requires the exact type T to implement Comparable<T>. A class that implements Comparable<Object> (comparing against any object, a looser but still valid contract) doesn\'t literally satisfy Comparable<T> for T=Shape. The fix used throughout the JDK (Collections.max\'s real signature) is <T extends Comparable<? super T>> — this accepts any Comparable implementation as long as it can compare against T or a supertype of T, which is exactly PECS applied to a bound rather than a wildcard parameter. It\'s a subtle but recurring pattern: bounds need the same producer/consumer thinking as parameter wildcards, not just "the obvious" self-reference.',
  keyPoints: ['T extends Comparable<T> is stricter than it looks — an exact-match requirement', 'T extends Comparable<? super T> is the more permissive, more correct bound', 'This is Collections.max\'s actual declared signature'],
  seniorFollowUps: ['Why doesn\'t the JDK just declare Comparable<T> generically looser in the first place?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Expert',
  seniority: 'Staff',
  tags: ['generics', 'bounded-types', 'pecs', 'comparable'],
},
{
  id: 'java-gen-q15',
  question: 'How would you explain, to someone who only knows that "generics prevent ClassCastException," what actually changed in the bytecode versus what changed only for the compiler between a pre-generics `List` and a modern `List<String>`?',
  shortAnswer: 'Nothing changed in the runtime type or bytecode of List itself — a List<String> is the exact same java.util.List class at runtime as a raw List always was. What changed is entirely compile-time: javac inserts the type check at every add and an invisible cast at every get, and rejects code that would violate T. At runtime, generics are gone — this is type erasure.',
  detailedAnswer: 'Before generics, `List list = ...; String s = (String) list.get(0);` required an explicit, unchecked cast that could fail at runtime if the list held something else. After generics, `List<String> list = ...; String s = list.get(0);` looks cast-free, but the compiler is silently inserting the exact same checkcast bytecode instruction after the call to get() — you just don\'t see it in source. The real value generics add is entirely at compile time: the compiler now refuses to let you add anything but a String into that list in the first place, so the inserted cast becomes provably safe rather than a leap of faith. This is worth internalizing because it explains every other erasure gotcha in one shot: nothing at runtime distinguishes List<String> from List<Integer>, because nothing about T survives compilation at all.',
  keyPoints: ['The runtime class is identical before and after generics', 'The compiler inserts the same cast bytecode either way — generics just make it provably safe', 'Every erasure-related gotcha follows from this one fact'],
  seniorFollowUps: ['Would reified generics (like C#) have been possible to retrofit into Java 5 instead? Why did the JDK team not go that route?'],
  topic: 'java',
  subtopic: 'generics',
  difficulty: 'Intermediate',
  seniority: 'Senior',
  tags: ['generics', 'type-erasure', 'fundamentals'],
},

  // ── Concurrency Questions ────────────────────────────────────────
  {
    id: 'java-conc-q1',
    question: 'You call thread.run() instead of thread.start(). What actually happens?',
    shortAnswer:
      'run() is just a regular method call that executes synchronously in the current thread, not a new one. The method blocks until it completes, rather than spawning a concurrent thread.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['threads'],
  },
  {
    id: 'java-conc-q2',
    question: 'Is calling Thread.start() on the same thread object twice safe?',
    shortAnswer:
      'No — it throws IllegalThreadStateException. A Thread object can only be started once; reuse requires creating a new Thread object.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['threads'],
  },
  {
    id: 'java-conc-q3',
    question: 'Two threads call synchronized instance methods on different objects of the same class. Do they block each other?',
    shortAnswer:
      'No — each object has its own intrinsic lock. Synchronization is per-object, not per-class (unless the method is static, in which case the lock is on the class object).',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['synchronization', 'locks'],
  },
  {
    id: 'java-conc-q4',
    question: 'A field is written by one thread and read by another, both without synchronization. Is visibility guaranteed?',
    shortAnswer:
      'No — the write may sit in a cache or register indefinitely and never become visible to other threads. volatile or synchronized on both sides is required to guarantee visibility.',
    detailedAnswer:
      'The Java Memory Model does not guarantee that unsynchronized writes to a non-volatile field become visible to other threads without some explicit synchronization action. Compiler optimizations and CPU caching can delay or prevent visibility indefinitely.',
    keyPoints: [
      'Visibility requires a happens-before relationship (synchronized/volatile/atomic)',
      'A visibility issue can exist even in single-threaded access patterns within a multi-threaded system',
    ],
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['visibility', 'memory-model'],
  },
  {
    id: 'java-conc-q5',
    question: 'What is volatile int count = 5; count++; a race condition?',
    shortAnswer:
      'Yes. volatile guarantees visibility (every read sees the latest write), but not atomicity. count++ is read-modify-write, and two threads can interleave it, producing lost updates.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['volatile', 'atomicity', 'race-condition'],
  },
  {
    id: 'java-conc-q6',
    question: 'Walk through what happens when Thread A writes to a variable while Thread B reads it (both unsynchronized) from the JMM perspective.',
    shortAnswer:
      'There is no happens-before relationship, so the write may never be visible. The JMM permits the CPU to keep the write in a cache, and the reader may observe the stale value indefinitely. Synchronization establishes the relationship that makes it guaranteed visible.',
    detailedAnswer:
      'Without synchronization or volatile, the JMM makes no promises about when (or if) Thread B will see Thread A\'s write. Caches are valid, reordering is allowed, and nothing forces a flush to main memory or a cache invalidation on the reader\'s side.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['memory-model', 'visibility'],
  },
  {
    id: 'java-conc-q7',
    question: 'A synchronized method and an unsynchronized method access the same field. Can a race occur?',
    shortAnswer:
      'Yes. Synchronization only protects code that actually executes within the synchronized block. The unsynchronized method bypasses all locks, creating a race between it and the synchronized code.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['synchronization', 'race-condition'],
  },
  {
    id: 'java-conc-q8',
    question: 'You have synchronized (lockA) { synchronized (lockB) { ... } }, and elsewhere in the codebase synchronized (lockB) { synchronized (lockA) { ... } }. What can happen?',
    shortAnswer:
      'Deadlock. Thread 1 holds lockA and waits for lockB; Thread 2 holds lockB and waits for lockA. Both are permanently blocked with no way forward.',
    detailedAnswer:
      'This is a classic circular dependency. Lock ordering must be consistent across all code paths — if ever two locks are acquired together, acquire them in the same order everywhere.',
    keyPoints: [
      'Lock ordering violations are often subtle and can be hard to spot in review',
      'Timeouts can break deadlock but shouldn\'t be relied upon as the primary prevention strategy',
    ],
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['deadlock', 'lock-ordering'],
  },
  {
    id: 'java-conc-q9',
    question: 'A producer adds items to a list and calls lock.notify(); consumers wait on the same lock. Most items are missed — why?',
    shortAnswer:
      'notify() wakes only one arbitrary thread; if no consumer is waiting (or a different consumer wakes), items are silently lost. Use notifyAll() instead so every consumer that\'s waiting has a chance to wake and check.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['wait-notify', 'producer-consumer'],
  },
  {
    id: 'java-conc-q10',
    question: 'Why must wait() be called in a while loop checking a condition, not just an if?',
    shortAnswer:
      'Spurious wakeups can occur — a thread wakes from wait() without notify() being called. The while loop re-checks the condition; if it\'s false, the thread re-waits. Also, the condition may legitimately change before the thread wakes.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['wait-notify', 'spurious-wakeup'],
  },
  {
    id: 'java-conc-q11',
    question: 'Is synchronized(obj) { obj.wait(); } more efficient for a producer-consumer than spinning with a volatile flag?',
    shortAnswer:
      'Far more efficient. Spinning burns CPU in a tight loop; wait() releases the lock and suspends the thread, using no CPU until notified. For any real workload, wait/notify is the clear winner.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['wait-notify', 'performance'],
  },
  {
    id: 'java-conc-q12',
    question: 'AtomicInteger counter increments are faster than synchronized(lock) { count++; }. Always?',
    shortAnswer:
      'No. AtomicInteger uses CAS (compare-and-swap) which is faster under low contention, but under heavy contention both spin/retry, and the synchronized version may outperform atomic depending on implementation and platform. Measure for your workload.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['atomics', 'synchronization', 'performance'],
  },
  {
    id: 'java-conc-q13',
    question: 'A class uses AtomicInteger count and AtomicBoolean running. Are compound operations like "increment and check if running" atomic?',
    shortAnswer:
      'No — each atomic class is atomic in isolation, but compound operations across multiple atomics are not. Increment runs atomically, check runs atomically, but the sequence can be interleaved with other threads. Wrap both in a synchronized block if atomicity across both is needed.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['atomics', 'race-condition'],
  },
  {
    id: 'java-conc-q14',
    question: 'SimpleDateFormat is thread-unsafe, but a ThreadLocal<SimpleDateFormat> makes it safe to share across threads. Correct?',
    shortAnswer:
      'Correct — ThreadLocal gives each thread its own instance, so no sharing occurs and no synchronization is needed. Each thread formats with its own independent SimpleDateFormat.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['threadlocal', 'thread-unsafe'],
  },
  {
    id: 'java-conc-q15',
    question: 'A thread is blocked on lock.wait(), but the condition it\'s waiting for will never be true again. How do you unblock it?',
    shortAnswer:
      'Call notifyAll() on the lock to wake all waiting threads (so the blocked one wakes too). The thread will re-check its while condition, see it\'s false, and handle accordingly (e.g., throw an exception, exit gracefully). There\'s no way to forcefully exit a wait() from outside.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['wait-notify', 'deadlock'],
  },
  {
    id: 'java-conc-q16',
    question: 'Thread.currentThread().interrupt() is called on a blocked thread. What happens?',
    shortAnswer:
      'If the thread is blocked on wait(), sleep(), or a blocking I/O call, it wakes with an InterruptedException. If it\'s runnable but not blocked, the interrupted flag is set and the thread can check isInterrupted() on the next iteration. Thread.stop() would forcefully kill the thread (deprecated and dangerous); interrupt() is the cooperative alternative.',
    detailedAnswer:
      'Interruption is a polite request, not a forced stop. The interrupted thread has the opportunity to clean up, finish critical operations, or ignore the interrupt if it chooses.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['interruption', 'thread-lifecycle'],
  },
  {
    id: 'java-conc-q17',
    question: 'Catching InterruptedException clears the interrupted flag. If you catch it but don\'t re-throw, what should you do?',
    shortAnswer:
      'Call Thread.currentThread().interrupt() to restore the flag, so code higher up the call stack knows an interrupt was requested and can act on it. Swallowing the exception without restoring the flag hides the interrupt from the caller.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['interruption', 'exception-handling'],
  },
  {
    id: 'java-conc-q18',
    question: 'Design a class that holds a counter, increments it in multiple threads, and guarantees correctness. What approach?',
    shortAnswer:
      'Wrap the counter in synchronized methods if simple, or use AtomicInteger.incrementAndGet() for minimal contention. Immutability (creating a new Counter instance per increment) avoids synchronization entirely but may be impractical depending on usage patterns.',
    detailedAnswer:
      'For a pure counter: AtomicInteger is simplest and performs well. For more complex state, synchronized methods or explicit locks (covered in the locks subtopic) provide clarity. Immutability is elegant but works only if callers accept a new object each increment.',
    seniorFollowUps: [
      'If the counter must also track an associated label that can change, how would you adapt this design?',
    ],
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['design', 'atomics', 'synchronization'],
  },
  {
    id: 'java-conc-q19',
    question: 'An immutable class shared across threads needs no synchronization. True?',
    shortAnswer:
      'True — if an object cannot change after construction, there are no race conditions. Threads can read it concurrently with zero synchronization overhead.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['immutability', 'thread-safety'],
  },
  {
    id: 'java-conc-q20',
    question: 'List<String> list is accessed only from one thread\'s event-handling loop, never shared. Does it need synchronization?',
    shortAnswer:
      'No — thread confinement means the thread owns the list exclusively. Confinement is enforced by design and discipline (never passing the reference outside), not by the language.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['thread-confinement', 'thread-safety'],
  },
  {
    id: 'java-conc-q21',
    question: 'What\'s the difference between "this" being synchronized vs. a private lock object?',
    shortAnswer:
      'Synchronizing "this" locks the instance itself, which is public-facing — external code can also synchronize on the same instance, potentially interfering. A private lock object is under your control and can\'t be misused by callers. For a class published as part of a library, a private lock is safer.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['synchronization', 'design'],
  },
  {
    id: 'java-conc-q22',
    question: 'A service uses a synchronized method for write and an unsynchronized accessor for read to "optimize" the read path. Can this create a race?',
    shortAnswer:
      'Yes — the unsynchronized read has no happens-before relationship with the write, so it may see stale values, or not see updates at all. Both must synchronize on the same lock, or both must use volatile, to guarantee correctness.',
    topic: 'java',
    subtopic: 'concurrency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['synchronization', 'visibility', 'race-condition'],
  },

  // ── JMM Questions ────────────────────────────────────────────────
// Expert
  {
    id: 'java-jmm-q5',
    question: 'A thread writes several plain fields and then a volatile flag. Another thread spins reading that volatile flag and, once true, reads the plain fields. Why is this safe, and what specifically would break if the flag were a plain (non-volatile) boolean?',
    shortAnswer: 'The volatile write acts as a store barrier: all earlier writes on the writer thread cannot be reordered past it, and the volatile read establishes a happens-before edge, so the reader is guaranteed to see every plain write that preceded the flag write. Without volatile, there is no happens-before edge at all — the JIT/CPU could reorder the flag write ahead of the plain writes, and even if it didn\'t, the reader thread might never observe the flag flip due to caching, or might see it flip while the plain fields are still stale.',
    detailedAnswer: 'This is the "safe publication via volatile flag" pattern and it depends on two separate guarantees working together: (1) program-order-preserving barriers around the volatile write prevent the compiler/CPU from moving the plain writes after it, and (2) the volatile-write-happens-before-volatile-read rule gives the reading thread a JMM-guaranteed view of everything that happened-before that write, transitively including the plain field writes. Drop volatile and both guarantees vanish simultaneously — not only could the reader spin forever on a cached stale value of the flag, but even if it eventually observed a flip (e.g. via lucky cache eviction), the plain fields could still show stale or partially-written values because there is no ordering constraint forcing them to be visible.',
    keyPoints: [
      'Volatile write = store barrier + happens-before edge, not just "always read from memory"',
      'The guarantee is specifically transitive: writes before the volatile write are visible after the matching volatile read',
      'Removing volatile removes both the ordering constraint and the cross-thread visibility guarantee — two independent failure modes, not one',
    ],
    seniorFollowUps: ['Would replacing the volatile flag with a synchronized block around both the writes and the reads change the correctness story? What would it cost?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['volatile', 'happens-before', 'memory-barrier', 'safe-publication'],
  },
  {
    id: 'java-jmm-q6',
    question: 'A class has only final fields and looks fully immutable, but its constructor passes `this` to a static registry before returning. Why does that break the final-field safe-publication guarantee, and how would you fix it without giving up the registration?',
    shortAnswer: 'The JMM\'s final-field guarantee only applies once the constructor has finished executing. If `this` escapes to the registry mid-construction, another thread can pull the reference out of the registry and observe the object before its final fields are guaranteed visible — the object can appear partially constructed even though every field is final. Fix it by moving the registration to after construction (e.g. a static factory method that constructs the object, then registers it), so the escape happens strictly after the constructor returns.',
    detailedAnswer: 'The final-field guarantee is anchored to constructor completion, not to the final keyword alone. "This escaping the constructor" is the standard name for the bug: any path that hands out a reference to the object under construction — registering it, starting a thread with it, passing it to a listener, even assigning it to a static field before the constructor body finishes — defeats the guarantee for every thread that later reads that reference through the escape path, not just the field the constructor was working on. The fix is always structural: never let a reference to `this` leave the constructor. A static factory method that constructs first and registers second is the idiomatic solution, and it composes well with the initialization-on-demand holder pattern for singletons.',
    keyPoints: [
      'Final-field visibility is guaranteed as of constructor completion, not as of field assignment',
      '"This escaping the constructor" is the general name for the family of bugs this creates',
      'The fix is structural (separate construction from publication), not an extra keyword',
    ],
    seniorFollowUps: ['Does the same escape hazard apply if the constructor merely logs `this.toString()` to a logger that another thread reads?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['final', 'safe-publication', 'constructor-escape'],
  },
  {
    id: 'java-jmm-q9',
    question: 'A team argues ConcurrentHashMap.get() must be "basically free" because it doesn\'t take a lock, but wonders how a value put by thread A is ever guaranteed visible to thread B calling get() without any explicit synchronization in the caller\'s code. What\'s actually guaranteeing that?',
    shortAnswer: 'ConcurrentHashMap\'s internals establish the happens-before edge for you: writes to bucket entries go through volatile-backed (or Unsafe/VarHandle equivalent) fields internally, so a put() happens-before a get() that observes it, exactly like a plain volatile write/read pair. The caller gets correctness without writing any synchronization because the library already did the JMM work — that\'s the entire point of using a library over a hand-rolled HashMap-plus-manual-locking.',
    detailedAnswer: 'This is a specific instance of a general pattern in java.util.concurrent: high-throughput lock-free or fine-grained-lock data structures still need to satisfy the JMM, and they do it via volatile fields and CAS on the bucket/node level rather than a single coarse lock. For ConcurrentHashMap specifically, the table array reference and the head-of-bin node references are effectively volatile, so a successful put()\'s node write happens-before a subsequent get() traversing the same bin sees it. This is exactly why these classes are documented with explicit "happens-before" language in their Javadoc (e.g. actions in a thread prior to placing an object into any concurrent collection happen-before access or removal from that collection in another thread) — it\'s not folklore, it\'s a spec-level guarantee the class is contractually required to provide.',
    keyPoints: [
      'Lock-free doesn\'t mean unsynchronized — it means synchronized via volatile/CAS at a finer grain than a single lock',
      'The relevant Javadoc explicitly documents the happens-before contract, not just the algorithmic behavior',
      'This is a general pattern across java.util.concurrent, not a ConcurrentHashMap-specific trick',
    ],
    seniorFollowUps: ['How does this compare to the happens-before edge BlockingQueue.put()/take() provides for a producer-consumer handoff?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['concurrenthashmap', 'happens-before', 'java-util-concurrent'],
  },
  {
    id: 'java-jmm-q12',
    question: 'Thread A does two writes in order: `x = 1;` then `volatile flag = true;` (flag is a distinct volatile field from x). Is thread A\'s write to x guaranteed visible to any other thread that observes flag == true, even if that thread never touches flag itself again afterward — and does the reverse hold, that a later read of flag by thread A is guaranteed to still be true?',
    shortAnswer: 'Seeing volatile flag == true guarantees visibility of the earlier x = 1 write through happens-before. It does not guarantee flag remains true; another thread may change it.',
    detailedAnswer: 'Program order places x = 1 before the volatile write. A read that observes that volatile write creates a happens-before edge, so the earlier x write is visible transitively. Volatile provides visibility and ordering, not ownership or permanence: another thread can write false later, and a racing read may observe that newer value.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['volatile', 'happens-before', 'ordering'],
  },

  // Advanced
  {
    id: 'java-jmm-q3',
    question: 'Why would you mark a shared, non-volatile `long` counter field as volatile even in a scenario where you don\'t actually need cross-thread visibility of updates for correctness — just that reads never return a garbage value?',
    shortAnswer: 'Volatile guarantees atomic reads and writes for long and double, preventing a reader from seeing half of the old value and half of the new one.',
    detailedAnswer: 'This is a narrow but real gotcha specific to long and double — every other primitive type (int, boolean, reference, etc.) is guaranteed atomic for read/write by the JLS even without volatile. The risk is concentrated on 64-bit values because a 32-bit JVM (or certain non-mainstream 64-bit implementations) may implement the write as two separate 32-bit stores. In practice, on essentially all production 64-bit JVMs on common hardware today, longs and doubles are written atomically anyway — but that\'s a platform characteristic, not a spec guarantee for non-volatile fields, so code that must be portable and provably correct should not rely on it. volatile long also comes bundled with full visibility/ordering semantics as a side effect, which is usually desirable anyway.',
    keyPoints: [
      'Tearing risk is unique to long/double among primitives — the JLS carves out an exception for them specifically',
      'volatile long fixes tearing as a side effect of forcing a single atomic 64-bit write, not because tearing and visibility are the same problem',
      'Modern mainstream 64-bit JVMs don\'t tear in practice, but relying on that is relying on an implementation detail rather than the spec',
    ],
    seniorFollowUps: ['If you needed atomic tear-free updates AND a compound read-modify-write, would volatile long alone be sufficient?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['volatile', 'word-tearing', 'long', 'double'],
  },
  {
    id: 'java-jmm-q7',
    question: 'Pre-Java-5, the classic double-checked locking singleton (null-check, lock, null-check again, then `instance = new Singleton()`) was broken even on a single-CPU machine with no visible thread scheduling anomalies. What was actually going wrong?',
    shortAnswer: 'The constructor\'s internal field writes and the assignment of the reference to the shared `instance` variable are two separate, independently-reorderable actions with no happens-before edge forcing the constructor to fully finish before the reference becomes visible. The compiler or JIT could legally reorder the reference assignment ahead of some of the constructor\'s writes, so a second thread could see `instance` as non-null and skip the lock, then read fields off a partially-constructed object.',
    detailedAnswer: 'The bug isn\'t about CPU cache coherence or "stale values" in the intuitive sense — it\'s about legal instruction reordering allowed by the pre-Java-5 memory model. Nothing in the old spec prevented the JIT from hoisting the `instance = <address>` assignment ahead of some of the constructor\'s field initializations, because from the perspective of the constructing thread alone (as-if-serial semantics), the reordering is invisible — the object appears fully built to that thread. A second thread performing the unsynchronized fast-path null check has no happens-before relationship to the first thread\'s constructor at all, so it\'s entitled to see any interleaving the reordering allows, including a non-null reference to an object whose fields are still default/zero values. This is precisely what made the bug so insidious: it required no unusual hardware and could pass extensive testing while still being a real, spec-sanctioned failure mode.',
    keyPoints: [
      'The bug is reordering (constructor writes vs. reference assignment), not cache staleness',
      'It requires no synchronization on the reading thread\'s fast path — that absence is exactly what removes any happens-before edge',
      'It\'s a legal outcome under the old model, not an obscure JVM bug — which is why Java 5 had to fix the model itself (JSR-133), not just patch a JVM',
    ],
    seniorFollowUps: ['Why does marking the field volatile fix this specifically, rather than requiring a change to the constructor?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['double-checked-locking', 'reordering', 'jsr-133'],
  },
  {
    id: 'java-jmm-q8',
    question: 'Why does adding `volatile` to the singleton field fix double-checked locking, as opposed to, say, adding a memory barrier only around the constructor call?',
    shortAnswer: 'Volatile\'s write barrier applies specifically to the reference assignment `instance = new Singleton()`: it prevents any of the constructor\'s internal writes from being reordered past that assignment, so by the time another thread observes the non-null reference (via a volatile read), the object is guaranteed fully constructed. The fix has to live at the assignment/publication point because that\'s the actual race — a barrier anywhere else wouldn\'t constrain the specific reordering that causes the bug.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['double-checked-locking', 'volatile', 'memory-barrier'],
  },
  {
    id: 'java-jmm-q10',
    question: 'A producer thread calls `queue.put(task)` on a BlockingQueue and a consumer thread calls `queue.take()`. Neither thread uses synchronized or volatile directly. What guarantees the consumer sees the task object in a fully-initialized state?',
    shortAnswer: 'BlockingQueue implementations are documented to provide a happens-before guarantee: actions in a thread prior to placing an object in the queue happen-before actions after that object is retrieved by another thread. Internally this is implemented with locks or volatile/CAS on the queue\'s own state, but the caller doesn\'t need to know the mechanism — the contract itself is what you rely on, the same way you rely on synchronized\'s contract without inspecting bytecode.',
    detailedAnswer: 'This is the standard producer-consumer happens-before edge and it\'s explicitly called out in java.util.concurrent\'s package documentation, not something you have to derive from first principles each time. The queue\'s internal implementation (ArrayBlockingQueue uses a ReentrantLock, LinkedBlockingQueue uses two locks, SynchronousQueue uses CAS-based handoff) is irrelevant to the caller — what matters is the documented contract that a successful put() happens-before the corresponding successful take() that retrieves that element. This lets you build correct producer-consumer pipelines by composing documented happens-before contracts (queue put/take, executor submit/run, latch countDown/await) rather than reasoning about volatile fields and locks directly every time — which is the entire value proposition of the library.',
    keyPoints: [
      'The guarantee is explicitly documented at the java.util.concurrent package level, not something to infer',
      'Different BlockingQueue implementations use different internal mechanisms (locks vs. CAS) but all honor the same happens-before contract',
      'The practical skill is composing documented happens-before contracts across the library rather than re-deriving visibility guarantees from volatile/synchronized each time',
    ],
    seniorFollowUps: ['If a producer mutates a task object after calling put() but before the consumer\'s take() returns, is that safe?'],
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['blockingqueue', 'happens-before', 'producer-consumer'],
  },
  {
    id: 'java-jmm-q13',
    question: 'Does calling AtomicInteger.incrementAndGet() from one thread and AtomicInteger.get() from another give you the same happens-before guarantee as a volatile write followed by a volatile read?',
    shortAnswer: 'Yes — the underlying value in AtomicInteger (and the other atomic classes) is itself volatile-backed, and every successful CAS operation carries the same happens-before semantics as a volatile write. So a get() that observes the value produced by a prior incrementAndGet() is guaranteed to also see everything that happened-before that increment on the writing thread, exactly like the volatile write/read rule.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['atomic', 'cas', 'volatile', 'happens-before'],
  },

  // Intermediate
  {
    id: 'java-jmm-q1',
    question: 'Two engineers disagree: one says "happens-before means A executes before B," the other says "happens-before means B is guaranteed to see A\'s effects, regardless of timing." Who\'s right, and why does the distinction matter in practice?',
    shortAnswer: 'The second engineer is right. Happens-before is a visibility/ordering guarantee defined by the JMM, not a real-time execution guarantee — two actions can even happen "at the same instant" on different cores and still satisfy happens-before as long as the JMM rule applies. The distinction matters because reasoning about concurrent code by imagining a global clock leads to wrong conclusions about what\'s actually guaranteed safe.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['happens-before', 'fundamentals'],
  },
  {
    id: 'java-jmm-q2',
    question: 'A main thread starts a worker thread, and after the worker finishes (confirmed via join()), the main thread reads a plain (non-volatile) field the worker wrote. Is this safe under the JMM, and which rule makes it safe?',
    shortAnswer: 'Yes, it\'s safe. Thread.start() happens-before any action in the started thread, and every action in a thread happens-before another thread\'s successful return from join() on it. So the worker\'s field write happens-before the main thread\'s join() returning, which happens-before the subsequent read — no volatile or synchronized needed for this specific pattern.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['thread-join', 'happens-before'],
  },
  {
    id: 'java-jmm-q4',
    question: 'A developer declares `private volatile int requestCount;` and increments it with `requestCount++` from multiple request-handling threads, then is surprised the final count is lower than the actual number of requests. What went wrong?',
    shortAnswer: 'volatile guarantees visibility of the latest write, not atomicity of compound operations. `requestCount++` is read-modify-write across three steps, and two threads can both read the same value before either writes back, silently dropping an increment. The fix is AtomicInteger (incrementAndGet()) or a synchronized block around the whole read-modify-write.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['volatile', 'atomicity', 'race-condition'],
  },
  {
    id: 'java-jmm-q11',
    question: 'When would you reach for a volatile field instead of synchronized to protect shared state, and when would that choice be wrong?',
    shortAnswer: 'volatile is right for single-writer status flags or published references where you only need visibility of the latest value and no compound operation — it\'s cheaper because it never blocks. It\'s wrong whenever multiple threads perform a read-modify-write on the field (lost updates), or when you need multiple related fields to update as one atomic, consistent unit — synchronized (or an Atomic/Lock-based structure) is required there because volatile only ever guarantees visibility of one field at a time.',
    topic: 'java',
    subtopic: 'jmm',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['volatile', 'synchronized', 'comparison'],
  },

  // ── Locks Questions ─────────────────────────────────────────────
{
    id: 'java-locks-q1',
    question: 'A junior engineer refactors a synchronized block to use ReentrantLock but forgets the try/finally and just calls unlock() as the last line of the method. What breaks, and when?',
    shortAnswer: 'Nothing breaks until an exception is thrown inside the critical section — then unlock() is never reached, the lock stays held forever, and every other thread trying to acquire it blocks permanently. synchronized would have released automatically on the exception; explicit locks give you no such safety net.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['reentrantlock', 'try-finally', 'gotcha'],
  },
  {
    id: 'java-locks-q2',
    question: 'You need a lock that a thread waiting to acquire can be cancelled out of via Thread.interrupt(). synchronized cannot do this. What can, and what is the mechanism?',
    shortAnswer: 'ReentrantLock.lockInterruptibly() — it blocks like lock() but throws InterruptedException if the waiting thread is interrupted, instead of ignoring the interrupt until acquisition succeeds like synchronized and plain lock() both do.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['lockinterruptibly', 'interruption'],
  },
  {
    id: 'java-locks-q3',
    question: 'Two threads each hold one of two locks and each is trying to acquire the other\'s lock — classic deadlock. How would you redesign the acquisition logic using tryLock() to avoid it entirely, without imposing a global lock ordering?',
    shortAnswer: 'Have each thread attempt tryLock() on the second lock with a timeout; if it fails, release the first lock it already holds and retry the whole sequence (possibly with a randomized backoff). Because neither thread ever blocks indefinitely while holding a lock, the circular-wait condition required for deadlock cannot form.',
    detailedAnswer: 'Lock ordering prevents deadlock by construction but requires a consistent global order across the whole codebase, which becomes hard to maintain as the system grows and locks are acquired dynamically or from data structures without a natural order. tryLock() sidesteps the problem differently: instead of preventing circular wait structurally, it breaks it dynamically. A thread that cannot get the second lock within a bounded time gives up the first lock and retries — no thread ever holds one lock while blocked forever on another, so the deadlock precondition of threads waiting on each other indefinitely simply never occurs. The cost is added complexity (retry loops, potential livelock if timeouts and backoff are not randomized) and no forward-progress guarantee under pathological scheduling, though randomized backoff makes livelock vanishingly unlikely in practice.',
    keyPoints: [
      'Deadlock requires four conditions including hold-and-wait and circular wait — tryLock with backoff removes hold-and-wait by never blocking indefinitely while holding a resource',
      'Randomized backoff between retries avoids livelock (both threads retrying in lockstep forever)',
      'Tradeoff versus lock ordering: no code discipline required across the codebase, but adds retry complexity and no strict fairness guarantee',
    ],
    seniorFollowUps: [
      'How would you bound the number of retries, and what should happen if a thread exhausts them?',
      'Does this approach still work if the locks guard operations with side effects that are not idempotent?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['trylock', 'deadlock', 'lock-ordering'],
  },
  {
    id: 'java-locks-q4',
    question: 'Why does a fair ReentrantLock often perform dramatically worse than the default unfair one under high contention, even though both are correct?',
    shortAnswer: 'Fair mode forces strict FIFO handoff to the longest-waiting thread, so a thread that finishes its critical section and immediately wants the lock again must go to the back of the queue even if no other thread has started waiting yet — this "wake up a parked thread and context-switch to it" path is far more expensive than letting a running thread barge in and grab the just-released lock, which unfair mode allows.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['fairness', 'reentrantlock', 'performance'],
  },
  {
    id: 'java-locks-q5',
    question: 'A bounded blocking queue is built on one ReentrantLock with two Conditions, notFull and notEmpty. Why not just use a single Condition (or plain wait/notifyAll) for both producer and consumer waiting?',
    shortAnswer: 'A single wait-set forces notifyAll() to wake every waiter — producers and consumers alike — even though only one side\'s predicate actually changed, wasting wakeups on threads that will just re-check and go back to sleep. Two Conditions let a producer signal only notEmpty (waking consumers) and a consumer signal only notFull (waking producers), so wakeups are targeted.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['condition', 'producer-consumer'],
  },
  {
    id: 'java-locks-q6',
    question: 'Why does calling readLock().lock() and then writeLock().lock() on the same thread, intending to "upgrade" from a read to a write lock, deadlock on a standard ReentrantReadWriteLock?',
    shortAnswer: 'The write lock is exclusive against readers, including the calling thread\'s own outstanding read lock — the write acquisition blocks waiting for all read locks (including its own) to be released, but the thread can\'t release its read lock because it\'s blocked trying to get the write lock first. There is no atomic upgrade path in the standard API; you must fully release the read lock, then acquire the write lock (accepting that another thread may act in between).',
    detailedAnswer: 'ReentrantReadWriteLock enforces the invariant that a writer has exclusive access and no readers may hold the lock simultaneously. Even though re-entrant acquisition normally lets the same thread acquire a lock it already holds, this does not extend across read-to-write: the implementation treats "any reader holds the lock" as a blocking condition for the writer, without special-casing "the only reader is me." So a thread holding a read lock that requests the write lock blocks forever, waiting on a condition (all readers released) it alone is preventing from becoming true. The documented workaround is to release the read lock first and then acquire the write lock, which is not atomic — another thread can slip in and modify state between the release and the write acquisition, so any invariant the code assumed still held after "upgrading" must be re-validated. StampedLock actually solves this specific problem with tryConvertToWriteLock(), which can attempt the upgrade without releasing first, though it can fail and require falling back to full lock ordering.',
    keyPoints: [
      'The write lock blocks on "any reader holds the lock," including the calling thread — no exception for self-held read locks',
      'Read-to-write upgrade is not offered by ReentrantReadWriteLock; must fully release then reacquire, non-atomically',
      'StampedLock offers tryConvertToWriteLock() as an actual (best-effort, non-blocking) upgrade path',
    ],
    seniorFollowUps: [
      'Why is downgrading (write to read) safe when upgrading is not?',
      'If you needed atomic upgrade semantics, what would you reach for instead?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['readwritelock', 'deadlock', 'lock-upgrade'],
  },
  {
    id: 'java-locks-q7',
    question: 'A read-heavy cache using ReentrantReadWriteLock is healthy at moderate load, but under sustained high read throughput a background writer thread that periodically refreshes the cache starts taking minutes to get the write lock. What is happening, and what is the fix?',
    shortAnswer: 'This is writer starvation: with the default non-fair policy, a continuous stream of new readers can keep barging ahead of a queued writer indefinitely, since the lock favors granting reads while any are already flowing. Constructing the lock with fair=true forces new readers to queue behind a waiting writer, trading some read throughput for a bounded writer wait.',
    detailedAnswer: 'ReentrantReadWriteLock in non-fair mode is documented to favor readers over a waiting writer under sustained read arrival: a new reader can acquire the read lock even while a writer is queued, as long as the internal ordering heuristics permit it, which they typically do when readers keep streaming in continuously. Individually each reader only holds the lock briefly, so the system looks healthy by every per-request metric, but the writer\'s wait time grows unbounded as long as the read stream never lets up — it is starved, not deadlocked, so nothing throws and nothing times out on its own; it just never runs. Constructing the lock with the fair=true flag changes the acquisition policy so that once a thread is queued waiting for the lock (writer or reader), later-arriving threads must queue behind it rather than barge ahead, which caps the writer\'s wait at roughly the time to drain the readers already in flight. The cost is that fair mode also throttles reader-to-reader concurrency somewhat, since new readers now respect the queue rather than freely piling on.',
    keyPoints: [
      'Non-fair mode intentionally favors readers arriving continuously over a single queued writer — documented behavior, not a defect',
      'The failure mode is starvation (unbounded wait, no error), not deadlock or exception — much harder to detect from logs or timeouts alone',
      'fair=true bounds writer wait by making new arrivals queue behind whichever thread is already waiting, at some cost to reader throughput',
    ],
    seniorFollowUps: [
      'How would you detect writer starvation in production before a customer notices stale data?',
      'Is there a way to get bounded writer latency without paying the full fair-mode throughput cost on the read side?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['readwritelock', 'starvation', 'fairness'],
  },
  {
    id: 'java-locks-q8',
    question: 'What does StampedLock.validate(stamp) actually check, and what is the one thing it does NOT guarantee about the values you read during an optimistic read?',
    shortAnswer: 'validate() checks whether a write lock was acquired (and thus the state may have changed) at any point since the stamp was issued — it returns true only if no write occurred in that window. It does not itself make the reads atomic or consistent with each other: if you read multiple fields during the optimistic window, a torn/partial update between two of those reads followed by a later matching write can still slip through undetected in edge cases, so optimistic reads are best used for single-field or otherwise carefully bounded reads.',
    detailedAnswer: 'tryOptimisticRead() returns a stamp representing the lock\'s current write-state without acquiring anything — no memory is written, no CAS happens, readers are completely wait-free. The caller then reads whatever fields it needs directly, and afterward calls validate(stamp), which compares the current write-stamp to the one issued and returns false if any write lock was acquired (successfully) in between. If validate() returns true, it means no writer intervened during the read window, so the values read are guaranteed consistent with a single point in time. The subtlety is that this guarantee only holds for the exact window between tryOptimisticRead() and validate() — if the calling code reads fields, does something with them, and only checks validate() much later, or if it re-reads a field after validate() succeeds, the guarantee no longer covers that later read. Practically, the discipline is: copy all needed fields into locals first, call validate() immediately after, and only trust the locals (not the live fields) once validated.',
    keyPoints: [
      'validate() detects whether a write lock was taken since the stamp was issued, nothing more',
      'The optimistic-read window is exactly between tryOptimisticRead() and the validate() call — reads or logic outside that window are not covered',
      'Correct usage: copy fields to locals, validate immediately, fall back to a real readLock() on failure',
    ],
    seniorFollowUps: [
      'Why is StampedLock not a drop-in replacement for ReentrantReadWriteLock in code that relies on Condition or reentrancy?',
      'How would you unit test correct usage of optimistic reads under concurrent writers?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['stampedlock', 'optimistic-read', 'memory-consistency'],
  },
  {
    id: 'java-locks-q9',
    question: 'A method using StampedLock calls another method on the same object that also tries to acquire the read lock, on the same thread. What happens, and how is this different from ReentrantReadWriteLock?',
    shortAnswer: 'It deadlocks — StampedLock is not reentrant, so a thread that already holds the read (or write) lock blocks forever trying to acquire it again, even recursively through a nested call. ReentrantReadWriteLock explicitly supports this: the same thread can reacquire a lock it already holds, tracked via a per-thread hold count.',
    detailedAnswer: 'StampedLock represents lock state purely as a long stamp with no per-thread ownership or hold-count bookkeeping at all — that lightness is exactly what makes it fast, but it also means the implementation has no way to recognize "this thread already holds this lock" and grant a nested acquisition. A recursive or indirect call that tries to acquire the read (or write) lock again from the same thread is treated identically to a request from any other thread: it queues and waits for the lock to become available, which it never will because the only holder is the thread now blocked waiting on itself. ReentrantReadWriteLock, by contrast, is built on AQS state that explicitly tracks per-thread hold counts for both the read and write sides, so it recognizes a reacquiring thread and just increments the count instead of blocking. The practical implication is that StampedLock is a poor fit for code with any call-graph uncertainty — utility methods that might be called both with and without the lock already held — unless you refactor to guarantee locking happens at exactly one call-site per operation.',
    keyPoints: [
      'StampedLock tracks only a version-like long stamp, with no per-thread ownership or hold count',
      'A reentrant call from the same thread queues behind itself and deadlocks — no special-casing exists',
      'ReentrantReadWriteLock tracks per-thread hold counts via AQS and safely allows reacquisition',
    ],
    seniorFollowUps: [
      'How would a code review catch a hidden reentrant call path before it ships?',
      'If you must migrate performance-critical code from ReentrantReadWriteLock to StampedLock, what structural guarantee do you need first?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['stampedlock', 'reentrancy', 'gotcha'],
  },
  {
    id: 'java-locks-q10',
    question: 'You have a hash map-like structure shared by many threads, and a single global lock is causing severe contention. Lock striping is proposed. What does it cost you when you need to compute the total size across all stripes?',
    shortAnswer: 'A global operation like size() either has to acquire every stripe\'s lock (serializing the whole structure temporarily and hurting the concurrency the striping was meant to provide) or accept an approximate, weakly-consistent count computed without locking everything at once. There is no free lunch: striping trades the correctness/simplicity of a single consistent global view for higher concurrency on per-key operations.',
    detailedAnswer: 'Lock striping\'s entire benefit comes from the fact that most operations only ever need to touch one stripe, so unrelated keys never contend. That assumption breaks down completely for any operation whose correctness requires seeing the whole structure at once — size(), a global iteration, or a whole-map clear(). To get an exact answer, you must acquire every stripe\'s lock, which momentarily collapses the structure back into single-lock behavior and blocks all concurrent access for the duration, the exact cost striping was introduced to avoid. Legacy ConcurrentHashMap sidestepped this by not guaranteeing an exact size() at all — its size/isEmpty results are best-effort estimates computed by summing per-segment counts without a consistent global lock, which can be stale or slightly wrong under concurrent modification but never requires blocking every writer. The architectural lesson generalizes: any sharding or partitioning scheme for scalability pushes the cost of whole-structure operations from "expensive" to "either expensive or approximate," and the right choice depends on whether callers actually need exactness.',
    keyPoints: [
      'Per-key operations stay fast because they touch one stripe; whole-structure operations must touch all stripes or accept approximation',
      'Legacy ConcurrentHashMap size()/isEmpty() were documented as best-effort estimates for exactly this reason',
      'The tradeoff generalizes to any sharded/partitioned design, not just locks',
    ],
    seniorFollowUps: [
      'How would you design an exact, still-performant size() for a striped structure without serializing all writers?',
      'What other operations besides size() typically become the pain points when striping a structure?',
    ],
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['lock-striping', 'concurrenthashmap', 'tradeoffs'],
  },
  {
    id: 'java-locks-q11',
    question: 'What is the practical difference between a Semaphore with 1 permit and a ReentrantLock, and why can that difference cause a subtle production bug?',
    shortAnswer: 'A Semaphore has no concept of ownership — any thread can call release(), not just the one that called acquire() — and it is not reentrant, so the same thread calling acquire() twice with 1 permit deadlocks itself. A ReentrantLock enforces ownership and reentrancy. The bug pattern: code that assumes "acquire/release" behaves like a lock can end up with a different thread accidentally releasing a permit it never held (masking a real bug) or the owning thread deadlocking on a second acquire it assumed was safe.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['semaphore', 'reentrantlock', 'ownership'],
  },
  {
    id: 'java-locks-q12',
    question: 'When would you reach for CyclicBarrier over CountDownLatch, and what is the risk unique to CyclicBarrier that a latch does not have?',
    shortAnswer: 'Use CyclicBarrier when the same fixed group of threads needs to repeatedly synchronize across multiple phases of a computation (it auto-resets after each release), versus CountDownLatch for a one-shot "wait until N things happen" signal, possibly from a different set of threads than the waiters. The unique risk: if any one of the barrier\'s parties fails to arrive (crashes, throws, or hangs), the barrier is broken for every other waiting thread, which gets a BrokenBarrierException — one straggler jams the whole group, whereas a latch has no equivalent all-or-nothing coupling.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['cyclicbarrier', 'countdownlatch', 'comparison'],
  },
  {
    id: 'java-locks-q13',
    question: 'What happens if countDown() is called more times than the initial count on a CountDownLatch, or if it is called by a thread that never calls await()?',
    shortAnswer: 'Nothing goes wrong — the count simply floors at zero and extra countDown() calls are no-ops; there is no requirement that the calling thread ever call await() itself, since the latch is intentionally asymmetric between the threads that count down and the threads that wait.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['countdownlatch'],
  },
  {
    id: 'java-locks-q14',
    question: 'You are asked to build a double-buffering pipeline where a producer thread fills a buffer and a consumer thread drains a different one, then they swap. Which java.util.concurrent.locks / synchronizer classes fit naturally, and why is Exchanger a good candidate here specifically?',
    shortAnswer: 'Exchanger is built for exactly this: two threads meet at exchange() and atomically swap their objects, so the producer can hand over its full buffer and receive an empty one back in a single rendezvous, with no separate locking or copying needed. A general Lock/Condition pair could implement the same protocol manually, but Exchanger expresses the two-party swap directly and more simply.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['exchanger', 'double-buffering'],
  },
  {
    id: 'java-locks-q15',
    question: 'Given that synchronized is well-optimized by modern JVMs (biased/thin/fat locking) and cannot leak due to a forgotten release, what is the actual decision rule for reaching for an explicit Lock instead?',
    shortAnswer: 'Default to synchronized. Switch to an explicit Lock only when you need a capability synchronized structurally cannot provide: tryLock (non-blocking or timed attempts), lockInterruptibly (cancellable waiting), configurable fairness, multiple independent Conditions on one lock, or non-block-structured locking where acquire and release happen in different methods or across a hand-off. Reaching for Lock without needing one of those just adds the try/finally footgun for no benefit.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['reentrantlock', 'synchronized', 'design'],
  },
  {
    id: 'java-locks-q16',
    question: 'A batch job acquires a StampedLock write lock, does substantial work, and needs to log progress through a shared logger that itself takes an unrelated lock. What is the deadlock risk here compared to holding a plain ReentrantLock during the same call?',
    shortAnswer: 'The risk is identical in kind to any lock-ordering deadlock — it is not specific to StampedLock: holding lock A while blocking to acquire lock B is dangerous if some other thread holds B and is trying to acquire A. The StampedLock-specific wrinkle is that because it is not reentrant and has no owner tracking, if the logging path circles back and tries to touch anything guarded by the same StampedLock, you get a silent, hard-to-diagnose self-deadlock with no thread-owner information available in a thread dump, unlike ReentrantLock which reports its owning thread.',
    topic: 'java',
    subtopic: 'locks',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['stampedlock', 'deadlock', 'diagnostics'],
  },

  // ── Concurrent Collections Questions ──────────────────────────────
{
    id: 'java-cc-q1',
    question: 'Why does ConcurrentHashMap.put(key, null) throw NullPointerException when HashMap.put(key, null) is perfectly legal?',
    shortAnswer: 'In a concurrent map, get(key) returning null is ambiguous between "key absent" and "key mapped to null," and resolving that with a follow-up containsKey() call is not atomic with the get — another thread could mutate the map in between. HashMap avoids this ambiguity concern because a single-threaded caller can trust nothing changed between the two calls.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['concurrenthashmap', 'null', 'design'],
  },
  {
    id: 'java-cc-q2',
    question: 'A teammate writes counts.computeIfAbsent(key, k -> counts.get(otherKey) == null ? new AtomicLong() : counts.get(otherKey)) on a ConcurrentHashMap and it intermittently hangs. What\'s happening, and how would you fix it?',
    shortAnswer: 'The mapping function passed to computeIfAbsent is running while the bin\'s internal lock for that key is held, and calling back into the same map (even for a different key, in some JDK versions/table states) can deadlock or throw IllegalStateException — the map explicitly forbids re-entrant modification from within the mapping function. Fix: read whatever you need from the map before calling computeIfAbsent, and keep the lambda a pure computation with no map access.',
    detailedAnswer: 'ConcurrentHashMap guarantees compute()/computeIfAbsent()/merge() run atomically per key by holding that bin\'s synchronized lock (or CAS-installing the new node) for the duration of the mapping function call. If that function tries to mutate or sometimes even read the same map, you can hit a self-deadlock (same thread re-entering a lock it holds is fine for a plain synchronized, but ConcurrentHashMap detects certain recursive update attempts and throws IllegalStateException instead of silently corrupting the table) or, depending on bin state, genuinely block against another key\'s in-progress resize. The fix is structural: never call back into the same ConcurrentHashMap from inside a compute-family lambda — snapshot any needed values first.',
    keyPoints: [
      'compute-family methods hold a per-bin lock for the whole call',
      'Re-entrant map access from the lambda risks deadlock/IllegalStateException',
      'Fix: read dependencies before calling computeIfAbsent, keep the lambda side-effect-free on the same map',
    ],
    seniorFollowUps: ['Would the same hazard exist if the mapping function called a *different* ConcurrentHashMap instead?'],
    example: 'AtomicLong existing = counts.get(otherKey);\ncounts.computeIfAbsent(key, k -> existing != null ? existing : new AtomicLong());',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['concurrenthashmap', 'compute', 'deadlock'],
  },
  {
    id: 'java-cc-q3',
    question: 'How did ConcurrentHashMap\'s internal locking strategy change between Java 7 and Java 8, and what practical difference does it make?',
    shortAnswer: 'Java 7 divided the table into a fixed number of Segments, each guarded by its own ReentrantLock, so concurrency was capped by the segment count chosen at construction. Java 8 removed segments entirely and locks at the level of individual bins (only when a bin is non-empty; empty-bin inserts use CAS), so concurrency scales with the actual table size rather than a fixed upfront number.',
    detailedAnswer: 'Under Java 7, concurrencyLevel effectively set the number of independent locks — writes to keys hashing into different segments could proceed in parallel, but writes within the same segment always serialized regardless of table size, and you had to guess an appropriate concurrencyLevel upfront. Java 8\'s redesign uses CAS to install the first node in an empty bin lock-free, and only synchronizes on the bin\'s head node when appending to an existing chain (or treeifying) — so the number of effective "locks" is proportional to table capacity, which grows with the map, removing the upfront tuning knob and generally reducing contention at scale. Resize is also cooperative in Java 8: multiple threads can help move bins into the new table concurrently.',
    keyPoints: [
      'Java 7: fixed Segment array, each with its own ReentrantLock',
      'Java 8: per-bin locking, CAS for empty-bin insert, synchronized only on head node otherwise',
      'Java 8 concurrency scales with table size, not a fixed constructor parameter',
    ],
    seniorFollowUps: ['Why does get() never need to acquire a lock in either version?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['concurrenthashmap', 'internals', 'java8'],
  },
  {
    id: 'java-cc-q4',
    question: 'Your code iterates a ConcurrentHashMap to build a report, and a concurrent writer updates several entries mid-iteration. What exactly will the iterator see, and what will it never do?',
    shortAnswer: 'It will never throw ConcurrentModificationException, and it\'s guaranteed to reflect the state at (or since) iterator creation, but there\'s no guarantee about which concurrent updates during the traversal it will or won\'t pick up — it may see some, all, or none of them. This is the "weakly consistent" contract.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['concurrenthashmap', 'iterator', 'weakly-consistent'],
  },
  {
    id: 'java-cc-q5',
    question: 'When is CopyOnWriteArrayList a good choice, and when does it become a liability?',
    shortAnswer: 'Good for read-heavy, write-rare collections like listener/observer lists — reads are lock-free and as fast as ArrayList since they always traverse an immutable snapshot array. It becomes a liability under any meaningful write volume, since every single mutation copies the entire backing array (O(n) time and a full allocation), so a list with frequent adds/removes will thrash memory and GC.',
    detailedAnswer: 'The whole design bets that writes are rare enough that O(n)-per-write is acceptable in exchange for zero-cost, zero-contention reads. Typical fit: an event bus or listener registry where dispatch (read/iterate) happens constantly but subscribe/unsubscribe (write) happens occasionally, often at startup/shutdown. Misuse: using it as a general-purpose shared list that both reader and writer threads mutate frequently — each write reallocates and copies the full array, and under high write throughput this both burns CPU and pressures GC with a stream of short-lived array garbage.',
    keyPoints: [
      'Every mutation copies the whole backing array and swaps a volatile reference',
      'Reads never lock, never see partial writes, and never throw ConcurrentModificationException',
      'Wrong fit: high write frequency — O(n) copy cost per write dominates',
    ],
    seniorFollowUps: ['How would CopyOnWriteArraySet\'s contains()/add() cost compare to HashSet\'s, and why?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['copyonwrite', 'tradeoffs'],
  },
  {
    id: 'java-cc-q6',
    question: 'You start iterating a CopyOnWriteArrayList, and on another thread you add a new element before the iteration finishes. Does the iterator see the new element? Can you call iterator.remove() to delete something during the loop?',
    shortAnswer: 'No, the iterator never sees the new element — it\'s bound to the array snapshot taken at iterator-creation time and reflects nothing added afterward, even from the same thread. And no, iterator.remove() (along with add/set) throws UnsupportedOperationException, since the snapshot is read-only by design.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['copyonwrite', 'iterator'],
  },
  {
    id: 'java-cc-q7',
    question: 'Design a bounded producer-consumer pipeline where a burst of fast producers should slow down automatically when consumers fall behind, without producers ever needing to poll or sleep. Which BlockingQueue would you pick and why, and what mistake would silently remove the backpressure you\'re relying on?',
    shortAnswer: 'A bounded ArrayBlockingQueue (or LinkedBlockingQueue constructed with an explicit capacity) — put() blocks once full, which is exactly the backpressure mechanism: producers stall automatically instead of needing to poll. The silent mistake is using LinkedBlockingQueue\'s no-arg constructor, which defaults to Integer.MAX_VALUE capacity — the queue then accepts everything, backpressure never kicks in, and the pipeline just grows memory usage until it OOMs instead of throttling.',
    detailedAnswer: 'The core requirement is that a producer thread calling put() gets blocked by the JVM itself when the queue is full, rather than the application polling isFull() and sleeping — that\'s what put()/take() give you over offer()/poll(). ArrayBlockingQueue is simplest and has a hard fixed capacity by construction, so there\'s no way to accidentally leave it unbounded. LinkedBlockingQueue can match it in behavior with an explicit capacity argument and offers separate put/take locks for slightly better producer/consumer concurrency, but its danger is that omitting the capacity argument silently switches it to effectively unbounded — a very easy review-time miss since the code still compiles and runs fine until load reveals the missing backpressure.',
    keyPoints: [
      'Bounded queue + blocking put()/take() is the backpressure mechanism',
      'ArrayBlockingQueue: fixed capacity by construction, single lock shared by both ends',
      'LinkedBlockingQueue no-arg constructor defaults to unbounded — the silent footgun',
    ],
    seniorFollowUps: ['Why does LinkedBlockingQueue use two separate locks (put/take) while ArrayBlockingQueue uses one — what does that buy you?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['blockingqueue', 'backpressure', 'producer-consumer'],
  },
  {
    id: 'java-cc-q8',
    question: 'Why does Executors.newCachedThreadPool() use a SynchronousQueue internally, and what does that mean for the pool\'s thread count under load?',
    shortAnswer: 'SynchronousQueue has zero storage capacity — a put() only succeeds once some thread calls take() to receive it directly. In the cached thread pool, that means a submitted task can never sit "in the queue" waiting; if no idle worker thread is available to take it immediately, the executor must spin up a brand-new thread instead. That\'s exactly why newCachedThreadPool can grow its thread count without bound under sustained load rather than queuing excess work.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['synchronousqueue', 'executors', 'threadpool'],
  },
  {
    id: 'java-cc-q9',
    question: 'What ordering guarantee does PriorityBlockingQueue give, and what capacity risk does it carry that ArrayBlockingQueue does not?',
    shortAnswer: 'It orders elements by their natural ordering or a supplied Comparator rather than FIFO — take()/poll() always return the smallest element first. Unlike ArrayBlockingQueue, it is unbounded by default, so a producer that outpaces consumers grows it without limit and risks exhausting heap instead of the producer being throttled by a full queue.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['priorityblockingqueue', 'ordering', 'capacity'],
  },
  {
    id: 'java-cc-q10',
    question: 'How does ConcurrentLinkedQueue achieve thread safety without any locks, and what operations does it deliberately not support as a result?',
    shortAnswer: 'It uses a Michael-Scott-style lock-free algorithm: offer()/poll() attempt a compare-and-swap on node links and retry the CAS loop on contention rather than blocking on a lock, so no thread can ever be stalled holding a lock that others wait on. As a tradeoff, it\'s not a BlockingQueue — there\'s no put()/take() that waits for space or an element, only immediately-returning offer()/poll() — and its size() is O(n) and only approximate under concurrent modification.',
    detailedAnswer: 'The CAS-retry-loop approach means progress is guaranteed system-wide (some thread always makes progress even if others are contending) rather than guaranteed for any individual thread, which is the classic lock-free tradeoff versus a lock-based structure where a preempted lock-holder can stall everyone. Because there\'s no locking mechanism to also implement wait/notify-style blocking on, ConcurrentLinkedQueue can\'t offer put()/take() semantics at all — if you need blocking behavior you need LinkedBlockingQueue instead. size() requiring a full traversal (and being stale by the time it returns, given concurrent mutation) is explicitly called out in its Javadoc as a reason not to rely on it for control flow.',
    keyPoints: [
      'Lock-free via CAS retry loops on node links (Michael-Scott algorithm), not locks',
      'Not a BlockingQueue: only non-blocking offer()/poll(), no put()/take()',
      'size() is O(n) and approximate — explicitly discouraged for logic decisions',
    ],
    seniorFollowUps: ['How does this differ from ConcurrentHashMap\'s hybrid CAS + synchronized approach — why can the queue go fully lock-free where the map can\'t always?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['concurrentlinkedqueue', 'lock-free', 'cas'],
  },
  {
    id: 'java-cc-q11',
    question: 'Why is there no "ConcurrentTreeMap" in java.util.concurrent, and what does ConcurrentSkipListMap use instead to get sorted, thread-safe access?',
    shortAnswer: 'A red-black tree (what TreeMap uses) needs whole-subtree rotations to stay balanced after insert/delete, and those rotations don\'t decompose into small independent CAS steps — making a lock-free red-black tree impractical. ConcurrentSkipListMap instead uses a skip list, a probabilistic multi-level linked structure where insertion is a series of localized CAS operations on links, giving average O(log n) operations without ever needing a global lock or a tree rebalance.',
    detailedAnswer: 'Skip lists achieve balance probabilistically rather than by strict invariant: each inserted node is randomly assigned a "level," and higher levels act as express lanes that skip over many lower-level nodes, giving expected O(log n) search depth without any rebalancing step at all. Because insertion only ever needs to CAS-link a new node into each level it participates in — a small, local, retryable operation — it composes naturally with lock-free techniques the way tree rotation does not. TreeMap\'s red-black balancing, by contrast, can require restructuring an arbitrarily large connected portion of the tree on a single insert, which is exactly the kind of wide-blast-radius mutation that\'s hard to make lock-free.',
    keyPoints: [
      'Red-black tree rebalancing requires wide, non-local restructuring — bad fit for lock-free CAS',
      'Skip list balance is probabilistic (random per-node level), not by strict invariant',
      'ConcurrentSkipListMap gives average O(log n), same asymptotic class as TreeMap',
    ],
    seniorFollowUps: ['What happens to ConcurrentSkipListMap\'s guarantees if the random level generator is badly seeded and levels end up skewed?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['concurrentskiplistmap', 'lock-free', 'internals'],
  },
  {
    id: 'java-cc-q12',
    question: 'Contrast the iterator contract of java.util.concurrent collections with the fail-fast iterators of java.util collections like HashMap and ArrayList. Why the deliberate difference?',
    shortAnswer: 'java.util collections use fail-fast iterators that track a modCount and throw ConcurrentModificationException the moment they detect a structural change made outside the iterator, on the assumption that concurrent modification during single-threaded-style iteration signals a bug worth surfacing loudly. java.util.concurrent collections instead give weakly consistent iterators that never throw — they\'re built for genuinely concurrent access, where pausing or failing on a concurrent write would defeat the purpose of the structure existing at all.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['fail-fast', 'weakly-consistent', 'iterator'],
  },
  {
    id: 'java-cc-q13',
    question: 'You need a thread-safe map, but your keys arrive with occasional legitimate null values from an upstream system you can\'t change. What are your realistic options?',
    shortAnswer: 'ConcurrentHashMap is off the table since it rejects null outright. Options: wrap null values in a sentinel object (e.g. Optional.empty() or a dedicated NULL marker) before inserting, or fall back to Collections.synchronizedMap(new HashMap<>()) which does tolerate null but pays for it with a single coarse-grained lock on every operation and requires manual external synchronization when iterating.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['concurrenthashmap', 'null', 'tradeoffs'],
  },
  {
    id: 'java-cc-q14',
    question: 'What does DelayQueue guarantee about element ordering, and what has to be true of the elements you put into it?',
    shortAnswer: 'DelayQueue only allows an element to be taken once its delay has expired, and among expired elements it returns the one with the earliest expiry first. Every element must implement the Delayed interface, which requires getDelay() and a compareTo() consistent with that remaining delay.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['delayqueue', 'ordering'],
  },

  // ── Async Questions ──────────────────────────────────────────────
{
    id: 'java-async-q1',
    question: 'A service under Executors.newFixedThreadPool(20) starts silently accumulating memory during a traffic spike, with no exceptions logged, until it eventually OOMs. What\'s the likely mechanism, and how would you redesign the pool?',
    shortAnswer: 'newFixedThreadPool backs onto an unbounded LinkedBlockingQueue, so once the 20 workers are busy, excess tasks queue indefinitely instead of being rejected — the queue itself becomes the unbounded memory leak. Fix by constructing ThreadPoolExecutor directly with a bounded queue (e.g. ArrayBlockingQueue) and an explicit RejectedExecutionHandler like CallerRunsPolicy to apply back-pressure.',
    detailedAnswer: 'The fixed pool never grows beyond corePoolSize=maximumPoolSize=20 since core and max are equal, so all overload gets absorbed by the queue rather than by spawning threads. Because the queue is unbounded, there is no signal back to callers that the system is overloaded — tasks just pile up as objects on the heap, each holding whatever state/closures it captured, until GC pressure or raw memory exhaustion causes an OOM that looks unrelated to the actual cause. The fix is to size a bounded queue based on acceptable latency/backlog, choose a rejection policy that matches the workload (CallerRunsPolicy for natural throttling, AbortPolicy plus explicit retry/circuit-breaker logic for APIs that need to fail fast), and monitor queue depth as a leading indicator before it becomes a crisis.',
    keyPoints: [
      'Root cause: unbounded LinkedBlockingQueue inside newFixedThreadPool',
      'Overload manifests as silent backlog growth, not exceptions, until OOM',
      'Fix: explicit ThreadPoolExecutor with bounded queue + rejection policy',
    ],
    seniorFollowUps: [
      'How would CallerRunsPolicy change the behavior of the upstream caller under sustained overload?',
      'What metric would you alert on to catch this before it becomes an outage?',
    ],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['threadpoolexecutor', 'backpressure', 'production-incident'],
  },
  {
    id: 'java-async-q2',
    question: 'You configure a ThreadPoolExecutor with corePoolSize=5, maximumPoolSize=20, and an unbounded LinkedBlockingQueue. Under heavy load, how many threads will actually be running, and why?',
    shortAnswer: 'It will stay at 5 threads. ThreadPoolExecutor only creates threads beyond corePoolSize when the queue is full, and an unbounded queue never fills — so maximumPoolSize is unreachable and effectively dead configuration.',
    detailedAnswer: 'ThreadPoolExecutor\'s task-admission order is: use an idle core thread if available; otherwise enqueue; only spin up a thread above core (up to max) once the queue rejects further offers, which for an unbounded queue never happens. This is a very common misconfiguration — people set maximumPoolSize expecting it to provide burst capacity, not realizing it is gated behind queue saturation that will never occur. To actually use the extra capacity, the queue must be bounded (e.g. new ArrayBlockingQueue(100)), at which point once both the queue and the 5 core threads are full, the pool grows toward 20 before rejecting.',
    keyPoints: [
      'Order: core threads -> queue -> threads up to max -> reject',
      'Unbounded queue means max pool size is never reached',
      'Must bound the queue to make maximumPoolSize meaningful',
    ],
    seniorFollowUps: ['If you bound the queue to fix this, what new failure mode do you now need a rejection policy for?', 'How would you size maximumPoolSize relative to corePoolSize for a bursty but mostly-idle workload?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['threadpoolexecutor', 'queueing', 'internals'],
  },
  {
    id: 'java-async-q3',
    question: 'What is wrong with this chain: fetchUser(id).thenApply(user -> fetchOrders(user))?',
    shortAnswer: 'fetchOrders returns a CompletableFuture, so thenApply produces a nested CompletableFuture<CompletableFuture<Orders>> instead of a flat CompletableFuture<Orders>. It should use thenCompose, which flattens the result of a function that itself returns a CompletableFuture.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['completablefuture', 'thencompose', 'composition'],
  },
  {
    id: 'java-async-q4',
    question: 'You need to fetch a user\'s pricing tier and their shipping address concurrently, then combine both into a checkout summary. Which CompletableFuture combinator fits, and how does it differ from chaining with thenCompose?',
    shortAnswer: 'thenCombine, because the two lookups are independent and can run in parallel rather than depend on each other\'s output. thenCompose is for sequential dependency (stage B needs stage A\'s result as input); thenCombine is for two unrelated futures that both need to finish before a joining function runs.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['completablefuture', 'thencombine', 'composition'],
  },
  {
    id: 'java-async-q5',
    question: 'A CompletableFuture chain uses supplyAsync(() -> callSlowLegacyApi()) with no executor argument, deep inside a service that also runs parallel Stream operations elsewhere. Under load, both start getting slower together. What\'s the likely connection?',
    shortAnswer: 'supplyAsync without an explicit Executor runs on the common ForkJoinPool by default — the same pool parallel streams use internally. A blocking legacy-API call sitting on that shared, CPU-sized pool starves worker threads that parallel streams elsewhere in the JVM need, so both slow down together even though they look unrelated.',
    detailedAnswer: 'The common pool is sized around availableProcessors() - 1 specifically because it is meant for short, CPU-bound, non-blocking work — that\'s the assumption parallel streams and default async CompletableFuture stages are built on. A blocking network call occupies one of those few threads for the duration of the call, which is orders of magnitude longer than a typical fork/join task. With only a handful of threads total, even a modest number of concurrent blocking calls can exhaust the pool, and every unrelated parallel stream or default-async CompletableFuture stage submitted meanwhile queues up behind them. The fix is to run any blocking work on a dedicated Executor passed explicitly to the *Async variant, reserving the common pool for genuinely non-blocking CPU work.',
    keyPoints: [
      'supplyAsync with no Executor defaults to the common ForkJoinPool',
      'Common pool is shared by parallel streams and CompletableFuture defaults JVM-wide',
      'Blocking calls there starve unrelated CPU-bound work sharing the same pool',
    ],
    seniorFollowUps: ['How would you detect this in production before a customer notices?', 'Would switching to virtual threads for the legacy API call fix this, and why or why not?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['completablefuture', 'forkjoinpool', 'common-pool', 'production-incident'],
  },
  {
    id: 'java-async-q6',
    question: 'What is the practical difference between handle() and exceptionally() on a CompletableFuture?',
    shortAnswer: 'exceptionally() only runs if the previous stage failed, and only lets you produce a fallback value. handle() runs unconditionally on both success and failure, receiving both the result and the exception (one will be null), letting you transform either outcome in one place.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['completablefuture', 'exception-handling'],
  },
  {
    id: 'java-async-q7',
    question: 'Why does whenComplete() not "swallow" an exception the way exceptionally() can?',
    shortAnswer: 'whenComplete() is meant for observing an outcome (e.g. logging, metrics) without altering it — it always rethrows the original exception (wrapped in a CompletionException if needed) after your callback runs, whereas exceptionally() actually replaces the failed outcome with a fallback value.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['completablefuture', 'whencomplete', 'exception-handling'],
  },
  {
    id: 'java-async-q8',
    question: 'Compare Future and CompletableFuture from an API-design standpoint, not just a features standpoint.',
    shortAnswer: 'CompletableFuture is strictly more capable — composable stages, exception handling, manual completion — but that capability is also a liability: because it exposes complete()/completeExceptionally(), any holder of the reference can force its outcome, breaking the "only the producing task decides the result" guarantee a plain Future gives. Public APIs sometimes intentionally return the narrower Future (or CompletionStage) type to hide that mutability from callers.',
    detailedAnswer: 'Future is a read-only handle from the caller\'s perspective: it can poll, block on get(), or attempt cancel(), but it has no way to force the task\'s outcome. CompletableFuture implements both Future and CompletionStage, and additionally exposes complete(T), completeExceptionally(Throwable), and obtrudeValue/obtrudeException, which let any code holding the reference override the result regardless of what the original task computed. That\'s a deliberate feature for cases like manually bridging a callback-based API into a CompletableFuture, but it means passing a CompletableFuture around as a public return type hands out more power than most callers should have. A common defensive pattern is to return the value as CompletionStage<T> (which drops the completion-mutation methods) or as a plain Future<T> when the composability isn\'t needed by the caller.',
    keyPoints: [
      'Future: read-only handle — poll, block, or cancel, no way to force the outcome',
      'CompletableFuture adds complete()/completeExceptionally(), which any holder can call',
      'Defensive APIs return CompletionStage<T> or Future<T> to hide that mutability',
    ],
    seniorFollowUps: ['When would you deliberately want callers to be able to complete a future manually?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['completablefuture', 'future', 'api-design'],
  },
  {
    id: 'java-async-q9',
    question: 'You have a List<CompletableFuture<Result>> from ten parallel calls and want a single CompletableFuture<List<Result>> once all finish. Why can\'t you just call CompletableFuture.allOf(futures) and return it directly?',
    shortAnswer: 'allOf returns CompletableFuture<Void> — it only signals completion, it discards the individual results. You need to allOf(...).thenApply(v -> futures.stream().map(CompletableFuture::join).collect(toList())) to actually gather the typed results, since join() on an already-completed future returns immediately without blocking.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['completablefuture', 'allof', 'composition'],
  },
  {
    id: 'java-async-q10',
    question: 'Explain the fork/join work-stealing algorithm: why does a worker steal from the tail of another worker\'s deque while pushing/popping its own tasks from the head?',
    shortAnswer: 'Each worker treats its own deque as a LIFO stack (push/pop from the head) for cache locality — the most recently forked, smallest subtask is likely still hot in cache. Idle workers steal from the opposite end (the tail), which holds the oldest, typically largest and coarsest subtasks, minimizing contention with the owner and maximizing the amount of work transferred per steal.',
    detailedAnswer: 'This design solves two problems at once. First, contention: if thieves stole from the same end the owner works from, every steal would race with the owner\'s own pop, requiring more synchronization. Operating on opposite ends means the common case (owner popping its own head) rarely collides with a steal (thief popping the tail). Second, granularity: because fork/join tasks recursively split into smaller pieces, the tail of a worker\'s deque holds tasks forked earliest — before as much subdivision had happened — so they tend to be the largest remaining chunks of work, meaning a steal gives the thief a meaningfully large piece rather than a sliver.',
    keyPoints: [
      'Owner operates LIFO on its own deque head — cache-friendly',
      'Thieves steal FIFO from the tail — minimizes contention with owner',
      'Tail tasks tend to be older/larger, so each steal transfers substantial work',
    ],
    seniorFollowUps: ['How does this differ from a naive fixed static work partition across threads, and when would static partitioning actually be preferable?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['forkjoinpool', 'work-stealing', 'internals'],
  },
  {
    id: 'java-async-q11',
    question: 'A RecursiveTask\'s compute() method forks both halves of a split and then joins both, like: var l = left.fork(); var r = right.fork(); return l.join() + r.join(). What\'s the inefficiency, and how would you fix it?',
    shortAnswer: 'Forking both halves means the current thread contributes no computation itself — it just waits on two joins, wasting a thread. The standard pattern forks one half (left.fork()) and computes the other half directly on the current thread (right.compute()), then joins only the forked one: return left.fork() plus right.compute() then left.join() combined — this way the calling thread does real work instead of purely waiting.',
    detailedAnswer: 'Forking a task submits it to the pool for another worker to potentially steal and run; it does not run for free. If a task forks both children and immediately joins both, the current thread becomes pure overhead — it did no actual computation, and now blocks until two separately scheduled tasks complete, when it could have computed one of them itself. The idiomatic pattern is: fork the left subtask (hand it off for possible stealing), call right.compute() directly (recurse synchronously on the current thread, avoiding scheduling overhead for that half), then join() the left result and combine. This halves the number of tasks that need to be scheduled/stolen for the same amount of parallelism.',
    keyPoints: [
      'Forking both halves wastes the current thread as a pure waiter',
      'Standard idiom: fork one half, compute() the other directly, then join',
      'Reduces scheduling overhead while preserving parallel opportunity via stealing',
    ],
    seniorFollowUps: ['How would you choose the sequential-cutoff threshold for the base case, and what happens if it\'s set too low vs too high?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['forkjoinpool', 'recursivetask', 'performance'],
  },
  {
    id: 'java-async-q12',
    question: 'Why does ForkJoinPool.commonPool() default to availableProcessors() - 1 threads, and what risk does that pose if you submit a blocking I/O task to it directly?',
    shortAnswer: 'It reserves one core for the calling/main thread, on the assumption the pool is used for short, CPU-bound, non-blocking work. A blocking I/O task occupies one of these few threads for far longer than intended, and since the pool is shared JVM-wide by parallel streams and default-async CompletableFuture stages, it can starve unrelated work.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['forkjoinpool', 'common-pool', 'sizing'],
  },
  {
    id: 'java-async-q13',
    question: 'A junior engineer proposes replacing a CPU-bound image-processing thread pool (currently sized to availableProcessors()) with Executors.newVirtualThreadPerTaskExecutor() to "get more concurrency for free." Is this a good idea?',
    shortAnswer: 'No. Virtual threads eliminate the cost of blocking/waiting, not the cost of computation — CPU-bound work still contends for the same fixed number of physical cores regardless of how many (virtual or platform) threads submit it. Flooding a CPU-bound workload with unlimited virtual threads just adds scheduling overhead and context-switch pressure without increasing real throughput; the pool should stay sized near the core count.',
    detailedAnswer: 'Virtual threads solve the specific problem of thread-per-blocking-task not scaling — a platform thread parked on I/O is an expensive, wasted OS resource, and virtual threads make that essentially free by unmounting from the carrier while blocked. But image processing is CPU-bound: every virtual thread doing this work still needs an actual core to execute on, and there\'s no unmount/remount benefit because the thread is never blocked, just computing. Creating one virtual thread per task here just means more runnable tasks contending for the same small number of cores, adding scheduling churn without adding parallelism. The right call is to keep a bounded pool (platform or virtual, it barely matters) sized close to availableProcessors() for this workload.',
    keyPoints: [
      'Virtual threads cut the cost of blocking/waiting, not computation',
      'CPU-bound work is still core-bound no matter the thread type',
      'Correct sizing for CPU-bound pools stays near availableProcessors() regardless of Loom',
    ],
    seniorFollowUps: ['For which part of a typical request-handling pipeline (parsing, business logic, I/O calls) would virtual threads actually help?'],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['virtual-threads', 'loom', 'cpu-bound', 'sizing'],
  },
  {
    id: 'java-async-q14',
    question: 'How does a virtual thread\'s handling of a blocking call differ from a platform thread\'s, at a mechanical level?',
    shortAnswer: 'A platform thread blocked on I/O parks the underlying OS thread, which sits idle consuming a kernel resource. A virtual thread instead unmounts from its carrier platform thread when it blocks, freeing that carrier to run a different virtual thread — the blocking call\'s wait is tracked by the JVM scheduler rather than tying up an OS thread for its duration.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['virtual-threads', 'loom', 'scheduling'],
  },
  {
    id: 'java-async-q15',
    question: 'What gap in CompletableFuture-based concurrency does structured concurrency (StructuredTaskScope) aim to close?',
    shortAnswer: 'Nothing in a CompletableFuture chain (allOf, thenCombine, etc.) prevents a sibling subtask from continuing to run after another subtask in the same logical operation fails or the caller stops caring — subtasks can effectively leak. StructuredTaskScope treats a set of subtasks as one unit with one lifetime, so a scope\'s cancellation or a ShutdownOnFailure trigger propagates to and cancels all of its children together.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Lead',
    tags: ['structured-concurrency', 'loom', 'lifecycle'],
  },
  {
    id: 'java-async-q16',
    question: 'You need a periodic health check that runs every 5 seconds, but the check itself occasionally takes 8 seconds to complete. Compare scheduleAtFixedRate vs scheduleWithFixedDelay for this case.',
    shortAnswer: 'scheduleAtFixedRate schedules based on the original start time, so an 8-second run under a 5-second period causes the next execution to fire immediately (it\'s already "late"), potentially queuing back-to-back catch-up runs once things recover. scheduleWithFixedDelay schedules 5 seconds after the previous run\'s completion, so an overrun simply pushes the whole schedule later without pile-up — almost always the safer choice for a health check whose duration can vary.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['scheduledexecutorservice', 'fixed-rate', 'fixed-delay'],
  },

  // ── JVM Internals Questions ────────────────────────────────────
{
    id: 'java-jvm-q1',
    question: 'Why does the parent-first delegation model prevent a malicious application-level class named java.lang.String from being used to spoof the real one?',
    shortAnswer: 'Every class loader delegates to its parent first, all the way up to bootstrap. Since java.* classes only exist on the bootstrap classpath, bootstrap always resolves java.lang.String before the application loader gets a chance, so an attacker-supplied java.lang.String on the classpath is never even asked for.',
    detailedAnswer: 'ClassLoader.loadClass() checks a cache, then calls the parent\'s loadClass() before ever calling its own findClass(). Because this happens recursively up to bootstrap, and bootstrap is the loader that actually has java.lang.String available, bootstrap resolves and returns its trusted version before the request ever reaches the application loader that holds the spoofed class. The application-defined class with that name never gets loaded for that fully-qualified name in that loader chain. Note this is a convention enforced by the default loadClass() implementation, not something the JVM enforces unconditionally — a custom loader that overrides loadClass() (not just findClass()) instead of preserving delegation can defeat this, which is why the JVM additionally restricts defining classes in the java.* package from non-bootstrap loaders at a lower level (SecurityException on attempts to define classes in protected packages).',
    keyPoints: ['Delegation walks all ancestors before a loader tries itself', 'Bootstrap always wins for java.* names because only it has them', 'Delegation is convention-level (loadClass), reinforced by a package-protection check at define-time'],
    seniorFollowUps: ['What stops a custom loader that overrides loadClass() directly from bypassing this protection entirely?', 'How does this interact with the module system\'s stronger encapsulation in Java 9+?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['classloader', 'delegation', 'security'],
  },
  {
    id: 'java-jvm-q2',
    question: 'An app server that hot-redeploys web applications shows steadily climbing Metaspace usage across redeploys, eventually throwing OutOfMemoryError: Metaspace, even though each redeploy is supposed to unload the old version. What\'s the likely root cause and how would you confirm it?',
    shortAnswer: 'A classloader leak: something outside the redeployed app\'s own classloader — a thread pool thread, a ThreadLocal, a static reference in a shared library, a JDBC driver registered globally — still holds a reference into the "old" deployment, which transitively pins its ClassLoader and every class it loaded. Confirm with a heap dump: look for multiple live instances of the app\'s ClassLoader class instead of just the current one.',
    detailedAnswer: 'For a class (and its static state) to be collected, both the Class object and its defining ClassLoader must become unreachable — every instance loaded by that loader must also be unreachable. Redeploy frameworks create a fresh classloader per deployment specifically to make bulk-unload possible, but any GC root outside the new deployment holding a reference into the old one (a still-running non-daemon thread started by the old app, a static field in a JDK class like driver registries, a ThreadLocal never cleared) keeps the whole old ClassLoader graph alive. Confirming it means taking a heap dump after a redeploy and using the dominator/retained-size view to find duplicate ClassLoader instances, then tracing the GC root path back from one to whatever is holding it.',
    keyPoints: ['Class + defining ClassLoader must both be unreachable to unload', 'One external reference into the old deployment pins the entire loader graph', 'Heap dump dominator analysis, not just symptom (OOM) inspection, finds the actual leak'],
    seniorFollowUps: ['Name two common JDK/library patterns that cause exactly this kind of leak.', 'Why does raising -XX:MaxMetaspaceSize only delay, not fix, this class of problem?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['classloader', 'metaspace', 'memory-leak'],
  },
  {
    id: 'java-jvm-q3',
    question: 'Two frameworks in the same JVM each independently load the same third-party jar with their own classloader. Code passes an instance from one framework into the other, and it throws ClassCastException even though both sides reference "the same" class. Why?',
    shortAnswer: 'Class identity in the JVM is the pair (fully qualified name, defining loader), not just the name. Even byte-identical bytecode loaded by two different loaders produces two distinct, mutually incompatible Class objects — instanceof and casts between them fail exactly as if they were unrelated types.',
    detailedAnswer: 'This is a structural consequence of how the JVM scopes type identity, and it\'s a very common real bug in plugin architectures, app servers, and OSGi-style module systems: two loaders each independently defining the same class name from the same or even byte-identical .class file still get two separate Class objects, because linking and verification happen per (name, loader) pair, not per bytecode content. An object created via one loader\'s Class carries that Class as its runtime type; casting it against the other loader\'s Class for "the same" class fails ClassCastException with a message that (confusingly) shows the identical class name on both sides. The fix is architectural: share the class through a common ancestor loader (e.g. put the shared jar on a parent classpath both frameworks delegate to) rather than let each load it independently.',
    keyPoints: ['Identity = (name, defining loader), not just name', 'Byte-identical bytecode from two loaders still yields incompatible types', 'Fix: share the class via a common parent loader instead of loading it twice'],
    seniorFollowUps: ['How would you diagnose this from just the exception message, which shows the same class name on both sides?', 'How does OSGi\'s classloader-per-bundle model manage to share types across bundles despite this?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['classloader', 'class-identity', 'classcastexception'],
  },
  {
    id: 'java-jvm-q4',
    question: 'You reference a class\'s static final int constant from another class, but changing that constant\'s value in the defining class and recompiling only the defining class (not the referencing class) doesn\'t change what the referencing class prints. Why, and what does this have to do with initialization timing?',
    shortAnswer: 'A compile-time constant expression (static final of a primitive or String, initialized with a constant) is inlined by the compiler directly into the referencing class\'s bytecode as a literal, not looked up at runtime. Recompiling only the referencing class would pick up the new value; recompiling only the defining class does not, since the old literal is already baked into the referencer\'s class file — which is also why merely referencing such a constant never triggers the defining class\'s initialization.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['class-loading', 'initialization', 'constants'],
  },
  {
    id: 'java-jvm-q5',
    question: 'What actually changed when PermGen was replaced by Metaspace in Java 8, and what problem did it solve?',
    shortAnswer: 'Class metadata moved from a fixed-size region inside the JVM-managed heap (PermGen) to native memory outside the heap (Metaspace), which by default grows unbounded instead of hitting a fixed cap. It solved the common "OutOfMemoryError: PermGen space" seen in apps that loaded many classes over time (frequent redeploys, heavy proxy generation), at the cost of needing to explicitly bound MaxMetaspaceSize to avoid unbounded native memory growth instead.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['metaspace', 'permgen', 'memory-areas'],
  },
  {
    id: 'java-jvm-q6',
    question: 'A service using a dynamic-proxy-heavy framework (generating a new proxy class per unique method signature combination at runtime) slowly climbs in Metaspace usage over days under varied traffic, then throws OutOfMemoryError: Metaspace. Is this necessarily a "leak," and how would you tell?',
    shortAnswer: 'Not necessarily — it could be legitimate unbounded class generation rather than a leak: if the framework generates a genuinely unbounded number of distinct proxy classes (one per unique signature it has never seen) and none of them are ever unloaded, usage grows even with zero leaked references. Distinguish by checking whether generated classes are collectible (their defining loaders eventually become unreachable) versus permanently rooted; a flat/plateauing class count under steady traffic points to bounded generation, while unbounded growth under repeated but varied traffic without plateauing points to a real generation-pattern problem.',
    detailedAnswer: 'The key diagnostic split is "is the number of distinct generated classes actually bounded by the problem domain, or does it grow with something like request diversity or time itself." Take periodic Metaspace usage snapshots (or class-loading counts via JFR/JMX) under representative load: if usage plateaus once the framework has seen most signature combinations, it is bounded generation that just needs a larger -XX:MaxMetaspaceSize headroom. If it keeps growing linearly with uptime or with request volume rather than with distinct input shapes, that points either to a true classloader leak (generated classes never becoming unreachable despite being logically done with) or to the framework generating a fresh class per call instead of caching/reusing by signature, which is a framework-level bug, not a leak in the classic reference sense. Heap dump analysis of ClassLoader/Class instance counts over time is the concrete way to tell them apart.',
    keyPoints: ['Unbounded but legitimate generation can look identical to a leak in raw metrics', 'Plateau under steady load = bounded generation; unbounded growth = leak or missing caching', 'JFR/JMX class-loading counts plus heap dump instance counts over time is the actual diagnostic'],
    seniorFollowUps: ['What JFR event would you enable to get class-loading/unloading counts without a full heap dump?', 'If it turns out to be legitimate unbounded generation, what design change fixes it at the framework level?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['metaspace', 'diagnostics', 'classloader'],
  },
  {
    id: 'java-jvm-q7',
    question: 'A thread dump shows one thread perpetually in RUNNABLE with a stack trace hundreds of frames deep, all in the same few application methods, right before the process crashes with a StackOverflowError. Meanwhile another service instance dies with OutOfMemoryError: Java heap space under sustained high request volume with normal-depth stacks. How do these two failures differ structurally, and what does each tell you about where to look?',
    shortAnswer: 'StackOverflowError is a single thread exceeding its own bounded JVM stack (-Xss), almost always from recursion without a proper base case or termination — the fix is in that thread\'s call logic, and other threads/the heap are unaffected. Heap-space OOM means the shared heap\'s live (reachable) object set exceeds -Xmx across the whole process — the fix is either in what\'s being retained (leak) or in sizing, and a heap dump/dominator analysis is the tool, not a thread dump.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['stackoverflow', 'oom', 'diagnostics'],
  },
  {
    id: 'java-jvm-q8',
    question: 'A JVM is configured with -Xmx4g on a container with an 8GB memory limit and a 200-thread pool using the default -Xss, yet the container gets OOM-killed by the OS well before heap usage approaches 4GB, and the JVM itself throws "unable to create new native thread." What\'s going on, and what two independent levers fix it?',
    shortAnswer: 'Total process memory isn\'t just heap — it\'s heap + metaspace + (thread count x -Xss) + JIT code cache + other native/off-heap memory, and separately the OS has its own process/thread ulimit. With 200 threads at a default 1MB stack that\'s already ~200MB of native stack memory before counting everything else, and if the container\'s thread/process ulimit (not just memory) is capped low, new thread creation fails outright regardless of how much memory is free. Fix by lowering -Xss and/or bounding the thread pool (reduces native memory and thread count), and separately raising the OS-level thread/process ulimit if that\'s the actual ceiling being hit.',
    detailedAnswer: 'This scenario deliberately conflates two different resource ceilings that both manifest as thread-creation failure. The first is memory-based: -Xmx only bounds the Java heap; every thread additionally consumes native stack memory (-Xss, default ~512KB-1MB depending on platform) plus the process carries Metaspace, JIT-compiled code cache, direct ByteBuffers, and GC bookkeeping structures — none of which -Xmx accounts for. Two hundred threads is real but modest native memory, so the more likely proximate cause named in the question ("unable to create new native thread," not an OOM-killer log line) is the second ceiling: OS-level limits on threads-per-process or processes-per-user (ulimit -u, or a container\'s pids cgroup limit), which fail thread creation regardless of how much memory is actually free. The two fixes are genuinely independent: shrinking -Xss and/or the pool size addresses memory-side pressure and reduces how close you get to any memory ceiling; raising the OS thread/process ulimit (or the container\'s pids limit) addresses the count-based ceiling directly. In practice you check both — `ulimit -u` and the cgroup pids controller — rather than assuming it\'s a memory problem just because heap is involved elsewhere in the picture.',
    keyPoints: ['Process memory = heap + metaspace + (threads x stack size) + code cache + off-heap, not just -Xmx', 'unable to create native thread often means an OS thread/process ulimit or cgroup pids limit, not memory exhaustion', 'Two independent fixes: reduce native footprint (Xss/pool size) and raise the OS-level ceiling'],
    seniorFollowUps: ['How would you check whether the actual limiting factor is memory or the pids/ulimit ceiling before making a change?', 'Why doesn\'t raising -Xmx do anything for this specific error?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['oom', 'threads', 'jvm-flags', 'containers'],
  },
  {
    id: 'java-jvm-q9',
    question: 'Why does the JVM verify bytecode at link time even for classes compiled by the same javac that will run them, instead of trusting the compiler?',
    shortAnswer: 'The JVM has no way to know a class file actually came from that trusted compiler — it could be hand-crafted, come from a different-language compiler, or arrive over the network from an untrusted source — so verification is a runtime security/safety boundary independent of provenance, not a redundant check on a trusted toolchain.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['bytecode', 'verification', 'security'],
  },
  {
    id: 'java-jvm-q10',
    question: 'Why did the JVM designers choose a stack-based instruction set over a register-based one, and what does the JIT compiler have to do because of that choice?',
    shortAnswer: 'A stack-based design needs no register file to model, which keeps bytecode simple and trivially portable across every target CPU\'s different register layouts — the tradeoff is more instructions per operation (values are pushed/popped explicitly) than a register machine needs. The JIT compiler\'s job includes register allocation: translating stack-based bytecode operations into efficient native code that actually uses the target CPU\'s physical registers instead of an emulated stack.',
    detailedAnswer: 'Register-based bytecode (like Dalvik\'s, by contrast) encodes operand locations directly in each instruction, which is denser and can be faster to interpret, but ties the bytecode format\'s complexity to decisions about register count/addressing that vary by target. A stack machine sidesteps that entirely: every instruction implicitly operates on the top of an operand stack, so the format stays simple and uniformly portable regardless of what the eventual execution target\'s register architecture looks like — a real concern for a "write once run anywhere" bytecode meant to run interpreted or JIT-compiled across arbitrary CPUs. The cost is paid at execution time: naively interpreting stack bytecode is slower than a register machine would be for the same logic, and it is JIT compilation\'s job to undo that abstraction — perform register allocation, eliminate redundant push/pop traffic, and emit native code that keeps hot values in physical registers rather than round-tripping through an emulated stack for every operation.',
    keyPoints: ['Stack-based bytecode avoids encoding target-specific register layouts, staying portable', 'Tradeoff is instruction count/interpreter overhead versus a register machine', 'JIT compilation performs the register allocation that turns stack semantics into efficient native code'],
    seniorFollowUps: ['How does this portability tradeoff compare to Android\'s Dalvik/ART register-based bytecode design choice?', 'Does interpreter overhead from the stack model still matter once a method is C2-compiled?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['bytecode', 'stack-machine', 'jit'],
  },
  {
    id: 'java-jvm-q11',
    question: 'What\'s the practical difference between HotSpot\'s C1 and C2 compilers in tiered compilation, and why keep both instead of just always using C2?',
    shortAnswer: 'C1 compiles quickly with lighter optimization and instruments the code with profiling counters; C2 compiles slower but applies much more aggressive optimization based on the profile data C1 gathered. Keeping both balances startup/warmup latency (C1 gets code running fast) against steady-state throughput (C2 gets it running well) — going straight to C2 for everything would make every method pay C2\'s heavier compilation cost even for code that only runs a handful of times and never justifies it.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['jit', 'tiered-compilation', 'c1-c2'],
  },
  {
    id: 'java-jvm-q12',
    question: 'A polymorphic call site that dispatches to one of five implementations, called in a tight loop, runs at compiled-native speed for the first four implementations encountered during warmup but noticeably slows down when a fifth implementation starts appearing. Explain what\'s structurally happening at the JIT level.',
    shortAnswer: 'C2 speculatively specializes call sites based on the small number of concrete types actually observed (monomorphic/bimorphic inline caches); once a site sees more implementing types than that specialization can handle (megamorphic), the earlier compiled assumption is invalidated, the code is deoptimized, and dispatch falls back to a slower generic virtual-call path — sometimes back through the interpreter until it recompiles for the new profile.',
    detailedAnswer: 'HotSpot\'s inline caching works in tiers: a call site starts uninlined, becomes monomorphic-inlined once it has only ever seen one receiver type, can widen to a bimorphic cache for two types, and falls back to a full virtual dispatch (megamorphic) once it sees more than that — losing the ability to inline or speculatively optimize based on a single expected type. If C2 had already compiled aggressive type-specialized code assuming a narrow set of implementations (e.g. inlining one implementation\'s body directly into the call site), a previously-unseen fifth type showing up violates that assumption, forcing a deoptimization: the compiled frame is discarded, execution resumes in the interpreter, and the JIT will recompile with a more general (and necessarily slower, since it can no longer inline a single implementation) strategy reflecting the now-wider observed type profile. This is a genuine structural performance cliff, not a resource issue — nothing shows up as an error, exception, or GC pause, which is exactly why it\'s hard to diagnose without JIT-level tooling (e.g. -XX:+PrintCompilation, JFR compiler events) rather than ordinary application logs.',
    keyPoints: ['Inline caches specialize for monomorphic/bimorphic call sites; megamorphic sites lose inlining', 'Exceeding the specialized assumption triggers deoptimization, not an error', 'Diagnosis needs JIT-level visibility (PrintCompilation/JFR), not application logs, since nothing throws'],
    seniorFollowUps: ['What JFR event or flag would surface this deoptimization happening in production without attaching a debugger?', 'How would you redesign the call site (e.g. via a different dispatch strategy) to avoid this cliff if the fifth implementation is unavoidable?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jit', 'deoptimization', 'polymorphism'],
  },
  {
    id: 'java-jvm-q13',
    question: 'What\'s the tradeoff in raising -Xss well above the default, versus leaving it low?',
    shortAnswer: 'A larger -Xss lets each thread recurse deeper before StackOverflowError, but every thread pays that reservation whether it needs the depth or not, so a large -Xss multiplied across a large thread pool meaningfully inflates total native memory footprint — a common contributor to container OOM-kills even when heap usage looks fine.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['jvm-flags', 'stack', 'tuning'],
  },
  {
    id: 'java-jvm-q14',
    question: 'What\'s the difference between -Xms and -Xmx, and why might a production service set them to the same value?',
    shortAnswer: '-Xms is the initial heap size, -Xmx the maximum; setting them equal skips the runtime heap-resizing the JVM would otherwise do as usage grows toward the max, avoiding the pauses and allocation overhead that come with resizing under load — a common latency-sensitive-service default.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['jvm-flags', 'heap', 'tuning'],
  },
  {
    id: 'java-jvm-q15',
    question: 'Why is a Method.invoke() call typically slower than calling the same method directly, and does that gap stay constant across many calls?',
    shortAnswer: 'Reflective invocation goes through extra access-checking and argument marshaling (boxing/unboxing, varargs packing into an Object array) that a direct call skips entirely. The gap narrows after warmup — HotSpot "inflates" a reflective call from a slow native path to a generated bytecode accessor after a threshold number of invocations (default ~15), so a hot reflective call site is much closer to direct-call cost than a cold one.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['reflection', 'performance'],
  },
  {
    id: 'java-jvm-q16',
    question: 'A library that worked fine on Java 8 using setAccessible(true) to read a private field on a java.util internal class now throws InaccessibleObjectException on a current JDK. What changed, and why can\'t setAccessible just override it the way it used to?',
    shortAnswer: 'The module system introduced strong encapsulation: JDK internal packages are no longer open by default, so setAccessible(true) alone is no longer sufficient to break into them the way it was pre-modules — the target module must explicitly export/open that package (via --add-opens or a module descriptor), which application code can\'t grant itself at runtime.',
    detailedAnswer: 'Pre-Java 9, setAccessible(true) suppressed all Java-level access checks (private/protected/package) uniformly, so reflection could reach into any class\'s internals given only a security manager\'s permission. The module system (JPMS, Java 9+) added a second, independent access dimension on top of that: whether a package is exported/opened by its module at all. Reflective deep access into a non-open JDK-internal package now fails with InaccessibleObjectException regardless of setAccessible(), because the module boundary is checked first and by default internal packages are neither exported nor opened. The workaround requires an external grant the caller can\'t self-issue: --add-opens on the command line (or an Add-Opens manifest entry, or a module-info requires/opens directive if you control the target module), none of which the reflecting code itself can silently apply. This has broken a substantial amount of reflection-heavy tooling (serialization libraries, mocking frameworks, ORMs) that historically depended on unrestricted setAccessible() against JDK internals specifically.',
    keyPoints: ['setAccessible bypasses Java access modifiers, not module export/open boundaries', 'JPMS added a second, independent access check that setAccessible cannot override alone', 'Fix requires an external grant (--add-opens) — the calling code cannot self-authorize it'],
    seniorFollowUps: ['If you don\'t control the target JVM\'s launch flags, what options are left for a library that depends on this access?', 'Why did the JDK maintainers consider this worth breaking backward compatibility for?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['reflection', 'modules', 'encapsulation'],
  },
  {
    id: 'java-jvm-q17',
    question: 'What role does the constant pool play in a .class file, and why don\'t method bytecode instructions embed literal values directly?',
    shortAnswer: 'The constant pool holds a class\'s literals, names, and symbolic type/method/field references once, and every other section — including method bytecode — refers to entries by index rather than duplicating the values inline. This keeps the format compact and lets tools rewrite or inspect references (instrumentation, bytecode manipulation libraries) by editing the pool without touching every instruction that uses a given value.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['bytecode', 'class-file', 'constant-pool'],
  },
  {
    id: 'java-jvm-q18',
    question: 'A framework using CGLIB-style dynamic proxies generates a new proxy subclass per unique base-class-plus-interceptor-set combination it encounters. Explain why this shows up as method-area/Metaspace pressure specifically, distinct from ordinary heap pressure from object allocation, and why the fix is different from a normal memory leak fix.',
    shortAnswer: 'Each generated proxy is a genuinely new class, not just a new object — its metadata (constant pool, method bytecode, vtable) lands in Metaspace, not the heap, so heap-focused fixes (more Xmx, tuning the collector) do nothing. The real fix is bounding class generation itself: caching/reusing proxy classes by the combination that defines them instead of regenerating, or bounding MaxMetaspaceSize as a stopgap while the generation pattern gets fixed.',
    detailedAnswer: 'This scenario deliberately targets a distinction many engineers blur: object allocation pressure (heap) versus class-generation pressure (method area / Metaspace) are structurally different resources with different remedies. A CGLIB-style proxy generator that mints a new subclass per unique (base class, interceptor set) tuple is creating new class metadata each time — a new Class object, a new constant pool, new compiled bytecode for the generated methods, potentially a new synthetic ClassLoader if the framework isolates generated classes per-load. None of that is heap-allocated instance data; it lives in Metaspace, and no amount of heap GC tuning or -Xmx increase touches it. If the space of (base class, interceptor set) combinations is actually unbounded — say, interceptor sets are built per-request rather than cached and reused — Metaspace grows without bound regardless of how well the heap-side objects are collected, since even a perfectly-collected heap doesn\'t reclaim the class metadata for classes that are still technically loaded and possibly still referenced by not-yet-collected proxy instances. The fix has to attack the generation pattern itself: memoize/cache generated proxy classes keyed by the combination that defines them so the same class is reused instead of regenerated, which is what production-grade proxy frameworks (Spring\'s CGLIB usage, for instance) do internally specifically to avoid this failure mode.',
    keyPoints: ['Class generation is a Metaspace cost, not a heap cost — heap tuning is irrelevant to it', 'Unbounded combination space means unbounded class count regardless of GC health', 'Fix is caching/reusing generated classes by their defining combination, not memory tuning'],
    seniorFollowUps: ['How would you detect this specific pattern in production before it becomes an outage, using JFR or JMX?', 'What\'s the tradeoff a proxy-caching fix introduces (e.g. around cache key correctness or memory)?'],
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['metaspace', 'dynamic-proxies', 'class-generation'],
  },
  {
    id: 'java-jvm-q19',
    question: 'Why is a StackOverflowError in one thread guaranteed not to corrupt heap state or affect other threads?',
    shortAnswer: 'Each thread has its own private JVM stack, separate from the shared heap and method area; overflowing that thread\'s own stack only affects data structures scoped to that stack (local variables, operand stack, frame linkage) and never touches heap objects\' internal consistency, which live in the shared area other threads also read from.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['stack', 'threads', 'memory-areas'],
  },
  {
    id: 'java-jvm-q20',
    question: 'Why is catching OutOfMemoryError to attempt graceful recovery generally considered unreliable, even though it\'s technically just an Error that can be caught?',
    shortAnswer: 'By the time OOM is thrown, the JVM is already in a resource-starved state, and the recovery code itself needs to allocate (to log, to free resources, to run cleanup logic) — allocations that can themselves fail the same way, and other threads across the process may already be failing unpredictably mid-operation, leaving shared state in a partially-updated condition that\'s unsafe to keep operating on.',
    topic: 'java',
    subtopic: 'jvm-internals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['oom', 'error-handling'],
  },

  // ── GC Questions ─────────────────────────────────────────────
{
    id: 'java-gc-q1',
    question: 'CMS was deprecated and then removed in favor of G1. What specific structural problem in CMS did G1 fix, and how?',
    shortAnswer: 'CMS never compacted the old generation — it only swept — so it fragmented over time and eventually hit "concurrent mode failure," falling back to a slow, non-compacting full GC. G1 fixed this by dividing the heap into regions and doing incremental, mostly-concurrent compaction of a subset of regions each cycle, so it never needs the same all-or-nothing fallback.',
    detailedAnswer: 'CMS\'s old-generation collector was mark-sweep without compaction, trading pause time for a fragmenting heap. Once fragmentation got bad enough that a large allocation couldn\'t find contiguous space, or the concurrent collector fell behind the application\'s allocation rate ("concurrent mode failure"), the JVM fell back to a single-threaded, fully stop-the-world, compacting collection — the exact multi-second pause CMS existed to avoid, at the worst possible moment (already under memory pressure). G1 avoids this by never treating the old generation as one contiguous space: it\'s partitioned into fixed-size regions, and G1 tracks live-data ratio per region so it can pick the emptiest regions to collect first (its namesake "garbage first" strategy) and compact just those regions within a pause, incrementally, without needing to compact the whole generation at once.',
    keyPoints: [
      'CMS: concurrent mark-sweep, no compaction, degrades via fragmentation into a full-GC fallback',
      'G1: region-based, incremental compaction avoids the all-or-nothing full-GC fallback',
      'Concurrent mode failure was the specific CMS operational failure mode G1 was designed to eliminate',
    ],
    seniorFollowUps: ['If a service still shows long full GC pauses under G1, what would you check before assuming it\'s a G1 bug rather than an application problem?'],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cms', 'g1', 'fragmentation', 'collector-comparison'],
  },
  {
    id: 'java-gc-q2',
    question: 'ZGC and Shenandoah both achieve sub-millisecond, heap-size-independent pauses, but use different mechanisms to relocate live objects concurrently. Compare colored pointers to Brooks pointers.',
    shortAnswer: 'ZGC stores object state (marked, remapped, relocating) in unused high bits of the 64-bit reference itself, so a load barrier can tell an object\'s state just from the pointer value with no extra memory access. Shenandoah instead adds one extra indirection word per object (a Brooks forwarding pointer) that always points to the object\'s current, up-to-date location, so every access goes through one level of indirection. Colored pointers cost address-space and pointer-bit budget; Brooks pointers cost a per-object memory word and a read indirection on every access.',
    detailedAnswer: 'Both collectors need to answer the same question during concurrent relocation: "if the application reads a reference to an object that GC just moved, how does it get the new address without stopping the world?" ZGC encodes metadata directly into spare bits of the pointer (viable because 64-bit addresses don\'t use all 64 bits), so a load barrier inspects the pointer\'s color bits on every reference load and self-heals it to the current location if stale — no separate memory lookup needed, but it constrains how much of the 64-bit space is usable for addressing and requires re-coloring pointers app-wide during a remap phase. Shenandoah instead gives every object an extra forwarding-pointer word that\'s kept up to date by a barrier on every access; the barrier just follows the pointer, which is conceptually simpler but adds a fixed per-object memory overhead and an indirection cost on every field access, not just during active relocation.',
    keyPoints: [
      'ZGC: metadata lives in the pointer bits, load barrier decodes state directly',
      'Shenandoah: metadata lives in an extra per-object word (Brooks pointer), barrier follows an indirection',
      'Trade-off: address-space/bit budget (ZGC) vs per-object memory + indirection cost (Shenandoah)',
    ],
    seniorFollowUps: ['Given a service on JDK 21 with a 200GB heap and a hard p99 latency SLA, which would you reach for first and what would you actually measure before deciding?'],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['zgc', 'shenandoah', 'colored-pointers', 'brooks-pointers', 'low-pause'],
  },
  {
    id: 'java-gc-q3',
    question: 'A ThreadLocal is used to cache a large per-request object in a service running on a fixed thread pool. Despite calling threadLocal.remove() being "best practice," the team skipped it. Walk through exactly why this leaks and why it\'s worse in a pooled-thread environment than in a short-lived-thread one.',
    shortAnswer: 'Each Thread owns a ThreadLocalMap keyed weakly by the ThreadLocal instance but with a strongly-referenced value. If the thread dies after the request, the map dies with it and nothing leaks. In a pool, the thread is reused indefinitely, so the ThreadLocalMap entry — and the large object it strongly references — survives across every future task on that thread, retained for the life of the pool unless explicitly removed.',
    detailedAnswer: 'Thread.threadLocals is a ThreadLocalMap whose entries are Entry extends WeakReference<ThreadLocal<?>> — the key (the ThreadLocal instance) is weak, but the value stored is a normal strong reference. When a ThreadLocal goes out of scope with no other strong references, its key can be collected and the entry becomes eligible for cleanup on next map access (set/get/remove sweep stale entries) — but that cleanup is opportunistic, not immediate, and critically the value stays strongly reachable through the entry until the entry itself is swept. In a short-lived-thread model (thread per request), the thread terminating destroys the ThreadLocalMap outright, so this doesn\'t matter. In a fixed thread pool, the same worker thread executes task after task indefinitely, so the ThreadLocalMap and every value ever set on it (and never removed) persists for the pool\'s entire lifetime — the large per-request object from task one is still strongly reachable when task ten thousand runs, unless remove() explicitly detached it.',
    keyPoints: [
      'ThreadLocalMap.Entry key is weak (ThreadLocal instance), value is strong — only the key being unreferenced is not enough to free the value',
      'Cleanup of stale entries is opportunistic on next map operation, not automatic on GC',
      'Pooled threads never die between tasks, so anything not explicitly removed accumulates for the pool\'s lifetime',
    ],
    seniorFollowUps: ['How would you audit an existing codebase for this pattern without waiting for a production OOM to reveal it?'],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['threadlocal', 'memory-leak', 'thread-pool'],
  },
  {
    id: 'java-gc-q4',
    question: 'What causes a classloader leak after a hot-redeploy in an application server, and why does it retain far more than just the classloader object itself?',
    shortAnswer: 'If any single object created by the old deployment\'s classloader — a lingering thread, a static field in a library, a JDBC driver still registered in DriverManager — stays reachable after redeploy, it keeps that classloader reachable. Since every Class the classloader loaded is reachable through it, and every static field of every one of those classes is reachable through the Class object, the entire old version of the application (code and static state) stays pinned in memory instead of being collected, compounding with every redeploy.',
    detailedAnswer: 'Classloaders sit in a reachability chain most engineers don\'t think about: Classloader -> Class objects it defined -> static fields of those classes -> whatever those static fields reference. A redeploy is supposed to let the whole old classloader and everything under it become garbage once the app context is stopped, but that only happens if literally nothing outside the old context still points into it. Common single points of failure: a non-daemon thread spawned by the old app and never stopped, a driver self-registered in java.sql.DriverManager (a JVM-global static) that\'s never deregistered, a static field in a shared/parent-loaded library holding a reference into app code, or a ThreadLocal value set on a thread pool thread that\'s shared across deployments. Because the chain is Classloader -> N classes -> each class\'s static state, a single retained root can pin megabytes to gigabytes, and it compounds — each hot-redeploy leaves another full generation of the old app in memory ("PermGen/Metaspace leak"), which is why these often present as slow metaspace growth across days of redeploys rather than an immediate OOM.',
    keyPoints: [
      'One retained reference into the old classloader keeps the whole classloader, and everything it loaded, reachable',
      'Common culprits: leaked non-daemon threads, DriverManager registrations, static fields in shared code, ThreadLocals on pooled threads',
      'Compounds across repeated redeploys, typically surfacing as metaspace growth rather than heap growth',
    ],
    seniorFollowUps: ['What tooling or heap-dump technique would you use to confirm a classloader leak versus a normal metaspace growth pattern?'],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['classloader-leak', 'metaspace', 'hot-redeploy'],
  },
  {
    id: 'java-gc-q5',
    question: 'Why is PhantomReference combined with ReferenceQueue considered safer than finalize() for cleanup logic, even though both let you run code when an object becomes unreachable?',
    shortAnswer: 'finalize() runs with the object still reachable (via `this`), so it can accidentally resurrect itself by storing a reference elsewhere, and it runs on an unpredictable, single, GC-managed thread that silently swallows exceptions and can be delayed indefinitely. PhantomReference.get() always returns null, so the cleanup code can never resurrect the object, and enqueueing to a ReferenceQueue lets you poll or process cleanup on your own thread with your own error handling and timing guarantees.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['phantomreference', 'finalize', 'cleanup'],
  },
  {
    id: 'java-gc-q6',
    question: 'A batch data pipeline job runs for 20 minutes, is CPU-bound, and has no user-facing latency requirement. Which collector would you choose and why would ZGC likely be the wrong choice here?',
    shortAnswer: 'Parallel (throughput) collector — the job cares about total wall-clock time, not pause smoothness, and Parallel maximizes throughput by letting stop-the-world pauses be efficient rather than paying ZGC\'s ongoing load-barrier and concurrent-thread CPU overhead for a latency guarantee the job never uses.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['collector-selection', 'parallel-gc', 'zgc', 'throughput'],
  },
  {
    id: 'java-gc-q7',
    question: 'What is a G1 "humongous" object allocation, and why can a workload that generates many of them cause more frequent full GCs even with plenty of free heap?',
    shortAnswer: 'An object larger than 50% of a G1 region size is classified humongous and allocated directly into one or more contiguous regions, bypassing normal young-gen (Eden) allocation. If these allocations happen frequently, they can fragment the region layout (needing contiguous free regions each time) and trigger more concurrent cycles or even full GCs to find enough contiguous space, even though the heap isn\'t actually full.',
    detailedAnswer: 'G1\'s whole design assumes most objects are small enough to fit comfortably within a region\'s young-gen allocation path. A humongous object breaks that assumption: it needs one or more entire contiguous regions immediately, allocated outside the normal Eden bump-pointer path, and it\'s never moved during a young or mixed collection (only reclaimed once fully unreachable, or specially handled during full GC). A workload that repeatedly allocates humongous objects — e.g., building large byte arrays for serialization — can fragment the region space with these fixed, non-relocatable allocations, forcing G1 to search harder for contiguous free regions and, when it can\'t find them, fall back to a full GC to compact and create space.',
    keyPoints: [
      'Humongous threshold: object size > 50% of region size',
      'Allocated in contiguous regions outside Eden, not moved during normal G1 collections',
      'Frequent humongous allocation can fragment regions and force otherwise-avoidable full GCs',
    ],
    seniorFollowUps: ['What tuning lever (or code change) would you reach for first if humongous allocations were identified as the root cause?'],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['g1', 'humongous-objects', 'fragmentation'],
  },
  {
    id: 'java-gc-q8',
    question: 'In a GC log you see steadily rising "before GC" heap occupancy across many successive collections, even though each collection still frees some memory. What does this pattern most likely indicate, and how is it different from a heap that\'s just undersized?',
    shortAnswer: 'A rising baseline occupancy that never returns to its prior floor after each collection is the classic signature of a slow memory leak — objects are staying reachable and accumulating. An undersized heap instead shows a healthy, repeating saw-tooth pattern (occupancy returns to roughly the same low point after each GC) but collections simply happen more frequently than desired — that\'s a sizing/tuning problem, not a leak.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['gc-logging', 'memory-leak-diagnosis'],
  },
  {
    id: 'java-gc-q9',
    question: 'Why does setting -Xms lower than -Xmx risk causing pauses that have nothing to do with garbage collection itself?',
    shortAnswer: 'Growing the heap at runtime (when -Xms < -Xmx and the JVM needs more space than it currently has committed) is itself a stop-the-world operation on most collectors, separate from any GC cycle — so a service can see an unexplained pause during warm-up traffic ramp-up that\'s actually heap expansion, not collection. Setting them equal avoids this by committing the full heap upfront.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['heap-sizing', 'tuning', 'xms-xmx'],
  },
  {
    id: 'java-gc-q10',
    question: 'Why does the young generation use a copying algorithm while the old generation typically uses mark-sweep-compact instead?',
    shortAnswer: 'The generational hypothesis means most objects in Eden are already dead by the time of a minor GC, so copying just the small surviving minority into a Survivor space is cheap and gives you compaction for free. The old generation has a much higher live-object ratio, so copying the majority of it every cycle would be expensive — mark-sweep-compact (or region-based incremental compaction in G1) amortizes that cost differently by not moving everything every time.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['copying-collector', 'mark-sweep-compact', 'generational-gc'],
  },
  {
    id: 'java-gc-q11',
    question: 'A cache implemented with SoftReference values is reported as "not actually saving memory" — entries seem to get evicted almost immediately under moderate load rather than only under real memory pressure. What\'s the likely explanation?',
    shortAnswer: 'Soft references are only guaranteed to be cleared before an OutOfMemoryError, but the JVM is free to clear them earlier under its own heuristics (roughly LRU across all soft refs, influenced by GC generation and free-memory ratio), and different collectors/flags tune this differently. If entries are evicting far earlier than expected, the actual heap headroom is probably tighter than assumed, or a generational collector is clearing soft refs during ordinary minor/major GC activity rather than only at the edge of OOM.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['softreference', 'caching', 'gc-heuristics'],
  },
  {
    id: 'java-gc-q12',
    question: 'What does G1\'s "remembered set" per region actually track, and why is it necessary for G1 to collect a subset of regions in one pause?',
    shortAnswer: 'Each region\'s remembered set tracks incoming references from other regions (backed by a card table recording which cards were written to). Without it, collecting just a few chosen regions would require scanning the entire rest of the heap to find any pointer into those regions as GC roots — the remembered set makes that lookup local and cheap, which is what makes G1\'s pick-any-subset collection strategy tractable.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['g1', 'remembered-set', 'card-table'],
  },

  // ── Performance Questions ──────────────────────────────────────
{
    id: 'java-perf-q1',
    question: 'A teammate benchmarks a "fast path" using System.currentTimeMillis() around a loop calling the method 10,000 times and reports it as 3x faster than the alternative. What specifically is wrong with trusting that number?',
    shortAnswer: 'The loop almost certainly never reaches steady-state JIT compilation within 10,000 interpreted-then-C1-then-maybe-C2 calls, so it\'s measuring a mix of interpreter and partial-JIT cost rather than the optimized code that will actually run in production; and if the loop\'s result is never used, dead code elimination can delete some or all of the measured work entirely, making the number meaningless in either direction.',
    detailedAnswer: 'Three independent problems stack up: (1) no warm-up — early iterations run interpreted or under lightly-optimized C1, so the measurement blends cold and (maybe) hot performance rather than isolating either; (2) dead code elimination — if the loop computes a value nothing observes, C2 can legally remove the computation, so a "3x faster" result might mean "the faster path was easier for the JIT to prove dead," not that it does less real work; (3) millisecond resolution and JIT compilation happening on a background thread mid-benchmark both add noise that a single wall-clock delta can\'t separate from real signal. The fix is JMH: forked JVMs, explicit warmup/measurement iteration phases, and Blackhole/return-value consumption to defeat DCE.',
    keyPoints: [
      'No warm-up separation between interpreted/C1 and steady-state C2 performance',
      'Dead code elimination can remove an unused result, silently invalidating the whole measurement',
      'Millisecond-resolution timing and JIT compilation running concurrently on background threads add unaccounted noise',
    ],
    seniorFollowUps: [
      'How would you rewrite this benchmark correctly, and what JMH annotations specifically defend against each problem you named?',
      'If someone can\'t use JMH (e.g. a constrained CI environment), what\'s the minimum you\'d insist on to make an ad-hoc benchmark trustworthy?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['jmh', 'benchmarking', 'jit', 'dead-code-elimination'],
  },
  {
    id: 'java-perf-q2',
    question: 'Two JMH benchmarks measure the same computation, but one uses a hardcoded input constant and the other reads the input from a @State field. Why can the first one report an impossibly fast result?',
    shortAnswer: 'When the input is a compile-time constant, C2 can constant-fold the entire computation to its result at compile time and reuse that precomputed value on every iteration, so the benchmark ends up measuring almost nothing. Reading the input from a @State field keeps it runtime-opaque to the compiler, forcing the real computation to run each time.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['jmh', 'constant-folding', 'benchmarking'],
  },
  {
    id: 'java-perf-q3',
    question: 'A hot loop processing polymorphic Shape objects (Circle, Square, Triangle, ...) through a common draw() call gets noticeably slower once a fourth implementation is added, even though each call does the same amount of work. Why?',
    shortAnswer: 'Up to two concrete receiver types at a call site, the JIT can use an inline cache to speculatively inline or fast-dispatch (monomorphic/bimorphic). A third distinct type flowing through the same call site makes it megamorphic, forcing a full virtual dispatch on every call and losing the inlining that would otherwise unlock further optimization.',
    detailedAnswer: 'Inline caches degrade in three states: monomorphic (single observed receiver type, direct call, eligible for inlining), bimorphic (two types, a two-way type check then direct call, still often inlinable), and megamorphic (three or more types, falls back to a full vtable/itable lookup with no inlining). This is purely a property of what types are actually observed flowing through the call site at runtime, not the number of implementations that exist in the codebase — a call site that only ever sees two of five possible Shape subtypes in practice stays bimorphic. Splitting the hot dispatch into type-specific loops, or restructuring so each call site only sees one or two types, can restore monomorphic dispatch.',
    keyPoints: [
      'Inline cache states: monomorphic -> bimorphic -> megamorphic based on observed receiver types at a call site',
      'Megamorphic dispatch loses inlining and falls back to full virtual dispatch every call',
      'It depends on runtime-observed types at that call site, not total number of implementations in the codebase',
    ],
    seniorFollowUps: [
      'How would you use async-profiler or JFR to confirm this is actually what\'s happening rather than assuming it?',
      'What refactor would you consider to get back to monomorphic dispatch without giving up polymorphism in the API?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jit', 'polymorphism', 'inlining', 'megamorphic'],
  },
  {
    id: 'java-perf-q4',
    question: 'Explain escape analysis and why you should not treat it as a reliable substitute for manually avoiding allocation in a hot path.',
    shortAnswer: 'Escape analysis lets the JIT prove a short-lived object never leaves the method/thread that created it and then avoid heap allocation entirely (scalar replacement). It\'s unreliable to depend on because it only applies once the method is JIT-warm, requires the escape proof to hold exactly (a debug log capturing the reference, a non-inlined call receiving it, or storing it in a field all break it), and is JVM/version/flag dependent — silently regressing without any code change is possible.',
    detailedAnswer: 'Escape analysis runs during C2 compilation and determines whether an allocated object\'s reference could possibly become visible outside the allocating method or thread. If it can prove non-escape, the JIT can perform scalar replacement — splitting the object\'s fields into individual scalar values kept in registers/stack slots — eliminating the allocation, its GC pressure, and even lock overhead if applicable (lock elision). The catch is that this is a best-effort, speculative optimization: it depends on the method being hot enough to reach C2, on every code path the object touches being inlined and analyzed, and on no capturing reference existing anywhere (even a conditionally-executed one). A refactor as small as adding a logging statement that captures the object, or a method no longer being inlined because it grew past the inlining threshold, can silently disable scalar replacement with no compiler warning — the code just gets slower.',
    keyPoints: [
      'Enables scalar replacement — fields become registers/stack values instead of a heap object',
      'Requires JIT warmth and a complete non-escape proof across all inlined code paths',
      'Fragile to unrelated changes (capturing references, inlining threshold shifts) with no compiler feedback when it stops applying',
    ],
    seniorFollowUps: [
      'How would you verify in a real JVM whether escape analysis actually fired for a given allocation (tooling/flags)?',
      'Given this fragility, when would you still deliberately design code assuming escape analysis will help, versus manually pooling/reusing objects?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['escape-analysis', 'scalar-replacement', 'jit', 'allocation'],
  },
  {
    id: 'java-perf-q5',
    question: 'A multi-threaded counter class has two independent volatile long fields, each updated by a different thread, and throughput is far worse than expected given there\'s no logical contention between them. What\'s the likely cause and how would you fix it?',
    shortAnswer: 'False sharing — the two fields likely land on the same 64-byte CPU cache line, so every write by either thread invalidates the other core\'s cached copy of that line via cache-coherency traffic, even though the threads never touch each other\'s data. Fix by padding the fields onto separate cache lines, e.g. with @jdk.internal.vm.annotation.Contended or manual padding fields.',
    detailedAnswer: 'Modern CPUs keep cache coherency at cache-line granularity (typically 64 bytes), not per-variable. If counterA and counterB are adjacent fields in the same object, they very likely share a line. When thread 1 writes counterA, the coherency protocol invalidates that line in every other core\'s cache — including thread 2\'s core, even though thread 2 only reads/writes counterB on that same line. Thread 2\'s next access has to refetch the line, and vice versa, producing hardware-level ping-pong that looks like contention despite no logical sharing. The fix is padding: either @Contended (JDK-internal annotation, needs -XX:-RestrictContended for use outside java.* packages pre-JDK 15, unrestricted after) which the JVM lays out with cache-line padding automatically, or manual dummy long fields around the hot fields to force spacing. This is also why LongAdder pads its internal Cell array — striping across padded cells avoids exactly this problem while still allowing lock-free per-cell updates.',
    keyPoints: [
      'CPU cache coherency operates at cache-line (64-byte) granularity, not per-variable',
      'Adjacent independent fields written by different threads cause hardware-level line ping-pong even with zero logical sharing',
      '@Contended (or manual padding) separates fields onto distinct cache lines',
    ],
    seniorFollowUps: [
      'How would you detect false sharing in production rather than guessing from code review?',
      'Why does LongAdder outperform AtomicLong under high contention, and how does it relate to this same idea?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['false-sharing', 'cache-lines', 'concurrency', 'contended'],
  },
  {
    id: 'java-perf-q6',
    question: 'Why is allocation rate (MB/sec) usually a more useful metric than allocation count when reasoning about GC-related performance problems?',
    shortAnswer: 'JVM allocation itself is cheap (a bump-the-pointer TLAB operation), so raw object count doesn\'t directly predict cost — what actually drives GC frequency and pause time is how fast the heap fills up, i.e. total bytes allocated per second, which depends on both count and object size.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['allocation', 'tlab', 'gc', 'profiling'],
  },
  {
    id: 'java-perf-q7',
    question: 'What is the practical difference between a sampling profiler and an instrumenting profiler, and why is production profiling almost always done with sampling?',
    shortAnswer: 'Instrumenting profilers inject tracking code at every method entry/exit, giving exact call counts but adding overhead large enough to distort the very hot paths being measured (sometimes reordering which methods look expensive). Sampling profilers periodically snapshot thread stacks with much lower overhead, trading exact counts for statistical accuracy — low enough overhead to run safely in production.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['profiling', 'sampling', 'instrumentation'],
  },
  {
    id: 'java-perf-q8',
    question: 'What was "safepoint bias" in older JVM sampling profilers, and how does async-profiler avoid it?',
    shortAnswer: 'Older profilers (using JVMTI-based stack walking) could only take a sample when the JVM reached a safepoint, so code between safepoint polls — notably tight counted loops the JIT doesn\'t insert polls into — was systematically under-sampled, skewing results away from real hot spots. async-profiler uses AsyncGetCallTrace and OS-level signal/perf_events-based sampling to capture stacks without waiting for a safepoint, removing that bias.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['profiling', 'safepoint', 'async-profiler'],
  },
  {
    id: 'java-perf-q9',
    question: 'In a flame graph, what does the width of a frame represent, and what\'s a common misreading of it?',
    shortAnswer: 'Width represents the relative proportion of samples in which that frame appeared on the stack, not call count or absolute wall-clock time. A common misreading is treating a wide frame near the bottom of the stack as "this specific function is slow" when it may just be a common entry point that many different leaf functions run under — the leaf frames at the top are where the actual time is being spent.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['profiling', 'flame-graphs'],
  },
  {
    id: 'java-perf-q10',
    question: 'A service running fine under steady load shows a latency spike immediately after every rolling deploy, even though the code and traffic pattern are unchanged. What are the two most likely JVM-level causes, and how would you confirm which one it is?',
    shortAnswer: 'Most likely JIT warm-up (fresh instances serve initial requests through interpreted/C1 code before hot methods reach C2) or, less commonly, class loading/verification cost during startup. Confirm with JFR compilation events correlated against the latency timeline — a spike that decays as invocation counts climb, matching C2 compilation events, points to warm-up.',
    detailedAnswer: 'Right after a JVM starts, all code runs interpreted, then transitions through C1 tiers, and only reaches fully-optimized C2 once a method\'s invocation/back-edge counters cross threshold — meaning identical logic can be an order of magnitude slower on a freshly-started instance than at steady state. This directly explains a latency spike that\'s worst immediately post-deploy and decays over the following minutes as hot paths warm up. A secondary, usually smaller, contributor is class loading and bytecode verification cost for classes touched for the first time. To confirm, correlate JFR\'s compilation events (or -XX:+PrintCompilation timestamps) against the request latency timeline for the new instance — if the spike\'s decay curve lines up with C2 compilation events for the hot request-handling methods, it\'s warm-up, not a real regression. Standard mitigations: delay adding the instance to the load balancer until synthetic warm-up traffic has been replayed, or explore AppCDS/CDS archives to cut class-loading cost.',
    keyPoints: [
      'JIT warm-up: interpreter/C1 to C2 transition takes real invocation volume, and fresh instances start cold',
      'Class loading/verification adds a smaller first-touch cost per class',
      'Confirm by correlating JFR compilation events against the latency spike\'s decay curve, not by assumption',
    ],
    seniorFollowUps: [
      'What deployment-level mitigation would you propose, and what\'s the tradeoff (e.g. slower rollout vs smoother latency)?',
      'How does this interact with autoscaling that adds instances reactively under load spikes — what failure mode does that create?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['warmup', 'jit', 'tiered-compilation', 'deploys'],
  },
  {
    id: 'java-perf-q11',
    question: 'Why can a change that improves average throughput in a benchmark be a net negative for a latency-sensitive production service?',
    shortAnswer: 'Throughput and latency optimizations frequently trade against each other — batching, larger buffers, and background compaction all raise average ops/sec while adding variance that worsens tail latency (p99/p999), which is usually what the SLA and user experience actually depend on, not the average.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['throughput', 'latency', 'tradeoffs'],
  },
  {
    id: 'java-perf-q12',
    question: 'Your service\'s p999 latency has occasional spikes that don\'t correlate with request volume. How would you determine whether GC is the cause, without guessing?',
    shortAnswer: 'Capture JFR (or GC logs with timestamps) and overlay GC pause events on the same timeline as the latency histogram — if the p999 spikes line up with pause timestamps, GC is implicated; if they don\'t, look elsewhere (e.g. lock contention, downstream calls, JIT deoptimization events, which JFR also captures on the same timeline).',
    detailedAnswer: 'The rigorous approach is correlation, not assumption. JFR records GC pause events, safepoint events, and thread state alongside method sampling and can be paired with request-level latency logging (or an APM span) using a shared clock. If a request that landed in the p999 tail has its span overlapping a recorded GC pause, that\'s direct evidence rather than circumstantial. If pauses don\'t line up with spikes, worth checking JIT deoptimization events (a hot method suddenly falling back to interpreted code), safepoint-inducing operations unrelated to GC (e.g. biased lock revocation historically, or explicit System.gc() calls from a library), or non-JVM causes entirely (network, downstream dependency). This investigation approach — not "GC algorithm internals" — is the testable skill here; algorithm-level GC tuning belongs to the gc subtopic.',
    keyPoints: [
      'Correlate GC pause timestamps against the latency histogram/timeline rather than assuming causation',
      'JFR gives both on one timeline, making this direct instead of inferred',
      'Rule out non-GC causes (deoptimization events, downstream calls) before concluding GC is responsible',
    ],
    seniorFollowUps: [
      'If GC pauses are confirmed as the cause, what levers do you have that don\'t require picking a different collector?',
      'How would you distinguish a GC-caused spike from a JIT deoptimization event in the JFR timeline?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['gc', 'tail-latency', 'jfr', 'diagnostics'],
  },
  {
    id: 'java-perf-q13',
    question: 'A numeric aggregation loop over List<Integer> is replaced with the same logic over int[], and throughput improves far more than the removed boxing allocation would explain. What\'s the rest of the speedup coming from?',
    shortAnswer: 'Cache locality. A List<Integer> is an array of references to separately-allocated Integer objects scattered across the heap, so each element access is a potential cache miss; an int[] is contiguous memory the CPU prefetcher handles efficiently, eliminating most of that pointer-chasing cost independent of the allocation savings.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['boxing', 'cache-locality', 'collections'],
  },
  {
    id: 'java-perf-q14',
    question: 'What does the Integer autoboxing cache (-128 to 127 by default) mean for code correctness and performance, and why is it a common source of subtle production bugs?',
    shortAnswer: 'Integer.valueOf() returns a shared cached instance for values -128..127 and a freshly allocated object outside that range, so == comparisons on boxed Integers can pass for small values and fail for larger ones despite identical-looking code — and performance-wise, boxing outside the cached range always costs a real allocation, meaning code behaves differently based purely on runtime data values.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['boxing', 'integer-cache', 'gotcha'],
  },
  {
    id: 'java-perf-q15',
    question: 'When would you reach for a DirectByteBuffer / off-heap memory instead of a regular on-heap byte array, and what\'s the cost of doing so?',
    shortAnswer: 'Reach for direct buffers when doing high-throughput I/O (NIO channels, memory-mapped files) where the OS/network layer needs a native memory address — direct buffers skip an extra JVM-heap-to-native copy and aren\'t scanned by GC. The cost is manual lifecycle discipline: allocation/deallocation is comparatively expensive, so per-request allocation is a mistake (pool instead), and running out is a separate exhaustion path (-XX:MaxDirectMemorySize) that doesn\'t show up in normal heap monitoring.',
    detailedAnswer: 'On-heap byte[] used for I/O typically requires the JVM to copy the data into a native buffer before an OS syscall can use it (since the OS can\'t safely hold a raw pointer into a heap the GC might relocate/compact). A DirectByteBuffer is allocated outside the managed heap specifically so the OS can operate on it directly, avoiding that copy — this matters most for high-volume network I/O and memory-mapped file access. The costs: direct buffer allocation is comparatively expensive and historically depended on GC-triggered Cleaner/PhantomReference-based deallocation (meaning a low-GC-activity period could delay reclaiming direct memory), so the standard practice is to pool and reuse buffers rather than allocate per request. It also has its own memory ceiling via -XX:MaxDirectMemorySize, separate from heap size — a service can throw OutOfMemoryError: Direct buffer memory while heap usage graphs look completely healthy, which is a common on-call confusion if the team isn\'t tracking direct memory as its own metric.',
    keyPoints: [
      'Avoids heap-to-native copy for I/O; not scanned by GC',
      'Allocation/deallocation cost is higher — pool and reuse instead of allocating per request',
      'Has its own memory limit (MaxDirectMemorySize) that fails independently of heap usage, and monitoring must track it separately',
    ],
    seniorFollowUps: [
      'How would you diagnose a direct memory leak in production, given it doesn\'t show up in heap dumps the normal way?',
      'What\'s the tradeoff of pooling direct buffers versus the added complexity of managing that pool correctly?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['off-heap', 'direct-buffer', 'nio', 'memory'],
  },
  {
    id: 'java-perf-q16',
    question: 'Why can a large, rarely-called helper method fail to get inlined into a hot caller even though the JIT considers the call site "hot"?',
    shortAnswer: 'Inlining decisions are gated by the callee\'s bytecode size against separate thresholds (a smaller default cap, with a larger allowance for methods the JIT considers frequently/"hot" themselves) — a call site being hot doesn\'t override the callee itself being too large to inline. A big helper method staying too large simply gets called normally, losing the cross-boundary optimizations inlining would otherwise enable.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['inlining', 'jit'],
  },

  // ── Production Questions ──────────────────────────────────────
{
    id: 'java-prod-q1',
    question: 'On-call: a service under Kubernetes gets restarted repeatedly during a partial database outage, even though the app process itself is healthy. What\'s the likely misconfiguration, and how do you fix it?',
    shortAnswer: 'The liveness probe is almost certainly checking the database (or another downstream dependency) rather than the process\'s own health. Kubernetes kills and restarts on liveness failure, so a DB outage causes cascading pod restarts that add restart churn on top of the outage instead of just marking the pod not-ready. Fix: liveness should only check that the process itself is alive and responsive; downstream health belongs in the readiness probe, which removes the pod from traffic without killing it.',
    detailedAnswer: 'Liveness and readiness answer different questions and Kubernetes acts on them very differently: liveness failure means "kill and restart this container," readiness failure means "stop sending it traffic, but leave it running." A liveness check that transitively depends on an external system conflates "my process is broken" with "something I depend on is broken." During a DB outage this causes every replica to fail liveness simultaneously, get restarted, come back up, immediately fail liveness again (since the DB is still down), and repeat — burning CPU/network on churn and potentially masking the real signal (readiness correctly reporting not-ready) under restart noise. Because restarting doesn\'t fix a downstream outage, this makes recovery slower, not faster, once the dependency does come back — cold, restarting pods now have to reconnect and warm up right when they should already be serving.',
    keyPoints: [
      'Liveness = restart-worthy process-level health only; readiness = current ability to serve traffic',
      'A downstream dependency check belongs in readiness, not liveness',
      'Restart churn during an outage adds recovery latency once the dependency comes back',
    ],
    seniorFollowUps: [
      'How would you design the probes if the service genuinely cannot function at all without the database (e.g. it has no fallback path)?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['kubernetes', 'health-checks', 'liveness', 'readiness'],
  },
  {
    id: 'java-prod-q2',
    question: 'A service is deployed with a 30-second Kubernetes termination grace period. After a rolling deploy, you start seeing a small but steady rate of client-visible errors during every deploy. What\'s the likely cause and fix?',
    shortAnswer: 'The pod is being sent SIGTERM and, likely, terminated before in-flight requests finish or before it stops accepting new ones — either the shutdown isn\'t draining at all, or readiness isn\'t flipped false immediately on SIGTERM so the load balancer keeps routing new requests into a pod that\'s already shutting down. Fix: on SIGTERM, flip readiness to false immediately (to stop new traffic) and drain in-flight requests within the grace period, using a shutdown hook only for genuinely fast cleanup.',
    detailedAnswer: 'There are two independent races here and either alone produces the symptom. First, a purely reactive shutdown hook that just calls System.exit-adjacent cleanup without draining active requests will cut off whatever\'s mid-flight the instant the hook runs. Second, even a well-drained shutdown doesn\'t help if the load balancer/service mesh doesn\'t immediately stop routing to the pod — there\'s an inherent propagation delay between "pod status changed" and "every downstream client sees it," so a pod that keeps accepting new connections into the grace period will still receive some. The robust pattern: on SIGTERM, flip readiness false first (so the ecosystem starts routing away), then stop accepting new requests, then drain the requests already in flight with a bounded timeout comfortably inside the grace period, then let the process exit. If the grace period is too short for real in-flight work (e.g. long-running requests), the actual fix is raising terminationGracePeriodSeconds, not just tightening the drain logic.',
    keyPoints: [
      'Two failure modes: no real draining, or readiness not flipped before draining starts',
      'There\'s always a propagation delay for "stop routing here" — draining alone isn\'t enough without it',
      'Grace period must exceed realistic drain time, or errors are inevitable regardless of app-side correctness',
    ],
    seniorFollowUps: [
      'How would this change for a service handling long-lived connections like WebSockets or SSE streams instead of short request/response calls?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['graceful-shutdown', 'kubernetes', 'sigterm', 'incident'],
  },
  {
    id: 'java-prod-q3',
    question: 'A containerized JVM service keeps getting killed and restarted, but no OutOfMemoryError ever appears in its logs. Where do you look first, and what\'s the likely explanation?',
    shortAnswer: 'Check the exit code and the container/orchestrator events, not the app logs — exit code 137 with no JVM stack trace points at the Linux cgroup OOM killer, not a heap OutOfMemoryError. That means total process memory (heap + metaspace + thread stacks + native/direct buffers + JIT code cache), not just the -Xmx heap, exceeded the container\'s memory limit.',
    detailedAnswer: 'A heap OutOfMemoryError is a JVM-level event: the JVM tried to allocate on the heap, couldn\'t, and threw (and typically logs it, especially with -XX:+HeapDumpOnOutOfMemoryError configured). An OOM-killed container is an OS-level event: the kernel\'s cgroup accounting saw the whole process\'s RSS exceed the container\'s memory limit and sent SIGKILL — the JVM gets no chance to log anything because it\'s killed, not thrown into. This distinction matters because the fix is different: a heap OOM often means tuning -Xmx or fixing a leak in heap objects, while a container OOM kill often means -Xmx was set with no headroom for the non-heap memory the JVM also needs — metaspace, per-thread native stacks (which scale with thread count), direct ByteBuffers, and JIT code cache. A common mistake is setting -Xmx to something close to the container limit; the fix is leaving meaningful headroom (often 20-30%+ depending on thread count and off-heap usage) and monitoring RSS, not just heap, against the container limit.',
    keyPoints: [
      'Exit code 137 + no stack trace = suspect OS/cgroup OOM killer, not heap OOM',
      'Container memory = heap + metaspace + thread stacks + native buffers + code cache, not just -Xmx',
      'Fix is headroom below the container limit, not just raising -Xmx',
    ],
    seniorFollowUps: [
      'A service under load spawns a large unbounded thread pool. How does that alone push it toward a container OOM kill even with a modest -Xmx?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['oom-killer', 'containers', 'memory', 'incident'],
  },
  {
    id: 'java-prod-q4',
    question: 'An app-server-hosted service is redeployed (hot-deployed) repeatedly without restarting the JVM, and after a few dozen redeploys it throws OutOfMemoryError: Metaspace even though the code hasn\'t grown. What\'s going on?',
    shortAnswer: 'Classic classloader leak: something from an old deployment — a static reference, an unclosed ThreadLocal, a lingering non-daemon thread, or a self-registering driver/logging framework — is keeping that version\'s classloader reachable after redeploy, so each redeploy\'s full set of classes accumulates in metaspace instead of being garbage collected.',
    detailedAnswer: 'Every hot redeploy in an app-server-style environment creates a new classloader for the new version of the app while the old one should become unreachable and eligible for collection once nothing references it. If any single object created by the old classloader\'s classes survives outside the redeploy — held by a JVM-lifetime static field, a ThreadLocal never cleared, a thread the old app started that\'s still running, or a framework (JDBC driver, java.util.logging, a shutdown-hook registry) that registers itself in a global, JVM-scoped table — that one reference transitively keeps the entire classloader, and every class it loaded, reachable. Each redeploy adds another leaked classloader\'s worth of classes to metaspace, and because none of them are ever collected, metaspace usage grows monotonically with each deploy until it exhausts. Diagnosis: a heap dump analyzed for duplicate classloader instances (one per historical deploy) with retained classes is the standard way to confirm and find the retaining reference.',
    keyPoints: [
      'A single stray reference outside the classloader is enough to leak the entire classloader and all its classes',
      'Common culprits: static registries, ThreadLocals, lingering threads, self-registering drivers',
      'Symptom is metaspace growth correlated with deploy count, not code size',
    ],
    seniorFollowUps: [
      'How would you confirm this via a heap dump, and what would you look for specifically?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['classloader-leak', 'metaspace', 'redeploy', 'incident'],
  },
  {
    id: 'java-prod-q5',
    question: 'Requests are timing out and thread pool metrics show most worker threads busy, but the database itself reports low CPU and query latency. What do you check next, and what\'s the likely root cause?',
    shortAnswer: 'Check connection pool checkout wait time and pool utilization, not query latency — this pattern (app threads blocked, DB itself idle) is the signature of connection pool exhaustion: requests are queued waiting for a pooled connection to free up, not waiting on the database to answer.',
    detailedAnswer: 'If the database were the bottleneck, its own latency and CPU metrics would show it. A low-DB-load, high-app-thread-busy pattern instead points at contention one hop earlier: threads blocked in pool.getConnection() (or the HTTP-client equivalent) waiting for a connection to be returned. Two distinct root causes produce the identical symptom and need to be told apart before fixing: (1) the pool is genuinely undersized for current load — more concurrent work is arriving than the pool has connections to serve, in which case sizing it up (bounded by downstream capacity) helps; (2) connections are leaking — some code path acquires a connection and never returns it (often a missing close() on an exception branch), so effective pool size shrinks over time even at constant load. Distinguishing them: leaks show pool utilization creeping toward 100% over time even under roughly steady traffic, while pure undersizing tracks load directly and recovers when load drops. Resizing a leak just delays the same failure at a higher threshold.',
    keyPoints: [
      'DB idle + app threads busy = look at connection pool checkout, not query performance',
      'Undersized pool vs leaking connections look similar but need different fixes',
      'Leak signature: utilization creeps up over time independent of load; undersizing tracks load directly',
    ],
    seniorFollowUps: [
      'What instrumentation would you add ahead of time so this diagnosis takes minutes instead of being reconstructed after the fact during an incident?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['connection-pool', 'incident', 'exhaustion'],
  },
  {
    id: 'java-prod-q6',
    question: 'You take a thread dump on an unresponsive service and see 40 threads all in BLOCKED state, all waiting on the same lock. Is this a deadlock? What do you do next?',
    shortAnswer: 'Not necessarily — BLOCKED-on-the-same-lock is the signature of heavy contention on a single lock, not a deadlock. A true deadlock needs a cycle of at least two threads each holding a lock the other wants, and the JVM will usually flag it explicitly as "Found one Java-level deadlock" in the dump. Take a second and third dump a few seconds apart: if it\'s contention, the set of blocked threads and which one holds the lock will shift over time; if it\'s a genuine cycle, the same threads stay stuck in the same relationship.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['thread-dump', 'deadlock', 'diagnostics'],
  },
  {
    id: 'java-prod-q7',
    question: 'Why is capturing a heap dump on a live, fully-loaded production instance risky, and how do you do it safely?',
    shortAnswer: 'jmap-style heap dumps trigger a stop-the-world pause roughly proportional to heap size — on a multi-GB heap that can freeze the JVM for tens of seconds, which itself can cause the outage you were trying to diagnose. Safer approach: cordon or drain one instance out of the load-balancing rotation first (so readiness is false and it\'s taking no traffic), then dump it, rather than dumping an instance still serving live requests.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['heap-dump', 'jmap', 'diagnostics'],
  },
  {
    id: 'java-prod-q8',
    question: 'Why can a correlation ID set via MDC at the start of a request go missing from later log lines in the same logical request?',
    shortAnswer: 'MDC is backed by a ThreadLocal, so it only travels with the thread it was set on. Once the request hands off to another thread — an async executor, a thread pool submission, a reactive scheduler — the new thread has no MDC context unless the framework or the code explicitly propagates it, so logs from that point on lose the correlation ID.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['mdc', 'logging', 'observability'],
  },
  {
    id: 'java-prod-q9',
    question: 'Why is a circuit breaker with no request timeout largely useless for protecting a service from a slow downstream dependency?',
    shortAnswer: 'A circuit breaker only trips based on observed failures, and a call with no (or a very long) timeout doesn\'t fail quickly — it just hangs, tying up the calling thread. The breaker never sees enough failures fast enough to open, so the calling service\'s own thread pool exhausts from accumulated blocked threads before the breaker ever protects anything. Timeouts and breakers are complementary, not substitutes.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['circuit-breaker', 'resilience', 'timeouts'],
  },
  {
    id: 'java-prod-q10',
    question: 'Why is average request latency a poor primary metric for detecting a JVM service degrading in production?',
    shortAnswer: 'Averages get diluted by the bulk of fast requests and hide tail problems — a thread pool with 1-2% of requests stalled can show a flat, healthy-looking average while those users see real timeouts. p99/p999 latency and error rate surface exactly the degradation an average smooths away.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['metrics', 'observability', 'latency'],
  },
  {
    id: 'java-prod-q11',
    question: 'What\'s the practical difference between baking environment-specific values into a build artifact versus externalizing config, from an incident-risk standpoint?',
    shortAnswer: 'If config is baked in per-environment, the artifact that passed staging isn\'t the same artifact running in production — you\'re no longer testing what you ship. Externalized config (env vars, a config service, mounted files) means one immutable, tested binary is promoted unchanged across environments, so a production incident can\'t be caused by a difference introduced at build time.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['configuration', 'deployment'],
  },

  // ── Modern Java (Records, Sealed, Virtual Threads) Questions ────────────────────
  {
    id: 'java-modern-q1',
    question: "A service migrates its blocking JDBC data-access layer from a fixed platform-thread pool to Executors.newVirtualThreadPerTaskExecutor(), but throughput barely improves and profiling shows carrier threads mostly idle waiting on a small number of busy ones. What's the likely cause?",
    shortAnswer: "Pinning — the DAO layer likely wraps its blocking JDBC calls in `synchronized` blocks (e.g. a legacy connection-cache guard), which pins the virtual thread to its carrier instead of letting it unmount during the blocking call, so the carrier pool can't be shared across the many virtual threads waiting on I/O.",
    detailedAnswer: 'Virtual threads only deliver their scalability win when they can unmount from the carrier thread while blocked. `synchronized` blocks/methods (as of JDK 21) prevent unmounting for the duration of the block, so a blocking JDBC call made inside a synchronized section keeps its carrier occupied exactly like a platform thread would — effectively capping concurrency at the small carrier pool size regardless of how many virtual threads exist. The fix is replacing `synchronized` with `java.util.concurrent.locks.ReentrantLock` around the same critical sections, since ReentrantLock does not pin. `-Djdk.tracePinnedThreads=full` surfaces exactly which call sites are pinning during a load test.',
    keyPoints: [
      'synchronized pins virtual threads to their carrier as of JDK 21; ReentrantLock does not',
      'Symptom is a throughput ceiling that looks like the old platform-thread-pool limit despite using virtual threads',
      '-Djdk.tracePinnedThreads is the diagnostic tool for finding pin sites',
    ],
    seniorFollowUps: [
      "If the synchronized block can't be removed because it's in a third-party library, what are your options?",
      'Would this pinning issue matter at all if the service were CPU-bound instead of I/O-bound?',
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['virtual-threads', 'pinning', 'synchronized', 'concurrency'],
  },
  {
    id: 'java-modern-q2',
    question: 'Why does converting a bounded ExecutorService (fixed thread pool) to a virtual-thread-per-task executor sometimes make an overloaded downstream dependency worse instead of better?',
    shortAnswer: 'A bounded platform-thread pool was accidentally providing backpressure — its fixed size capped concurrent requests to the downstream system. Virtual threads remove that cap (millions can run concurrently), so without an explicit limiter (e.g. a Semaphore or a bounded connection pool) at the call site, you can now flood the downstream dependency with far more concurrent requests than before.',
    detailedAnswer: "Thread pool sizing was doing double duty in a lot of legacy code: limiting resource usage/thread count AND acting as an implicit concurrency limiter on whatever the pool's tasks called downstream. Virtual threads correctly remove the first job (thread creation is cheap, no need to cap thread count), but naively swapping in newVirtualThreadPerTaskExecutor() also removes the second job as an unintended side effect, since there's no longer a queue-and-wait mechanism gating how many tasks run concurrently. Production migrations need to identify where pool size was implicitly protecting a downstream dependency (DB, external API) and replace that protection explicitly — a Semaphore, a bounded HTTP connection pool, or a rate limiter — rather than relying on thread pool size as a side-channel throttle.",
    keyPoints: [
      'Bounded platform-thread pools implicitly rate-limit whatever they call, not just thread count',
      'Virtual threads remove that implicit limiter along with the thread-count cap',
      'Backpressure must be reintroduced explicitly (Semaphore, connection pool bound, rate limiter) at the resource being protected',
    ],
    seniorFollowUps: [
      'Where would you put the Semaphore — around the executor submission, or around the specific downstream call?',
      'How would this interact with a circuit breaker already wrapping the downstream call?',
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['virtual-threads', 'backpressure', 'migration', 'concurrency'],
  },
  {
    id: 'java-modern-q3',
    question: 'You add a new subtype to a sealed interface used throughout the codebase in pattern-matching switches, but the build stays green and a switch statement silently falls into unexpected behavior at runtime. How is that possible given sealed types are supposed to force exhaustiveness checks?',
    shortAnswer: "Most likely one of the switches has a `default` branch (or an unrelated catch-all case) rather than being written to exhaustively enumerate the sealed type's permitted subtypes — the compiler only forces you to handle a new case when there's no default to silently absorb it, or one of the permitted branches uses a guard (`when`) that isn't exhaustive on its own and had a default backing it up.",
    detailedAnswer: "Sealed exhaustiveness checking is a compile-time convenience, not a runtime guarantee, and it only bites when the switch is written without a default/catch-all. If any switch over the sealed type includes `default ->` (common defensive style, or copy-pasted from pre-sealed code), adding a new permitted subtype compiles fine and routes into the default branch silently — which may not be the intended behavior for the new type. The same gap appears with guarded patterns (`case Circle c when c.r() > 100`) — a guard makes that branch's coverage conditional, so the compiler can't prove exhaustiveness from the guarded branch alone, and a default is often required to compile, reopening the same silent-fallthrough risk. The practical takeaway: avoid `default` on sealed-type switches when you actually want new-subtype compile failures, and audit guarded branches specifically since they're the case where exhaustiveness checking is weakest.",
    keyPoints: [
      'A `default` branch on a sealed-type switch defeats the exhaustiveness guarantee for new subtypes',
      "Guarded (`when`) branches aren't provably exhaustive by themselves, often forcing a default that reopens the gap",
      'Sealed exhaustiveness is a compile-time nudge, not an enforced invariant if defaults are used',
    ],
    seniorFollowUps: [
      'Would an oxlint/checkstyle rule banning default on sealed-type switches be worth adding, and what would it break?',
      "How does this interact with a switch expression (which requires exhaustiveness or a default to compile) versus a switch statement (which doesn't)?",
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['sealed', 'pattern-matching', 'switch', 'exhaustiveness'],
  },
  {
    id: 'java-modern-q4',
    question: 'A record used as a cache key (record CacheKey(int[] ids)) stops matching in a HashMap even when constructed with the same logical ids array contents. Why, and how would you fix it?',
    shortAnswer: "Records auto-generate equals()/hashCode() from their components using each component's own equals()/hashCode() — for an array component that's reference identity, not content equality, so two CacheKey instances built from different array objects with identical contents are unequal and hash differently. Fix by using a List<Integer> component instead, or by manually overriding equals()/hashCode() to use Arrays.equals()/Arrays.hashCode() if the array type must stay.",
    detailedAnswer: "This is one of the sharpest record gotchas because it's invisible until a specific bug appears: everything compiles, the record looks fully value-based, and it even works correctly if you reuse the exact same array instance as a key every time. It breaks the moment two logically-equal-but-distinct array instances are used, because the generated equals() delegates to int[].equals(), which Object provides as reference equality — arrays never override equals()/hashCode() to be content-based. The safest fix is swapping the component type to an immutable content-comparable type (List<Integer> via List.copyOf, or a String), which also sidesteps the record's shallow-immutability trap (the caller can't mutate the array after construction either way). If the array type is mandated by an external API, override equals()/hashCode()/toString() manually using Arrays.equals()/Arrays.hashCode() — legal on a record, and the compiler won't regenerate members you've explicitly declared.",
    keyPoints: [
      "Auto-generated record equals()/hashCode() delegate to each component's own equals()/hashCode()",
      'int[]/Object[] never override equals()/hashCode() — arrays compare by reference regardless of contents',
      'Prefer List<Integer> or another content-comparable immutable type over an array component; manual override is the fallback',
    ],
    seniorFollowUps: [
      "Would this same bug affect a record's toString() output, and how would you notice it in logs?",
      'Does wrapping the array in Collections.unmodifiableList fix the equality problem, or only the mutability problem?',
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['records', 'equals', 'hashcode', 'arrays'],
  },
  {
    id: 'java-modern-q5',
    question: 'Should a CPU-bound batch computation service (e.g. image resizing, heavy JSON parsing) migrate its worker pool to virtual threads to improve throughput?',
    shortAnswer: 'No — virtual threads only help by unmounting from a carrier while blocked on I/O; CPU-bound work never blocks, so a virtual thread doing computation occupies its carrier exactly as a platform thread would. Migrating gives no throughput benefit and adds needless churn; the actual lever for CPU-bound throughput is matching worker count to core count, which a platform-thread ForkJoinPool/fixed pool already does.',
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['virtual-threads', 'cpu-bound', 'concurrency'],
  },
  {
    id: 'java-modern-q6',
    question: "What's the difference between a compact constructor and just adding validation logic to a record's canonical constructor manually?",
    shortAnswer: "A compact constructor (no parameter list) lets you validate/normalize without writing the field assignments — the compiler still appends `this.x = x` for each component after your code runs. Writing a full canonical constructor with an explicit parameter list suppresses that implicit assignment entirely, so you must assign every field yourself; it's more verbose but gives full control (e.g. assigning a transformed value instead of the raw parameter).",
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['records', 'constructors'],
  },
  {
    id: 'java-modern-q7',
    question: "Why can't you use var to declare a method parameter or a class field, even though var works fine for local variables with initializers?",
    shortAnswer: "var is purely local type inference — the compiler derives the type from the initializer expression at the declaration site. A method parameter or field has no initializer expression at its declaration for the compiler to infer from (a field's assignment happens elsewhere, a parameter's value comes from the caller), so there's nothing to infer the type from.",
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['var', 'type-inference'],
  },
  {
    id: 'java-modern-q8',
    question: 'What does non-sealed mean on a permitted subtype, and why would a library author ever use it instead of just leaving the whole hierarchy unsealed?',
    shortAnswer: 'non-sealed reopens that one branch of an otherwise sealed hierarchy to unrestricted further extension, while every other branch stays closed. A library author uses it to get compiler-enforced exhaustiveness for the core, controlled cases while still giving consumers one designated extension point — clearer intent than leaving the entire hierarchy open, which would remove exhaustiveness checking everywhere.',
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['sealed', 'non-sealed'],
  },
  {
    id: 'java-modern-q9',
    question: "In a pattern-matching switch, what's the purpose of allowing case null as an explicit label instead of just null-checking before the switch?",
    shortAnswer: 'Before Java 21, a pattern-matching switch on a reference type threw NullPointerException on a null subject, forcing an external null check every time. case null lets the switch itself route null explicitly (including combined as `case null, default ->`), consolidating null-handling and type-dispatch logic into one construct instead of splitting it across a guard clause and the switch.',
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['switch', 'pattern-matching', 'null'],
  },
  // ── Exception Handling Questions ────────────────────
  {
    id: 'java-exc-q1',
    question: "A teammate insists all new service-layer exceptions should be checked, arguing it forces callers to handle failures. What's the counter-argument modern Java API design makes against that?",
    shortAnswer: "Checked exceptions don't compose with functional interfaces — you can't throw one from a Stream.map() or CompletableFuture stage without wrapping it — and they leak implementation detail across abstraction boundaries, forcing every intermediate caller to know about a failure it can't act on. That's why Streams, Optional, and CompletableFuture standardized on unchecked exceptions; checked exceptions work best narrowly, at a stable boundary, for a condition the immediate caller can actually recover from.",
    detailedAnswer: "The forced-handling argument sounds right but breaks down in practice: most callers of a checked exception either propagate it further (declaring throws all the way up, which just spreads the noise) or catch-and-ignore it to satisfy the compiler, neither of which is meaningfully safer than an unchecked exception with good documentation. The functional-composition problem is the sharper technical argument — java.util.function.Function<T,R> and friends don't declare checked exceptions, so any checked-throwing code inside a lambda must be caught and wrapped locally, which is exactly the boilerplate checked exceptions were meant to avoid. The practical middle ground: keep checked exceptions rare, specific, and only at boundaries (e.g. a repository throwing a checked DataAccessException-style type) where the caller has one clear recovery action, and default everything else to unchecked.",
    keyPoints: [
      "Functional interfaces can't declare checked exceptions — breaks Stream/CompletableFuture composition",
      "Checked exceptions leak implementation details across layers a caller shouldn't need to know about",
      'Catch-and-ignore-to-compile is common and defeats the safety argument',
      'Reserve checked exceptions for narrow, recoverable, boundary-level conditions',
    ],
    seniorFollowUps: [
      'How would you retrofit a legacy checked-exception-heavy API to work cleanly inside a Stream pipeline without a big rewrite?',
      "Is there a case where you'd deliberately introduce a new checked exception in a greenfield service today?",
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['checked-exceptions', 'api-design', 'streams'],
  },
  {
    id: 'java-exc-q2',
    question: "In a try-with-resources block, the try body throws WorkException and the resource's close() throws SQLException. Which exception does the caller see, and where did the other one go?",
    shortAnswer: 'The caller catches WorkException — the exception from the try body always takes priority. The SQLException from close() is attached to it as a suppressed exception, retrievable via WorkException.getSuppressed(), rather than being thrown separately or silently discarded.',
    detailedAnswer: "This is a deliberate Java 7 change from the pre-try-with-resources era, where developers had to manually close resources in a finally block — if close() threw there, it replaced whatever exception the try body had thrown, hiding the real root cause entirely. The suppressed-exception mechanism keeps both: the primary exception propagates normally through catch clauses and stack traces, while any close()-time exceptions (there can be several, one per resource) are appended to its suppressed array. Most loggers and printStackTrace() render these automatically under a 'Suppressed:' section, so nothing extra is needed to see them — but code that manually re-throws or wraps the caught exception should be aware it needs to preserve the object itself (not just its message) to keep the suppressed chain intact.",
    keyPoints: [
      'Try-body exception is primary and propagates; close()-time exceptions become suppressed, not thrown',
      'getSuppressed() exposes the array; printStackTrace()/most loggers render it automatically',
      'Pre-Java-7 manual finally-close silently replaced the real exception — this was the bug being fixed',
    ],
    seniorFollowUps: ['If the try body succeeds but close() throws, what does the caller see?'],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['try-with-resources', 'suppressed-exceptions', 'resource-management'],
  },
  {
    id: 'java-exc-q3',
    question: 'A production incident report says \'NullPointerException at line 47\' with no useful context about what actually failed upstream. Code review later finds `catch (SQLException e) { throw new ServiceException("query failed"); }`. What\'s wrong, and what\'s the one-line fix?',
    shortAnswer: 'The catch block constructs a brand-new ServiceException without passing the caught SQLException as its cause, so the original stack trace — the actual useful diagnostic information — is discarded entirely. Fix: `throw new ServiceException("query failed", e);` (or call initCause(e) if the constructor doesn\'t accept one), which preserves the full cause chain for logging and debugging.',
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['exception-chaining', 'logging', 'production'],
  },
  {
    id: 'java-exc-q4',
    question: "You're designing exception types for a payments module: InsufficientFundsException, InvalidAccountStateException, and a generic PaymentProcessingException. Which should be checked and which unchecked, and why?",
    shortAnswer: "InsufficientFundsException should likely be checked (or at least a distinct, deliberately-caught type) because it's an expected business condition the immediate caller can act on — retry, notify the user, offer an alternative. InvalidAccountStateException and a generic catch-all PaymentProcessingException are better as unchecked: the former usually indicates a bug or data-integrity issue the caller can't meaningfully recover from locally, and a generic catch-all checked exception just forces boilerplate without giving callers anything actionable to discriminate on.",
    detailedAnswer: "The design test is: does the immediate caller have one distinct, correct action to take for this specific failure? InsufficientFundsException passes — the caller can show a specific UI message or trigger a retry-with-different-source flow, and that's different from what it'd do for any other failure, so making it a checked type (or at minimum a distinctly-named, prominently-documented unchecked one) forces that decision to be made deliberately rather than accidentally swallowed by a broad catch(Exception). InvalidAccountStateException usually signals either a bug (state machine violated) or a race condition already handled elsewhere in the stack — forcing every caller several layers up to declare or catch it adds noise without adding safety. A single generic PaymentProcessingException as a catch-all defeats the purpose of a hierarchy: callers can't distinguish 'insufficient funds' from 'gateway timeout' from 'account frozen' without inspecting the message string, which is exactly the anti-pattern a proper hierarchy avoids.",
    keyPoints: [
      'Test: does the immediate caller have one distinct, correct recovery action?',
      'Recoverable, business-meaningful conditions lean checked/distinctly-typed',
      'Bugs and unrecoverable states lean unchecked',
      'Avoid one giant catch-all exception type — it collapses distinct failure modes into an undiscriminable string',
    ],
    seniorFollowUps: [
      'How would you evolve this hierarchy if a new payment provider introduces a failure mode none of your existing types capture?',
      'Would your answer change if this were a public SDK consumed by external teams rather than an internal module?',
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['exception-hierarchy', 'api-design', 'checked-exceptions'],
  },
  {
    id: 'java-exc-q5',
    question: 'What does this method return, and why: `try { throw new RuntimeException("boom"); } finally { return 42; }`?',
    shortAnswer: 'It returns 42 and the RuntimeException is completely discarded — a return (or throw) inside finally unconditionally overrides whatever was propagating from the try/catch, and this compiles without any warning from javac (though IDEs and static analyzers typically flag it).',
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['finally', 'control-flow', 'gotcha'],
  },
  {
    id: 'java-exc-q6',
    question: "A shutdown handler does cleanup work in a finally block, then the outer code path calls System.exit(0). QA reports the cleanup (flushing a write-ahead log) sometimes doesn't happen. Explain precisely why, and what to do instead.",
    shortAnswer: "System.exit() terminates the JVM immediately and does not unwind the stack through pending finally blocks, so any finally block that hasn't started executing yet when exit() is called simply never runs. The fix is to move the cleanup into a Runtime.addShutdownHook() thread, which the JVM is specifically designed to run during an orderly System.exit()-triggered shutdown (though not on a hard kill -9 or JVM crash).",
    detailedAnswer: "This trips people up because finally is taught as 'always runs' — true for return, normal completion, and thrown/caught exceptions, but false for a JVM-level halt. If System.exit() is called from within a try block whose finally hasn't executed yet, the JVM begins shutdown immediately; whether that specific finally runs depends entirely on whether exit() was reached before or after that stack frame started to unwind — in the reported bug, exit() was called from a sibling code path after the write-ahead-log method had already returned normally (finally should have run) but a separate later finally down the shutdown sequence called exit() again before an outer finally got its turn, so it never ran. The robust design is to never rely on finally for JVM-shutdown-critical work — use a registered shutdown hook, which the JVM guarantees to run (with a bounded time budget) for a normal exit() or Ctrl+C, though it's still skipped for kill -9 or a native crash.",
    keyPoints: [
      "System.exit() halts immediately, doesn't unwind through not-yet-executing finally blocks",
      'Runtime.addShutdownHook() is the mechanism designed for exit()-time cleanup',
      "Shutdown hooks also don't run on kill -9 or a JVM crash",
      "finally's 'always runs' guarantee excludes JVM-level termination",
    ],
    seniorFollowUps: [
      "What happens if a shutdown hook itself throws an exception, or runs long enough to hit the JVM's shutdown time budget?",
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['finally', 'system-exit', 'shutdown-hooks', 'gotcha'],
  },
  {
    id: 'java-exc-q7',
    question: "Profiling shows a hot validation path spending significant CPU time on exception construction, even though the exceptions are caught immediately and never logged with a stack trace. What's the likely cause, and how do you fix the exception class itself?",
    shortAnswer: 'The cost is almost certainly fillInStackTrace(), which walks the entire call stack on every `new` regardless of whether the trace is ever read. Since this exception is used purely as local control flow, override fillInStackTrace() to no-op in the custom exception class (or call the protected Throwable(String, Throwable, boolean, boolean) constructor with writableStackTrace=false), which skips the capture entirely.',
    detailedAnswer: "This is a well-known Java performance gotcha: throwing is not itself expensive, but Throwable's constructor calls fillInStackTrace() by default, which walks every frame on the current thread's call stack to build the array backing getStackTrace()/printStackTrace(). In a hot validation loop throwing thousands of times per second, that walk dominates. Since the exception here is caught immediately and its trace never inspected, there's zero value in paying that cost. The fix is either overriding fillInStackTrace() to return `this` without doing the walk, or — cleaner — using the four-arg protected Throwable constructor with writableStackTrace=false, which skips the capture at construction time entirely and is designed for exactly this use case. The tradeoff to flag to the team: doing this means if that exception ever does escape unexpectedly to a top-level handler, there'll be no stack trace to debug it with, so it should only be applied to exceptions used deliberately as fast-path control-flow signals, not to genuine error conditions.",
    keyPoints: [
      'fillInStackTrace() walking the call stack is the actual cost, not throw/catch itself',
      'Override fillInStackTrace() as a no-op, or use writableStackTrace=false constructor',
      'Tradeoff: the exception becomes undebuggable if it ever unexpectedly escapes to a real error path',
      'Only apply to exceptions genuinely used as high-frequency control flow, not real errors',
    ],
    seniorFollowUps: [
      "Would you reach for this optimization proactively, or only after profiling confirms it's a real bottleneck? Why?",
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['performance', 'fillinstacktrace', 'hot-path'],
  },
  {
    id: 'java-exc-q8',
    question: "A CompletableFuture chain processes a payment and has no exceptionally(), handle(), or terminal get()/join() call anywhere — it's fired and the code moves on. A stage throws partway through. What happens to that exception?",
    shortAnswer: "It's silently swallowed — nothing logs it, nothing crashes the application, the remaining chain stages are simply skipped and the completed-exceptionally future is discarded with no one ever inspecting it. This is a common source of 'the payment never went through and nobody knows why' incidents.",
    detailedAnswer: "CompletableFuture's design intentionally decouples the async computation from any obligation to surface its failure — that's what makes it composable, but it means failure visibility is entirely opt-in. Without a terminal call (get()/join(), which throw) or an explicit handler stage (exceptionally(), handle(), or whenComplete()), an exception thrown mid-chain simply marks that future as completed exceptionally and nothing more happens; downstream thenApply/thenAccept stages are skipped as designed, but if nothing downstream inspects the result, the failure evaporates. In production this is best defended against structurally: always terminate fire-and-forget chains with at minimum a .exceptionally() or .whenComplete() that logs, and treat a CompletableFuture with no such terminal handler as a code review flag.",
    keyPoints: [
      'No terminal get()/join() or exceptionally()/handle()/whenComplete() means failures are never observed',
      'Downstream stages are skipped but nothing surfaces that skip happened',
      'Defensive pattern: fire-and-forget chains should always end in at least a logging whenComplete()',
    ],
    seniorFollowUps: [
      'How would you retrofit alerting onto a codebase with many pre-existing fire-and-forget CompletableFuture chains without rewriting them all individually?',
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['completablefuture', 'async', 'error-handling'],
  },
  {
    id: 'java-exc-q9',
    question: "What's the practical difference between the exception you get from CompletableFuture.get() versus .join() when the same underlying stage failed?",
    shortAnswer: "get() throws a checked ExecutionException wrapping the real cause, so callers must catch it or declare it; join() throws the same cause wrapped in an unchecked CompletionException instead, which is why join() is preferred in stream/lambda contexts where checked exceptions don't compose. Either way, call getCause() to reach the original exception.",
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['completablefuture', 'get-vs-join'],
  },
  {
    id: 'java-exc-q10',
    question: 'You submit a Runnable to an ExecutorService via execute() (not submit()) and it throws an unhandled exception. Where does that exception go, and why might it never show up in your logs?',
    shortAnswer: "It's routed to the thread's UncaughtExceptionHandler — by default the JVM's built-in handler, which just prints to stderr. If your logging setup only captures what's explicitly logged (or your container's stderr isn't scraped into your log aggregator), the failure is effectively invisible even though it technically 'printed' somewhere.",
    detailedAnswer: "execute(Runnable) and submit(Runnable) behave differently on failure: submit() captures the exception into the returned Future, where it only surfaces if and when someone calls future.get(); execute() has no Future to capture it, so the exception propagates up to the running thread and hits its UncaughtExceptionHandler. Thread pools' worker threads use a default handler unless one was explicitly configured (via Thread.setDefaultUncaughtExceptionHandler() globally, or a custom ThreadFactory setting a per-thread handler), and the default just calls printStackTrace() to System.err. In many production setups, stderr from a container isn't wired into structured logging/alerting the same way explicit log calls are, so these failures can go unnoticed for a long time. The fix is deliberate: set a default uncaught exception handler at startup that routes through the real logger, and prefer submit() with an eventual get() (or a Future-checking supervisor) when failure visibility matters.",
    keyPoints: [
      'execute(Runnable) exceptions go to UncaughtExceptionHandler; submit(Runnable) exceptions are captured in the Future and only surface on get()',
      "Default handler just prints to stderr, which many log pipelines don't capture",
      'Fix: set Thread.setDefaultUncaughtExceptionHandler() or a custom ThreadFactory at startup',
      'A worker thread that dies from an uncaught exception can shrink the pool if not replaced',
    ],
    seniorFollowUps: [
      'How would you detect, after the fact, that a thread pool has silently lost worker threads to uncaught exceptions?',
    ],
    topic: 'java',
    subtopic: 'exceptions',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['executorservice', 'uncaught-exceptions', 'concurrency'],
  },
  // ── I/O & NIO Questions ────────────────────
  {
    id: 'java-io-q1',
    question: "A service processes thousands of concurrent slow client connections using one thread per connection over java.io streams, and it starts running out of memory under load even though CPU usage is low. What's happening and what are the two modern fixes?",
    shortAnswer: "Each blocked thread still holds its full stack (default ~512KB-1MB) even while idle waiting on I/O, so thread count times stack size exhausts heap/native memory well before CPU becomes the bottleneck. The two fixes are moving to NIO's Selector-based non-blocking model (one or few threads multiplex many connections) or, on Java 21+, switching to virtual threads so the blocking code stays but each virtual thread's footprint when parked is tiny.",
    detailedAnswer: "This is the classic thread-per-connection scaling wall (sometimes called C10K). With platform threads, memory pressure comes from stack allocation per thread, not from CPU work, since most threads are simply blocked in a read() syscall. NIO's Selector lets a small, fixed thread pool register interest in many channels and only wake up for channels that are actually ready, eliminating the per-connection thread entirely — at the cost of writing more complex, callback/state-machine-style handler code. Virtual threads sidestep that complexity: the code stays synchronous and blocking, but the JVM unmounts the virtual thread from its carrier platform thread during the blocking call, so thousands of parked virtual threads cost far less than thousands of parked platform threads.",
    keyPoints: [
      'Root cause is stack memory per blocked thread, not CPU',
      'NIO Selector: few threads, non-blocking channels, readiness-driven dispatch',
      'Virtual threads: keep blocking-style code, JVM handles the unmounting',
    ],
    seniorFollowUps: [
      'What kind of blocking call inside virtual-thread code can still pin the carrier thread and defeat this benefit?',
      'Would you migrate an existing NIO/Netty-based service to virtual threads? What would you actually gain?',
    ],
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['virtual-threads', 'nio', 'scalability', 'threads'],
  },
  {
    id: 'java-io-q2',
    question: "You fill a ByteBuffer from a channel, then loop calling get() on it and get nothing back. What's the bug and how do you fix it?",
    shortAnswer: 'After the channel.read() fill, position sits at the end of the written data and limit is still at capacity, so get() (which reads from position to limit) reads the empty remainder. Call buf.flip() before reading — it sets limit to the current position and resets position to 0, switching the buffer from write mode to read mode.',
    detailedAnswer: "Buffer state is tracked purely by three cursors (position, limit, capacity), not by a mode flag, so the same buffer object is reused for both writing and reading and it's on the caller to reposition the cursors correctly between phases. flip() is the write-to-read transition. If you then want to write into the same buffer again, clear() resets position to 0 and limit to capacity — but it doesn't erase old bytes, it just makes them eligible to be overwritten. compact() is the variant used for partial reads: it preserves any unread data between position and limit, shifts it to the buffer's start, and sets position right after it so a subsequent read can append without losing the leftover bytes.",
    keyPoints: [
      'flip(): limit = position, position = 0',
      'clear(): position = 0, limit = capacity, data not erased',
      'compact(): preserves unread remainder for partial-read loops',
    ],
    seniorFollowUps: ['When would you reach for compact() instead of clear()?'],
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['nio', 'bytebuffer', 'gotcha'],
  },
  {
    id: 'java-io-q3',
    question: 'Your Netty-style event-loop-based service suddenly has every connection stall, not just one. A code review finds a handler calling a blocking JDBC query directly on the event loop thread. Why does one slow query freeze everything?',
    shortAnswer: "The event loop thread is the same thread that both runs select() and dispatches every ready channel on that selector; a blocking call inside a handler occupies that thread for its full duration, so no other channel's readiness gets checked or dispatched until the blocking call returns — one slow query stalls all connections sharing that loop.",
    detailedAnswer: "The reactor pattern's whole value proposition is that a small number of threads can service many connections because each handler invocation is expected to be fast and non-blocking, returning control quickly so the loop can service the next ready channel. Any call that blocks the thread (a synchronous DB driver call, a blocking HTTP client call, even Thread.sleep) breaks that assumption and effectively serializes every connection on that loop behind the blocking call. The standard fixes are: use a non-blocking/reactive database driver, or explicitly offload the blocking call to a separate bounded worker thread pool and return control to the event loop immediately.",
    keyPoints: [
      'One event loop thread multiplexes many channels via select() + dispatch',
      'Any blocking call inside a handler occupies that shared thread',
      'Fix: non-blocking driver, or explicit offload to a worker pool',
    ],
    seniorFollowUps: [
      'How would virtual threads change this failure mode if the framework adopted them instead of a fixed event-loop pool?',
    ],
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['nio', 'reactor', 'selector', 'netty'],
  },
  {
    id: 'java-io-q4',
    question: 'When is a memory-mapped file (MappedByteBuffer) actually worth the added complexity over a plain BufferedInputStream?',
    shortAnswer: "It pays off for large files accessed randomly or repeatedly — random-access index structures, log-structured storage segments, repeated scans of the same file — because reads/writes become plain memory accesses backed by the OS page cache instead of repeated syscalls. For small files or a single sequential pass, the syscall savings don't matter and you're left with page-fault latency unpredictability and a fragile unmap lifecycle for no real gain.",
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['nio', 'mmap', 'performance'],
  },
  {
    id: 'java-io-q5',
    question: 'Why does Files.move(src, dst, ATOMIC_MOVE) give a stronger guarantee than the legacy File.renameTo()?',
    shortAnswer: "renameTo() returns a plain boolean with platform-dependent semantics for both success signalling and atomicity, so a caller can't reliably tell whether a failure happened or whether a partial rename occurred. ATOMIC_MOVE either completes the move atomically or throws a specific exception (e.g. AtomicMoveNotSupportedException), which is what a safe temp-file-then-publish pattern actually needs.",
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['nio2', 'files', 'atomicity'],
  },
  {
    id: 'java-io-q6',
    question: 'You wrap a raw FileInputStream in a BufferedReader before parsing a large log file line by line. What performance problem does this actually solve?',
    shortAnswer: 'Without buffering, each read on the raw stream is a separate system call, so processing a file byte-by-byte or via unbuffered small reads turns an I/O-bound job into one dominated by syscall and context-switch overhead. BufferedReader batches those into large internal reads (8KB default) and adds readLine() on top, so the syscall count drops by orders of magnitude.',
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['buffered-io', 'performance', 'syscalls'],
  },
  {
    id: 'java-io-q7',
    question: 'In a try-with-resources block, the try body throws an exception and then close() on the resource also throws. What exception does the caller actually see?',
    shortAnswer: "The caller sees the try block's original exception as the primary one; the exception thrown by close() is attached to it via addSuppressed() rather than replacing or masking it, so both are recoverable from the stack trace instead of one silently disappearing.",
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['try-with-resources', 'exceptions'],
  },
  {
    id: 'java-io-q8',
    question: "What's the practical difference between java.nio (channels/buffers/selectors) and java.nio.file (NIO.2, Path/Files)?",
    shortAnswer: "java.nio (Java 4) is about non-blocking, buffer-oriented I/O for sockets and files via channels and selectors. java.nio.file / NIO.2 (Java 7) is a completely separate, unrelated upgrade that replaced java.io.File with a richer filesystem API (Path, Files, WatchService, pluggable filesystems) — it has nothing to do with non-blocking I/O despite the shared 'nio' name.",
    topic: 'java',
    subtopic: 'io-nio',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['nio', 'nio2', 'terminology'],
  },
  // ── Serialization Questions ────────────────────
  {
    id: 'java-ser-q1',
    question: 'A service deserializes objects from an internal message queue where every producer is a trusted service you control. Is java.io.Serializable safe to use there?',
    shortAnswer: "Safer than for untrusted input, but 'trusted producer' isn't the same as 'trusted bytes' — if any producer's classpath or a shared dependency is compromised, or if the queue itself is reachable by something less trusted, the same gadget-chain risk applies. Most teams still prefer a schema-based format (Protobuf/Avro) or at minimum an ObjectInputFilter allowlist even for internal traffic.",
    detailedAnswer: "Native serialization's attack surface comes from transitively reachable classes already on the classpath, not from your own code being buggy. 'Trusted producer' reduces likelihood but doesn't eliminate the exposure: a compromised dependency, a misconfigured queue ACL, or a future consumer added without review can all reintroduce untrusted input into a code path that assumed it was safe. The pragmatic senior answer is defense in depth — use ObjectInputFilter (JEP 290) to allowlist exactly the classes expected on that queue, treat it as a stopgap rather than a long-term architecture, and prefer moving to a schema-based binary format when the service boundary is revisited, since that also solves versioning and cross-language concerns Serializable doesn't.",
    keyPoints: [
      'Gadget-chain risk lives in classpath dependencies, not your own classes',
      'ObjectInputFilter (Java 9+) allowlists acceptable classes as a mitigation, not a full fix',
      "'Trusted producer' is an assumption that degrades over time as systems evolve",
    ],
    seniorFollowUps: [
      'How would you retrofit ObjectInputFilter onto an existing deserialization path without breaking legitimate traffic?',
    ],
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['security', 'deserialization', 'gadget-chain'],
  },
  {
    id: 'java-ser-q2',
    question: 'Your team ships v1 of a class without an explicit serialVersionUID. In v2 you add an unrelated helper method and redeploy. What breaks, and why?',
    shortAnswer: "Deserializing any object that was serialized under v1 now throws InvalidClassException. Without an explicit UID, the JVM computes one from the class's structure — including its methods — so adding the helper method changes the computed value even though no serialized field changed, and the version check on read fails.",
    detailedAnswer: "The default serialVersionUID is derived via a hash over class name, modifiers, interfaces, and the descriptors of fields and methods (per the Java Object Serialization Specification's algorithm). Because methods factor in, a change with zero effect on the object's actual persisted shape — like adding an unrelated helper — still shifts the hash. On deserialization the stream's embedded UID is compared to the loading class's UID, and any mismatch (even where the fields are still fully compatible) throws InvalidClassException. Declaring an explicit serialVersionUID freezes that check to only fire when you decide it should, decoupling binary compatibility from incidental code changes.",
    keyPoints: [
      'Computed UID depends on the full class shape, not just serialized fields',
      'Explicit UID makes compatibility a deliberate decision instead of an accidental side effect',
      'javac/JVM implementation differences can also shift the computed value across environments',
    ],
    seniorFollowUps: [
      "If v2 genuinely needs to change a field's type, how would you evolve the class while still reading old v1 data?",
    ],
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['serialversionuid', 'compatibility', 'versioning'],
  },
  {
    id: 'java-ser-q3',
    question: "Compare Serializable's default mechanism against Externalizable in terms of what actually runs during deserialization.",
    shortAnswer: "Serializable's default path uses reflection to allocate the object without calling any constructor and then walks fields directly, which is exactly what lets malformed streams bypass invariant checks. Externalizable requires a public no-arg constructor that always runs first, then readExternal populates state explicitly — giving you a real code path to validate input, at the cost of writing every field by hand.",
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['externalizable', 'serializable', 'mechanics'],
  },
  {
    id: 'java-ser-q4',
    question: "Why does making a class a record change its exposure to the classic 'deserialization skips constructor validation' problem?",
    shortAnswer: "Records always deserialize through their canonical constructor, so any validation in a compact constructor runs on deserialized data too — unlike a regular class's default readObject, which reflectively sets fields directly and never calls a constructor at all.",
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['records', 'deserialization', 'validation'],
  },
  {
    id: 'java-ser-q5',
    question: 'Given the security and interoperability downsides, where does native Java serialization still legitimately show up in production systems?',
    shortAnswer: "Mainly same-JVM-version, same-trust-boundary scenarios: distributed cache products like Hazelcast or Ehcache moving objects between cluster members, servlet container session replication, and legacy RMI — cases where both ends run the identical class bytecode and aren't crossing an external trust boundary.",
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['distributed-cache', 'session-replication', 'production'],
  },
  {
    id: 'java-ser-q6',
    question: "A field holding a Thread reference is added to an existing Serializable class and it isn't marked transient. What happens?",
    shortAnswer: "It fails at runtime, not compile time — Thread isn't Serializable, so attempting to serialize an instance throws NotSerializableException the first time it's actually written, since Serializable is only a marker and the compiler can't verify field types are serializable ahead of time.",
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['transient', 'notserializableexception', 'mechanics'],
  },
  {
    id: 'java-ser-q7',
    question: 'Why might a team choose Protobuf over JSON for a high-throughput internal service, beyond just avoiding native Java serialization?',
    shortAnswer: 'Protobuf gives compact binary encoding (smaller payloads, faster parse) plus an explicit schema with defined evolution rules (field numbers, optional/required semantics), where JSON is verbose text with no built-in schema contract and native serialization ties both ends to identical bytecode.',
    topic: 'java',
    subtopic: 'serialization',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['protobuf', 'json', 'wire-format'],
  },
  // ── Reflection & Annotations Questions ────────────────────
  {
    id: 'java-refl-q1',
    question: "A teammate adds a custom @Auditable annotation and reads it reflectively in an interceptor, but it never shows up at runtime even though the annotation is clearly present in the source. What's the most likely cause?",
    shortAnswer: 'The annotation is missing @Retention(RetentionPolicy.RUNTIME) — without it, the annotation defaults to CLASS retention (kept in bytecode but stripped from the loaded runtime representation), so getAnnotation() silently returns null instead of throwing.',
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['annotations', 'retention', 'gotcha'],
  },
  {
    id: 'java-refl-q2',
    question: 'Why do frameworks that rely heavily on reflection (Spring, Hibernate) go out of their way to cache Method and Field lookups instead of just calling getDeclaredMethod each time?',
    shortAnswer: "getDeclaredMethod/getDeclaredField perform a linear scan and access-check resolution against the class's member table, which is the genuinely expensive part of reflection — the invoke() call itself is comparatively cheap, especially after JVM inflation kicks in. Resolving once and reusing the Method/Field object turns a per-call cost into a one-time startup cost.",
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['performance', 'caching', 'internals'],
  },
  {
    id: 'java-refl-q3',
    question: "You upgrade a reflection-heavy library from Java 8 to Java 17 and it starts throwing InaccessibleObjectException at runtime on code that compiled and worked fine before. Explain what changed and how you'd fix it.",
    shortAnswer: "Java 9+'s module system enforces strong encapsulation at runtime: deep reflection (setAccessible into private members) into another module's package now requires that module to explicitly 'open' the package, not just 'export' it. Code on the classpath (unnamed module) is more lenient, but a real module boundary blocks it. Fix: add an opens directive in module-info.java for the target package to the caller's module, or add --add-opens on the JVM command line as a stopgap.",
    detailedAnswer: "Before Java 9, reflection had essentially unrestricted access to any class it could load, gated only by a SecurityManager if one was configured. JPMS changes the default: a package not exported isn't even visible to reflection from outside the module, and even an exported package only allows normal (shallow) reflective access — deep reflection that bypasses private/protected checks via setAccessible(true) additionally requires the package to be 'opened'. This is why libraries like Hibernate, Mockito, and older Spring versions needed --add-opens flags (e.g. to java.base) when users first moved to JDK 9+ — they reflectively touch JDK internals that weren't opened by default.",
    keyPoints: [
      'exports != opens: exports covers compile-time/public API visibility; opens additionally covers deep runtime reflection',
      'Unnamed module (plain classpath) code stays in the more permissive legacy mode',
      '--add-opens is a per-run stopgap; module-info.java opens directive is the proper long-term fix if you own the module',
    ],
    seniorFollowUps: [
      'How would this affect a plan to migrate an app to a native-image (GraalVM) build, where reflection needs to be known ahead of time anyway?',
    ],
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jpms', 'modules', 'encapsulation', 'migration'],
  },
  {
    id: 'java-refl-q4',
    question: "Lombok's @Getter and Spring's @Autowired are both annotations, but they impose completely different runtime costs. Why?",
    shortAnswer: "Lombok is an annotation processor (APT): it runs at compile time and generates a real getter method directly into the .class file, so at runtime it's an ordinary method call with zero reflection involved. @Autowired is read via runtime reflection by Spring's container — Spring inspects RUNTIME-retained annotations, resolves the field/constructor, and injects reflectively, which is real (if largely front-loaded, cached) runtime cost that APT-based code never pays.",
    detailedAnswer: "The two mechanisms don't overlap: APT processors implement javax.annotation.processing.Processor and hook into javac's compilation rounds, reading the AST and annotations to emit new source/bytecode before the class ever exists as a runtime artifact. Reflection-based frameworks do nothing at compile time — they load the already-compiled class at runtime and query RUNTIME-retained annotations via getAnnotation(). This is also why an annotation meant purely for an APT processor doesn't need RUNTIME retention (SOURCE is enough, and cleaner, since it disappears from bytecode entirely), while any annotation a framework like Spring needs to see reflectively must be RUNTIME-retained or the framework silently sees nothing.",
    keyPoints: [
      'APT cost is paid once at build time; reflection cost is paid at class-load/startup and potentially per call unless cached',
      'Retention requirements differ: APT only needs the annotation to survive to the AST/bytecode it processes; reflection needs RUNTIME retention specifically',
      "GraalVM native-image and Spring AOT processing push more of Spring's traditionally-reflective work toward compile/build time, closing part of this gap",
    ],
    seniorFollowUps: [
      "Could you replace Spring's reflective dependency injection with an APT-based approach entirely? What would you gain and lose?",
    ],
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['apt', 'lombok', 'spring', 'annotations'],
  },
  {
    id: 'java-refl-q5',
    question: 'You call a @Transactional method on a Spring-managed bean, but from another method within that same class — and no transaction ever starts. Why, mechanically, does this happen?',
    shortAnswer: "Spring implements @Transactional (and most interface-based AOP) with a runtime proxy wrapping the real bean — external callers get a reference to the proxy, and the proxy's InvocationHandler runs the transactional advice before delegating to the real object via reflection. A call from inside the same class (this.otherMethod()) never goes through the proxy at all, since 'this' refers to the real, unwrapped instance — so the advice is skipped entirely.",
    detailedAnswer: "This is a direct consequence of how JDK dynamic proxies (or CGLIB for class-based proxying) work: Proxy.newProxyInstance produces a new object implementing the bean's interfaces, and every call arriving at that proxy object is routed through a single InvocationHandler before optionally delegating to the real target. Spring's context registers the proxy as the bean in the application context, so anything injected via @Autowired gets the proxy. But inside the class's own bytecode, 'this' is a direct reference to the real object, bypassing the proxy and therefore all AOP advice — transactions, caching, retries, security checks, anything implemented this way.",
    keyPoints: [
      'Proxy-based AOP only intercepts calls that arrive from outside the proxied object',
      'Workarounds: inject a self-reference proxy (AopContext.currentProxy() with exposeProxy=true, or self-autowiring), or refactor the transactional method into a separate bean',
      'CGLIB (subclass-based proxying) has the same self-invocation limitation as JDK interface proxies — the root cause is proxying, not the specific proxy mechanism',
    ],
    seniorFollowUps: [
      "Why does Spring fall back to CGLIB instead of JDK dynamic proxies when a bean doesn't implement any interfaces, and what tradeoff does that introduce (e.g. final methods/classes)?",
    ],
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['aop', 'proxies', 'transactional', 'spring'],
  },
  {
    id: 'java-refl-q6',
    question: "What's the practical difference between getMethods() and getDeclaredMethods(), and why does it matter when writing a reflective serializer like a mini Jackson?",
    shortAnswer: 'getMethods() returns all public methods, including inherited ones, but nothing private or package-private. getDeclaredMethods() returns everything declared directly on that class — including private/protected/package-private — but nothing inherited from a superclass. A serializer that wants every property, including private fields from a base class, has to walk the class hierarchy manually with getSuperclass() and call getDeclaredFields() at each level.',
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['introspection', 'class-hierarchy'],
  },
  {
    id: 'java-refl-q7',
    question: 'A reflective call throws, but the stack trace shows InvocationTargetException instead of the exception you expected. Why does this happen, and how do you handle it correctly?',
    shortAnswer: "Method.invoke() wraps any exception thrown by the invoked method's body in an InvocationTargetException so reflective callers can distinguish 'the invoked code threw' from 'reflection itself failed' (e.g. IllegalAccessException). To get the real error, catch InvocationTargetException and call getCause() to unwrap it — treating the wrapper as the actual error is a common bug that surfaces the wrong exception type in logs and catch blocks.",
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['exceptions', 'invoke', 'gotcha'],
  },
  {
    id: 'java-refl-q8',
    question: 'Why does Spring generally prefer constructor injection over field injection when both ultimately rely on reflection?',
    shortAnswer: 'Field injection needs setAccessible(true) to break into a private field on an already-constructed object, meaning the object exists in a partially-invalid state until the container reflectively pokes values into it, and it makes the class harder to construct/test outside the container (no way to pass dependencies without reflection or a framework). Constructor injection uses Constructor.newInstance() with resolved arguments up front — the object is fully valid the moment it exists, dependencies are explicit in the API, and it works fine in plain unit tests with `new` and no framework at all.',
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dependency-injection', 'spring', 'design'],
  },
  {
    id: 'java-refl-q9',
    question: "Why can't java.lang.reflect.Proxy create a dynamic proxy for a concrete class with no interfaces, and what do frameworks use instead?",
    shortAnswer: "JDK dynamic proxies work by generating a new class that implements a given set of interfaces and routes their methods through an InvocationHandler — there's no interface to implement for a concrete class, so there's nothing for the generated class to be. Frameworks fall back to bytecode generation (CGLIB in Spring's case) that subclasses the target class at runtime and overrides its methods instead, which is also why final classes and final methods can't be proxied this way.",
    topic: 'java',
    subtopic: 'reflection',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['proxies', 'cglib', 'aop'],
  },
  // ── Design Patterns Questions ────────────────────
  {
    id: 'java-dp-q1',
    question: "You review a PR with double-checked-locking Singleton where the instance field isn't volatile. The author says it 'works fine in testing.' What's the actual risk, and why doesn't testing catch it?",
    shortAnswer: "Without volatile, the JMM allows the constructor's field writes to be reordered after the instance reference is published, so another thread can see a non-null but partially-initialized object. This is a race that depends on specific CPU/JIT reordering behavior and thread interleaving timing — it's rare enough that a test run, or even most production traffic, may never trigger it, then shows up under load or on a different architecture.",
    detailedAnswer: "The bug is a visibility/ordering issue, not a logic issue, so it's inherently nondeterministic and platform-dependent — the same code can be safe on one JIT/CPU combination and broken on another. Fixing it just means adding volatile to the instance field (restores the happens-before edge) or switching to the initialization-on-demand holder idiom, which sidesteps DCL entirely by relying on classloader init guarantees.",
    keyPoints: [
      'Reordering, not logic, is the bug',
      'volatile fixes it by establishing happens-before on publish',
      'Holder idiom is a simpler correct alternative',
    ],
    seniorFollowUps: [
      'Why does the holder idiom not need volatile at all?',
      'Would you flag this in review as a blocking issue or a nice-to-have — how would you justify that to the author?',
    ],
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['singleton', 'double-checked-locking', 'jmm', 'volatile'],
  },
  {
    id: 'java-dp-q2',
    question: "A teammate proposes replacing a Spring-managed singleton bean with a classic Singleton pattern (private constructor + static getInstance()) to 'reduce framework magic.' What's your pushback?",
    shortAnswer: "Both give you one shared instance, but classic Singleton hardcodes the dependency behind a static call that tests can't intercept or substitute, while a container-managed bean is injected — visible in the constructor and trivially replaceable with a mock. The container isn't adding magic here, it's what preserves testability that the classic pattern gives up.",
    detailedAnswer: "The single-instance guarantee is orthogonal to testability — DI containers already give you singleton scope by default, so switching to the GoF pattern doesn't buy anything except losing the ability to inject a test double, and introducing global mutable state that can leak between tests unless carefully reset.",
    keyPoints: [
      'Singleton scope and testability are separable concerns',
      'Static getInstance() is an invisible, unmockable dependency',
      'Container singleton scope already gives one instance without losing DI',
    ],
    seniorFollowUps: [
      'Is there ever a legitimate case for the classic Singleton pattern inside an otherwise DI-managed codebase?',
    ],
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['singleton', 'dependency-injection', 'testability'],
  },
  {
    id: 'java-dp-q3',
    question: 'When would you still reach for a Builder instead of just using a record for a value object?',
    shortAnswer: 'When the object has several optional fields with different sensible defaults, or construction needs staged/conditional assembly — a record forces every field through one fixed constructor call, which gets unreadable and error-prone past a handful of optional params. For a small object with mostly-required fields, a record alone is simpler and gives you immutability plus equals/hashCode for free.',
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['builder', 'records', 'value-objects'],
  },
  {
    id: 'java-dp-q4',
    question: 'Is it safe to share and reuse a single Builder instance across multiple threads to stamp out several objects concurrently?',
    shortAnswer: 'No — a mutable Builder is a single-use, single-threaded scratchpad. Sharing one across threads races on its internal fields exactly like any other unsynchronized mutable state; the fix is one Builder per object per thread, not synchronizing the Builder.',
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['builder', 'thread-safety'],
  },
  {
    id: 'java-dp-q5',
    question: "You see a UIFactory interface with createButton() and createCheckbox() methods, each implemented by DarkUIFactory and LightUIFactory. A junior engineer calls this 'just Factory Method.' How do you correct the terminology, and does it matter?",
    shortAnswer: "It's Abstract Factory — an interface bundling several related Factory Methods so a caller gets a consistent family of products (all-dark or all-light) without knowing the concrete family. Each individual create method is a Factory Method; the interface coordinating them into a family is what makes it Abstract Factory. The terminology matters less for correctness than for spotting the actual design constraint: Abstract Factory exists specifically to keep the produced family internally consistent, which single Factory Methods don't guarantee.",
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['factory-method', 'abstract-factory'],
  },
  {
    id: 'java-dp-q6',
    question: 'A codebase has a StrategyBase abstract class with five subclasses, each implementing one stateless method used to compute a discount. How would you refactor this for modern Java, and when would you leave it as class hierarchy instead?',
    shortAnswer: 'If each strategy is genuinely stateless and single-method, collapse the hierarchy into a functional interface and pass lambdas or method references — five small classes become five one-line lambda assignments. Leave it as a class hierarchy only if a strategy needs its own state, multiple coordinated methods, or is complex enough to deserve its own unit tests as a named type.',
    detailedAnswer: "The refactor is mechanical once the functional interface is defined: PricingStrategy discount = order -> ...; replaces each subclass. The judgment call is recognizing which strategies don't fit that mold — e.g. a strategy that caches computed state between calls, or implements two related methods (apply() and rollback()) — those still deserve a real class.",
    keyPoints: [
      'Stateless single-method strategies collapse to lambdas cleanly',
      'Stateful or multi-method strategies still warrant a named class',
      'This is largely a code-smell-recognition question, not a syntax one',
    ],
    seniorFollowUps: ["How would you unit test a lambda-based strategy the same way you'd test a class-based one?"],
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['strategy', 'lambdas', 'refactoring'],
  },
  {
    id: 'java-dp-q7',
    question: "A synchronous Observer implementation — a subject calling each registered listener inline on its own thread — starts causing request timeouts as more listeners get added. What's happening, and how would you fix it without breaking the existing listener API?",
    shortAnswer: "Each additional listener adds directly to the publisher's call latency because listeners run inline, sequentially, on the calling thread — one slow listener (or a growing count of fast ones) stalls the whole publish. Fix by decoupling publish from execution: hand notifications to an executor or async event bus (e.g. Spring's @Async @EventListener, or a message queue) so the publisher returns immediately and listeners run independently, without changing the listener interface itself.",
    detailedAnswer: "This is also where a single throwing listener becomes a correctness bug, not just a latency one — an unhandled exception in one listener can abort the loop and block the remaining listeners from running (or even affect the publisher's own control flow), so isolating each listener's execution matters as much as making it async.",
    keyPoints: [
      'Synchronous Observer serializes listener latency onto the publish call',
      "One listener's exception can break notification of the rest",
      'Decouple via executor/async event bus without changing the listener contract',
    ],
    seniorFollowUps: [
      'What ordering or delivery guarantees do you lose by moving from synchronous in-process notification to an async event bus?',
    ],
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['observer', 'event-driven', 'performance'],
  },
  {
    id: 'java-dp-q8',
    question: "Structurally, a Decorator and a dynamic Proxy both implement the same interface as the object they wrap and delegate calls to it. If someone asked you to tell them apart from a class diagram alone, could you — and what's the real distinguishing test?",
    shortAnswer: "Not reliably from structure alone — the real distinction is intent, not shape. Decorator composes additional responsibility onto every call (buffering, compression, formatting); Proxy mediates or controls access to the same call without changing what it means (auth checks, lazy initialization, remote dispatch). The question to ask is 'does this wrapper change what the call does, or just gate whether/how it happens?'",
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['decorator', 'proxy', 'structural-patterns'],
  },
  {
    id: 'java-dp-q9',
    question: "A Spring @Transactional method calls another @Transactional method on the same class via `this.otherMethod()`, and the second method's transaction boundary is silently ignored. Why, in terms of what pattern is actually in play here?",
    shortAnswer: '@Transactional is implemented via a JDK/CGLIB proxy wrapping the bean — calls from outside go through the proxy and get the transactional advice, but a self-invocation (this.otherMethod()) bypasses the proxy entirely and calls the real object directly, so no advice runs. This is the proxy-based-AOP limitation: the proxy only intercepts calls that come in through it.',
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['proxy', 'spring', 'aop', 'gotcha'],
  },
  {
    id: 'java-dp-q10',
    question: "Someone claims Dependency Injection is 'a Spring thing' and asks why you'd bother learning it separately. How do you respond?",
    shortAnswer: 'DI is a general design principle — a class declares its dependencies (via constructor) instead of constructing or looking them up itself — that predates Spring and works with zero framework at all, e.g. wiring everything by hand in main(). Spring/Guice/Dagger just automate that wiring at scale; understanding the principle is what lets you reason about testability and coupling even outside a DI framework.',
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dependency-injection', 'inversion-of-control'],
  },
  {
    id: 'java-dp-q11',
    question: "A codebase has AbstractOrderProcessor with 10 subclasses, each overriding a single processPayment() step, and nothing else varies between them. A staff engineer flags this as over-engineered. What's the judgment call here?",
    shortAnswer: "If the only variation is one method's behavior and there's no shared mutable state or multi-step coordination needed, a Strategy-via-lambda (a Map<PaymentType, PaymentProcessor> of functional interfaces) replaces 10 classes with 10 lambda entries — less boilerplate, easier to test in isolation, and no inheritance coupling to a shared base class. The pattern (subclassing per variation) isn't wrong, it's just the pre-Java-8 way of doing what a functional interface now does more cheaply — the flag is really about recognizing when a pattern has been superseded by a simpler language feature, not that patterns themselves are bad.",
    detailedAnswer: "This is the core senior judgment test in this subtopic: patterns aren't good or bad in the abstract, they're a tradeoff against the complexity they replace. Ten near-identical subclasses differing by one method is a textbook case where the class-hierarchy version of Strategy has more ceremony than the problem needs — but the same pattern (as a class hierarchy) is the right call the moment a variant needs its own state, multiple coordinated methods, or non-trivial setup that a lambda can't express cleanly.",
    keyPoints: [
      'Pattern overhead should track actual variation complexity, not habit',
      'Ten one-method subclasses is a signal to collapse to Strategy-via-lambda',
      'The same pattern as a class hierarchy is still correct once state/multi-method needs appear',
    ],
    seniorFollowUps: [
      'How would you migrate this incrementally without a big-bang rewrite of all 10 subclasses at once?',
    ],
    topic: 'java',
    subtopic: 'design-patterns',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['strategy', 'over-engineering', 'refactoring', 'judgment'],
  },
  // ── Java Internals — Advanced Questions ────────────────────
  {
    id: 'java-adv-q3',
    question: "A code review flags `Integer a = 127; Integer b = 127; assert a == b;` as fine, but the same pattern with 200 as broken. Why does changing only the value change correctness, and what's the right fix?",
    shortAnswer: 'Autoboxing routes through Integer.valueOf(), which returns a shared cached instance only for -128..127 — inside that range == happens to compare the same cached object, outside it each box is a distinct allocation, so == silently starts comparing identity of two different objects. The fix is to never use == on boxed types; use .equals() (or unbox to primitive int before comparing) regardless of the value range.',
    detailedAnswer: "The Integer cache exists purely as a memory/allocation optimization for the very common small-integer case, not as a language guarantee about boxed equality — the JLS deliberately leaves caching outside that range unspecified, and code that relies on == working must not depend on any particular range. The bug class this produces is nasty because it passes tests and code review with small sample data (loop counters, small IDs) and fails only once real data exceeds 127, often in production, often intermittently depending on which specific values happen to collide with cache boundaries. The durable fix isn't 'know the cache range' — it's discipline: boxed types are compared with equals(), primitives are compared with ==, and any code mixing the two idioms is a latent bug regardless of whether it currently passes.",
    keyPoints: [
      'Integer.valueOf() caches -128..127 by default; identical code just outside that range allocates fresh objects',
      'The cache range is an implementation optimization, not a JLS guarantee — code should never depend on it',
      'Correct fix: always use .equals() for boxed-type comparison, never ==',
    ],
    seniorFollowUps: [
      'Would this bug reproduce the same way with Long or Short? What about Boolean?',
      'How would you write a static-analysis rule (or use an existing linter) to catch == on boxed types before it ships?',
    ],
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['autoboxing', 'integer-cache', 'gotcha'],
  },
  {
    id: 'java-adv-q4',
    question: 'How does the JVM execute a switch statement on a String, given that the bytecode instruction set has no notion of String at all?',
    shortAnswer: "javac desugars it into two nested int switches: the first switches on the String's hashCode() to jump toward a candidate case, and the second confirms the match with equals() before running that case's code — this makes hash collisions between case labels safe (they fall through correctly) even though the JVM never natively understood switching on a String.",
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['bytecode', 'string-switch', 'desugaring'],
  },
  {
    id: 'java-adv-q5',
    question: 'How does a lambda expression actually get executed by the JVM, and how is that different from how an anonymous inner class runs?',
    shortAnswer: 'A lambda compiles to an invokedynamic call site whose bootstrap method is LambdaMetafactory; on the first call, the metafactory generates the implementing class at runtime and links the call site to it directly, so later calls run at full speed with no extra indirection. An anonymous inner class, by contrast, is a fully-generated .class file baked in at compile time — no runtime code generation, no invokedynamic, and a real named nested class you can find on disk.',
    detailedAnswer: "This distinction matters beyond trivia: invokedynamic call sites can be re-linked (which lambdas don't exploit but other invokedynamic users, like dynamic languages on the JVM, do), the generated lambda class isn't a discoverable compiled artifact (nothing to find via javap on the enclosing class beyond the bootstrap method reference), and the first invocation of any given lambda expression pays a one-time bootstrap cost that a compile-time-generated anonymous class never pays. It also explains why decompilers/IDE 'go to implementation' sometimes struggle with lambdas in ways they never did with anonymous classes — there's genuinely nothing statically compiled to point to.",
    keyPoints: [
      'Lambdas use invokedynamic + LambdaMetafactory to generate the implementing class lazily at first invocation',
      'Anonymous classes are compiled directly to a named nested .class file at compile time, with no runtime generation step',
      'Method references (Foo::bar) go through the identical invokedynamic path as lambda expressions',
    ],
    seniorFollowUps: [
      'Why might a startup-latency-sensitive application (e.g. a CLI tool or Lambda/serverless function) care about this distinction?',
      'Is invokedynamic specific to lambdas, or was it introduced in the JVM for something else entirely?',
    ],
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['invokedynamic', 'lambdas', 'bytecode'],
  },
  {
    id: 'java-adv-q6',
    question: "A teammate refactors a logging call from a fixed-arity overload to a varargs one for flexibility. What's the hidden runtime cost, and when would you push back on that change for a hot path?",
    shortAnswer: "Every call site that doesn't already pass an array now allocates a fresh array (and boxes any primitive arguments) on each invocation — invisible in the source, real in a profiler. On a genuinely hot path (tight loop, high-QPS request handler) this is worth pushing back on in favor of a small set of fixed-arity overloads, even though it's less flexible and slightly more code.",
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['varargs', 'allocation', 'performance'],
  },
  {
    id: 'java-adv-q7',
    question: 'Why do getters and other tiny methods generally not cost anything at runtime, but the same reasoning breaks down once an interface has many implementations at one call site?',
    shortAnswer: 'The JIT inlines small, hot methods directly into their call site, eliminating call overhead entirely and enabling further optimizations like escape analysis. But inlining a virtual/interface call relies on inline caching, which only works well when a call site sees one or two actual implementations in practice — once it sees three or more (megamorphic), the JIT can no longer confidently inline, and you pay real virtual-dispatch and lost-optimization cost.',
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['inlining', 'polymorphism', 'jit'],
  },
  {
    id: 'java-adv-q8',
    question: 'Why do JMH-style microbenchmarks insist on a warmup phase before measuring, rather than just timing the first run?',
    shortAnswer: "The first runs execute in the interpreter or early C1 tiers, which are meaningfully slower than the steady-state C2-compiled code the method eventually reaches once HotSpot's invocation counters mark it hot enough to promote — timing cold runs measures interpreter/compilation overhead, not the algorithm's real steady-state performance.",
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['jit', 'benchmarking', 'tiered-compilation'],
  },
  {
    id: 'java-adv-q9',
    question: "What's the practical value of running javap -c on a piece of code during a performance investigation, versus just trusting the source?",
    shortAnswer: "Bytecode shows what the compiler actually produced — whether a string concatenation became a StringBuilder chain or an indy-based call, whether a constant expression got folded, whether autoboxing was inserted — none of which is visible from source alone. It's the fastest way to confirm or refute an optimization theory without guessing from observed behavior.",
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['bytecode', 'javap', 'debugging'],
  },
  {
    id: 'java-adv-q10',
    question: 'Does passing an int[] array to a method with signature process(Object... args) spread the array elements as individual varargs arguments?',
    shortAnswer: 'No — int[] is not an Object[], so it gets passed as a single element wrapped inside a new Object[1] array, not spread. Only an Object[] (or subtype array) passed to Object... args is used directly as the backing array without spreading semantics changing.',
    topic: 'java',
    subtopic: 'advanced-internals',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['varargs', 'gotcha'],
  },
  // ── Modern Java Concurrency Updates Questions ────────────────────
  {
    id: 'java-modern-q10',
    question: 'A team upgrades a virtual-thread service from JDK 21 to JDK 24 and deletes every ReentrantLock migration that had replaced synchronized. Is that automatically safe and complete?',
    shortAnswer: 'JDK 24 removes the common case where blocking while holding a monitor pins a virtual thread, so synchronized is no longer an automatic scalability defect. But reverting mechanically is unsafe: lock semantics, contention, fairness, interruptible acquisition, conditions, native-call pinning, and measured throughput still matter. Re-test under representative load and use JFR evidence rather than applying either rule universally.',
    detailedAnswer: 'JEP 491 changes the runtime scheduling behavior, not the synchronization contract. Code that moved to ReentrantLock may rely on tryLock, timed or interruptible acquisition, fairness, or Condition objects, so replacing it with synchronized can change behavior. Conversely, retaining ReentrantLock solely to avoid JDK 21 monitor pinning may now add complexity without benefit. Native or foreign calls can still pin, and lock contention can serialize work even when carriers are freed. The correct upgrade review separates semantic reasons for the lock choice from the old runtime workaround, then validates carrier utilization, pinning events, downstream saturation, and latency under load.',
    keyPoints: [
      'JDK 24 changes monitor pinning, not lock semantics',
      'Native or foreign calls can still pin',
      'Contention still serializes work without pinning',
      'Preserve ReentrantLock where its richer API is required',
    ],
    seniorFollowUps: [
      'Which JFR signals would you compare before and after the upgrade?',
      'How would you test a library that performs blocking native calls inside synchronized code?',
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['virtual-threads', 'jdk-24', 'pinning', 'synchronized', 'jfr'],
  },
  {
    id: 'java-modern-q11',
    question: 'An API request starts three virtual-thread subtasks. One fails quickly, one ignores interruption while blocked in a vendor client, and the handler times out. What does structured concurrency solve, and what does it not solve?',
    shortAnswer: 'It gives the subtasks one owner and a bounded lifetime: failure or timeout can trigger sibling cancellation, joining, and consolidated error handling before the scope closes. It cannot forcibly make non-cooperative code stop; a vendor call that ignores interruption may delay scope closure or continue tying up its underlying resource, so client timeouts and cancellation-aware APIs remain necessary.',
    detailedAnswer: 'The structured scope makes ownership explicit: forked work is joined within the lexical scope, policies can shut down siblings after success or failure, and the parent cannot casually return while forgetting its children. This fixes the bookkeeping and orphan-task hazards of unrelated Futures. Cancellation is still a request, normally delivered through interruption, not process-level preemption. Every blocking boundary needs a real deadline, and drivers or clients must release sockets and other resources when cancelled. Also, in JDK 25 StructuredTaskScope remains a preview API under JEP 505, so enabling it is an explicit deployment and compatibility choice rather than an invisible library implementation detail.',
    keyPoints: [
      'Structured concurrency supplies ownership, joining, cancellation policy, and error aggregation',
      'Cancellation remains cooperative',
      'Resource and network deadlines are still mandatory',
      'StructuredTaskScope is preview in JDK 25',
    ],
    seniorFollowUps: [
      'Would you retry the failed child inside or outside the scope?',
      'How would you propagate a request deadline and trace context to every child?',
    ],
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['structured-concurrency', 'virtual-threads', 'cancellation', 'timeouts', 'jdk-25'],
  },
  {
    id: 'java-modern-q12',
    question: 'A request ID is currently copied into a ThreadLocal before every virtual-thread task and removed in finally. Why might ScopedValue be a better fit on JDK 25?',
    shortAnswer: "A request ID is immutable context with a lexical lifetime, exactly ScopedValue's model. Bind it around the request operation and callees can read it without mutation or manual remove logic; structured child tasks can inherit the binding under the API's rules. This reduces stale-context and per-thread-copying mistakes, but it is not a replacement for mutable per-thread caches.",
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['scoped-values', 'threadlocal', 'context-propagation', 'jdk-25'],
  },
  {
    id: 'java-modern-q13',
    question: 'Production shows 300,000 live virtual threads and rising request latency. Why is the thread count alone insufficient, and what would you inspect next?',
    shortAnswer: 'Virtual threads are deliberately cheap, so a high count can simply represent concurrent blocked requests. Inspect their ages and states, carrier utilization, JFR virtual-thread and pinning events, request deadlines, downstream connection-pool or semaphore saturation, and whether tasks outlive their request. The bottleneck is usually a scarce dependency, pinning/native boundary, CPU saturation, or missing cancellation—not the count itself.',
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['virtual-threads', 'observability', 'jfr', 'production'],
  },
  {
    id: 'java-modern-q14',
    question: 'Should a library expose StructuredTaskScope directly in its stable public API when targeting JDK 25?',
    shortAnswer: 'Usually not without an explicit preview-API policy. StructuredTaskScope is still preview in JDK 25, so consumers must compile and run with preview enabled and may face source or binary changes in later JDKs. Keep it behind an internal abstraction or offer a stable domain-level API unless callers intentionally accept that coupling.',
    topic: 'java',
    subtopic: 'modern-java',
    difficulty: 'Intermediate',
    seniority: 'Lead',
    tags: ['structured-concurrency', 'preview', 'api-design', 'jdk-25'],
  },
  // ── Production GC Diagnostics Questions ────────────────────
  {
    id: 'java-gc-q13',
    question: 'A ZGC service reports sub-millisecond stop-the-world pauses, yet p99 latency periodically jumps to seconds while allocation rate spikes. How can both observations be true?',
    shortAnswer: 'Short STW phases do not guarantee the application never waits for memory. If concurrent GC cannot reclaim or relocate fast enough, allocating threads can stall even though recorded pauses remain tiny. Correlate allocation-stall events, concurrent-cycle duration, allocation rate, live set, free headroom, and GC CPU with the latency spike before changing pause targets.',
    detailedAnswer: 'Concurrent collectors move most work out of stop-the-world phases, but that work still consumes CPU and must stay ahead of the mutators. A burst can exhaust available allocation headroom before the concurrent cycle finishes, forcing application threads to wait for progress. CPU throttling in a container, a larger live set, undersized heap headroom, or a sudden allocation-rate increase can all produce this symptom. Investigate with unified GC logs and JFR aligned to request latency and container CPU throttling. Possible remedies differ: reduce allocation, restore CPU headroom, increase safe memory headroom, or revisit collector and workload configuration. Merely celebrating the maximum pause metric misses the actual latency mechanism.',
    keyPoints: [
      'Concurrent does not mean free',
      'Allocation stalls are distinct from STW pause duration',
      'GC must keep pace with mutator allocation',
      'Time-correlate GC, workload, CPU, and latency evidence',
    ],
    seniorFollowUps: [
      'How would container CPU limits change your diagnosis?',
      'Which mitigation would you test first if the live set is stable but allocation rate doubled?',
    ],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['zgc', 'allocation-stall', 'latency', 'jfr', 'production'],
  },
  {
    id: 'java-gc-q14',
    question: 'A pod is OOM-killed at its 4 GB memory limit, but the heap graph falls back to 1.8 GB after every GC and -Xmx is 2 GB. Build the next diagnostic steps.',
    shortAnswer: 'Treat it as whole-process memory pressure, not proof of a heap leak. Account for metaspace, direct buffers, thread stacks, code cache, GC structures, agents, JNI allocations, and mapping or allocator overhead. Check thread and classloader trends, direct-memory usage, OS/container RSS, and Native Memory Tracking if it was enabled; do not increase -Xmx because that reduces native headroom.',
    detailedAnswer: 'First align the container kill timestamp with RSS, heap committed/used, thread count, class count, direct-buffer metrics, and deployment changes. A stable post-GC heap means ordinary object reachability is unlikely to explain the missing memory. Use jcmd VM.native_memory summary or baseline/diff when NMT was enabled, remembering it covers JVM-tracked categories rather than every third-party native allocation. Estimate thread-stack reservation from thread count and -Xss, inspect direct ByteBuffer or Netty allocator metrics, check classloader/metaspace growth, and review profilers or agents. Leave operational room for the JVM outside -Xmx; the cgroup limit governs total RSS and related charged memory, not only heap occupancy.',
    keyPoints: [
      'Container limit covers the whole process',
      'Flat post-GC heap points toward non-heap/native categories',
      'NMT must be enabled and is not omniscient',
      'Raising -Xmx can make the kill happen sooner',
    ],
    seniorFollowUps: [
      'How would you investigate a suspected JNI leak that NMT does not attribute?',
      'What trade-off does lowering -Xss introduce?',
    ],
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['native-memory', 'containers', 'oom', 'nmt', 'diagnostics'],
  },
  {
    id: 'java-gc-q15',
    question: 'During an incident, an engineer proposes doubling the heap and changing from G1 to ZGC in the same restart. Why is that a poor experiment?',
    shortAnswer: 'It changes two major variables and destroys attribution: improvement or regression could come from heap headroom, collector behavior, or their interaction. Preserve logs and JFR, establish the live set and allocation profile, define the SLO being fixed, then change one controlled variable under comparable load with an explicit rollback condition.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['gc-tuning', 'experiments', 'g1', 'zgc', 'incident-response'],
  },
  {
    id: 'java-gc-q16',
    question: 'A batch job has a 12 GB heap, finishes 8% faster with Parallel GC than G1, and its longest pause is four seconds. Is Parallel GC the wrong choice?',
    shortAnswer: 'Not if the job has no interactive latency SLO and the pause does not violate a deadline or external lease. Parallel GC optimizes throughput, so total completion time can be the deciding metric. Verify that pauses do not trigger timeouts, health-check failures, coordination lease loss, or unacceptable operational behavior before accepting the trade.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['parallel-gc', 'throughput', 'slo', 'collector-selection'],
  },
  {
    id: 'java-gc-q17',
    question: "Why must a performance report say both 'JDK 21 ZGC' or 'JDK 23 ZGC' rather than simply 'ZGC'?",
    shortAnswer: 'Generational ZGC was opt-in in JDK 21 and became the default ZGC mode in JDK 23, while collector implementations also evolve between releases. Without the exact JDK and mode, differences in throughput, CPU, and allocation behavior cannot be reproduced or attributed reliably.',
    topic: 'java',
    subtopic: 'gc',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['zgc', 'generational-zgc', 'jdk-versions', 'benchmarking'],
  },
  // ── Production Performance Diagnostics Questions ────────────────────
  {
    id: 'java-perf-q17',
    question: 'During an incident, request p99 rises from 200 ms to 8 seconds while process CPU falls from 70% to 15%. An async-profiler CPU flame graph has no obvious wide application leaf. What do you collect next, and why?',
    shortAnswer: 'Collect a wall-clock profile scoped to request workers, plus thread-state and downstream latency data. CPU sampling excludes time parked on sockets, pool checkout, locks, or scheduling, so falling CPU with rising latency strongly suggests waiting rather than expensive computation; the quiet CPU graph is consistent with the incident, not evidence that the JVM is healthy.',
    detailedAnswer: 'Start by preserving the incident window, then record wall-clock stacks so blocked and sleeping time is represented. Split stacks by state and thread group: many workers parked in a connection-pool acquire path points to pool starvation; socket reads with the same downstream client point to dependency latency; BLOCKED stacks converging on one monitor point to contention. Correlate that with pool checkout time, dependency spans, queue depth, and timeout counts. A CPU profile would become useful again only if the wall-clock evidence shows workers runnable and competing for cores.',
    keyPoints: [
      'CPU profiles answer where on-core execution time went, not where elapsed request time went',
      'Falling CPU plus rising latency is a waiting or throttling signal',
      'Scope wall-clock samples to relevant workers so idle threads do not dominate',
    ],
    seniorFollowUps: [
      'How would you distinguish downstream socket wait from connection-pool checkout wait in the stacks?',
      'What changes if cgroup throttling metrics are high even though reported process CPU looks modest?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['async-profiler', 'wall-clock', 'incident', 'latency'],
  },
  {
    id: 'java-perf-q18',
    question: 'A JFR opened in JMC shows no monitor-blocked events during a latency spike, so an engineer concludes there was no lock contention. What must you verify before accepting that conclusion?',
    shortAnswer: "Verify the recording settings: whether the event was enabled, whether stack traces were captured, and what duration threshold filtered events. JFR's low-overhead profile can intentionally omit or threshold short events, so absence in the file may mean absence of instrumentation. If contention remains plausible, run a short targeted recording with an appropriate threshold and correlate it with thread parks and request latency.",
    detailedAnswer: 'Treat a JFR file as the output of a configured measurement, not a complete transcript of the JVM. Inspect its event settings in JMC or the source JFC profile. A threshold of 20 ms, for example, hides thousands of shorter acquisitions that can still consume meaningful aggregate time or form a convoy. Also distinguish Java monitor enter events from waits implemented with LockSupport or native synchronization. Narrow the hypothesis first, then enable the relevant events and stack traces for a bounded interval rather than switching on every expensive event fleet-wide.',
    keyPoints: [
      'No recorded event is not proof of no behavior until capture settings are checked',
      'Thresholds can hide frequent short stalls',
      'Use a bounded targeted recording after an always-on low-overhead recording narrows the window',
    ],
    seniorFollowUps: [
      'How would you choose a threshold without making the recording too noisy?',
      'Which evidence would reveal contention implemented with ReentrantLock rather than synchronized?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['jfr', 'jmc', 'contention', 'profiling'],
  },
  {
    id: 'java-perf-q19',
    question: 'After a release, GC frequency doubles. An allocation flame graph points to JSON deserialization, while a heap dump is dominated by a large application cache. Which finding explains the regression, and what should you do before changing code?',
    shortAnswer: 'They answer different questions: the allocation profile can explain increased GC churn, while the dominator tree explains retained live memory. Compare allocation rate and stacks before versus after the release, and compare post-GC live-set size. Fix deserialization only if its byte allocation increased with the regression; resize or fix the cache only if retained live set grew or constrained headroom. Do not assume the largest allocator is the leak.',
    detailedAnswer: "Establish two timelines. If bytes allocated per second doubled but the post-GC floor stayed stable, the regression is transient churn and the allocation profile is the relevant evidence; investigate payload shape, parser configuration, and temporary buffers. If the post-GC floor grew while allocation rate stayed comparable, retention is the issue and the cache's keys, bounds, and eviction policy deserve attention. Both can coexist, but each needs its own causal comparison to the pre-release baseline. A heap histogram by count alone is insufficient; use retained size and GC roots for the cache.",
    keyPoints: [
      'Allocation rate predicts collection work; retained size explains the live set',
      'Compare against a pre-change baseline under equivalent traffic',
      'Post-GC floor separates transient churn from accumulating retention',
    ],
    seniorFollowUps: [
      'How could TLAB allocation sampling undercount a rare large allocation path?',
      'What guardrails would you add before changing cache size in production?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['allocation', 'heap-dump', 'gc', 'diagnosis'],
  },
  {
    id: 'java-perf-q20',
    question: 'A load test reports p99 below 300 ms at 2,000 requests per second, but production sees multi-second queues at the same average rate during brief stalls. The test client sends the next request only after the previous response. What is wrong with the result?',
    shortAnswer: 'The client introduces coordinated omission: when the server stalls, it also stops offering requests, so it fails to measure the requests that would have arrived and queued during that stall. Use an open or constant-arrival-rate test, report offered load separately from completed throughput, and include queue depth, rejections, and corrected latency histograms.',
    detailedAnswer: 'A closed loop can be useful for modeling a single user who waits before doing the next action, but it cannot establish service capacity under independent arrivals. During a five-second pause, production may receive ten thousand requests; the test may record only one five-second response and generate nothing else until it returns. That suppresses both offered load and the tail. Rerun with arrival scheduling independent of responses, enough client capacity to sustain the schedule, realistic bursts and payload skew, and a hard overload limit so the test exposes rather than hides queue growth.',
    keyPoints: [
      'Closed-loop waiting removes samples exactly when the server is slow',
      'Offered load and achieved throughput are different measurements',
      'A valid capacity test must expose queueing and rejection behavior',
    ],
    seniorFollowUps: [
      'When would a closed-loop workload still be the correct user model?',
      'How do you prevent the load generator itself from becoming the bottleneck?',
    ],
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['load-testing', 'coordinated-omission', 'latency', 'capacity'],
  },
  {
    id: 'java-perf-q21',
    question: 'A flame graph has a very wide Spring dispatcher frame at the bottom and several narrower application leaves above it. Which frame should you optimize first?',
    shortAnswer: "None based on width alone. A bottom frame is often wide because it is the common ancestor of nearly every sampled request, not because its own code consumed the time. Inspect the widest relevant leaf stacks and their callers, use JMC's hot-method or self-time view where appropriate, and compare an equivalent baseline before choosing a target.",
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['flame-graph', 'interpretation', 'profiling'],
  },
  {
    id: 'java-perf-q22',
    question: 'JVM logs show a 900 ms stop-the-world interval, but the GC phase inside it took only 40 ms. Why is changing collectors a premature fix?',
    shortAnswer: 'Most of the interval may be time-to-safepoint: the JVM requested a safepoint but waited for one or more threads to become safe before the 40 ms operation could begin. Correlate JFR safepoint events or unified safepoint logs to separate synchronization time from operation time and identify the lagging condition before tuning GC.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['safepoint', 'gc', 'latency', 'diagnosis'],
  },
  {
    id: 'java-perf-q23',
    question: 'A team proposes doubling a database connection pool because threads wait for connections. What evidence and guardrails do you require before approving the change?',
    shortAnswer: 'Prove whether the wait comes from genuine concurrency, slow queries, or leaked/overlong transactions; doubling a leak only delays failure and doubling legitimate concurrency may overload the database. Check checkout time, active/idle count, hold-time distribution, DB saturation and connection limits, then canary one change with rollback thresholds for DB CPU, lock waits, errors, and service p99.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['tuning', 'connection-pool', 'canary', 'guardrails'],
  },
  {
    id: 'java-perf-q24',
    question: 'When would you choose an async-profiler allocation recording over a CPU recording for a service whose main symptom is frequent young collections?',
    shortAnswer: 'Choose allocation profiling when the hypothesis is excessive byte churn: it attributes sampled allocated bytes to creation stacks, which CPU samples may not highlight because TLAB allocation itself is cheap. Correlate the result with allocation rate and GC frequency; switch to heap-retention analysis if the post-GC live set, rather than transient allocation rate, is growing.',
    topic: 'java',
    subtopic: 'performance',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['async-profiler', 'allocation', 'gc', 'tlab'],
  },
  // ── Container and Incident Operations Questions ────────────────────
  {
    id: 'java-prod-q12',
    question: 'A JVM pod has a 1-CPU limit. During traffic bursts, p99 jumps, runnable threads accumulate, but node CPU is only 45% and no lock dominates thread dumps. What container signal do you check, and how can the JVM amplify the problem?',
    shortAnswer: 'Check cgroup CPU throttled periods and throttled time. The pod can exhaust its one-CPU quota early in each enforcement period and be paused even while the node has idle cores. If the JVM believes more processors are available, it may size GC, JIT, ForkJoin, or other pools too aggressively, creating extra runnable work competing for the same quota; verify ActiveProcessorCount and container support.',
    detailedAnswer: 'Host utilization is the wrong denominator for a quota-bound process. Correlate request spikes with container_cpu_cfs_throttled_periods_total or the equivalent cgroup v2 throttling counters, run-queue pressure, and GC/JIT activity. Confirm the JDK sees the intended processor count using JVM flags or runtime metrics. The safe fix may be removing or raising the hard limit, explicitly correcting ActiveProcessorCount for older/misconfigured runtimes, and right-sizing application parallelism; simply adding request threads increases queueing and context switching under the same quota.',
    keyPoints: [
      'A cgroup can throttle on an otherwise idle host',
      'Runnable stacks without one hot lock can be quota starvation',
      'JVM and application pool ergonomics must match the effective CPU budget',
    ],
    seniorFollowUps: [
      'What is the operational tradeoff between setting only a CPU request and also setting a limit?',
      'How would you canary a limit change without risking noisy-neighbor impact?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cgroups', 'cpu-throttling', 'containers', 'incident'],
  },
  {
    id: 'java-prod-q13',
    question: "A container's RSS grows by 400 MB per day while the post-GC Java heap stays flat. Heap dumps look normal. How do you narrow the source without guessing?",
    shortAnswer: 'Treat it as native or unmapped process growth: compare Native Memory Tracking baseline diffs for threads, class metadata, code, GC, and internal categories; correlate thread count and direct-buffer metrics; inspect process mappings and allocator/JNI telemetry for the RSS not explained by NMT. NMT must have been enabled at startup and does not cover every third-party native allocation.',
    detailedAnswer: 'First reconcile measurements taken at the same time: heap committed/used, NMT committed categories, direct buffer pools, thread count multiplied by stack reservation, and process RSS. NMT growth in Thread suggests thread proliferation; Class suggests classloader or generated-class growth; Internal or Arena can narrow a JVM subsystem. If RSS keeps rising while NMT totals do not, investigate JNI/native libraries or allocator fragmentation with OS-native tools and library-specific metrics. Do not raise Xmx: heap is stable and a larger heap reduces remaining cgroup headroom.',
    keyPoints: [
      'Flat post-GC heap rules against an ordinary Java object-retention leak',
      'NMT category diffs are more useful than one absolute snapshot',
      'The RSS-minus-accounted-memory gap can point to JNI or allocator behavior outside NMT',
    ],
    seniorFollowUps: [
      'How would unbounded thread creation appear in both NMT and cgroup memory?',
      'Why can reserved thread-stack memory differ materially from resident memory?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['native-memory', 'nmt', 'rss', 'diagnostics'],
  },
  {
    id: 'java-prod-q14',
    question: 'An alert fires for a rapidly degrading pod. Restarting it is known to restore service, but this is the third recurrence and prior restarts left no evidence. What should the runbook do before restart?',
    shortAnswer: 'If customer impact and remaining capacity permit, drain or isolate the instance and capture a time-bounded incident bundle before restart: exact timestamps and identity, pod events and limits, metrics/traces for the window, several thread dumps, a dump of the continuous JFR ring buffer, and relevant pool/cgroup state. Then restart as mitigation. If impact is critical, mitigate first and rely on automated capture rather than delaying recovery.',
    detailedAnswer: 'The runbook must separate mitigation priority from evidence preservation. Automation should trigger low-risk captures immediately when the symptom appears, because humans arriving later may only see a recovered process. Avoid heavyweight heap dumps unless memory evidence specifically warrants them and the instance is drained. Persist artifacts outside ephemeral container storage, attach deployment/config versions, and use a shared clock/time range for correlation. The goal is not to collect everything; it is to retain enough volatile state to test the leading hypotheses without extending the outage.',
    keyPoints: [
      'Restart is recovery, not root-cause analysis',
      'Bounded automated capture avoids forcing a choice between users and evidence',
      'Artifacts must survive pod deletion and carry instance, version, and timestamp metadata',
    ],
    seniorFollowUps: [
      'Which captures would you omit when disk latency is already the suspected bottleneck?',
      'How would you test this runbook safely before a real incident?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['incident-response', 'jfr', 'runbook', 'evidence'],
  },
  {
    id: 'java-prod-q15',
    question: 'A pod has a 2 GiB memory limit and Xmx is increased from 1.2 GiB to 1.8 GiB to reduce GC. It now disappears with exit code 137 during peak traffic. Explain the regression and propose a safer sizing method.',
    shortAnswer: 'The larger heap consumed headroom required by metaspace, code cache, direct buffers, thread stacks, GC structures, and other native allocations, so total cgroup memory crossed 2 GiB and the kernel killed the process. Size Xmx from the whole-process budget using measured peak non-heap/native use plus margin, validate under peak concurrency, and monitor RSS against the cgroup limit rather than heap alone.',
    detailedAnswer: 'A heap-only dashboard made the original change appear safe. Reconstruct peak total memory and attribute what you can using NMT, buffer-pool metrics, class/thread counts, and RSS. Account for bursty direct buffers and thread growth, not only steady-state averages. Choose an Xmx or MaxRAMPercentage that leaves explicit headroom plus uncertainty, then load-test and canary it. Reducing allocation or concurrency may be safer than growing heap; raising the container limit is valid only if node capacity and scheduling economics support it.',
    keyPoints: [
      'The cgroup kills on total process memory, not Java heap usage',
      'Increasing heap can trade GC frequency for fatal native-memory pressure',
      'Use measured peaks and margin, then verify under realistic concurrency',
    ],
    seniorFollowUps: [
      'Why might percentage-based heap sizing still be unsafe across two services with the same limit?',
      'Would enabling HeapDumpOnOutOfMemoryError help with this particular failure?',
    ],
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['container-memory', 'xmx', 'oom-killer', 'capacity'],
  },
  {
    id: 'java-prod-q16',
    question: 'Shortly after an error storm begins, the metrics backend becomes slow and starts dropping samples. A new metric labels every exception by message and request path. What happened, and how do you redesign it?',
    shortAnswer: 'Unbounded label values caused a cardinality explosion: each distinct message and raw path created another time series, with failures generating novelty fastest. Replace them with bounded route templates and controlled error categories; put detailed messages, customer IDs, and trace IDs in sampled traces or structured logs, and alert on active-series growth and dropped telemetry.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['metrics', 'cardinality', 'observability', 'incident'],
  },
  {
    id: 'java-prod-q17',
    question: 'Why is an alert that pages whenever JVM CPU exceeds 80% often worse than an SLO burn-rate alert?',
    shortAnswer: 'CPU is a cause candidate, not a user outcome: a healthy CPU-bound service can run above 80%, while a service waiting on a failed dependency can violate availability at low CPU. Burn-rate alerts page when the allowed error or latency budget is being consumed quickly, and multi-window rules distinguish a brief severe outage from persistent slow degradation; CPU remains useful diagnostic context and a capacity warning.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['slo', 'burn-rate', 'alerting', 'observability'],
  },
  {
    id: 'java-prod-q18',
    question: 'You enabled a continuous JFR recording in every pod, but the recording is gone whenever a crash-looping pod is replaced. What operational pieces are missing?',
    shortAnswer: 'The ring buffer needs bounded age/size, a tested trigger to dump it on alert or termination, and export to durable storage outside the pod before deletion. Also record pod identity, deployment version, and timestamps so the file can be correlated with metrics and traces; merely enabling JFR does not make the artifact survive an ephemeral container lifecycle.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['jfr', 'kubernetes', 'incident', 'ephemeral-storage'],
  },
  {
    id: 'java-prod-q19',
    question: 'A native-memory report shows 8 GiB reserved for thread stacks but only a fraction committed, while the container has a 4 GiB limit. Is the pod already over its memory limit?',
    shortAnswer: 'Not from that number alone. Reserved virtual address space is an address range the process may use; committed or resident pages are what materially contribute to physical/cgroup memory accounting. Still investigate the thread count because stacks commit pages as threads use them, and excessive threads also add scheduling and native overhead even before every reserved stack page becomes resident.',
    topic: 'java',
    subtopic: 'production',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['nmt', 'thread-stacks', 'reserved-memory', 'containers'],
  },
  // ── Async Lifecycle & Isolation Questions ────────────────────
  {
    id: 'java-async-q17',
    question: 'An HTTP request times out, so its handler calls cancel(true) on a CompletableFuture created with supplyAsync. Production traces show the supplier still calling the downstream service afterward. Why, and what would make cancellation real?',
    shortAnswer: "CompletableFuture.cancel marks the future cancelled but does not use mayInterruptIfRunning to interrupt its executing supplier. Cancellation of the handle is therefore not cancellation of the work. Put a deadline on the HTTP operation itself and use the client's cancellation API, or run interrupt-cooperative work behind a Future/task abstraction whose cancellation is explicitly wired to the underlying operation.",
    detailedAnswer: 'CompletableFuture is a completion graph, not an ownership handle for a particular thread. Its cancel implementation is equivalent to exceptional completion with CancellationException, and mayInterruptIfRunning has no effect. Dependent stages observe failure, but an already-running supplier may continue and consume its connection and executor worker. Robust cancellation needs an end-to-end deadline propagated to the downstream client plus a concrete abort path: closing/cancelling the request, interrupt-aware blocking where supported, or an explicit cancellation token checked by computation. The application should also make late results harmless because cancellation always races completion.',
    keyPoints: [
      'CompletableFuture cancellation changes graph state, not necessarily execution state',
      'mayInterruptIfRunning is not honored by CompletableFuture',
      'Propagate deadlines and wire cancellation to the resource-owning operation',
    ],
    seniorFollowUps: [
      'How would you make cleanup idempotent when completion and cancellation race?',
      'Does cancelling a dependent stage cancel its parent stage?',
    ],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['completablefuture', 'cancellation', 'deadlines', 'resource-lifecycle'],
  },
  {
    id: 'java-async-q18',
    question: 'A team adds orTimeout(200, MILLISECONDS) around a slow database future and retries on TimeoutException. Database load doubles and the executor remains saturated. Explain the failure and redesign it.',
    shortAnswer: 'orTimeout stops the caller waiting after 200 ms, but it does not necessarily stop the query. Each retry can overlap the still-running timed-out query, multiplying work. Enforce a database/driver statement timeout below the request deadline, bound concurrency and retries, and propagate one overall deadline rather than granting every attempt a fresh 200 ms budget.',
    detailedAnswer: "The timeout decorates completion semantics: the future becomes exceptional when the timer wins, while the JDBC call may remain blocked on its worker and connection. Immediate retry creates retry amplification because abandoned attempts and replacements coexist. Layer deadlines from the outside in: the request has an absolute deadline; queueing and each attempt consume that remaining budget; the driver or database has a statement timeout that actually aborts work; concurrency is capped to the dependency's capacity; and retries require a budget, backoff, and idempotency. Observe both caller timeouts and outstanding downstream work because they can diverge.",
    keyPoints: [
      'Completion timeout and resource cancellation are different',
      'Retries can overlap abandoned work and amplify an outage',
      'Use an absolute deadline plus downstream-native timeout and a concurrency limit',
    ],
    seniorFollowUps: [
      'Where should connect timeout, pool-acquisition timeout, and statement timeout sit inside the overall deadline?',
    ],
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['completablefuture', 'timeout', 'retry-amplification', 'database'],
  },
  {
    id: 'java-async-q19',
    question: 'Payments and recommendations share one 100-thread executor. A recommendations outage makes payment latency spike even though the payment dependency is healthy. What architectural property is missing?',
    shortAnswer: 'Workload isolation. Blocking recommendation calls occupy the shared workers and queue, starving payment tasks before they can reach their healthy dependency. Give each dependency a bounded executor or explicit concurrency limiter sized to its budget, with independent queueing, rejection, timeout, and saturation metrics.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['executor', 'bulkhead', 'starvation', 'isolation'],
  },
  {
    id: 'java-async-q20',
    question: 'All eight workers in a fixed pool are blocked in Future.get, the queue contains eight tasks, CPU is idle, and JVM deadlock detection reports none. What pattern should you suspect?',
    shortAnswer: 'Thread-starvation deadlock: each worker submitted a child task to the same bounded pool and then waited for it, leaving no free worker to execute the queued children. Remove the blocking join through composition, compute child work inline, or place genuinely independent child work on a separately sized executor.',
    topic: 'java',
    subtopic: 'async',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['thread-pool', 'starvation', 'future', 'deadlock'],
  },
  // ── Concurrent Collection Correctness Questions ────────────────────
  {
    id: 'java-cc-q15',
    question: 'A ConcurrentHashMap stores remaining inventory. Two threads both run get(sku), verify the value is positive, then put(sku, value - 1). How can stock be oversold if every map operation is thread-safe?',
    shortAnswer: 'Thread safety applies to each get and put separately, not to the read-check-write sequence. Both threads can read the same value and both write the same decrement, losing an update or violating the inventory rule. Express the transition as one atomic compute operation, or use a lock/transaction if the invariant spans multiple keys or external state.',
    detailedAnswer: 'ConcurrentHashMap provides linearizable atomic operations, but two linearizable calls do not fuse into one transaction. The decision made between get and put is based on state that can become stale immediately. compute can serialize the remapping for that key and reject a decrement at zero, provided the function stays short and side-effect-free. If reserving stock must also create an order, debit another key, or update a database, a map-level atomic method is no longer enough; ownership, a higher-level lock, or a transactional store must cover the complete invariant.',
    keyPoints: [
      'Per-operation thread safety does not make compound logic atomic',
      'Use compute/merge/putIfAbsent for one-key transitions',
      'Cross-resource invariants need a broader coordination boundary',
    ],
    seniorFollowUps: [
      'Why is performing a remote call inside compute a bad fix?',
      'How would the design change if inventory is persisted in a database?',
    ],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['concurrenthashmap', 'linearizability', 'compound-action', 'inventory'],
  },
  {
    id: 'java-cc-q16',
    question: 'A custom lock-free stack reads head=A, pauses, and later successfully CASes head from A to A.next. Meanwhile another thread removed A, changed the stack, then reused A as the head. Why can the successful CAS still corrupt the stack?',
    shortAnswer: 'The CAS proves only that head equals A now, not that it remained A continuously. The A-to-B-to-A sequence hides the intervening mutation, so A.next may describe a different logical stack than the first thread observed. Pair the reference with a version stamp, prevent node reuse, or use an algorithm whose marking/reclamation scheme detects the transition.',
    detailedAnswer: 'CAS compares a bit pattern or reference identity, so returning to the old value satisfies it. The paused thread then commits a decision derived from an obsolete topology and can unlink live nodes. AtomicStampedReference makes the expected state (reference A, version n); after remove/reinsert the reference may again be A but the version differs, causing the CAS to fail and retry. Garbage collection removes many manual-memory reclamation hazards, yet ABA remains possible when a reference is deliberately reused or a scalar state cycles back to its former value.',
    keyPoints: [
      'Same current value does not imply no intervening change',
      'Versioned or marked references extend what CAS validates',
      'GC helps memory safety but is not a universal ABA solution',
    ],
    seniorFollowUps: ['What tradeoff does AtomicStampedReference introduce compared with AtomicReference?'],
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cas', 'aba', 'lock-free', 'atomicstampedreference'],
  },
  {
    id: 'java-cc-q17',
    question: 'A metrics endpoint records millions of increments per second into ConcurrentHashMap<String, AtomicLong>, and one hot key becomes a scalability bottleneck. What replacement helps, and what correctness tradeoff does it make?',
    shortAnswer: 'Use ConcurrentHashMap<String, LongAdder> with computeIfAbsent(...).increment(). LongAdder spreads hot updates across striped cells and sums them on read, reducing CAS contention. Its sum is not an atomic snapshot during concurrent updates, so it is ideal for telemetry but wrong for balances, quotas, or decisions requiring an exact instantaneous total.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['concurrenthashmap', 'longadder', 'contention', 'metrics'],
  },
  {
    id: 'java-cc-q18',
    question: 'A team replaces a locked queue with ConcurrentLinkedQueue expecting every producer call to have a bounded completion time. Is that guarantee provided?',
    shortAnswer: 'No. ConcurrentLinkedQueue is lock-free, so the system keeps making progress, but an individual producer can repeatedly lose CAS races and retry without a fixed bound. A wait-free algorithm would guarantee bounded per-operation progress; lock-free primarily removes lock-holder blocking and convoying, not all tail-latency risk.',
    topic: 'java',
    subtopic: 'concurrent-collections',
    difficulty: 'Advanced',
    seniority: 'Lead',
    tags: ['concurrentlinkedqueue', 'lock-free', 'wait-free', 'progress'],
  },
]
