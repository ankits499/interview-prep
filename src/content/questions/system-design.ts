import type { Question } from '../../types'

export const systemDesignQuestions: Question[] = [
  // ── Fundamentals Questions ────────────────────────────────────
{
    id: 'sd-fund-q1',
    question: 'You are designing a service currently on one large machine. Traffic has grown 20x and is expected to keep growing. Walk through how you decide between scaling vertically further vs. re-architecting for horizontal scaling.',
    shortAnswer: 'Check whether vertical scaling still has headroom (bigger instance available, cost acceptable) and whether growth is expected to plateau soon — if so, vertical buys time cheaply. If growth is open-ended or already near the largest instance size, horizontal scaling is unavoidable, which first requires making the service stateless (externalize sessions/state) before adding instances behind a load balancer.',
    detailedAnswer: 'Start by quantifying the runway: what is the largest available instance, what is current utilization, and what is the growth trajectory? If vertical scaling covers 12-24 months of growth at acceptable cost, it is usually the right near-term move — it avoids the engineering cost of distributed coordination. Once vertical scaling approaches its ceiling, or a single-machine failure becomes an unacceptable availability risk, plan the horizontal migration: identify all in-process state (sessions, caches, local files), externalize it to a shared store, add a load balancer, and validate that any instance can serve any request. This migration should happen before it is forced by an outage.',
    keyPoints: [
      'Vertical scaling is the pragmatic first move when there is a clear ceiling far enough away and no availability requirement forcing redundancy',
      'The real gating factor for horizontal scaling is statelessness, not just adding a load balancer',
      'A single large instance is also a SPOF risk independent of capacity — that alone can justify going horizontal early',
    ],
    seniorFollowUps: [
      'How would you migrate a stateful service to stateless with zero downtime?',
      'What signals in production would tell you that you are approaching the vertical scaling ceiling before an outage forces the issue?',
    ],
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['scaling', 'architecture', 'tradeoffs'],
  },
  {
    id: 'sd-fund-q2',
    question: 'A team says their new API has "99.99% availability" but keeps missing its SLA. What is likely misconfigured in how they defined their SLI/SLO/SLA, and how would you fix it?',
    shortAnswer: 'Likely the SLO (internal target) was set equal to or looser than the SLA (external commitment), leaving no error budget buffer — any normal operational blip immediately becomes an SLA breach. Fix: set the SLO stricter than the SLA (e.g. SLO 99.95% internal vs 99.9% SLA), and make sure the SLI actually measures what customers experience (e.g. end-to-end success rate, not just server-side 200s that miss client-side timeouts).',
    detailedAnswer: 'The SLI/SLO/SLA chain only works as an early-warning system if there is a gap between the SLO and the SLA. If they are equal, the team has zero error budget to absorb noise, deploys, or partial degradations before violating the customer-facing commitment — so they are always firefighting reactively instead of catching burn early. Separately, check whether the SLI itself is measured correctly: a common mistake is measuring availability at the load balancer or server (which shows healthy) while client-observed availability is worse due to network issues, client-side timeouts, or a dependency failure that returns a technically-valid but wrong response. Fixing this means tightening the internal SLO relative to the SLA, and auditing the SLI definition against what users actually experience, then alerting on SLO burn rate rather than waiting for an SLA violation.',
    keyPoints: [
      'SLO should be stricter than SLA to leave an error budget',
      'SLI must reflect actual user-observed experience, not just server-side success codes',
      'Alert on error-budget burn rate, not on the SLA threshold itself',
    ],
    seniorFollowUps: [
      'How would you design an error-budget policy that ties into deploy velocity (e.g. freezing releases when budget is exhausted)?',
    ],
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['availability', 'sla', 'slo', 'sli'],
  },
  {
    id: 'sd-fund-q3',
    question: 'In a design interview, the interviewer gives you just "design a URL shortener." What do you do in the first five minutes?',
    shortAnswer: 'Clarify functional scope (custom aliases? expiration? analytics?) and non-functional constraints (expected QPS, read/write ratio, latency target, availability needs) before proposing any architecture — a URL shortener at 100 QPS and one at 100k QPS are different systems.',
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['interview-framework', 'requirements'],
  },
  {
    id: 'sd-fund-q4',
    question: 'Your system is read-heavy (500:1 read/write) in steady state, but a marketing campaign causes a burst of new-record writes for a few hours a month. What breaks if you designed purely around the steady-state ratio, and how do you handle it?',
    shortAnswer: 'A design purely optimized for read-heavy traffic (heavy caching, few write-capacity guarantees) can see its write path saturate during the burst — the database write throughput or a hot partition becomes the bottleneck exactly when it matters most (the campaign). Handle it by decoupling the burst: buffer writes through a queue and process asynchronously, and provision write capacity (or partitioning) for the burst case, not just the average.',
    detailedAnswer: 'Steady-state read/write ratio should inform the primary architecture (caching, read replicas), but bursts need to be explicitly identified as a separate load profile during the estimation phase — this is exactly the kind of non-functional detail that separates a senior answer. If the burst writes are not immediately consistency-critical (e.g. campaign signups), put a queue in front of the write path so the burst is absorbed and drained at a sustainable rate instead of hitting the database directly. If writes must land immediately, plan write capacity and partitioning for burst-peak QPS, not average QPS, and load-test against that peak explicitly.',
    keyPoints: [
      'Average ratios can hide burst load profiles that need separate provisioning',
      'Async buffering (queue) is the standard lever when burst writes can tolerate a short delay',
      'Provisioning and load testing should target the burst peak, not steady-state averages',
    ],
    seniorFollowUps: [
      'How would you detect that the write path is approaching saturation before it causes visible failures?',
    ],
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['read-heavy', 'write-heavy', 'bottleneck', 'capacity-planning'],
  },
  {
    id: 'sd-fund-q5',
    question: 'A downstream service starts responding slowly. Upstream callers keep sending requests at the same rate, and eventually the upstream service runs out of memory holding pending requests. What concept was missing, and what would you add?',
    shortAnswer: 'Back-pressure was missing — the upstream had no mechanism to detect the downstream slowdown and throttle itself. Add a bounded queue with explicit rejection (or a circuit breaker) so the upstream fails fast or sheds load once the downstream falls behind, instead of buffering unboundedly.',
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['back-pressure', 'resilience', 'bottleneck'],
  },
  {
    id: 'sd-fund-q6',
    question: 'Why would a senior candidate deliberately choose asynchronous communication between two services even when the synchronous version is simpler to build and reason about?',
    shortAnswer: 'To break availability and latency coupling — a synchronous call means the caller\'s success depends on the callee being up and fast right now, while async decouples that so the caller can succeed even if the downstream work is delayed or the downstream service is briefly unavailable, at the cost of eventual consistency and needing idempotent processing.',
    detailedAnswer: 'Synchronous calls create a hard dependency: the caller\'s latency becomes at least the sum of every synchronous hop, and the caller\'s availability becomes the product of every hop\'s availability (a chain is only as reliable as its weakest, most numerous links). This is fine for genuinely required, low-latency operations, but it is a poor fit for anything that can be deferred — sending an email, updating an analytics count, generating a report. Choosing async there means the primary request path stays fast and resilient to that downstream\'s issues, at the cost of added complexity: the caller no longer knows synchronously whether the work succeeded, so consumers must be built idempotently to handle retries/redelivery, and the system now needs monitoring for queue lag as a new failure mode.',
    keyPoints: [
      'Sync calls compound latency and availability risk across every hop in the chain',
      'Async trades immediate confirmation for resilience and independent scaling of producer/consumer',
      'The cost is real: eventual consistency, idempotency requirements, and a new failure mode (queue lag/backlog)',
    ],
    seniorFollowUps: [
      'How do you decide, component by component, which calls in a request path must stay synchronous?',
      'How would you monitor and alert on a growing async backlog before it becomes user-visible?',
    ],
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['synchronous', 'asynchronous', 'coupling', 'resilience'],
  },
  {
    id: 'sd-fund-q7',
    question: 'You have redundant instances of a service running across three servers in the same data center rack. Is your single point of failure actually eliminated?',
    shortAnswer: 'No — the redundancy is correlated, not independent. The rack itself (power, top-of-rack switch) is now the SPOF; a rack-level failure takes down all three instances together. True redundancy requires spreading instances across independent failure domains (racks, availability zones, or regions).',
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['redundancy', 'spof', 'availability'],
  },
  {
    id: 'sd-fund-q8',
    question: 'During a design interview deep-dive, you are asked to add a cache in front of the database to "fix the bottleneck." What should you clarify or push back on before agreeing this is the right fix?',
    shortAnswer: 'Confirm the bottleneck is actually read load and not write load or a hot key — a cache does nothing for write-heavy contention or a single hot partition being hammered by many small writes. Also check the read pattern: caching helps for skewed, repeat-read access (e.g. celebrity profile) but adds limited value for uniformly random reads with low cache hit rate.',
    detailedAnswer: 'Bottlenecks migrate, and a wrong fix can waste effort while leaving the real constraint untouched. Before adding a cache, confirm via the read/write ratio and access pattern that reads are actually the constraint, and that the read pattern is skewed enough (Zipfian/hot-key) to get a meaningful cache hit rate — a cache in front of uniformly random reads over a huge key space barely moves the needle and just adds staleness risk and operational complexity for little benefit. If the actual bottleneck is write throughput or a single hot partition, the correct lever is partitioning/sharding or write buffering, not caching.',
    keyPoints: [
      'Verify the bottleneck is read-side before reaching for a cache',
      'Cache effectiveness depends on access pattern skew, not just read volume',
      'Naming the wrong bottleneck and "fixing" it anyway is a common interview trap',
    ],
    seniorFollowUps: [
      'How would you measure cache hit rate expectations before implementing, using only the requirements gathered so far?',
    ],
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['bottleneck', 'caching', 'interview-framework'],
  },
  {
    id: 'sd-fund-q9',
    question: 'You are told a system must be "strongly consistent and always available even during a network partition." Without going into CAP theorem mechanics, how do you respond in the interview?',
    shortAnswer: 'Point out these two requirements are in direct tension during an actual partition — a partitioned node can either keep serving (favoring availability, at the risk of stale/conflicting data) or refuse requests until it can confirm consistency (favoring consistency, at the cost of availability), but it cannot do both. Ask the interviewer which one matters more for this specific use case so the tradeoff can be made explicit rather than assumed away.',
    topic: 'system-design',
    subtopic: 'sd-fundamentals',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['cap-tradeoff', 'requirements', 'availability'],
  },

  // ── CAP & Consistency Questions ────────────────────────────────
{
    id: 'sd-cap-q1',
    question: 'A candidate says "our system is AP, so we chose availability over consistency." What is imprecise about this statement, and how would you correct it?',
    shortAnswer: 'CAP only forces a choice between consistency and availability during an actual network partition — outside of a partition the system can, and usually does, offer both. Calling it "AP" as a blanket label glosses over that nuance, and many real systems tune the tradeoff per-operation (e.g. via quorum W/R settings) rather than committing to one label system-wide.',
    detailedAnswer: 'The correction: describe what the system does specifically when a partition is detected — e.g. "writes are still accepted on both sides of the partition and conflicts are resolved later via vector clocks or last-write-wins" — rather than a static label. Also worth noting that PACELC captures the tradeoff that exists even without a partition (latency vs consistency), which is actually the more common operating condition and often the more consequential design decision day to day.',
    keyPoints: [
      'CAP tradeoff is conditional on an active partition, not a permanent state',
      'Many systems tune C vs A per-request via quorum settings rather than picking one mode globally',
      'PACELC (latency vs consistency) governs behavior the rest of the time',
    ],
    seniorFollowUps: [
      'How would you detect that a partition is actually occurring versus a slow node?',
      'What does your system do to reconcile conflicting writes once the partition heals?',
    ],
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cap-theorem', 'pacelc', 'tradeoffs'],
  },
  {
    id: 'sd-cap-q2',
    question: 'Explain the difference between linearizability and serializability with a concrete example where a system has one but not the other.',
    shortAnswer: 'Linearizability guarantees a single value\'s reads reflect the most recent write in real time; serializability guarantees a multi-statement transaction behaves as if run alone in some order, not necessarily the real-time order. A system can be serializable but not linearizable: e.g. a database that correctly serializes all transactions against each other but serves reads from a lagging replica — transactions are internally consistent, but a read can return data older than a write that already completed elsewhere in wall-clock time.',
    detailedAnswer: 'Concretely: two transactions T1 (writes x=5) and T2 (reads x) run on different nodes. If the system is serializable, T2\'s result is consistent with some valid ordering of all transactions — but that ordering might place T2 before T1 even though T1 finished first in real time, because serializability doesn\'t reference wall-clock time at all. Linearizability would forbid that: since T1 completed before T2 started in real time, T2 must see T1\'s write. Strict serializability combines both properties and is what systems like Spanner aim for.',
    keyPoints: [
      'Linearizability: single-object, real-time recency',
      'Serializability: multi-object, transactional, any valid ordering suffices',
      'Strict serializability = both combined',
    ],
    seniorFollowUps: [
      'Which guarantee does a typical SQL database\'s SERIALIZABLE isolation level actually provide, and does it imply linearizability?',
    ],
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['linearizability', 'serializability', 'consistency-models'],
  },
  {
    id: 'sd-cap-q3',
    question: 'You are designing a distributed key-value store with N=5 replicas. What W and R would you choose for (a) a session token store and (b) an inventory count for checkout, and why?',
    shortAnswer: 'For the session token store, favor low latency: something like W=1, R=1 is fine since a slightly stale session read just causes a re-login at worst, and speed matters more. For inventory at checkout, choose W and R such that W+R>N (e.g. W=3, R=3 with N=5) to guarantee reads see the latest committed value and avoid overselling stock.',
    detailedAnswer: 'The general principle: W+R>N guarantees the read and write quorums overlap by at least one node, so a read is guaranteed to observe the most recent acknowledged write. For inventory, overselling is a real financial and customer-trust cost, so paying the extra write/read latency to get that guarantee is worth it. For session tokens, the cost of a rare stale read (user has to log in again) is low and recoverable, so it\'s better to optimize for latency and availability with a lower W and R. Note that W+R>N is necessary but not sufficient for full linearizability — concurrent writes still need a conflict-resolution policy (vector clocks, LWW) on top.',
    keyPoints: [
      'W+R>N guarantees quorum overlap, which guarantees freshness of reads relative to acknowledged writes',
      'Choose per-feature based on the real cost of staleness, not a single global policy',
      'Quorum overlap alone doesn\'t resolve concurrent write conflicts',
    ],
    seniorFollowUps: [
      'What happens to your checkout guarantee if a node holding part of the write quorum crashes right after acknowledging but before the read?',
    ],
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['quorum', 'nwr', 'tradeoffs'],
  },
  {
    id: 'sd-cap-q4',
    question: 'A user posts a comment, then immediately replies to it, but a friend viewing the thread sees the reply appear before the original comment. What consistency failure is this, and what model fixes it?',
    shortAnswer: 'This is a causal consistency violation — the reply causally depends on the comment, but the system delivered them out of causal order. Causal consistency (typically implemented with logical/vector clocks tracking happens-before relationships) fixes it by guaranteeing causally related operations are seen in the same order by every observer, without requiring full strong consistency.',
    detailedAnswer: 'Plain eventual consistency makes no ordering guarantee at all, so this failure is expected under it. Strong consistency would also fix it but at a much higher coordination cost applied to every write in the system, not just causally related ones. Causal consistency is the targeted fix: the system tags writes with happened-before metadata (e.g. via vector clocks or dependency lists) and withholds delivering a causally-dependent write until its dependencies are visible.',
    keyPoints: [
      'This is specifically a causal ordering violation, not general staleness',
      'Causal consistency uses happens-before tracking, commonly via vector clocks',
      'Cheaper than strong consistency since it only orders causally related operations',
    ],
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['causal-consistency', 'vector-clocks', 'ordering'],
  },
  {
    id: 'sd-cap-q5',
    question: 'Why can\'t you just always choose strong consistency and avoid the whole modeling exercise?',
    shortAnswer: 'Strong consistency requires coordinating with enough replicas on every operation, which raises latency on every request and reduces availability whenever a replica is slow or unreachable — costs that are wasted on data where staleness is harmless, like a view counter or a recommendation feed.',
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['strong-consistency', 'tradeoffs'],
  },
  {
    id: 'sd-cap-q6',
    question: 'A user updates their profile picture, then refreshes the page and sees the old one, even though your database is described as eventually consistent by design. Is this a bug, and what is the minimal fix?',
    shortAnswer: 'It is expected behavior under plain eventual consistency, not necessarily a bug, but it is bad UX. The minimal fix is read-your-writes consistency for that client — route that user\'s subsequent reads to the replica they just wrote to (or the primary) for a short window, rather than upgrading the whole system to strong consistency.',
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['read-your-writes', 'eventual-consistency'],
  },
  {
    id: 'sd-cap-q7',
    question: 'What is the practical difference between session consistency and read-your-writes consistency?',
    shortAnswer: 'Read-your-writes only guarantees a client sees its own prior writes on later reads. Session consistency is broader — within one session it also guarantees monotonic reads (you never see an older version after seeing a newer one), combining read-your-writes with ordering guarantees, but resets once the session ends.',
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Basic',
    seniority: 'Senior',
    tags: ['session-consistency', 'read-your-writes'],
  },
  {
    id: 'sd-cap-q8',
    question: 'Two vector clocks for the same key are compared and neither is found to have happened-before the other. What does this mean, and what should the system do?',
    shortAnswer: 'The two writes are concurrent — they were made without either replica knowing about the other, so there is a genuine conflict, not just a delay. The system must resolve it via some policy: merge the values, apply last-write-wins using a timestamp, or surface both versions to the application/user to resolve manually (as Dynamo-style systems sometimes do with sibling values).',
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['vector-clocks', 'conflict-resolution'],
  },
  {
    id: 'sd-cap-q9',
    question: 'Why is PACELC considered a more complete model than CAP for describing a real production system\'s behavior?',
    shortAnswer: 'CAP only describes behavior during the rare case of an active network partition. PACELC adds the tradeoff that exists during the much more common case of normal operation — latency versus consistency — which is what actually governs day-to-day performance and design decisions like whether reads hit a local replica or wait for quorum confirmation.',
    detailedAnswer: 'A system\'s PACELC classification (e.g. PA/EL for Cassandra-style systems, or PC/EC for a system doing synchronous cross-region writes) tells you both how it behaves under partition and what it optimizes for the rest of the time, which is a strictly more useful characterization for capacity planning and SLA-setting than the CAP label alone.',
    keyPoints: [
      'CAP only applies during a partition, which is a rare event',
      'PACELC\'s ELC half governs everyday latency/consistency tradeoffs',
      'A full PACELC classification (e.g. PA/EL) is more informative than a CAP label alone',
    ],
    seniorFollowUps: [
      'How would you empirically determine whether a given managed database is PA/EL or PC/EC without reading its internals docs?',
    ],
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['pacelc', 'cap-theorem'],
  },
  {
    id: 'sd-cap-q10',
    question: 'If a quorum read/write setup satisfies W + R > N, can you call the system linearizable?',
    shortAnswer: 'Not automatically. W+R>N guarantees the read and write sets overlap, so a read is guaranteed to see the latest acknowledged write\'s version — but without additional coordination for concurrent writes (conflict resolution via timestamps or vector clocks, and careful read-repair), the system can still exhibit anomalies that a strict linearizability guarantee would forbid. Quorum overlap is necessary for freshness, not sufficient for full linearizability.',
    topic: 'system-design',
    subtopic: 'sd-cap-consistency',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['quorum', 'linearizability'],
  },

  // ── API Design Questions ──────────────────────────────────────
{
    id: 'sd-api-q1',
    question: 'A team wants to add a new required field to an existing, widely-consumed API response. Why is this breaking even though it\'s "just a field addition," and what\'s the safer path?',
    shortAnswer: 'Adding a *required* field is breaking for producers/mocks and for strict schema validators on the client side, even though existing clients that just ignore unknown fields survive. The safer path is to add it as optional first, backfill/dual-write it, let consumers adopt it, and only make it required in a new API version once all consumers are confirmed to send/handle it.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['versioning', 'compatibility', 'breaking-change'],
  },
  {
    id: 'sd-api-q2',
    question: 'Why does cursor-based pagination scale better than offset-based pagination for a feed that has frequent inserts, and what does the API give up by switching?',
    shortAnswer: 'Offset pagination re-scans and discards all skipped rows on every request (getting slower with higher offsets) and shifts under concurrent inserts/deletes, causing duplicate or missing items across pages. Cursor pagination anchors to a stable key so it stays O(page size) and immune to shifting, but the client gives up the ability to jump to an arbitrary page number.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['pagination', 'rest'],
  },
  {
    id: 'sd-api-q3',
    question: 'Design an idempotency mechanism for a payments POST /charges endpoint that must survive client-side retries after network timeouts. What are the failure modes if you get it wrong?',
    shortAnswer: 'Require a client-generated Idempotency-Key header on every request; the server stores a mapping from key to the eventual response (with a TTL) the first time it processes that key, and replays the stored response on any duplicate. Get it wrong and you either double-charge the customer (no dedup) or leave a request permanently "stuck" if a duplicate arrives while the original is still in flight and isn\'t handled (should return 409 or block until resolution).',
    detailedAnswer: 'The key insight is that a network timeout is fundamentally ambiguous to the client — it cannot distinguish "request never reached the server" from "server processed it but the response was lost." Blind retry of a non-idempotent POST in that ambiguity window is what causes double-charges. The idempotency key shifts deduplication responsibility to the server, which can definitively know if it already processed that logical request. Implementation details matter: the key must be generated once per logical operation (before the first attempt, not regenerated per retry), the server needs a fast lookup store (e.g. Redis) keyed on (endpoint, key, maybe hash of body) to detect conflicting reuse of the same key with different payloads, and there needs to be explicit handling for the race where a duplicate request arrives while the first is still mid-flight — typically a 409 Conflict or a lock-and-wait.',
    keyPoints: [
      'Idempotency key generated client-side, reused across retries of the same logical operation',
      'Server persists key -> final response mapping with a TTL (e.g. 24h)',
      'In-flight duplicate detection needed to avoid double-processing before the first attempt completes',
      'Reject reuse of a key with a different request body (likely client bug) rather than silently returning the old response',
    ],
    seniorFollowUps: [
      'How long should the idempotency key TTL be, and what happens to retries that arrive after it expires?',
      'How would you extend this pattern to a multi-step workflow (e.g. charge + inventory reserve) rather than a single endpoint?',
    ],
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['idempotency', 'rest', 'payments', 'reliability'],
  },
  {
    id: 'sd-api-q4',
    question: 'Your team is building an internal API for a UI that renders differently on mobile (needs a slim payload) and desktop (needs a rich, nested payload). REST, gRPC, or GraphQL — how do you decide?',
    shortAnswer: 'GraphQL is the strongest fit here specifically because the pain point is variable client-side field needs — each client requests exactly the shape it wants from one schema, avoiding either a slim mobile-specific REST endpoint or a bloated shared one. REST would require either over-fetching on mobile or maintaining parallel endpoints; gRPC solves neither problem (it\'s about typed, low-latency service calls, not flexible client-shaped responses).',
    detailedAnswer: 'The decision axis isn\'t speed or "modernity" — it\'s who needs control over response shape. REST puts the server in control (one shape per endpoint), which is fine when clients have similar needs, but here mobile and desktop diverge meaningfully, so REST forces a choice between over-fetching (send desktop\'s full payload to mobile) or endpoint proliferation (maintain /mobile/orders and /desktop/orders separately, doubling maintenance). GraphQL hands shape control to the client via field selection, solving exactly this without endpoint duplication. The cost is real: the team now owns resolver performance (N+1 risk), query cost bounding, and a shared schema whose evolution affects every client at once. If the variability were about latency/throughput between internal services rather than client-shaped UI data, gRPC would be the better answer instead.',
    keyPoints: [
      'Decision axis: who controls response shape, not raw performance',
      'REST here means over-fetching or duplicate mobile/desktop endpoints',
      'GraphQL cost: N+1 resolver risk, query cost/depth limiting, single shared schema blast radius',
      'gRPC is the wrong axis entirely for this specific problem (service-to-service typed calls, not flexible client shaping)',
    ],
    seniorFollowUps: [
      'How would you prevent a single expensive nested GraphQL query from degrading the API for everyone else?',
      'If mobile and desktop later need very different SLAs (mobile on flaky networks), does that change the recommendation?',
    ],
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['graphql', 'rest', 'grpc', 'api-style-choice'],
  },
  {
    id: 'sd-api-q5',
    question: 'A GraphQL query that lists 200 orders with each order\'s customer name is taking seconds to respond, but a REST equivalent with a JOIN is fast. What\'s the likely root cause?',
    shortAnswer: 'Classic N+1: the naive resolver for the customer field is issuing one database query per order (200 extra queries) instead of batching them into a single query. The fix is a batched/deduplicated data loader keyed on order or customer id, collapsing the 200 lookups into one IN-clause query per request tick.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['graphql', 'n-plus-1', 'performance'],
  },
  {
    id: 'sd-api-q6',
    question: 'Why is PUT considered idempotent but POST is not, and does that distinction actually matter in practice for retry logic?',
    shortAnswer: 'PUT replaces a resource at a known URL with a full representation, so sending it N times leaves the same end state as sending it once. POST typically creates a new resource under a collection URL, so repeating it (without extra dedup) creates N resources. It matters directly for retry logic: HTTP clients and proxies can safely auto-retry PUT/GET/DELETE on failure, but must not auto-retry POST without an idempotency mechanism.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['rest', 'idempotency', 'http-semantics'],
  },
  {
    id: 'sd-api-q7',
    question: 'Compare URL-path versioning (/v2/orders) against header/content-negotiation versioning for a public API. What operational tradeoff usually decides it?',
    shortAnswer: 'URL versioning is simple to route, cache, log, and test (visible everywhere, works in a browser bar), but implies the resource identity itself changed across versions. Header/content-negotiation versioning keeps a stable URL and is more "correct" REST, but is harder to cache correctly (needs Vary headers), invisible in logs/browsers, and easy for clients to forget. Most teams pick URL versioning for operational simplicity unless they have strong reasons (fine-grained per-client version control) to pay the header-negotiation cost.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['versioning', 'rest'],
  },
  {
    id: 'sd-api-q8',
    question: 'You\'re designing a webhook system for a billing API to notify partners when an invoice is paid. What reliability guarantees should the contract explicitly NOT promise, and what should receivers assume instead?',
    shortAnswer: 'The contract should not promise exactly-once delivery or strict ordering — network retries mean receivers may see the same event more than once, and events for the same resource can arrive out of order. Receivers should be designed to be idempotent (dedup on event id) and should not assume delivery order reflects event order (use a timestamp or sequence field in the payload instead).',
    detailedAnswer: 'Webhook delivery is fundamentally at-least-once from the sender\'s side: if the receiver\'s endpoint is slow, down, or returns a non-2xx, the sender retries with backoff, which means duplicates are a normal, expected occurrence, not an edge case. Similarly, retries plus queuing/concurrency on the sender side mean two events for the same resource can arrive at the receiver out of the order they were generated. A well-designed webhook contract makes this explicit: each event carries a unique event id (for receiver-side dedup) and either a strictly increasing sequence number or timestamp the receiver can use to detect and discard stale/out-of-order events, rather than assuming HTTP delivery order equals event order. Payloads are also often kept thin (event type + resource id, not the full resource) so the receiver calls back into the API for current state — this avoids serving stale embedded data and reduces the blast radius if a payload leaks.',
    keyPoints: [
      'At-least-once delivery: sender retries on failure, so duplicates are expected, not exceptional',
      'No ordering guarantee across delivery attempts — use a sequence/timestamp field, not arrival order',
      'Receivers must be idempotent, deduping on a unique event id',
      'Thin payloads (event type + id) pushed by the sender, full data pulled by the receiver, is the common pattern',
    ],
    seniorFollowUps: [
      'How would you let partners verify a webhook actually came from you and wasn\'t forged?',
      'How would you design the retry/backoff schedule so a partner outage doesn\'t create an unbounded backlog?',
    ],
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['webhooks', 'reliability', 'idempotency'],
  },
  {
    id: 'sd-api-q9',
    question: 'What does a well-designed 429 response need beyond the status code itself, and why?',
    shortAnswer: 'It needs a Retry-After header (seconds or an HTTP date) and ideally RateLimit-Remaining/RateLimit-Limit headers, so client libraries can back off correctly and predictably instead of immediately retrying and hammering the endpoint again, or guessing at wait times.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['rate-limiting', 'api-contract'],
  },
  {
    id: 'sd-api-q10',
    question: 'Why is protobuf field numbering (not naming) the thing you must never reuse or change in a gRPC schema evolution, and what breaks if you do?',
    shortAnswer: 'Protobuf serializes using field tag numbers, not field names, so the wire format has no idea what a field is "called" — only its number and wire type. Reusing a retired field number for a new field of a different type causes old binary-encoded messages to be misinterpreted (wrong type read at that tag), silently corrupting data instead of failing loudly.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['grpc', 'protobuf', 'schema-evolution'],
  },
  {
    id: 'sd-api-q11',
    question: 'A public GraphQL API is getting hit with deeply nested queries that are timing out the database. What design-level defenses exist besides just adding more database capacity?',
    shortAnswer: 'Add query complexity/cost analysis or a max query-depth limit so the server rejects overly expensive queries before execution, and consider persisted queries (client sends a pre-registered query hash rather than arbitrary query text) to eliminate ad-hoc unbounded queries against a public endpoint entirely.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['graphql', 'security', 'query-cost'],
  },
  {
    id: 'sd-api-q12',
    question: 'What\'s the practical difference between a 422 Unprocessable Entity and a 400 Bad Request, and why bother distinguishing them?',
    shortAnswer: '400 means the request itself is malformed (bad JSON, missing required field, wrong type) — the server couldn\'t even parse/validate the shape. 422 means the request is well-formed and passed basic validation but is semantically invalid (e.g. an end date before a start date). Distinguishing them lets clients tell "fix your request structure" apart from "fix your business logic," which matters for automated retry/validation tooling.',
    topic: 'system-design',
    subtopic: 'sd-api-design',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['rest', 'http-semantics', 'error-handling'],
  },

  // ── Load Balancing Questions ──────────────────────────────────
{
    id: 'sd-lb-q1',
    question: 'A service migrates from round robin to least connections and latency gets worse for some requests, not better. What likely went wrong?',
    shortAnswer: 'Least connections tracks connection count, not actual load — with persistent/multiplexed connections (HTTP/2, WebSockets, keep-alive pools) a backend can have few connections but be handling many concurrent requests over each one, so the LB under-counts its real load and keeps routing more traffic to it.',
    detailedAnswer: 'Least connections assumes connection count is a reasonable proxy for backend load, which holds when each connection roughly maps to one in-flight request (classic HTTP/1.1 without keep-alive). Once connections are long-lived and multiplexed — HTTP/2 streams over one TCP connection, WebSocket sessions, connection-pooled clients — one connection can carry many concurrent requests, so an instance handling a lot of work can still look "lightly loaded" by connection count. The fix is either switching to a request-level or latency-aware metric (e.g. weighted response-time balancing, or actual outstanding-request count rather than connection count) or, at L7, having the LB track in-flight requests per backend directly instead of relying on the L4-style connection count.',
    keyPoints: [
      'Least connections is a proxy for load, and proxies break when their assumption (1 connection ~ 1 unit of load) stops holding',
      'HTTP/2 multiplexing, WebSockets, and connection pooling all break the connection-count-to-load mapping',
      'Fix: track actual in-flight requests or response-time at L7, not raw connection count',
    ],
    seniorFollowUps: ['How would you validate this hypothesis in production before rolling the change back?', 'What metric would you propose instead, and what would it cost the LB to compute it?'],
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['least-connections', 'load-balancing', 'http2', 'algorithms'],
  },
  {
    id: 'sd-lb-q2',
    question: 'Why does consistent hashing minimize redistribution when a node is added, and what does adding virtual nodes actually fix that plain consistent hashing doesn\'t?',
    shortAnswer: 'Consistent hashing only remaps the keys on the arc between the new node and its ring predecessor — roughly 1/N of keys — instead of the near-total remap a mod-N hash causes. But without virtual nodes, each physical node occupies one random point on the ring, so its arc size (and thus its load share) is luck-of-the-draw and can be very uneven; virtual nodes give each physical node many small arcs across the ring so its total share averages out close to 1/N.',
    detailedAnswer: 'The core guarantee of consistent hashing is local, not global: a topology change only affects the ring segment adjacent to the changed node, because every key\'s owner is determined by "first node clockwise," and that only changes for keys whose clockwise walk now hits the new/removed node before the one it used to hit. That\'s the answer to the redistribution question. But this says nothing about how big each node\'s arc is on the ring — with one hash point per physical node, arc sizes are effectively random, so one node might own 40% of the ring and another 5%, causing severe load skew even though redistribution-on-change is minimized. Virtual nodes fix the skew problem specifically: hashing each physical node to, say, 100-200 points spreads its ownership across many small, scattered arcs, and by law of large numbers the sum of those arcs converges close to an even 1/N share. More virtual nodes reduce variance further but cost more metadata to track.',
    keyPoints: [
      'Minimal redistribution comes from the "first node clockwise" rule being local to a change',
      'Virtual nodes solve a separate problem: uneven load distribution from random single-point placement',
      'These are two distinct properties of consistent hashing that are easy to conflate',
    ],
    seniorFollowUps: ['How many virtual nodes per physical node would you pick, and what\'s the tradeoff in going higher?', 'How would you handle heterogeneous node capacity (some nodes twice as powerful) within this scheme?'],
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['consistent-hashing', 'virtual-nodes', 'sharding'],
  },
  {
    id: 'sd-lb-q3',
    question: 'A team wants to route /video/* requests to a specialized backend fleet and everything else to the general fleet, all behind one public IP. Why does this require an L7 load balancer and not just an L4 one?',
    shortAnswer: 'L4 load balancers forward based on IP/port without decrypting or parsing the payload, so they never see the HTTP path. Content-based routing on the URL path requires terminating the connection (including TLS) and reading the application-layer request, which is exactly what defines L7 load balancing.',
    seniorFollowUps: ['What does terminating TLS at the gateway cost you operationally that a pure L4 passthrough avoids?'],
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['layer4', 'layer7', 'routing'],
  },
  {
    id: 'sd-lb-q4',
    question: 'Your service relies on sticky sessions because in-memory shopping carts live on individual instances. What breaks when you try to autoscale this fleet, and what\'s the fix?',
    shortAnswer: 'Scaling down forcibly strips carts off any terminated instance (their sessions have nowhere else to go), and scaling up doesn\'t help existing sticky clients since they keep routing to their original instance — so autoscaling can add capacity without actually relieving load on hot instances. The fix is externalizing cart state to a shared store (cache or database) so any instance can serve any request, removing the need for affinity entirely.',
    detailedAnswer: 'Sticky sessions create a hidden dependency between "which instance a client is pinned to" and "where that client\'s state lives," which conflicts with two things autoscaling assumes: that any instance can take any request, and that removing an instance is safe as long as capacity elsewhere is sufficient. With stickiness, a scale-down event either has to drain sessions gracefully (wait out a timeout, which delays the scale-down and may not be acceptable under time pressure) or drop them (data loss for in-flight carts). Scale-up is similarly compromised: because the LB doesn\'t rebalance already-pinned clients, adding fresh instances only helps new sessions, not the ones piling load onto older instances — undermining the whole point of autoscaling to redistribute load. The durable fix is to make application servers stateless with respect to session data by moving cart state into an external, shared store (e.g. a distributed cache or database), so the load balancer can route by any algorithm it likes and any instance can serve any request.',
    keyPoints: [
      'Stickiness couples client to instance, autoscaling assumes any instance is fungible',
      'Scale-down forces a choice between draining stranded sessions or dropping them',
      'Scale-up doesn\'t relieve load on hot instances because pinned clients don\'t rebalance',
      'Fix: externalize session state, make instances stateless',
    ],
    seniorFollowUps: ['If you can\'t change the app to be stateless in the short term, what interim mitigation would you deploy?'],
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['sticky-sessions', 'autoscaling', 'statelessness'],
  },
  {
    id: 'sd-lb-q5',
    question: 'A load balancer using only active health checks takes 30 seconds to notice a backend is failing every request. What would passive health checking add, and would you drop the active checks in favor of it?',
    shortAnswer: 'Passive checks observe real request outcomes as they happen, so a backend erroring on live traffic gets ejected within the next few requests instead of waiting for the next probe interval — much faster reaction. You wouldn\'t drop active checks, though: passive checks need actual traffic to detect a problem, so a low-traffic or newly-added instance with no live requests yet would go undetected until real users hit it, and active checks also validate readiness before an instance ever receives production traffic.',
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['health-checks', 'reliability'],
  },
  {
    id: 'sd-lb-q6',
    question: 'Why is client-IP-based hashing a poor choice for session affinity when many of your users sit behind a corporate NAT or mobile carrier gateway?',
    shortAnswer: 'NAT and carrier gateways funnel many distinct end users through a small number of public IPs, so hashing on client IP collapses all of them onto the same backend, creating hot instances instead of even distribution — the hash function is fine, but its input isn\'t actually unique per user.',
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['ip-hash', 'sticky-sessions'],
  },
  {
    id: 'sd-lb-q7',
    question: 'What\'s the practical difference between putting rate limiting, auth, and routing in an API gateway versus duplicating that logic in every backend service?',
    shortAnswer: 'Centralizing in the gateway means each service can trust a request already passed those checks, avoiding duplicated (and potentially inconsistent) implementations across services — at the cost of making the gateway a shared-fate dependency and an extra network hop for every request.',
    detailedAnswer: 'Duplicating auth/rate-limiting/routing logic per service is a maintenance and consistency hazard: a bug fix or policy change (e.g. a new auth scheme) has to be rolled out to every service independently, and services can drift out of sync. Centralizing it in a gateway gives one place to change behavior and one enforcement point every request must pass, simplifying each backend\'s trust model. The cost is architectural: the gateway now sits on the critical path of every request (added latency), and an outage or bug in the gateway is an outage for everything behind it, not just one service — so the gateway needs its own redundancy, health checks, and horizontal scaling like any other high-criticality service, and its per-request logic needs to stay cheap since it taxes every single call.',
    keyPoints: [
      'Centralization trades duplicated-logic risk for shared-fate risk',
      'Gateway becomes a mandatory hop on every request — latency-sensitive',
      'Gateway must itself be redundant/horizontally scaled, or it becomes the fleet\'s single point of failure',
    ],
    seniorFollowUps: ['How would you decide which cross-cutting concerns belong in the gateway versus a service mesh sidecar?'],
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['api-gateway', 'architecture', 'tradeoffs'],
  },
  {
    id: 'sd-lb-q8',
    question: 'You lower a DNS record\'s TTL from 1 hour to 30 seconds expecting faster failover during an outage. Why might failover still take much longer than 30 seconds in practice?',
    shortAnswer: 'Many resolvers and OS/browser-level DNS caches don\'t strictly honor low TTLs, and some clients cache the resolved IP for the life of a long-running connection or process regardless of TTL — so DNS-based failover speed is only a best-effort lower bound, not a guarantee, which is why critical paths use a dedicated load balancer in the request path instead.',
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dns', 'failover'],
  },
  {
    id: 'sd-lb-q9',
    question: 'When would weighted round robin be a better fit than least connections?',
    shortAnswer: 'When request cost is roughly uniform but backend capacity isn\'t — e.g. a heterogeneous fleet with mixed instance sizes, or during a rolling deploy where you want to slowly ramp traffic to newly added instances — weighting a predictable, simple algorithm is enough, and least connections\' extra bookkeeping buys nothing if load is already predictable per-instance.',
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['round-robin', 'algorithms'],
  },
  {
    id: 'sd-lb-q10',
    question: 'What does an API gateway aggregating multiple backend calls into one response actually buy a mobile client, and what does it cost?',
    shortAnswer: 'It reduces round trips for the client (one request instead of several over a possibly high-latency, unreliable mobile network), at the cost of the gateway now doing extra work per request (fanning out, waiting on the slowest downstream call, combining results) and coupling the gateway to the shape of multiple backend APIs.',
    topic: 'system-design',
    subtopic: 'sd-load-balancing',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['api-gateway', 'aggregation'],
  },

  // ── SQL vs NoSQL Questions ──────────────────────────────────────
{
    id: 'sd-sql-q1',
    question: 'You\'re designing the order-and-payment core of an e-commerce system. A teammate proposes MongoDB for everything to "keep it simple and flexible." What\'s your response?',
    shortAnswer: 'Push back for this specific piece: orders and payments need atomic multi-record consistency (an order, its line items, and a payment must all succeed or all fail together) and benefit from ad-hoc reporting joins — that\'s squarely relational/ACID territory. A document store\'s weaker cross-document transaction guarantees and lack of joins would push that complexity into application code. Reserve document/key-value stores for parts of the system without those requirements, like a product catalog or session cache.',
    detailedAnswer: 'The core argument is matching guarantees to requirements, not database popularity. Order+payment consistency needs atomicity across multiple related records — if the payment succeeds but the order write fails, you have a real business problem, and reconciling that without database-level transactions means building your own compensation logic, which is strictly more complex than using a relational database\'s ACID transaction. Ad-hoc reporting ("total revenue by region last quarter, filtered by three conditions") also favors SQL\'s flexible query language over needing to have anticipated every access pattern in advance. The "simplicity" argument for MongoDB is real but misapplied here — it\'s simpler for schema iteration, not for transactional integrity, and this is the domain where transactional integrity is non-negotiable.',
    keyPoints: [
      'Multi-record atomicity (order + line items + payment) is a core ACID use case',
      'Ad-hoc financial reporting favors SQL\'s flexible joins over pre-modeled access patterns',
      'Document stores are still the right fit elsewhere in the same system (catalog, sessions) — this is a per-component decision, not all-or-nothing',
    ],
    seniorFollowUps: [
      'If the catalog and orders live in different databases, how do you keep a product price change from silently going stale in an in-flight order?',
    ],
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['acid', 'transactions', 'polyglot-persistence', 'e-commerce'],
  },
  {
    id: 'sd-sql-q2',
    question: 'A wide-column store like Cassandra advertises "tunable consistency." What does that actually mean, and why would a team choose it over a fixed consistency level?',
    shortAnswer: 'Tunable consistency means each individual read or write specifies how many replicas must acknowledge it (e.g. ONE, QUORUM, ALL) rather than the database enforcing one fixed level for everything. Teams use it to let different operations on the same cluster make different latency/consistency tradeoffs — e.g. a metrics write can use a fast, low-consistency level, while a financial-adjacent read uses QUORUM for stronger guarantees — without needing separate database clusters for each requirement.',
    detailedAnswer: 'This matters because it decouples the consistency decision from the database as a whole and makes it a per-query concern, closer to how an application actually varies in its needs. A write with consistency ONE returns as soon as a single replica acks it — fastest, but a subsequent read could hit a replica that hasn\'t received it yet. QUORUM requires a majority of replicas to ack, giving strong-enough guarantees for most purposes (a QUORUM write plus a QUORUM read guarantees the read sees the write, assuming replication factor and quorum math line up) while still tolerating some replica unavailability. The tradeoff is operational: engineers must understand and correctly choose consistency level per query, which is a new failure mode (silently picking too weak a level) that a single fixed-consistency relational database doesn\'t have.',
    keyPoints: [
      'Consistency level is chosen per read/write operation, not fixed database-wide',
      'QUORUM write + QUORUM read gives strong-enough read-your-writes guarantees in most configurations',
      'The flexibility shifts responsibility onto engineers to pick the right level per use case',
    ],
    seniorFollowUps: [
      'What happens to your consistency guarantee if replication factor is 3 and you use consistency level ONE for both reads and writes?',
    ],
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cassandra', 'wide-column', 'consistency', 'base'],
  },
  {
    id: 'sd-sql-q3',
    question: 'You need a database to power "friends of friends within 3 hops" for a social feature. Why does a relational database struggle here even with proper indexes, and what changes with a graph database?',
    shortAnswer: 'Each additional hop in a relational schema means another join, and each join re-scans an index and re-executes a query plan — cost grows multiplicatively with hop count even with good indexes. A graph database stores relationships as direct pointers between nodes, so traversing a hop is a constant-time pointer follow rather than an index lookup, keeping multi-hop traversal cost roughly linear instead of exploding.',
    seniorFollowUps: [
      'If this is the only relationship-heavy feature in an otherwise standard CRUD app, would you still introduce a whole graph database for it, or handle it another way?',
    ],
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['graph-database', 'traversal', 'joins'],
  },
  {
    id: 'sd-sql-q4',
    question: 'What\'s the actual mechanism by which normalization slows down reads, and why doesn\'t that automatically mean you should denormalize everything?',
    shortAnswer: 'Normalization spreads related data across multiple tables to avoid redundancy, so reconstructing a full record for a read requires joining those tables back together, which costs query planning and index lookups per join. But denormalizing everything means every write that touches duplicated data must update every copy, and it\'s easy to miss one, causing silent inconsistency — so the right call depends on your actual read:write ratio and how tolerant the data is of temporary staleness.',
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['normalization', 'denormalization', 'tradeoffs'],
  },
  {
    id: 'sd-sql-q5',
    question: 'You\'re modeling a document store schema for a blogging platform. Should a comment be embedded inside its parent post document, or stored as a separate document referencing the post?',
    shortAnswer: 'It depends on the access pattern and growth shape, not on "correctness": embed if comments are always read together with the post and stay bounded in number (avoids extra round trips), but store them separately and reference the post id if comment count is unbounded/high-volume or if comments are also queried independently (e.g. "all comments by a user") — embedding an unbounded, independently-queried list inside a parent document is the classic NoSQL modeling mistake.',
    detailedAnswer: 'Document stores typically have per-document size limits and read the whole document as a unit, so an unbounded embedded array (comments on a viral post) risks hitting size limits and forces reading/transferring the entire history just to show the post. The deciding question is always "what will I query, and how does this document grow": if comments are read only in the context of their post and stay small, embedding avoids a second query entirely. If comments need independent queries (moderation dashboards, per-user comment history) or can grow unbounded, they should be their own collection referenced by post id, accepting the extra query as the cost of correct scaling — this is modeling for access patterns rather than for the entity relationship itself.',
    keyPoints: [
      'Decision driver is query pattern and growth bound, not normalization purity',
      'Unbounded embedded arrays risk document size limits and force full-document reads for partial data',
      'Independent query needs (query comments without their post) push toward a separate, referenced collection',
    ],
    seniorFollowUps: [
      'How would you handle a post that suddenly goes viral and accumulates far more comments than your embedding assumption planned for?',
    ],
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['document-store', 'data-modeling', 'access-patterns'],
  },
  {
    id: 'sd-sql-q6',
    question: 'Why is "design your schema around your access patterns" specifically a NoSQL modeling principle, rather than something you'+"'"+'d say the same way for a relational schema?',
    shortAnswer: 'A relational schema can be designed around the entities and their true relationships because joins let you answer new, unanticipated queries later at some CPU cost. Most NoSQL stores don\'t support joins, so if you didn\'t shape the data for a query in advance, there may be no reasonably efficient way to answer it later without a migration or a new denormalized copy of the data — so the access pattern has to be known and designed for up front.',
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['data-modeling', 'access-patterns', 'joins'],
  },
  {
    id: 'sd-sql-q7',
    question: 'A startup wants to use DynamoDB for everything because "it\'s what scales." What questions would you ask before agreeing?',
    shortAnswer: 'Whether their access patterns are actually known and stable enough to model up front (DynamoDB requires this far more than SQL), whether they need multi-item transactions or ad-hoc queries across attributes (DynamoDB\'s secondary index and transaction support is more limited than a relational database\'s), and whether their actual scale even needs it yet — a single well-tuned relational database handles far more load than most systems ever reach, and premature adoption of a key-value store just trades away query flexibility for scaling headroom they may not need for years.',
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dynamodb', 'key-value', 'premature-optimization'],
  },
  {
    id: 'sd-sql-q8',
    question: 'What does BASE mean, and how is it different from just having no consistency guarantees at all?',
    shortAnswer: 'BASE (Basically Available, Soft state, Eventually consistent) is a deliberate, bounded tradeoff: the system stays available and accepts that replicas may briefly disagree, but guarantees they converge to the same value once writes stop propagating. That\'s different from "no guarantees" — it\'s a specific, well-understood window of staleness traded for availability and throughput, and many systems let you tighten it per-operation (e.g. quorum reads) when needed.',
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['base', 'eventual-consistency'],
  },
  {
    id: 'sd-sql-q9',
    question: 'Two teams disagree: one wants a single relational database for the whole platform for simplicity, the other wants a different database per service. What\'s the actual cost being traded off?',
    shortAnswer: 'A single database is operationally simpler (one system to run, monitor, and back up, and cross-entity transactions/joins stay easy) but forces every workload to accept that database\'s tradeoffs even where they don\'t fit well. Per-service databases (polyglot persistence) let each workload use the best-fit tool, but multiply operational surface area and make any workflow spanning two services lose single-database transactional guarantees, requiring explicit handling of partial failure between stores.',
    topic: 'system-design',
    subtopic: 'sd-sql-vs-nosql',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['polyglot-persistence', 'operational-cost'],
  },

  // ── Replication & Partitioning Questions ────────────────────────
{
    id: 'sd-repl-q1',
    question: 'A system uses fully synchronous replication to a single follower for durability. During a regional network blip, that follower becomes briefly unreachable. What happens to writes, and what would you change?',
    shortAnswer: 'Every write blocks (or fails) because the leader cannot get the synchronous follower\'s acknowledgment — availability drops to zero for writes even though the leader itself is healthy. A common fix is semi-synchronous replication: acknowledge once any one of several followers confirms, so a single unreachable replica doesn\'t stall writes, while still guaranteeing the data exists on at least two nodes.',
    detailedAnswer: 'Synchronous replication to exactly one follower creates a hard dependency: the leader is healthy and reachable, but writes still can\'t complete because the durability guarantee requires that specific follower\'s ack. This converts a network blip into a full outage for writes. The fix is to decouple "durable" from "this one specific node": replicate synchronously to a quorum of N followers (any W of them), or promote/demote which follower is the synchronous one dynamically so a slow node doesn\'t become a standing liability. The tradeoff is that quorum synchronous replication adds latency (wait for the fastest majority, not the fastest one) but removes the single-follower single point of failure.',
    keyPoints: ['Sync replication to exactly one node is a single point of failure for write availability, not just leader loss', 'Semi-synchronous (ack on any 1-of-N) or quorum-based synchronous replication removes the dependency on one specific follower', 'Durability and availability trade off directly here — more required acks means stronger durability but a larger blast radius for any one follower\'s hiccup'],
    seniorFollowUps: ['How would you detect and automatically demote a synchronous follower that has become the bottleneck?', 'How does this interact with the leader failover story — does the synchronous follower get failover priority?'],
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['replication', 'synchronous', 'availability', 'durability'],
  },
  {
    id: 'sd-repl-q2',
    question: 'Users report that after updating their profile, the change sometimes appears to "undo itself" on refresh before showing up again permanently. What\'s the likely cause and how would you fix it?',
    shortAnswer: 'This is a monotonic-read violation from reads being load-balanced across followers with different replication lag — a refresh can hit a less caught-up follower after a previous read hit a more current one, making the update appear to disappear. Fix by routing a given user\'s session to the same replica (sticky reads) or by routing reads to the leader for a short window after that user\'s own write.',
    detailedAnswer: 'The symptom is a classic sign of leaderless or round-robin read routing across followers with inconsistent lag: request 1 lands on a follower that has already applied the update, request 2 (the refresh) lands on a different follower that hasn\'t caught up yet, so the user sees data go backward. Two independent guarantees are usually conflated here — read-your-writes (your own write is visible to your own subsequent reads) and monotonic reads (you never see time go backward across reads, even unrelated ones). Sticky session routing to one replica fixes monotonic reads. Routing post-write reads to the leader (or a replica verified caught up to a version marker) fixes read-your-writes specifically.',
    keyPoints: ['Symptom matches monotonic-read violation, not just read-your-writes', 'Root cause is read traffic distributed across followers with uneven replication lag', 'Sticky routing per session and leader-routing after writes are the two standard mitigations, addressing different guarantees'],
    seniorFollowUps: ['What would you use as the "caught up enough" marker to safely route a read to a follower instead of the leader?', 'How does this change if the write path is multi-leader instead of single-leader?'],
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['replication-lag', 'read-your-writes', 'monotonic-reads', 'consistency'],
  },
  {
    id: 'sd-repl-q3',
    question: 'A shard for a multi-tenant SaaS product is sharded by tenant_id using range partitioning, and one enterprise tenant now generates 80% of all traffic on its shard while others sit idle. Diagnose and propose a fix.',
    shortAnswer: 'Range partitioning by tenant_id put one large tenant\'s entire dataset and traffic on a single shard with no way to split it further within that key. Fixing it usually means moving that tenant to a dedicated shard (or several, sub-partitioned by a secondary key like user_id or resource_id within the tenant), rather than trying to keep range partitioning by tenant_id alone.',
    detailedAnswer: 'Range partitioning by tenant_id assumes tenants are roughly similar in size; a single dominant tenant breaks that assumption completely because a range shard can\'t be subdivided below the granularity of the key it\'s ranging over. The practical fix is a composite key: shard by (tenant_id, secondary_key) so a giant tenant\'s data spreads across multiple shards via the secondary dimension, while small tenants naturally colocate. Alternatively, give oversized tenants dedicated shard(s) as a special case ("shard-per-large-tenant") and keep the rest pooled — a common real-world hybrid. Straight hash partitioning on tenant_id would fix even distribution across tenants but doesn\'t solve one oversized tenant either, since that tenant\'s key still lands entirely on one shard.',
    keyPoints: ['A single hot key value (not just a hot range) breaks both range and plain hash partitioning if the shard key doesn\'t include a secondary dimension', 'Composite/compound shard keys let one logical entity spread across multiple physical shards', 'Dedicating shards to outlier tenants is a legitimate operational pattern, not a workaround to be embarrassed about'],
    seniorFollowUps: ['How would you migrate the existing oversized tenant\'s data without downtime once you change the shard key scheme?', 'How do you detect this kind of skew before it becomes an incident?'],
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['sharding', 'shard-key', 'hotspot', 'multi-tenant'],
  },
  {
    id: 'sd-repl-q4',
    question: 'Why does consistent hashing reduce data movement during resharding compared to a naive hash(key) % N scheme, and what does it cost you?',
    shortAnswer: 'With hash(key) % N, changing N changes almost every key\'s target shard because the modulo result shifts for nearly all keys. Consistent hashing places shards and keys on a ring so adding or removing one shard only affects the keys in its immediate neighborhood, remapping roughly 1/N of the keyspace instead of nearly all of it. The cost is a more complex routing layer and the need for virtual nodes to avoid uneven load when the ring has few physical shards.',
    detailedAnswer: 'Modulo hashing ties every key\'s owner to N directly, so any change to N is effectively a full-cluster data migration — infeasible to do live at scale. Consistent hashing decouples that: shards occupy fixed positions on a hash ring (often many virtual positions each, to smooth out load), and each key belongs to the next shard clockwise from its hash. Inserting a new shard only steals the keys between its ring position and the next one, and removing a shard only redistributes its keys to its neighbor(s) — bounding the migration to roughly 1/N of the data instead of nearly all of it. The cost is indirection (a ring lookup instead of simple arithmetic) and the need for virtual nodes, since with only a handful of physical shards a bare ring can land unevenly by chance.',
    keyPoints: ['Modulo hashing couples every key\'s shard assignment to the total shard count N', 'Consistent hashing bounds remapped keys to roughly 1/N by only affecting a shard\'s ring neighborhood', 'Virtual nodes are necessary in practice to avoid uneven load distribution from a small number of ring points'],
    seniorFollowUps: ['How do virtual nodes interact with replica placement — do you also use the ring to decide which nodes replicate a given shard?'],
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['consistent-hashing', 'sharding', 'resharding'],
  },
  {
    id: 'sd-repl-q5',
    question: 'When would you choose multi-leader replication over single-leader, given that it introduces write conflicts single-leader never has?',
    shortAnswer: 'Multi-leader makes sense when write locality across regions or devices matters more than avoiding conflicts — e.g. multiple data centers each need to accept local writes without a cross-WAN round trip to a single leader, or clients need to write while offline and sync later. You accept the operational cost of conflict detection/resolution (LWW, merge functions, CRDTs) in exchange for lower write latency and continued write availability when one region or device is disconnected.',
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['multi-leader', 'replication', 'conflicts'],
  },
  {
    id: 'sd-repl-q6',
    question: 'A leader crashes and a follower is automatically promoted. Shortly after, some writes that clients believed succeeded are missing. What went wrong, and does synchronous replication fully prevent this?',
    shortAnswer: 'The old leader had acknowledged writes to the client that hadn\'t yet replicated to the promoted follower (typical with asynchronous replication), so they\'re lost once that follower becomes the new leader. Fully synchronous replication to that follower would prevent it for acknowledged writes, but only for the specific followers included in the synchronous set — if a different, non-synchronous follower gets promoted instead, the same gap reappears.',
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['failover', 'replication-lag', 'durability'],
  },
  {
    id: 'sd-repl-q7',
    question: 'What is split-brain in the context of leader-based replication, and why do consensus protocols like Raft specifically guard against it?',
    shortAnswer: 'Split-brain is when two nodes both believe they are the current leader and both accept writes simultaneously, usually after a network partition isolates the old leader while the rest of the cluster elects a new one. Consensus protocols require a majority quorum to elect a leader and to commit writes, which mathematically guarantees at most one leader can have quorum support at any time, preventing two leaders from both being able to make committed progress.',
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['split-brain', 'failover', 'consensus'],
  },
  {
    id: 'sd-repl-q8',
    question: 'Why does hash-based partitioning make range queries expensive, and how do systems that need both even write distribution and efficient range scans work around it?',
    shortAnswer: 'Hashing scrambles key order, so keys that were adjacent in the original keyspace end up on unrelated shards, forcing a range query to fan out to every shard and merge results. Systems that need both typically use a compound key — hash on one field (e.g. a partition/tenant ID) to spread writes evenly, and keep a range-ordered second field (e.g. timestamp) within each shard so scans within a partition stay efficient.',
    topic: 'system-design',
    subtopic: 'sd-replication',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['hash-partitioning', 'range-queries', 'compound-key'],
  },

  // ── Caching Questions ────────────────────────────────────────────
// Expert
  {
    id: 'sd-cache-q1',
    question: 'A key with a 60-second TTL sits under heavy read traffic. Every 60 seconds you see a latency spike as thousands of requests hit the database simultaneously before the cache refills. Walk through why, and give two independent fixes.',
    shortAnswer: 'This is a cache stampede: the key expires and every concurrent reader misses at once, all racing to the database. Fix with request coalescing (only the first miss queries the DB, others wait on that in-flight result) and/or jittered TTLs so identical keys across replicas or related keys don\'t expire in lockstep.',
    detailedAnswer: 'The root cause is that TTL expiry is a single instant shared by every reader of that key, so the cache-aside pattern\'s "miss triggers a DB load" collapses from one load to N concurrent loads exactly when the key is hottest. Two classes of fix: (1) reduce the number of DB hits per expiry — request coalescing/single-flight locks one goroutine/thread to do the reload while others block or serve the stale value briefly; a distributed lock on the cache key works similarly across processes. (2) spread out expiry so it never happens for many requests at the exact same moment — add random jitter to the TTL (e.g. 60s +/- 10s), or use a "soft TTL" where the value is marked stale but still served while one background request refreshes it (stale-while-revalidate).',
    keyPoints: [
      'Root cause: TTL expiry is a shared instant, so cache-aside collapses one reload into N concurrent reloads',
      'Request coalescing / single-flight locking limits it to one DB hit per expiry',
      'Jittered TTLs prevent synchronized expiry in the first place',
      'Stale-while-revalidate serves the old value during refresh instead of blocking readers',
    ],
    seniorFollowUps: [
      'How would this change if the key were sharded across a Redis cluster rather than a single instance?',
      'What happens to this fix if the database itself becomes slow during the reload — does coalescing make that worse?',
    ],
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['cache-stampede', 'thundering-herd', 'ttl'],
  },
  {
    id: 'sd-cache-q2',
    question: 'Your product has one influencer profile page that gets 50x the traffic of any other page. Even though your Redis cluster has plenty of aggregate capacity, that one page\'s latency degrades under load. Diagnose and propose a fix.',
    shortAnswer: 'This is the hot key problem: hash-based sharding routes all requests for that one key to a single Redis node, so cluster-wide capacity is irrelevant — one node is saturated. Fix by replicating that key\'s value across multiple nodes (or into an in-process local cache on each app server) and spreading reads across the copies, ideally with automatic hot-key detection rather than a manual list.',
    detailedAnswer: 'Standard consistent-hashing or hash-slot sharding maps a key deterministically to one shard, which is exactly what you want for even distribution — until one key\'s traffic is wildly disproportionate. Adding more Redis nodes does nothing because the hot key still lives on exactly one of them. The fix has to break the "one key, one node" assumption for hot keys specifically: replicate the value to N nodes and route reads round-robin or by client hash across the replicas (accepting eventual consistency between replicas on writes), or push it up a level into per-instance local memory (in-process cache) so most reads never leave the app server. Detection matters too — either track access frequency and promote keys crossing a threshold automatically, or accept a manual allowlist for known-hot entities like a trending profile.',
    keyPoints: [
      'Root cause: hash-based sharding pins one key to one shard regardless of cluster size',
      'Adding nodes does not help — the hot key stays on one node',
      'Fix: replicate the hot value across nodes/instances and spread reads, or cache it locally in-process',
      'Needs some hot-key detection (frequency tracking or manual allowlist) to know which keys to treat specially',
    ],
    seniorFollowUps: [
      'How would you detect a key becoming hot in near-real-time without adding significant overhead to every request?',
      'What consistency problem does replicating the key across nodes introduce, and how would you bound it?',
    ],
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['hot-key', 'sharding', 'redis'],
  },
  {
    id: 'sd-cache-q3',
    question: 'Compare write-through and write-behind caching for a system recording user activity events at very high write volume, where losing a few seconds of recent events on a crash is acceptable but write latency directly affects user-facing responsiveness.',
    shortAnswer: 'Write-behind fits better here: it acknowledges the write as soon as the cache is updated and flushes to the database asynchronously in batches, minimizing write latency and reducing database load through batching — at the cost of losing acknowledged-but-unflushed writes if the cache node crashes, which the stated requirements tolerate. Write-through would add database latency to every write for a durability guarantee this use case doesn\'t need.',
    detailedAnswer: 'The deciding factor is the durability requirement: write-through makes cache and database updates part of one synchronous operation, so nothing is ever acknowledged without being durably persisted, but every write pays both the cache and database write latency plus whatever contention exists on the database at that moment. Write-behind decouples the two: the cache write is fast and immediately acknowledged, and the database write is deferred and can be batched (e.g. flush every 500ms or every N events), which both lowers latency and reduces the number of discrete database operations under high volume. The tradeoff is a durability window — anything acknowledged but not yet flushed is lost if the cache node dies. Given the stated tolerance for losing a few seconds of recent events, write-behind is the right choice; if this were financial transaction data instead of activity events, write-through (or a durable log) would be non-negotiable.',
    keyPoints: [
      'Write-through: synchronous, fully durable per write, pays full write latency every time',
      'Write-behind: async flush, lower latency, can batch database writes, but has a durability gap',
      'Choice hinges entirely on the stated durability tolerance, not on general "which is better"',
    ],
    seniorFollowUps: ['How would you bound the durability window in write-behind without giving up the latency win entirely?'],
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['write-through', 'write-behind', 'durability'],
  },
  {
    id: 'sd-cache-q4',
    question: 'In a cache-aside setup, why is it generally safer to delete a cache entry on write rather than update it in place with the new value?',
    shortAnswer: 'Updating in place is vulnerable to a race: a concurrent read that started loading the old value from the database before the write happened can finish and overwrite the cache after the write\'s update, leaving stale data cached indefinitely (or until TTL). Deleting forces the next read to reload fresh from the database, which is a much narrower and self-correcting window.',
    seniorFollowUps: ['Does deleting fully eliminate the race, or just shrink the window?'],
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['cache-aside', 'invalidation', 'race-condition'],
  },
  {
    id: 'sd-cache-q5',
    question: 'A CDN is caching an API response that should differ by user\'s language preference, but users are intermittently seeing the wrong language. What\'s the most likely misconfiguration?',
    shortAnswer: 'The cache key almost certainly doesn\'t include the Accept-Language header (or an equivalent Vary header isn\'t set), so the CDN treats requests for different languages as the same cache entry and serves whichever version was cached first to everyone.',
    detailedAnswer: 'CDNs and shared caches key responses primarily by URL by default; any other request dimension that changes the response body must be explicitly folded into the cache key or declared via a Vary header so the cache knows to store separate variants. If Accept-Language affects the response but isn\'t part of the key/Vary, the first request in a given language populates the shared cache entry and every subsequent request — regardless of its own language header — gets that cached version until it expires. The fix is adding Vary: Accept-Language (or normalizing to a small fixed set of language buckets in the key, since raw Accept-Language values can be too granular and fragment the cache badly).',
    keyPoints: [
      'Root cause: cache key/Vary header doesn\'t account for the dimension that varies the response',
      'First cached version "wins" and is served to all subsequent requests regardless of their actual header value',
      'Fix: Vary: Accept-Language, ideally normalized to a small bucket set to avoid over-fragmenting the cache',
    ],
    seniorFollowUps: ['What\'s the hit-rate cost of adding Vary: Accept-Language naively versus normalizing to a fixed language list first?'],
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['cdn', 'cache-key', 'vary-header'],
  },
  {
    id: 'sd-cache-q6',
    question: 'When would you choose LFU eviction over LRU for an application cache?',
    shortAnswer: 'LFU fits when a stable set of keys is consistently hot and you want to protect them from being evicted by a burst of one-off accesses (e.g. a batch job or scan touching many cold keys once) — LRU would treat that scan as "most recent" and evict the genuinely hot keys to make room. LFU needs frequency decay, though, or historically-hot keys never get evicted even after they stop being accessed.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['lfu', 'lru', 'eviction'],
  },
  {
    id: 'sd-cache-q7',
    question: 'Why is negative caching (caching a "not found" result) important for an endpoint that looks up user profiles by ID, and what\'s the main risk in setting its TTL too long?',
    shortAnswer: 'Without it, repeated requests for an ID that doesn\'t exist (bot scanning, a stale client retrying, a broken link) force a full database lookup every time, since a cache-aside miss on a genuinely-missing key never gets populated. Caching the negative result with a short TTL avoids that. Setting the TTL too long risks masking a resource that was just created — clients would keep getting a cached "not found" even after the record exists.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['negative-caching', 'ttl'],
  },
  {
    id: 'sd-cache-q8',
    question: 'What\'s the practical difference between Cache-Control: no-cache and Cache-Control: no-store?',
    shortAnswer: 'no-cache allows storing the response but requires revalidating with the origin (e.g. via ETag) before reusing it; no-store forbids caching it at all, anywhere. no-cache is for content that might be reused once confirmed still fresh, no-store is for genuinely sensitive or always-changing content.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Intermediate',
    seniority: 'Mid',
    tags: ['http-headers', 'cache-control'],
  },
  {
    id: 'sd-cache-q9',
    question: 'Why doesn\'t adding an in-process (local) cache on every application instance guarantee consistent reads across instances, even with the same TTL configured on each?',
    shortAnswer: 'Each instance populates and expires its local cache independently based on when it happens to see a miss, so at any given moment different instances can be holding different ages of the same key — one might have just refreshed while another is about to expire. There\'s no shared invalidation signal between them the way a single distributed cache provides.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['in-process-cache', 'consistency'],
  },
  {
    id: 'sd-cache-q10',
    question: 'What does an origin shield do in a CDN setup, and what problem does it prevent?',
    shortAnswer: 'An origin shield is a single edge tier positioned between all other edge nodes and the origin server, so that a cache miss at any edge location routes through the shield rather than directly to origin. It prevents every edge node worldwide from independently missing and hitting origin simultaneously for the same popular content, which would otherwise cause an origin-side stampede.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['cdn', 'origin-shield'],
  },
  {
    id: 'sd-cache-q11',
    question: 'Is a CDN a good fit for caching a logged-in user\'s personalized dashboard response?',
    shortAnswer: 'Generally no — the response varies per user, so caching it at a shared edge layer either fragments the cache into near-zero hit rate (one entry per user) or, worse, risks leaking one user\'s data to another if the cache key doesn\'t perfectly isolate by identity. Personalized responses are usually better served uncached at the edge (or cached only in a private, per-user store) with the CDN handling just the static assets on the page.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['cdn', 'personalization'],
  },
  {
    id: 'sd-cache-q12',
    question: 'What is cache warming and when would you use it?',
    shortAnswer: 'Cache warming is proactively loading expected-hot data into a cache before real traffic arrives, rather than waiting for organic misses to populate it. It\'s typically used before a deploy or scale-out event, so new instances or a freshly-restarted cache don\'t all take a burst of cold-cache database hits at once.',
    topic: 'system-design',
    subtopic: 'sd-caching',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['cache-warming'],
  },

  // ── Messaging Questions ──────────────────────────────────────────
{
    id: 'sd-msg-q1',
    question: 'A team building a video encoding pipeline wants each uploaded video processed by exactly one worker, but a notifications team wants to know about every upload too. How would you wire the messaging so both needs are met without one team\'s consumers stealing the other\'s messages?',
    shortAnswer: 'Publish upload events to a pub/sub topic; have the encoding workers consume as one consumer group (so partitions/messages are split across them, each processed once) and have notifications consume as a separate consumer group (so it independently gets a full copy of every event). One topic, two groups, each group internally competing.',
    detailedAnswer: 'This is the classic hybrid pattern: pub/sub at the topic level for fan-out to different teams/services, point-to-point within each team\'s own consumer group for load-balanced processing. If you instead put both teams\' workers in the same consumer group, they would compete for the same messages and each upload would only be handled by one of the two teams\' logic — a real bug pattern when someone underestimates the difference between "group" and "consumer count".',
    keyPoints: [
      'Consumer group boundary determines competing vs independent consumption',
      'Adding a new interested team later requires zero changes to the producer or existing consumers',
      'Partition count still caps parallelism within each group',
    ],
    seniorFollowUps: [
      'What happens to ordering guarantees if the encoding workers scale from 3 to 20 instances?',
      'How would you handle a video that consistently crashes every encoding worker that picks it up?',
    ],
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['pubsub', 'consumer-groups', 'fanout'],
  },
  {
    id: 'sd-msg-q2',
    question: 'Your payment-processing consumer occasionally double-charges customers even though the queue guarantees at-least-once delivery and you never see errors in the logs. What is the most likely mechanism, and how do you fix it without weakening the delivery guarantee?',
    shortAnswer: 'At-least-once delivery means duplicates are expected, not a bug — a consumer crash (or a slow ack under a tight visibility timeout) after the charge succeeds but before the ack is sent causes redelivery of an already-processed message. Fix it by making the charge operation idempotent, e.g. dedupe on a client-supplied idempotency key stored atomically with the charge, rather than trying to eliminate duplicates at the transport layer.',
    detailedAnswer: 'Weakening to at-most-once would trade double-charges for silently dropped charges, which is worse. The right fix keeps at-least-once semantics and makes the business logic tolerant of duplicates: an idempotency key (often the message ID or an explicit client-generated key) checked and recorded in the same transaction as the charge itself. If the dedup check and the charge aren\'t atomic, you\'ve just moved the race condition rather than closed it.',
    keyPoints: [
      'Duplicates are inherent to at-least-once, not a failure to fix at the queue level',
      'Idempotency key must be checked and written atomically with the side effect',
      'Visibility timeout misconfiguration is a common amplifier — check whether it\'s shorter than worst-case processing time',
    ],
    seniorFollowUps: [
      'How would you design the idempotency key if the client can also legitimately retry the same logical charge with a new request?',
    ],
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['idempotency', 'at-least-once', 'delivery-guarantees'],
  },
  {
    id: 'sd-msg-q3',
    question: 'A vendor advertises "exactly-once processing" for their managed queue. What follow-up question should you ask before relying on that claim for a system that calls an external payment API on each message?',
    shortAnswer: 'Ask whether the exactly-once guarantee extends past the vendor\'s own system boundary to your external side effect. Almost always it doesn\'t — the guarantee covers internal writes (e.g. offset commits, transactional produces), not calls to a separate system like a payment API, so you still need to make that external call idempotent yourself.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['exactly-once', 'delivery-guarantees'],
  },
  {
    id: 'sd-msg-q4',
    question: 'You partition a Kafka topic by user ID to keep each user\'s events in order. Six months later you need more throughput and double the partition count. What breaks?',
    shortAnswer: 'Doubling partitions changes the hash-to-partition mapping for most keys, so a given user\'s events before and after the change can land on different partitions — you lose the guarantee that all of a user\'s historical and future events are ordered relative to each other on one partition. In-flight ordering within the new partition layout is fine going forward, but you can\'t assume continuity with pre-change history.',
    detailedAnswer: 'Partition count is effectively a fixed design decision at topic-creation time for any use case relying on per-key ordering. Repartitioning is usually done by creating a new topic with the desired partition count and migrating, rather than resizing in place, precisely to avoid silently breaking the key-to-partition invariant consumers depend on.',
    keyPoints: [
      'Partition assignment is typically hash(key) mod partition_count — changing count changes the mapping',
      'Only ordering within a single partition is ever guaranteed, never across a repartitioning event',
      'Standard fix is topic recreation/migration, not in-place resize',
    ],
    seniorFollowUps: [
      'How would you migrate to a new partition count with zero downtime and no lost ordering guarantees for active users?',
    ],
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['ordering', 'partitioning', 'kafka'],
  },
  {
    id: 'sd-msg-q5',
    question: 'When would you choose a strict global FIFO queue over a partitioned system with per-key ordering, given the throughput cost?',
    shortAnswer: 'When the workload is genuinely low-volume and correctness depends on a single total order across all messages, not just order within a logical entity — e.g. a small queue of infrastructure commands that must apply in sequence. If throughput matters or ordering is really only needed per-entity (per user, per order), partition-level ordering gets you both scale and the ordering you actually need.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['ordering', 'fifo', 'tradeoffs'],
  },
  {
    id: 'sd-msg-q6',
    question: 'A consumer group\'s processing rate suddenly needs to double. You have a 10-partition topic already at 10 consumers, one per partition. What are your actual options?',
    shortAnswer: 'Adding an 11th consumer does nothing — partition count caps parallelism, so idle consumers just sit unused. Real options: increase the topic\'s partition count (with the ordering-continuity caveat that comes with it), speed up each consumer\'s per-message processing (e.g. offload slow work asynchronously), or split the work differently so more of it can run in parallel per partition.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['consumer-groups', 'scaling', 'partitioning'],
  },
  {
    id: 'sd-msg-q7',
    question: 'What is a visibility timeout, and what specifically goes wrong if you set it too short relative to actual processing time?',
    shortAnswer: 'A visibility timeout is how long a queue hides a message from other consumers after it\'s been picked up, waiting for an ack before assuming the consumer died and redelivering. If it\'s shorter than real processing time, a still-working consumer\'s message gets redelivered to another consumer while the first is still processing it, causing duplicate (and possibly concurrent) processing of the same message.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['visibility-timeout', 'ack', 'delivery-guarantees'],
  },
  {
    id: 'sd-msg-q8',
    question: 'In a strict-ordering point-to-point queue, one malformed message keeps failing and being redelivered. What\'s the operational impact if there\'s no dead letter queue configured, and how does a DLQ fix it?',
    shortAnswer: 'Without a DLQ, the poison message gets retried indefinitely; in a FIFO/ordered queue it also blocks every message queued behind it since order must be preserved, stalling the whole queue rather than just wasting one worker\'s time. A DLQ removes the message after a max-retry threshold, routing it aside so processing of everything else can continue, at the cost of needing someone to monitor and handle the DLQ separately.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['dead-letter-queue', 'poison-message', 'ordering'],
  },
  {
    id: 'sd-msg-q9',
    question: 'Your producers can generate messages faster than consumers can process them during a traffic spike, and the queue has no depth limit. Why is that a problem even if nothing is failing yet?',
    shortAnswer: 'An unbounded queue lets memory (or disk) usage on the broker grow without limit as backlog accumulates, and it hides the growing consumer lag from anyone not actively watching it — by the time the broker runs out of resources or lag becomes user-visible, the problem is already large. Bounding the queue and applying backpressure (blocking or shedding producers) surfaces and contains the problem immediately instead of deferring it into an outage.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['backpressure', 'bounded-queue', 'consumer-lag'],
  },
  {
    id: 'sd-msg-q10',
    question: 'What is the practical difference between at-most-once and at-least-once delivery in terms of where a consumer acks relative to processing?',
    shortAnswer: 'At-most-once acks (or removes the message) before or during processing, so a crash after the ack loses the message forever. At-least-once acks only after processing completes successfully, so a crash before the ack causes redelivery and a possible duplicate — data is never silently lost, but consumers must tolerate duplicates.',
    topic: 'system-design',
    subtopic: 'sd-messaging',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['delivery-guarantees', 'ack'],
  },

  // ── Resilience Patterns Questions ─────────────────────────────
{
    id: 'sd-resil-q1',
    question: 'A downstream recommendation service starts responding slowly (not erroring, just slow) instead of failing outright. Walk through what happens to the calling service with no resilience patterns in place, and what you\'d add.',
    shortAnswer: 'Without a timeout, calling threads block waiting on slow responses and pile up until the caller\'s own thread/connection pool is exhausted, taking down unrelated calls too. Fixes: an explicit timeout so calls fail fast, a circuit breaker to stop calling once failures cross a threshold, a bulkhead so this dependency\'s pool is isolated from others, and a fallback (cached/default recommendations) so the caller degrades gracefully instead of erroring.',
    detailedAnswer: 'Slow-but-not-erroring is actually the worse failure mode than a clean error, because nothing tells the caller to stop trying — it just ties up resources waiting. The chain of failure: no timeout means threads block indefinitely; a shared thread pool means those blocked threads starve calls to unrelated healthy dependencies; no circuit breaker means every new request keeps trying against the slow dependency instead of failing fast; no fallback means users see errors instead of degraded-but-functional recommendations. Layering timeout -> bulkhead -> circuit breaker -> fallback addresses each stage in order.',
    keyPoints: ['Slow is often worse than down — it doesn\'t trigger fast failure on its own', 'Resource exhaustion in a shared pool is the real cascading risk, not just this one dependency\'s failure', 'Each pattern addresses a different stage of the same failure'],
    seniorFollowUps: ['If you can only add one of these patterns today, which do you pick and why?', 'How would you validate the timeout value you chose is actually right in production?'],
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['circuit-breaker', 'bulkhead', 'timeout', 'cascading-failure'],
  },
  {
    id: 'sd-resil-q2',
    question: 'Three services call each other in a chain (A -> B -> C), and each layer independently retries failed calls 3 times with no coordination. C degrades under load. What happens, and how do you fix it without removing retries entirely?',
    shortAnswer: 'Retries compound multiplicatively down the chain — a single user request from A can become up to 9x load on C through B\'s retries alone (and more with A\'s own retries on top), turning a partial C degradation into a full outage. Fix with retry budgets (cap total retry traffic as a percentage, not per-call), restrict retries to one layer (typically the edge) while inner layers fail fast, and circuit breakers at each hop so a struggling C gets protected quickly instead of amplified.',
    detailedAnswer: 'This is the retry-storm/amplification problem. The naive intuition that "retrying is more resilient" is locally true but globally false in a chain, because each layer\'s retry logic has no visibility into the fact that layers above and below are also retrying the same underlying failure. The fix isn\'t to remove retries — transient blips are real and retrying helps — it\'s to coordinate: pick one layer to own retries (usually the outermost, closest to the user, since that\'s the layer that actually needs the eventual success), let inner layers propagate failure fast, use retry budgets so total retry volume is capped as a fraction of overall traffic regardless of how many callers are retrying, and put circuit breakers ahead of C so once its failure rate crosses a threshold, calls stop reaching it at all rather than getting amplified retries.',
    keyPoints: ['Amplification is multiplicative across hops, not additive', 'The failure looks like it\'s "C\'s fault" but the retries are what turn degradation into outage', 'Retry budgets and single-layer retry ownership are the standard fixes'],
    seniorFollowUps: ['How would you detect this pattern is happening in production before it fully cascades?', 'Does adding jitter to each layer\'s backoff fix this problem, or just a different one?'],
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['retry-storm', 'cascading-failure', 'call-chain'],
  },
  {
    id: 'sd-resil-q3',
    question: 'Why does a circuit breaker protect the failing dependency, not just the caller?',
    shortAnswer: 'Once open, the breaker stops sending traffic to the dependency entirely, which removes load from a system that\'s already struggling — continuing to hit it at full volume while it\'s failing (or during recovery) can prevent it from ever catching up, especially if the failure is load-related in the first place.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['circuit-breaker'],
  },
  {
    id: 'sd-resil-q4',
    question: 'What determines whether it\'s safe to automatically retry a failed request?',
    shortAnswer: 'Whether the operation is idempotent — repeating it must produce the same end state as doing it once. Reads and properly designed idempotent writes (e.g. via a client-supplied idempotency key the server dedupes on) are safe to retry; a plain POST without one is not, since a timeout doesn\'t tell you whether the original write already applied.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['idempotency', 'retry'],
  },
  {
    id: 'sd-resil-q5',
    question: 'You set a network call timeout to 30 seconds "to be safe." What\'s wrong with that reasoning, and what should drive the actual number?',
    shortAnswer: 'A 30s timeout means a caller thread can sit blocked for 30 seconds on one bad call, which at any real traffic volume exhausts the caller\'s own connection/thread pool long before 30 seconds is up. The timeout should be driven by the dependency\'s actual observed latency (a small multiple of p99 or p99.9), not a round, arbitrary number — and it should get tighter, not looser, as you move deeper into a multi-hop call chain since the budget has to be shared across hops.',
    detailedAnswer: 'The instinct behind "be safe, set it high" backwards: a long timeout doesn\'t protect you, it just delays the failure while consuming resources the whole time. The right approach starts from the dependency\'s real latency distribution — set the timeout as a small multiple of p99 or p99.9 latency so it rarely fires on healthy traffic but fires fast on genuinely stuck calls. In a call chain, the total time budget for a user request is fixed, so each hop\'s timeout has to be a fraction of what\'s left, meaning deeper hops need tighter timeouts, not the same or looser ones. Too tight has its own cost though — it turns healthy-but-momentarily-slow responses into retries, adding load exactly when a dependency is under pressure — so it\'s a genuine tuning tradeoff, not a "smaller is always better" rule.',
    keyPoints: ['A long timeout delays resource exhaustion, it doesn\'t prevent it', 'Base the value on observed p99/p99.9 latency, not intuition', 'Timeout budget should shrink going deeper into a call chain'],
    seniorFollowUps: ['How would you handle a dependency whose latency is bimodal — usually fast, occasionally very slow for legitimate reasons?'],
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['timeout', 'latency'],
  },
  {
    id: 'sd-resil-q6',
    question: 'What\'s the difference between load shedding and rate limiting, and when would you reach for each?',
    shortAnswer: 'Rate limiting enforces a per-client quota for fairness or abuse prevention, applied regardless of overall system health. Load shedding is a system-wide self-protection decision — reject some fraction of requests, from any client, because the service itself is near capacity right now. You can be well under every client\'s rate limit and still need to shed load if the aggregate is too high for current capacity.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['load-shedding', 'rate-limiting'],
  },
  {
    id: 'sd-resil-q7',
    question: 'A team adds a fallback that serves cached data whenever the primary dependency call fails, and considers the resilience work done. What\'s the gap?',
    shortAnswer: 'A fallback with no expiry or staleness signal can mask a dependency being down for a long time — users silently get old data indefinitely instead of anyone noticing and fixing the outage. It also needs to distinguish dependencies where stale-but-plausible data is fine (recommendations) from ones where it\'s actively wrong to fake success (payment authorization) — those should fail loudly instead.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['fallback', 'graceful-degradation'],
  },
  {
    id: 'sd-resil-q8',
    question: 'Why add jitter to exponential backoff instead of just increasing the base delay?',
    shortAnswer: 'A larger fixed delay still synchronizes every client that failed at roughly the same time into a retry spike at roughly the same later time — it just moves the spike, it doesn\'t spread it out. Jitter randomizes each client\'s actual retry time within the backoff window, so retries arrive spread across time instead of as a synchronized burst.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['backoff', 'jitter', 'retry'],
  },
  {
    id: 'sd-resil-q9',
    question: 'A service has one shared thread pool for all outbound calls. What failure mode does the bulkhead pattern specifically prevent that a circuit breaker alone does not?',
    shortAnswer: 'A circuit breaker trips based on a dependency\'s own failure rate, but if that dependency is merely slow rather than failing outright, the breaker may not trip while its calls still occupy threads long enough to starve the shared pool and block calls to unrelated, healthy dependencies. Bulkheads fix this by giving each dependency its own resource pool, so a slow dependency can only ever exhaust its own allocation.',
    topic: 'system-design',
    subtopic: 'sd-resilience-patterns',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['bulkhead', 'circuit-breaker', 'resource-isolation'],
  },

  // ── Microservices Questions ──────────────────────────────────────
{
    id: 'sd-micro-q1',
    question: 'A startup with one small engineering team is designing a new product from scratch and wants to start with microservices "so it scales later." What would you push back on?',
    shortAnswer: 'Push back on paying the distributed-systems tax before the domain boundaries are known and before there is a team structure that benefits from independent deployability. A monolith-first approach lets them discover real seams under actual usage, then extract services once a boundary proves stable — extracting late is far cheaper than un-splitting a wrong boundary drawn on day one.',
    detailedAnswer: 'The stated justification is scale, but scale is rarely the actual bottleneck for a new product with a small team — the real cost microservices impose upfront is operational: multiple deploy pipelines, service discovery, distributed tracing, and cross-service debugging, none of which the team has road-tested yet. Worse, service boundaries drawn before the domain is understood are usually wrong, and fixing a wrong boundary means a network-level refactor instead of a code-level one. The monolith-first argument says: build a modular monolith with enforced internal boundaries, let the domain reveal itself under real traffic and real feature requests, then extract services once a module has proven it changes independently and deploys on its own cadence. The counter-case is a genuinely large, multi-team org from day one where Conway\'s Law pressure already exists — but a small team building a new product is exactly the case where waiting costs little and guessing costs a lot.',
    keyPoints: [
      'Scale alone does not justify the split — operational overhead is paid regardless of request volume',
      'Boundaries drawn before the domain is proven are usually wrong, and wrong boundaries are expensive to fix across a network',
      'Monolith-first: build modular, extract once boundaries are proven stable',
      'Exception: a genuinely multi-team org from day one, where org-alignment pressure already exists',
    ],
    seniorFollowUps: [
      'How would you structure the monolith\'s internal module boundaries so a future extraction is cheap?',
      'What signal would tell you a module is actually ready to be extracted into its own service?',
    ],
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['monolith-first', 'decomposition', 'tradeoffs'],
  },
  {
    id: 'sd-micro-q2',
    question: 'A system has 15 microservices, but any given feature deploy still requires coordinating a synchronized release across 8 of them, in a specific order. What is actually going on here, and what would you look for to diagnose it?',
    shortAnswer: 'This is a distributed monolith: the services are deployed separately but not actually independent — deploy-order coupling means a hard dependency chain exists underneath the service boundaries. Look for synchronous call chains that must all succeed together, or a shared database several of these services are reading and writing directly.',
    detailedAnswer: 'The tell is the deploy-order requirement itself — true independent deployability means any service can ship on its own schedule without breaking others, so a required synchronized release across 8 services proves that promise is broken. The two usual root causes: a synchronous request chain where a caller directly depends on a callee\'s exact contract shipping first, or a shared database/shared schema that several services read and write, so a schema change in one breaks a query in another. The fix is not to abandon microservices, it is to find where the coupling actually lives and break it — introduce backward-compatible API versioning and independent contract evolution for the sync-chain case, or split the shared database and force cross-service reads through an API or event stream for the shared-DB case. If neither is fixable without heavy rework, the honest conclusion may be that these particular services were never real boundaries and should be merged back.',
    keyPoints: [
      'Deploy-order coupling is the diagnostic signal for a distributed monolith',
      'Root cause #1: synchronous call chains with hard version dependencies',
      'Root cause #2: shared database across services',
      'Sometimes the right fix is admitting the boundary was wrong and merging services back',
    ],
    seniorFollowUps: [
      'How would you migrate a shared-database coupling to database-per-service without a big-bang cutover?',
      'What contract-versioning practice would let two services deploy independently even with a synchronous dependency between them?',
    ],
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['distributed-monolith', 'anti-pattern', 'coupling'],
  },
  {
    id: 'sd-micro-q3',
    question: 'Why is "database-per-service" considered close to non-negotiable in microservices design, and what does a team give up by enforcing it?',
    shortAnswer: 'It is what makes independent deployability real — without it, a schema change in one service can silently break another service\'s queries, re-coupling services that appear independent in the deploy pipeline. The cost is that cross-service reads that used to be a SQL join now require an API call, event-driven replication, or a deliberately duplicated read model — all of which introduce eventual consistency.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['database-per-service', 'data-ownership'],
  },
  {
    id: 'sd-micro-q4',
    question: 'Two services need data that used to live in one table via a join — Orders needs current pricing that Inventory owns. In a database-per-service world, what are the realistic options, and what do they trade off?',
    shortAnswer: 'Options: (1) Orders calls Inventory\'s API synchronously per request — simplest, but adds latency and a runtime dependency on Inventory\'s availability. (2) Inventory publishes pricing-change events and Orders keeps a local read-optimized copy — decouples availability and latency, at the cost of eventual consistency (Orders can serve stale pricing briefly). (3) A shared read-only replica or data warehouse for reporting-style access, not live transactional reads. The right choice depends on how stale a price Orders can tolerate and whether Orders can function if Inventory is briefly down.',
    detailedAnswer: 'This is the fundamental cost of database-per-service: a query that used to be free (a join in one transaction) now has to cross a service boundary, and every option for doing that has a real tradeoff, not a free lunch. Synchronous API calls keep data fresh but couple Orders\' availability and latency to Inventory\'s — if Inventory is slow or down, Orders either blocks or needs a fallback. Event-driven replication (Inventory publishes changes, Orders subscribes and maintains its own local pricing cache) decouples runtime availability entirely but introduces a staleness window, and now Orders owns a derived copy of data it does not consider itself the source of truth for, which needs its own consistency handling if events are missed or reordered. A shared reporting store is fine for analytics but should never be the path a live request depends on, since it reintroduces the shared-database coupling problem. Seniority in this answer shows in recognizing that "how stale can Orders tolerate" is a business question, not a technical one — that answer picks the pattern, not vice versa.',
    keyPoints: [
      'Synchronous API call: freshest data, but couples Orders\' availability/latency to Inventory',
      'Event-driven local copy: decouples availability, introduces eventual consistency and staleness window',
      'Shared reporting store: fine for analytics, not for live request paths',
      'The staleness tolerance is a business decision that determines which pattern fits',
    ],
    seniorFollowUps: [
      'If Orders\' local pricing copy misses an event due to a consumer outage, how would you detect and recover from the drift?',
      'How does this decision interact with whether Orders and Inventory are allowed to be briefly inconsistent from a business-correctness standpoint (e.g. can an order be placed at a stale price)?',
    ],
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['database-per-service', 'eventual-consistency', 'data-ownership'],
  },
  {
    id: 'sd-micro-q5',
    question: 'A team decomposes their monolith into a "presentation service", a "business logic service", and a "data access service". What is wrong with this split?',
    shortAnswer: 'It decomposed by technical layer instead of business capability, so any single feature (e.g. placing an order) still has to touch all three services in sequence. They get the full network, deployment, and operational overhead of microservices without the independent-deployability benefit, since the three layers still change together for almost every feature.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['decomposition', 'bounded-context'],
  },
  {
    id: 'sd-micro-q6',
    question: 'How does Conway\'s Law argue against splitting a service that is owned end-to-end by a single small team, even if the code has a clean internal seam?',
    shortAnswer: 'Conway\'s Law predicts architecture mirrors communication structure — if one team owns both sides of a proposed split, there is no organizational seam for the service boundary to relieve. The team pays the full cost (network calls, two deploy pipelines, two on-call surfaces) while gaining no team-autonomy benefit, since the same people already coordinate freely within themselves.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['conways-law', 'org-design'],
  },
  {
    id: 'sd-micro-q7',
    question: 'What does the "inverse Conway maneuver" mean in practice, and why would an organization deliberately restructure teams before restructuring a system?',
    shortAnswer: 'Instead of letting architecture passively mirror whatever team structure already exists, an org restructures teams to match the desired future service boundaries first, then lets the software architecture follow the new communication paths. It is used when the current org structure would otherwise force the wrong boundaries onto a new architecture regardless of the technical design.',
    detailedAnswer: 'Conway\'s Law is usually described as an observation, but the inverse maneuver treats it as a lever: if you want a target architecture of N independently-owned services, form N teams with clear ownership first, because left alone the org\'s existing communication structure (reporting lines, who talks to whom daily) will reassert itself in the software regardless of an architecture diagram. This matters concretely during a monolith-to-microservices migration — if the org keeps one team responsible for the entire domain, that team will keep coordinating tightly across whatever service lines get drawn, and the "microservices" will drift into a distributed monolith over time even if the initial split was clean, because the people maintaining it still work and communicate as one unit. Restructuring teams first is disruptive and political, which is why it is often skipped, but skipping it is a common reason technically sound service boundaries erode back into tight coupling within a year or two.',
    keyPoints: [
      'Team structure precedes and predicts architecture, not the other way around, if left unmanaged',
      'Restructuring teams first is a deliberate lever to make a target architecture stick',
      'Skipping it is a common reason clean service boundaries erode into a distributed monolith over time',
    ],
    seniorFollowUps: [
      'What signals in day-to-day team behavior would tell you a "clean" service split is starting to erode back toward tight coupling?',
    ],
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['conways-law', 'org-design', 'team-topology'],
  },
  {
    id: 'sd-micro-q8',
    question: 'Why use the strangler fig pattern instead of a full rewrite when migrating a large legacy monolith to microservices?',
    shortAnswer: 'A full rewrite is high-risk on a system large enough to need decomposition in the first place — it delays all value until the entire rewrite ships and offers no way to validate correctness incrementally. Strangler fig routes one capability at a time to a new service behind a facade while the monolith keeps serving everything else, so each extraction can be validated in production, and a bad boundary can be routed back with limited blast radius.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['strangler-fig', 'migration'],
  },
  {
    id: 'sd-micro-q9',
    question: 'When is a monolith actually the better long-term choice, not just a starting point to migrate away from?',
    shortAnswer: 'When the domain is small enough or stable enough that one or two teams can own the whole thing without stepping on each other, and when the operational maturity (deployment automation, observability, on-call practices) needed to run many services safely does not exist yet. Independent scaling and tech heterogeneity only pay off when different parts of the system actually have different scaling profiles or genuinely need different tech stacks — absent that, a monolith with clean internal modules gets most of the maintainability benefit without the network tax.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['monolith', 'tradeoffs'],
  },
  {
    id: 'sd-micro-q10',
    question: 'A mobile app and a web app both consume the same set of 10 backend microservices, and both clients complain about slow load times from making many round trips. How does a BFF address this, and what would you be careful about when introducing one?',
    shortAnswer: 'A BFF aggregates the fan-out — it calls the 10 services server-side (often in parallel, closer to the services on the network) and returns one shaped response per client, cutting client-side round trips and letting each client get a payload tailored to its needs (mobile trimmed, web fuller). The care point is scope creep: a BFF should stay a thin aggregation/shaping layer owned by the frontend team, not accumulate business logic that belongs in the domain services, or it becomes another monolith in disguise.',
    detailedAnswer: 'The underlying problem is that microservice decomposition, done well for the backend\'s sake, pushes composition cost onto whoever consumes the services — a client needing data from 6 services either makes 6 round trips (slow, especially over mobile networks) or the backend needs to do that composition for them. A BFF solves this by sitting server-side, close to the services, aggregating and reshaping responses per client type, and is usually owned by the team that owns that client, which also fits the general org-alignment argument for microservices (the mobile team controls its own BFF\'s release cadence independent of the web team\'s). The risk worth calling out at a senior level: a BFF that starts as thin aggregation tends to accumulate business logic over time (since it is a convenient place to put it) and can become a second monolith, or a single point where every client\'s failure gets tied to one aggregation layer\'s availability — so it needs its own resilience thinking, and its logic should stay strictly presentation/aggregation, not domain rules.',
    keyPoints: [
      'BFF cuts client-side round trips by aggregating multiple service calls server-side',
      'Lets each client type get a differently shaped response without forcing one generic API to compromise',
      'Risk: logic creep turning the BFF into a second monolith',
      'Usually owned by the frontend team it serves, consistent with the org-autonomy argument for microservices',
    ],
    seniorFollowUps: [
      'If the BFF itself becomes a single point of failure for all clients, how would you think about its own availability and resilience?',
      'Where is the line between aggregation logic that belongs in a BFF versus domain logic that should stay in the owning service?',
    ],
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['bff', 'api-aggregation', 'client-composition'],
  },
  {
    id: 'sd-micro-q11',
    question: 'What is the difference between splitting services too finely and splitting them at the right granularity, and what heuristic would you use to catch over-fragmentation?',
    shortAnswer: 'Over-fine splitting (nanoservices) turns single business operations into long synchronous call chains, multiplying latency and failure surface for little benefit. A useful heuristic: if two services always change together and always deploy together, they are one service artificially split — merge them, since the split is adding network cost without adding independent deployability.',
    topic: 'system-design',
    subtopic: 'sd-microservices',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['granularity', 'decomposition'],
  },

  // ── Case Studies Questions ────────────────────────────────────────
{
    id: 'sd-case-q1',
    question: 'Design a URL shortener that must handle 100M new URLs/day and a 100:1 read:write ratio. Walk through your ID generation choice and why.',
    shortAnswer: 'At that write volume a single auto-increment counter is a bottleneck, so hand out pre-allocated ID ranges (e.g. blocks of 1M) to each app server from a lightweight coordinator, convert each ID to base62 for a ~7-character code, and back the read path with a consistent-hashed cache in front of a simple key-value mapping store, since reads dominate 100:1.',
    detailedAnswer: 'Range-based allocation avoids per-write coordination (unlike a shared counter) and avoids collision-check reads (unlike hash-and-truncate). Each app server checkpoints a block from a coordinator (e.g. an atomic increment in a small dedicated store) at startup and increments locally in memory until exhausted, then fetches the next block. Base62 encoding of the resulting 64-bit-ish counter keeps codes short. On the read side, apply a cache-aside layer with consistent hashing across cache nodes (URL popularity is heavily skewed, so a modest cache absorbs most traffic) backed by a plain key-value store keyed on short_code — no need for a complex schema since the access pattern is a single-key lookup.',
    keyPoints: [
      'Range/block allocation trades a small number of wasted IDs (if a server dies mid-block) for eliminating per-write coordinator round trips',
      'Base62 alphabet and target code length should be stated with numbers, not just asserted',
      'Custom aliases require a uniqueness check against the same short-code keyspace (unique index)',
    ],
    seniorFollowUps: [
      'How would you support user-chosen custom aliases without breaking the ID allocation scheme?',
      'How do you handle a server crashing with half of an allocated ID block unused — is that a real problem at this scale?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['url-shortener', 'id-generation', 'caching'],
  },
  {
    id: 'sd-case-q2',
    question: 'Should a URL shortener redirect use HTTP 301 or 302? What does each choice cost you?',
    shortAnswer: '302 (temporary) keeps every click routed through your server, which is required if you need click analytics or want to change the mapping later; 301 (permanent) lets browsers cache the redirect client-side and reduces server load, but you lose per-click visibility and cannot easily repoint the URL once browsers have cached it.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['url-shortener', 'http', 'redirects'],
  },
  {
    id: 'sd-case-q3',
    question: 'A social app is choosing between fan-out-on-write and fan-out-on-read for its news feed. What breaks each approach at scale, and what\'s the actual production answer?',
    shortAnswer: 'Fan-out-on-write (push) makes reads cheap but a post from a user with millions of followers triggers millions of writes; fan-out-on-read (pull) makes writes cheap but a user following thousands of accounts pays an expensive merge on every feed load. Production systems use a hybrid: push for normal users into precomputed follower inboxes, and pull-on-demand (merged at read time) for accounts above a follower-count threshold, so a celebrity post never triggers mass fan-out.',
    detailedAnswer: 'The hybrid threshold (e.g. accounts with >100K followers are treated as pull-only) is itself a tunable that requires tracking follower counts as a signal. On read, the client\'s feed service merges their precomputed push inbox with freshly-pulled posts from any celebrity accounts they follow, then runs both through the same ranking step so push and pull content interleave correctly. This also naturally solves ranking, since a unified scoring pass sees both sources at once rather than ranking two separately-assembled lists.',
    keyPoints: [
      'Push = write-amplification risk on hot accounts; pull = read-amplification risk on heavy followers',
      'Hybrid threshold on follower count is the standard real-world fix (this is roughly Twitter\'s documented approach)',
      'Merge-at-read must feed into a single ranking pass, not two separately ranked lists stitched together',
    ],
    seniorFollowUps: [
      'How would you pick the follower-count threshold, and what would you monitor to know it needs adjusting?',
      'How does this change if the product also needs a chronological (non-ranked) feed option?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['feed', 'fanout', 'celebrity-problem'],
  },
  {
    id: 'sd-case-q4',
    question: 'Your rate limiter runs identical logic on every API server, but clients are still exceeding the configured limit by roughly the number of servers behind the load balancer. What\'s wrong and how do you fix it?',
    shortAnswer: 'Each server is counting independently with no shared state, so a client hitting N different servers effectively gets N times the intended limit. Fix it by moving the counter into a shared, atomic store (typically Redis) keyed per client, using an atomic increment-plus-expire (or a Lua script) so concurrent requests from different servers cannot race past the limit.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['rate-limiter', 'distributed-systems', 'redis'],
  },
  {
    id: 'sd-case-q5',
    question: 'Your distributed rate limiter\'s Redis cluster becomes unreachable. What should happen to incoming traffic, and how do you decide?',
    shortAnswer: 'This is a fail-open vs fail-closed decision: fail-open lets all traffic through when the limiter is unreachable (protects availability, risks the backend being overwhelmed); fail-closed blocks all traffic (protects the backend, turns a limiter outage into a full outage for legitimate users). Most production systems fail-open for a rate limiter specifically, since an unavailable limiter is rare and short, and failing closed converts a minor availability blip into a total outage.',
    detailedAnswer: 'The decision should be explicit and stated as a tradeoff, not assumed. Failing open is usually correct for a rate limiter because its failure mode (occasional over-admission) is far less damaging than the failure mode of failing closed (total denial of service during a dependency outage) — this mirrors the general resilience-pattern guidance of degrading gracefully rather than cascading a failure. A common middle ground is a local, coarser fallback limiter (e.g. an in-memory approximate limit per server) that engages only while the shared store is unreachable, bounding the blast radius without a full open or full closed choice.',
    keyPoints: [
      'Fail-open protects availability at the cost of temporarily weaker limiting',
      'Fail-closed protects backend capacity but turns a dependency outage into a customer-facing outage',
      'A local fallback limiter is a reasonable middle ground during shared-store unavailability',
    ],
    seniorFollowUps: [
      'How would you detect and alert on the rate limiter silently fail-opening in production?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['rate-limiter', 'resilience', 'availability'],
  },
  {
    id: 'sd-case-q6',
    question: 'In a chat system, two messages sent to the same group chat arrive at the server out of send-order because of network jitter. How do you guarantee all participants see a consistent order?',
    shortAnswer: 'Do not trust client timestamps — assign a monotonically increasing sequence number per conversation at the server when a message is durably accepted, and have all clients render messages ordered by that server-assigned sequence, not by when they were sent or received.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['chat', 'ordering', 'consistency'],
  },
  {
    id: 'sd-case-q7',
    question: 'A chat message gets delivered twice to the same client after a brief disconnect and client retry. Is this a bug, and what should the design already account for?',
    shortAnswer: 'This is expected under an at-least-once delivery guarantee, not a bug — the fix is not "never redeliver" but client-side dedup: each message carries a client-generated idempotent ID, and the server (or client) drops duplicates it has already processed by that ID before assigning/displaying it again.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['chat', 'at-least-once', 'idempotency'],
  },
  {
    id: 'sd-case-q8',
    question: 'Would you shard chat message storage by conversation_id or by user_id? Justify it against the dominant access pattern, and explain how your choice behaves for a 5,000-member group chat.',
    shortAnswer: 'Shard by conversation_id: the dominant read is opening one conversation and scrolling its history, which becomes a single-shard range scan. Sharding by user_id (fanning out a copy of every message to every participant\'s own timeline) makes "unread across all chats" cheap but multiplies writes by group size — for a 5,000-member group, one message becomes 5,000 writes, which is the same fan-out blowup a feed system hits with celebrity accounts.',
    detailedAnswer: 'conversation_id-based sharding keeps a group\'s full message history colocated regardless of group size, so posting to a 5,000-member group is still a single write. The cost is that "show me all my unread messages across every conversation" now requires fanning a query out across however many conversation shards the user participates in, or maintaining a separate lightweight per-user unread-count index updated asynchronously — a much cheaper structure than duplicating full message bodies per user. This mirrors the exact write-vs-read tradeoff in feed fan-out, just applied to conversations instead of followers.',
    keyPoints: [
      'conversation_id sharding avoids per-message fan-out blowup for large groups',
      'user_id sharding trades that off for cheap cross-conversation unread queries, at write-multiplication cost',
      'A lightweight async unread-count index is the usual way to get user_id-style query cheapness without full per-user message duplication',
    ],
    seniorFollowUps: [
      'How would you keep per-user unread counts consistent if the count-update event is itself only delivered at-least-once?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Expert',
    seniority: 'Staff',
    tags: ['chat', 'sharding', 'storage-layout'],
  },
  {
    id: 'sd-case-q9',
    question: 'A notification event fans out to 3 channels (push, email, SMS) for 10 million users. A worker processing the email channel crashes and retries after a queue redelivery. What prevents 10 million duplicate emails from being sent twice?',
    shortAnswer: 'The queue guarantees at-least-once delivery, so redelivery after a crash is expected; a dedup key (event_id + user_id + channel) checked against a short-TTL store before actually calling the email gateway prevents the retried batch from resending to users already handled before the crash.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['notifications', 'dedup', 'at-least-once'],
  },
  {
    id: 'sd-case-q10',
    question: 'Design the high-level pipeline for a notification system that must fan out one triggering event to millions of devices across push, email, and SMS, respecting per-user preferences and provider rate limits.',
    shortAnswer: 'Publish one event to a queue; run a separate consumer pool per channel (push/email/SMS) that pulls from it, checks per-user preference/quiet-hours state before sending, batches calls to the third-party gateway where supported (e.g. one FCM multicast instead of one call per device), and routes permanently-failing sends to a dead-letter queue instead of blocking the pipeline.',
    detailedAnswer: 'Decoupling the trigger from delivery via a queue means the triggering service does not wait on slow, unreliable third-party gateways (APNs, FCM, SMTP, SMS providers), and each channel scales independently since email throughput and push throughput have very different bottlenecks. Preference and quiet-hours checks happen at the consumer, not baked into the event, so one event can respect different users\' differing opt-in states without needing to be republished per user. Batching (e.g. FCM multicast to 500 tokens per call) is a real throughput lever that separates a strong answer from one that naively calls the provider API once per device. A dead-letter queue isolates permanently-failing sends (bad token, unsubscribed address) so they cannot stall the main queue — the same resilience pattern used anywhere else a consumer can hit a poison message.',
    keyPoints: [
      'Queue decouples trigger from slow/unreliable third-party delivery, and lets each channel scale independently',
      'Preference/quiet-hours checks belong at consume time, not baked into the published event',
      'Batching provider calls and using a dead-letter queue for permanent failures are the throughput/resilience levers to name explicitly',
    ],
    seniorFollowUps: [
      'How would you avoid re-sending a notification a user already read in-app before the push had a chance to fire?',
      'How do you handle a third-party provider (e.g. FCM) itself rate-limiting your batch calls?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['notifications', 'fanout', 'queues'],
  },
  {
    id: 'sd-case-q11',
    question: 'Why can\'t you just use each shard\'s local auto-increment column as a globally unique ID once a table is sharded, and why is UUIDv4 often also the wrong fix?',
    shortAnswer: 'Independent auto-increment sequences on different shards will generate the same integer on each shard, so they collide once merged; forcing a single global sequence recreates the exact write bottleneck sharding was meant to eliminate. UUIDv4 solves uniqueness with zero coordination but is random, not time-ordered, which hurts B-tree/index locality on insert and makes chronological sorting impossible without a separate timestamp field.',
    detailedAnswer: 'Snowflake-style IDs resolve both problems by packing a timestamp, a worker/machine ID, and a per-millisecond sequence number into one integer, generated locally per node with no per-ID coordination — only a one-time worker-ID assignment at startup (via something like a Zookeeper/etcd lease). Because the timestamp occupies the leading bits, IDs come out roughly sorted by creation time, which preserves index locality and lets the ID double as a natural pagination/sort key, something UUIDv4 cannot offer.',
    keyPoints: [
      'Global auto-increment recreates a single-writer bottleneck; per-shard auto-increment collides across shards',
      'UUIDv4 is unique with no coordination but sacrifices time-ordering and index locality',
      'Snowflake layout: leading timestamp bits + worker ID + per-ms sequence, generated coordination-free per node',
    ],
    seniorFollowUps: [
      'What happens to ID generation on a node if its system clock jumps backward, and how do production Snowflake implementations guard against it?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['id-generation', 'snowflake', 'sharding'],
  },
  {
    id: 'sd-case-q12',
    question: 'A search-box typeahead needs to return the top 5 completions for any prefix in under 100ms at massive query volume. Would you rank completions live on every keystroke, or precompute them? Why?',
    shortAnswer: 'Precompute: build an offline trie from query-frequency logs where every node caches its own top-k completions, rebuilt periodically (e.g. hourly) and swapped in atomically, so the online read path is a fixed-depth trie traversal plus a cached lookup rather than a live aggregation over all matching queries on every keystroke.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Intermediate',
    seniority: 'Senior',
    tags: ['typeahead', 'trie', 'caching'],
  },
  {
    id: 'sd-case-q13',
    question: 'Your typeahead trie is rebuilt hourly from batch query logs, but a breaking-news term needs to surface as a top completion within minutes, not hours. How do you reconcile this with the batch-rebuild design?',
    shortAnswer: 'Keep the batch-rebuilt trie as the steady-state source of truth, but layer a small, fast, real-time cache of override completions on top (populated from a streaming signal of sudden query-volume spikes) that the read path checks first before falling back to the trie — this avoids mutating the trie live while still surfacing fast-breaking terms quickly.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Staff',
    tags: ['typeahead', 'freshness', 'caching'],
  },
  {
    id: 'sd-case-q14',
    question: 'A distributed web crawler with 1,000 parallel workers starts crawling and immediately gets several domains\' servers to block its IP range. What went wrong in the frontier design, and how do you fix it?',
    shortAnswer: 'The frontier was treated as one shared pool that any worker can pull from, so many workers ended up hammering the same domain in parallel with no per-domain rate limiting — the fix is to partition the frontier into per-domain queues, each drained by at most one worker at a time with an enforced delay between requests (politeness), typically also respecting that domain\'s robots.txt crawl-delay.',
    detailedAnswer: 'Politeness is a first-class structural requirement, not a rate limiter bolted on afterward: the frontier\'s data structure itself must map domain to queue so that concurrency is naturally capped per domain regardless of overall worker count. Priority within and across domains (freshness of important pages vs. discovering new ones) is handled by making the frontier a priority structure fed by signals like page importance and update frequency, not a plain FIFO. robots.txt is fetched and cached per domain with its own TTL and checked before a URL from that domain is even enqueued.',
    keyPoints: [
      'Politeness requires structural per-domain queue partitioning, not a global rate limiter',
      'Frontier is a priority structure (importance/freshness signals), not a plain FIFO',
      'robots.txt must gate enqueue, cached per domain with its own TTL',
    ],
    seniorFollowUps: [
      'How would you dedup URLs across a frontier holding billions of entries without storing every URL string exactly?',
    ],
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Advanced',
    seniority: 'Senior',
    tags: ['web-crawler', 'politeness', 'url-frontier'],
  },
  {
    id: 'sd-case-q15',
    question: 'How would you deduplicate URLs in a web crawler that has already seen billions of pages, without storing every seen URL exactly?',
    shortAnswer: 'Use a Bloom filter over seen URLs — it answers has this URL possibly been seen with a tunable, small false-positive rate, using a fraction of the memory a full set or index of billions of URL strings would need; the tradeoff is occasionally skipping a genuinely-new URL, which is acceptable at crawler scale.',
    topic: 'system-design',
    subtopic: 'sd-case-studies',
    difficulty: 'Basic',
    seniority: 'Mid',
    tags: ['web-crawler', 'dedup', 'bloom-filter'],
  },

]